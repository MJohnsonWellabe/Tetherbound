# Lane W04-PORTRAITS — every NPC speaks with the player's face (Phase 1.1 / CL-G11)

Branch: `ralph/W04-PORTRAITS-0904`. Model tier: creating art + data.

**Why.** `assets/ui/portraits/` holds `trainer.png` and `grandpa.png`; ~81 `portrait` references across `data/dialogue/*.json` name `trainer.png`, so Mira, Oskar, Tam, Bram, Halda, every Team Tether rank and the Warden are drawn with the player's face. Owner: "fix the picture during dialogue always being the main character." `scripts/ui/dialogue_panel.gd` draws what the line names, so the fix is art plus data. No new meshes: render portraits from the installed humanoid rigs (`docs/art/HUMANOID_ASSET_INVENTORY.md` is authoritative; the Warden is `assets/characters/warden/warden_lod0.glb`).

**Portrait file contract (fixed — another lane re-points `data/dialogue/stronghold.json` to these exact names, so do not rename):** `assets/ui/portraits/trainer.png` (exists), `grandpa.png` (exists), `villager_male.png`, `villager_female.png`, `grunt.png`, `officer.png`, `captain.png`, `warden.png`. If a named villager has a palette variant in `scripts/world/character_model.gd` / `data/config/village_npcs.json` and it is cheap, you may add per-NPC variants (`mira.png`, `halda.png`, ...) on top of the eight, but the eight must exist.

**Owns:** `assets/ui/portraits/*` (+ `.import`), a new `tools/_capture_portraits.gd` (render each rig head-and-shoulders on a neutral backdrop in the game's renderer under xvfb, matching `trainer.png`'s framing, size and style — look at the existing two files first and match them), every `portrait` field in `data/dialogue/*.json` EXCEPT `data/dialogue/stronghold.json` (another lane owns that file; leave it untouched), a new `tests/test_dialogue_portraits.gd`, and `ralph/reports/W04-PORTRAITS-0904/`. Do not edit `dialogue_panel.gd` unless a portrait cannot be shown at all.

**Do.** 1) Inventory speakers: for each conversation, who speaks (`speaker` field or the NPC that opens it — read `scripts/world/village_npcs.gd`, `trainer_npc.gd`, `data/config/village_npcs.json`, band `trainers.json` to map speaker → rig and gender/rank). 2) Render the portraits (one xvfb run, all rigs, small viewport — this does not need the full world; build a minimal scene with the rig, a light and a camera in the capture script). 3) Re-point every `portrait` to the speaker's file; lines spoken by the player keep `trainer.png`. 4) `tests/test_dialogue_portraits.gd`: for every conversation line, the portrait file exists and loads, and no line whose speaker is not the player resolves to `trainer.png`; see it fail before the re-point (run it on the old data first). 5) Run `tests/smoke_dialogue_clears_the_world_hud.gd`, `test_dialogue_runner.gd`, `test_band_dialogue.gd`, and any dialogue schema test. 6) Capture one real in-game conversation frame (Halda or Mira) at 1280×800 and have a code-blind judge confirm the portrait reads as the speaker and matches the existing portrait style; commit the sheet.

**Acceptance.** The test above is green and was seen red; a played conversation shows the speaker's face. Report with the speaker→portrait table.
