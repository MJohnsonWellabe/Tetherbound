# Lane W07-WARRENS — "Burrow warrens looks terrible" (Phase 1.4 / CL-O7 / CL-E8)

Branch: `ralph/W07-WARRENS-0904`. Model tier: visual judgement + world composition.

**Why.** Owner, on hardware, twice: the Warrens looks terrible. Four prior blind passes judged only the guardian; all four verdicts on the *room* are superseded. `docs/acceptance/MEADOWS_EXIT_CRITERION.md` E5 calls it "the standing GOOD example" — the owner's newer statement wins. Read `docs/FINISH_THE_MEADOWS.md` §1.4, closure plan CL-O7/CL-E8, `docs/VISUAL_BIBLE.md` §1–§3, `docs/prompts/63-BAND2-finished-quarry-warrens.md` for what the Warrens is meant to be, and `ralph/reports/G3-BAND2-0903/` for the last state.

**Owns:** `data/config/burrow_warrens.json`, `scripts/world/burrow_warrens.gd`, `shaders/warrens_boulder_stain.gdshader`, warrens-specific materials under `assets/` that only the Warrens uses, `tools/_capture_warrens_*.gd` (new or existing), `tests/smoke_warrens.gd`, `ralph/reports/W07-WARRENS-0904/`. Do not touch `creature_body.gd`, band vegetation, or the guardian's encounter data. CL-G7 (the `material_get_instance_shader_parameters` null-material error at `burrow_warrens.gd:2957` → `creature_body.gd:492` that opens every boot log) is yours to root-cause from the Warrens side; if the fix belongs in `creature_body.gd`, write the two-line patch in the report and do not apply it.

**Do.** 1) Capture the room as a player sees it: entry mouth, the first chamber, the approach to the guardian, the guardian chamber, and the exit — at eye height on the walked path (`smoke_warrens.gd` knows the path), day lighting as authored, one contact sheet BEFORE any change. 2) Code-blind judge on that sheet with the rubric; get the ranked defects (expect: flat lighting, one mid-tone, no value structure, walls that read as untextured primitives, no foreground/mid/far layering, no sense of a dug burrow, scale disagreement). 3) Fix by composition and material inside the installed families — light pools and dark passages (a real value range), root/rock mid-layer, floor litter, fungus/glow accents as the band's identity (rootstone, the guardian), fog/depth inside the cave, entry silhouette from outside. No new meshes. 4) Re-capture the SAME stands, judge again blind; iterate at most twice. 5) `smoke_warrens.gd` green; perf: measure draws/primitives inside the Warrens with `tools/perf_render_stats.gd` before/after and stay within the same order.

**Acceptance.** The after-judge names the room as reading like a designed burrow with a value range and layering, with the top-ranked before-defects named as improved on the same stands; no new artefacts; smoke green. Update `docs/CURRENT_STATE.md` (CL-O7) and note the E5 supersession in `docs/acceptance/MEADOWS_EXIT_CRITERION.md` (one line).
