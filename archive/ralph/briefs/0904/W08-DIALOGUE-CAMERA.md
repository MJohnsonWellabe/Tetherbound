# Lane W08-DIALOGUE-CAMERA — the conversation push-in (Phase 1.5 / CL-G10 / D73 §6)

Branch: `ralph/W08-DIALOGUE-CAMERA-0904`. Model tier: implementation (the approach is decided).

**Why.** Villagers read too small in conversation on the handheld. Decided (D73 §6, owner #8): a conversation camera push-in to a two-shot at ~3.5 m over the dialogue fade — NOT a change to villager scale (the owner has had that cut and re-cut). Read `docs/decisions/D73-*.md` §6, closure plan CL-G10, `scripts/player/camera_rig.gd` (a `SpringArm3D`), `scripts/ui/dialogue_panel.gd`, and how dialogue opens/closes (`scripts/story/dialogue_runner.gd` or wherever `narrative_modal` is owned — grep `input_owner`).

**Owns:** `scripts/player/camera_rig.gd`, a new `scripts/player/conversation_camera.gd` if you prefer a separate node, the hook where dialogue opens/closes (one call each way; name the file in the report), a `data/config/camera.json` entry for the tunables (distance, height, lateral offset, blend time), `tools/_capture_dialogue_camera.gd` (new), `tests/test_conversation_camera.gd` (new), `ralph/reports/W08-DIALOGUE-CAMERA-0904/`. Do not touch `dialogue_panel.gd` layout, `village_npcs.gd`, or scale.

**Do.** On dialogue open, blend the camera to a two-shot: the speaker framed head-to-waist on one third, the trainer's shoulder on the other, ~3.5 m, slightly above eye height, never clipping into walls (use the spring arm's collision) and never inside the NPC; on close, blend back to the previous rig state. Must work indoors (Mira's cottage, Bram's inn — cramped rooms; fall back to a closer over-shoulder if the two-shot collides) and while mounted. Controller camera input is ignored during the conversation. Tests: a real open/close cycle sets and restores the rig (position/arm length within tolerance), and the collision fallback engages in a walled fixture. Smokes to re-run: `smoke_dialogue_clears_the_world_hud`, `smoke_post_modal_control`, `smoke_menu`, `smoke_opening`, `smoke_gate_b_continuous` (core prefix, as CI runs it — see `verify-gate-b-core` in `.github/workflows/ci.yml`) and `smoke_tournament_bracket`. Capture one 1280×800 frame of Halda outdoors and one of Bram indoors mid-conversation; a code-blind judge confirms the speaker reads at handheld size and nothing clips.

**Acceptance.** Those smokes green first attempt; both frames judged legible with no clipping; tunables in `data/config`. Update `docs/CURRENT_STATE.md` P2 "villagers read too small" row to fixed with the frame path.
