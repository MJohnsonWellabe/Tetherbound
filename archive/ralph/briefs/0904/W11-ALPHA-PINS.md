# Lane W11-ALPHA-PINS — alphas pin to the map at 300 m and stay pinned until caught or beaten (CL-W1)

Branch: `ralph/W11-ALPHA-PINS-0904`. Model tier: implementation.

Read `docs/owner/OWNER_DIRECTIVES_2026-09-04-B.md` D-0904B-1 and amendment A-3; closure plan CL-W1. 16 `alpha`/`elder` entries exist across `data/config/bands/*/spawns.json`, unadvertised. `autoload/map_state.gd` already has `add_dynamic_marker(id, icon, world_pos)` / `remove_dynamic_marker(id)`; `scripts/ui/minimap.gd::_draw_landmarks()` draws `dynamic` markers with collision handling; the full map (`scripts/ui/tab_map.gd` or equivalent — find it) must draw them too. `tests/test_wild_once.gd` shows how once-ids work for alphas/elders.

**Do.** 1) Proximity: when the player comes within 300 m (tunable in `data/config/map.json` or the spawn config) of an authored alpha/elder cluster position (from the spawn data, not from a live body — the body may not be streamed in), pin it: a distinct alpha icon (reuse an existing UI glyph from `assets/ui/icons/ui/` or draw one in `tools/gen_item_icons.py`'s language — small) at the cluster's authored position, with the species name on the full map. 2) Persistence: the pinned set lives in the save (`scripts/save/save_game.gd` write/read/migration defaults — all three places, bump the version) and is restored on load. **Fails if the pinned set is not persisted.** 3) Clearing: the pin is removed when that alpha's once-id fires (caught or beaten — `test_wild_once.gd`'s mechanism), including on load if the flag is already set. 4) A one-shot world message on first pin ("An alpha is near — marked on your map"). 5) Tests: `tests/test_alpha_pins.gd` (real `MapState` + a save round-trip: pin, save, load, pin present; fire the once flag, pin gone; seen red first); `test_save_format.gd`, `smoke_save_persistence.gd`, `test_map_baker.gd`, `smoke_wild_streaming.gd`; because the save format changes, run the FULL unit suite once (`tests/run_tests.gd`, ~28 min) and report the count.

**Owns:** `autoload/map_state.gd`, `scripts/ui/minimap.gd` and the full-map tab (dynamic-marker drawing only), a new `scripts/world/alpha_pins.gd` (the proximity/clear logic — keep it out of `encounter_director.gd`; one hook line there or in `playground_world.gd` to tick it), `scripts/save/save_game.gd` (the pinned set only), `data/config/map.json` tunable, the new icon, tests named above, `ralph/reports/W11-ALPHA-PINS-0904/`.

**Acceptance.** A probe drives a body to 300 m of a Band 1/2 alpha and the pin appears on minimap and full map; survives save/load; clears on KO. Capture one full-map frame with a pin at 1280×800 and have a code-blind judge confirm the pin is legible; commit the sheet.
