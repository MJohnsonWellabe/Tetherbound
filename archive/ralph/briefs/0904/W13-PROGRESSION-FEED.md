# Lane W13-PROGRESSION-FEED — bond and level progression the player can see while it happens (prompt 73, addendum §A, the load-bearing item)

Branch: `ralph/W13-PROGRESSION-FEED-0904`. Model tier: design + implementation (the largest lane).

**Read `docs/prompts/73-PROGRESSION-VISIBLE-bond-and-level-feedback.md` in full and implement it as written.** It carries the measured current state (do not re-derive), the feed contract (§2.1), the three loudness levels (§2.2), the Team screen requirements (§2.3), the ladder decision (§2.4), tunables (§2.5), ownership (§3), validation (§4), and done-when (§5). The owner's sentence it answers: "Bonding and leveling creatures is basically invisible. It needs to be a big thing. Not just when the bond goes up but also while trying to bond."

**Decisions you make and record (`docs/decisions/`, next free D-number):** §2.4 — pick option 1 (unordered ladder; recommended) unless you find a concrete reason not to; write the record before changing `bond_milestones.gd`. Anything else the prompt leaves open.

**Owns** (per prompt 73 §3): `autoload/game_state.gd` (the feed only — a queue + revision beside `push_world_message`), `scripts/creatures/creature_instance.gd` (`gain_xp`/`set_level` push), `scripts/creatures/bond_milestones.gd`, the five crediting sites it lists (in `combat_manager.gd` line ~926 only that call, `game_state.gd`, `tab_backpack.gd` feeds), `scripts/ui/party_strip.gd`, `scripts/ui/combat_hud.gd`, `scripts/ui/tab_creatures.gd`, `scripts/ui/bond_meter.gd`, `scripts/ui/playground_hud.gd` (the banner only), `data/config/progression_feedback.json` (new), `data/config/audio.json` (two cue ids), tests in §4. **Plus candy:** PR #39 added a `level_up` item effect handled in `tab_backpack.gd`; route it through `creature_instance` so it emits the same `xp_gained`/`level_up` events (addendum §B: "candy level gains use the same core feedback language"), and add `tests/test_candy_progression_safety.gd`: the cap (`cap: 50`) clamps with a visible message rather than silent waste; +2/+3 across the cap; a candy on a fainted creature; the feed sees one `level_up` with `levels_gained == 3` for Rare Candy. Do not place candy in the world (density lanes do). Other lanes are simultaneously adding: a VFX hook in `combat_manager.gd` (impact/level-up), a companion-reaction hook at the fight result beat, a `_flee_pressed()` change — keep your `combat_manager.gd` diff to the one crediting line so merges stay trivial. Do not touch `creature_body.gd`, `follower_creature.gd`, or `minimap.gd`.

**Verify** (prompt 73 §4, behaviour never source text): `tests/test_progression_feed.gd` (new), `test_bond.gd` (updated), `test_level_up_announcement.gd` (rewritten against a stub manager, seen red with the feed disconnected), `tests/smoke_progression_feedback.gd` (new; real fight/feed/landmark/rest; banner inside the 5 % safe area at 1280×800), plus `smoke_combat`, `smoke_gate_b_continuous` (core prefix), `smoke_tournament_bracket`, `smoke_creatures_tab_controller`, `smoke_backpack_player_eats`, `test_save_format`. Capture the level-up banner, a bond tick on the party strip, and the Team screen at 1280×800 (one sheet) and have a code-blind judge confirm legibility and that it does not cover combat controls or steal focus.

**Acceptance.** Prompt 73 §5's three bars, evidenced in the report; every new test seen red first; `docs/CURRENT_STATE.md` CL-W6 rewritten; the decision record committed.
