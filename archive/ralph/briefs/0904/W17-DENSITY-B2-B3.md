# Lane W17-DENSITY-B2-B3 — content after the village, bands 2 and 3: wilds, harvest, findables, candy (CL-O4 density half; addendum §B/§C)

Branch: `ralph/W17-DENSITY-B2-B3-0904`. Model tier: world content authoring (creating).

Owner: "There isn't enough to do anywhere. Creatures are only really around the village, sparse anywhere else. There's nothing to take you off the path." Measured: band 5 ships 23 spawns / 8 harvest vs band 1's 69 / 48. Read `docs/FINISH_THE_MEADOWS.md` Phase 2b "Density across bands 2–5", the addendum §B (candy: ~60/30/10 Good/Great/Rare across the chapter, regional batches, tiering by reward logic) and §C (100–150 useful pickups total, per-band measurements), `docs/owner/OWNER_DIRECTIVES_2026-09-04-C.md` §2–§3, `docs/prompts/63` and `64`, `docs/prompts/60-WILD-ecology-journey.md`, `docs/specs/GATE3_ENCOUNTER_CONTRACTS.md` (do not break authored encounter identities), D69/D70, `docs/WORLD_AND_CONTENT.md`, and the band files. **Ship density before the level gate and no-refight** — you are that dependency.

**Pickup data contract (fixed; the bands 4–5 lane authors to the same schema and consumes your loader after merge):** `data/config/bands/<band_dir>/pickups.json` = `{"_why": "...", "pickups": [{"id": "b2_candy_quarry_ledge", "item": "good_candy", "pos": [x, z], "y": optional, "tier": "side|detour|secret|critical", "why": "one line"}]}`. Ids are globally unique and are the save's once-flag (one persistent identity per authored location). Build the loader `scripts/world/band_pickups.gd`: reads every band's `pickups.json`, spawns each through the existing `scripts/world/item_cache_pickup.gd` seam (`setup(item_id, label, model_path, model_scale)` — `playground_world.gd::_item_cache_model()` shows how the item's own `world_model` is read), on real ground (`ground_height_at`), never inside solid scatter (`vegetation.gd::has_solid_scatter_near()`), persisted by id; ONE hook in `playground_world.gd` beside `_place_item_caches()`. Candy tier materials: `candy_pickup.glb` is one mesh — tint + medallion per tier via `material_override` at instancing (see `docs/prompts/75` §4.2 and the `tm_orb` pattern in `docs/specs/ASSET_LEDGER.md`); Rare gets two small primitive wings as children. Mushrooms: tint per tier, Wild = broader cap by non-uniform scale.

**Author bands 2 and 3** (Stone & Root; The River Lock): raise wild clusters and harvest nodes toward band 1's density scaled by band extent with authored reason (the quarry, the Warrens mouth, the river banks, the relay pickets), keeping each band's existing named encounters, level bands (D69: widen from the bottom) and the roster temptations; add ~35 pickups across the two bands following the addendum's tiering (critical path sparse and Good; side routes Good with occasional Great; detours Great; secrets/hard encounters Rare — roughly 20 Good / 10 Great / 4 Rare plus revives/potions/mushrooms placed where attrition bites, before camps rather than after). Every placement gets a `why`. Also take closure plan CL-E2's prop half (a three-prop Riverwatch post at Oreth's stand in band 3 `props.json`).

**Owns:** `data/config/bands/band2_stone_and_root/{spawns,harvest,props,pickups}.json`, `data/config/bands/band3_the_river_lock/{spawns,harvest,props,pickups}.json`, `scripts/world/band_pickups.gd` (new), the one hook in `playground_world.gd`, `tests/test_band_pickups.gd` (new: schema, unique ids, items exist, positions inside the band, save round-trip of a collected id), `tests/test_band_content.gd` fixtures if the baseline mirror needs a `_comment_` exemption (read its rules), `tools/_probe_band_density.gd` (new or existing census tool — D70's census exists somewhere under `tools/`; find it), `ralph/reports/W17-DENSITY-B2-B3-0904/`. **Do not touch** `vegetation.json` (another lane owns the bake), `trainers.json` (another lane), band 1/4/5 files, `items.json`.

**Verify.** `test_spawn_tables`, `test_spawns_data`, `test_band_content`, `test_harvest`, `test_starters_are_exclusive`, `test_wild_once`, `test_band_pickups`, `smoke_playground` (grep `^ERROR:`), `smoke_wild_streaming`, `smoke_warrens`, `smoke_relay`. Measure per band with the census/probe: spawn count, harvest count, pickups (critical vs side), worst gap between points of interest along the spine, and record them in the report against band 1's numbers. Capture one xvfb frame of a Great candy and one mushroom in place at normal play distance and have a code-blind judge say whether they read as pickups and whether tiers are distinguishable; commit the sheet.

**Acceptance.** Both bands' numbers move materially toward band 1's per-km values with authored `why`s; pickups persist; tests seen red first where new; `docs/WORLD_AND_CONTENT.md` band tables updated; `docs/CURRENT_STATE.md` CL-O4 (density half, bands 2–3) rewritten.
