# Lane W22-BRIDGE-SIGNPOST — the signpost and the bridge deck brought to board 18 without Meshy (prompt 74 §7); the South Bridge as a held crossing (CL-B3 in-rules half)

Branch: `ralph/W22-BRIDGE-SIGNPOST-0904`. Model tier: visual judgement + procedural geometry.

Read `docs/prompts/74-ART-REFERENCE-owner-boards-for-meshy.md` §7 in full (it is precise about what to change and what fails), `docs/art/reference/18_Signpost_Bridge_Modular_Props.png`, closure plan CL-B3, `ralph/reports/GATE2-EVIDENCE-0903/JUDGE.md` (the bridge-approach stand the judge used), `scripts/world/signpost.gd`, `scripts/world/gated_crossing.gd`, `scripts/world/south_bridge.gd`, `data/config/building_prefabs.json`, and PR #39 (`05e57a09`) which stood the generated checkpoint gate (`assets/environment/team_tether/south_bridge_gate.glb`) at the archway.

**Do.** 1) Signpost: rope band near the top, warmer/more saturated wood tones tuned against a render (not guessed), and a pointed single-plank arm silhouette — without breaking `_add_arm()`'s golden-angle mounting or `_label_scale()`'s fit (BAND1-DISCOVERY's 3× label fix). Render before/after on the two signpost stands (`tools/_capture_band1_signpost_legibility.gd`). 2) Bridge deck: follow §7's four steps — read what `building_prefabs.json` references, render it in isolation, compare to board 18's plank-and-rail panel (rope rails with knots at posts, squared posts on stone footings, plank seams), and close the gap by material/geometry inside `gated_crossing.gd`/prefabs, kitbashing installed Kenney/Quaternius pieces if closer. **Fails if** done by editing `south_bridge.gd`'s checkpoint dressing — that is the occupation half. 3) The occupation half, in-rules: with the generated gate in place, make the South Bridge read as a **held crossing from the approach** — a barricade, oxblood banners (only there), a posted grunt from the installed rig standing guard (dressing, non-interactive unless the gatekeeper already is), a lantern with the faint teal glow — using the Team Tether prop kit and `village_npcs.gd`'s set-dressing spec shape (see the relay's "Sentry" entries in `relay_site.json`; give them no prompt). 4) Render the bridge approach stand before/after; one sheet each; code-blind judge against board 18 for the signpost/deck and against the rubric for the crossing ("does it read as a held crossing from the approach?").

**Owns:** `scripts/world/signpost.gd`, `scripts/world/gated_crossing.gd`, `scripts/world/south_bridge.gd` (occupation dressing only), `data/config/building_prefabs.json` (deck/rail entries), `data/config/south_bridge*.json` if present, `tools/_capture_*signpost*.gd` / `_capture_*bridge*.gd`, `tests/smoke_traversal.gd` (must stay green; add a dressing-presence assert), `tests/test_signpost*.gd`, `ralph/reports/W22-BRIDGE-SIGNPOST-0904/`. Do not touch `data/config/bands/*`, vegetation, or the gate mesh.

**Acceptance.** Judge names the signpost and deck as closer to board 18 with labels still legible at 1280×800, and the crossing as held from the approach; `smoke_traversal`, `smoke_gate_b_continuous` (core) green; if the judge fails the deck or signpost twice, write the Meshy brief per §7 (do not generate). Update `docs/VISUAL_BIBLE.md` §4a and `docs/CURRENT_STATE.md` CL-B3.
