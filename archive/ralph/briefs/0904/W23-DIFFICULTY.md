# Lane W23-DIFFICULTY — "Beating creatures and other trainers is way too easy" (CL-O5 baseline, CL-G8 wild ceilings)

Branch: `ralph/W23-DIFFICULTY-0904`. Model tier: tuning with evidence.

Owner-reproduced on hardware. Named opponents got real behaviour from the per-encounter `combat` profiles (G-2); this says the **baseline** is soft. Read closure plan CL-O5, CL-G8, CL-G9; `docs/specs/GATE3_ENCOUNTER_CONTRACTS.md` G-1/G-3 (the fails-if: no profile one-shots a full-health entry-level creature; two profiles must be distinguishable); `docs/specs/MEADOWS_PROGRESSION_CURVE.md`; `data/config/chapter_curve.json`, `progression.json`, `creature_condition.json`, the wild AI (`scripts/world/wild_creature.gd`, the combat AI under `scripts/combat/`), `tools/_probe_pacing.py`, `tests/test_chapter_curve.gd`, `ralph/reports/G3-ECONOMY-0903/`.

**Method — measure before touching a number.** Build a headless combat harness (`tests/smoke_combat.gd` and `tests/helpers/` show how) that runs N scripted fights at each band's entry level: the player's typical party vs (a) that band's median wild, (b) its trainer floor, with the player piloting a simple competent policy (attack on cooldown, dodge nothing) — record time-to-win, HP lost, whether any potion/revive would have been needed. That is the baseline the owner calls too easy. Then tune: wild aggression/attack cadence, damage scaling in `progression.json` (defence/attack curves), the `wild_band` per band (CL-G8: bands 2 and 4 ceilings sit below the corrected team entry — apply D69, widen from the bottom, so arrival wilds are not all weaker than the team), trainer AI baseline behaviour (use moves, keep distance, switch at low HP if the rules allow). Targets to argue for and record in `docs/decisions/`: an ordinary wild at band entry costs a real fraction of HP (say 15–30 %); a trainer fight at the band's floor is lost by the competent-policy party roughly 1 in 4 times without preparation; the tournament stays winnable at its entry level 5 (`smoke_tournament_bracket` must still win); the Warden's measured length stays within the W-1 band. Re-measure after; put both tables in the report. Keep D42's 3–4 h in mind (CL-G9: the pacing probe says 4.71 h) — do not add grind, add danger.

**Owns:** `data/config/chapter_curve.json`, `progression.json`, `creature_condition.json`, the combat AI tunables (name the files), `scripts/combat/` AI baseline (not `combat_manager.gd` — three other lanes touch it), `scripts/world/wild_creature.gd` aggression only, `tests/test_chapter_curve.gd`, `tests/smoke_combat_baseline.gd` (new; the harness), `tools/_probe_pacing.py`, `ralph/reports/W23-DIFFICULTY-0904/`. Do not touch band `spawns.json`/`trainers.json` (density and small-fixes lanes) — if a per-band level needs to move, write the exact diff in the report.

**Verify.** `smoke_combat`, `smoke_boss`, `smoke_trainer_battle`, `smoke_aggression`, `smoke_tournament_bracket`, `smoke_gate_e_finale`, `test_chapter_curve`, `test_encounter_combat_override` green first attempt.

**Acceptance.** Before/after tables; the decision record; smokes green; `docs/CURRENT_STATE.md` CL-O5 rewritten with the numbers.
