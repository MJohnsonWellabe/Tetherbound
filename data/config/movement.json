{
  "_comment": "Third-person movement tuning. ALL of it is TUNABLE and none of it is a mechanic. These are starting values chosen to be playable, not balanced; the owner's job in M1 is to say 'too floaty', 'sprint too slow', 'camera too close', and these are the numbers that answer that. Nothing here should be treated as settled until traversal feels good on the Ally.",

  "locomotion": {
    "walk_speed": 5.0,
    "sprint_speed": 8.6,
    "_comment_accel": "Metres per second squared. Ground acceleration high enough that the character does not feel like it is on ice, air acceleration low enough that mid-jump direction changes read as momentum rather than flight.",
    "ground_acceleration": 42.0,
    "ground_friction": 38.0,
    "air_acceleration": 9.0,
    "_comment_turn": "How fast the character model swings to face travel direction, radians/sec. Too low reads as sluggish, too high reads as a turret.",
    "turn_speed": 11.0
  },

  "gait_feel": {
    "_comment": "MQ1A, TUNABLE. Momentum tilt: the trainer's model leans into acceleration and banks into turns (character_model.gd::apply_momentum_tilt, driven from trainer_model.gd). lean_deg_per_accel converts m/s^2 to degrees; at ground_acceleration 42 the launch lean approaches pitch_limit_deg. smoothing_rate is 1/seconds-to-settle-ish; higher snaps, lower wallows.",
    "lean_deg_per_accel": 0.18,
    "pitch_limit_deg": 7.0,
    "roll_limit_deg": 8.0,
    "smoothing_rate": 9.0
  },

  "jump": {
    "_comment": "Height in metres, converted to launch velocity against gravity so retuning gravity does not silently change jump height.",
    "height": 1.35,
    "gravity": 26.0,
    "_comment_gravity": "Well above real 9.8. Realistic gravity in a third-person game reads as floating; this is the single biggest lever on 'too floaty' feedback.",
    "fall_gravity_multiplier": 1.35,
    "_comment_fall_mult": "Falling accelerates faster than rising. Standard platformer trick that makes a jump feel weighty without shortening its rise.",
    "coyote_time": 0.12,
    "buffer_time": 0.12,
    "_comment_forgiveness": "Coyote time lets a jump register just after walking off an edge; buffer lets a jump pressed just before landing fire on touchdown. Both are invisible when they work and infuriating when absent."
  },

  "stamina": {
    "max": 100.0,
    "sprint_drain_per_second": 12.0,
    "jump_cost": 8.0,
    "regen_per_second": 18.0,
    "_comment_delay": "Seconds after the last stamina spend before regeneration starts. Without a delay, tapping sprint costs nothing and the meter is decoration.",
    "regen_delay": 1.1,
    "_comment_exhausted": "Below this, sprint is refused until the meter recovers past it. Stops a stutter-sprint exploit at 1 stamina.",
    "exhausted_below": 10.0
  },

  "fall_damage": {
    "_comment": "Damage is driven by impact SPEED, not by fall distance: the same landing hurts the same whether it came off a cliff or a launch. safe_speed is roughly a 2.5m drop under the gravity above.",
    "safe_speed": 12.0,
    "lethal_speed": 34.0,
    "_comment_curve": "Exponent on the normalised speed between safe and lethal. Above 1 makes small overshoots cheap and big ones brutal, which is what makes a player judge a drop rather than avoid all of them.",
    "curve": 2.0,
    "max_damage": 100.0
  },

  "health": {
    "max": 100.0
  },

  "riding": {
    "_comment": "R6.1. Deliberately SMALL: how fast a mount goes and what it needs to be ridden are per-species data (data/creatures/species.json's `rideable` block), because 'which creature is the mount' is a progression question the spec answers per band. What lives here is only the part that is about the RIG rather than about the animal -- the camera pose while mounted. All TUNABLE.",
    "camera": {
      "_comment": "Pulled back and raised a little from the walking pose (distance 5.2 / height 1.75 above): the thing being followed is a 2m animal with a rider on it, moving at twice walking pace, and the exploration framing puts its rump in your face. `retarget_lag` is the same easing combat uses when the rig swaps from the trainer to a creature -- mounting is the same handoff, and a hard cut loses the connection between the animal you walked up to and the animal you are now sitting on.",
      "distance": 6.8,
      "height": 2.35,
      "retarget_lag": 6.0
    }
  },

  "camera": {
    "_comment": "Orbit camera on a spring arm. distance/height are the resting pose; the arm shortens automatically when terrain intrudes.",
    "distance": 5.2,
    "height": 1.75,
    "_comment_pitch": "Degrees. Negative looks down. The lower bound stops the camera going under the terrain, the upper stops it looking at the sky behind the player's head.",
    "pitch_min_deg": -60.0,
    "pitch_max_deg": 32.0,
    "pitch_start_deg": -12.0,
    "_comment_sensitivity": "Gamepad is degrees/second at full stick. Mouse is degrees per pixel of motion. They are separate numbers because tuning one to fix the other always makes the other worse.",
    "gamepad_sensitivity": 190.0,
    "mouse_sensitivity": 0.16,
    "_comment_deadzone": "The Ally's sticks rest slightly off centre and drift with wear. Applied on top of the input map's own deadzone.",
    "stick_deadzone": 0.18,
    "invert_y": false,
    "_comment_follow": "How quickly the arm's origin catches up to the player, and how quickly it recovers after terrain pushes it in. Collision recovery is deliberately slower than intrusion, so the camera snaps away from a wall but eases back out.",
    "follow_lag": 14.0,
    "collision_recover_speed": 4.0,
    "_comment_collision_margin": "How far the spring arm stops short of whatever it hit. The Meadows scatter's trees and rocks collide (scripts/world/vegetation.gd), and are dense enough that a trunk can sit almost directly behind the player; the old 0.3 default then read as the camera buried in bark rather than a normal close-up.",
    "collision_margin": 0.6,
    "_comment_probe": "Radius, in metres, of the ball the spring arm sweeps along itself. A SpringArm3D with no `shape` falls back to a single infinitely thin raycast, which is what shipped: indoors the ray slipped between a chair back and a table edge and drew the camera inside them, and when it did hit something nearer than the margin the arm collapsed to nothing and put the camera in the player's head. Keep it small -- it stands in for the camera's near plane, not for the player, and a large ball shortens the arm for doorframes it would have fitted through.",
    "collision_probe_radius": 0.25
  },

  "torch": {
    "_comment": "OF24-era history: this used to describe an always-on kit fixture with no torch ITEM to craft or lose. OW12 (owner, 2026-08-16: 'torches need to be a carry able item not placeable one') superseded that -- the torch is now data/items/items.json's own `torch` entry, equipped off the hotbar/backpack like any other tool, and everything below only takes effect while it is the equipped tool (scripts/player/torch.gd). `auto_at_night` still means it needs no extra input once equipped: it lights itself the moment world_look.gd's is_dark() goes true, and torch_toggle (L / gamepad Start) is a manual OVERRIDE on top of that, not the only way to turn it on.",
    "auto_at_night": true,
    "_comment_placement": "Metres above the player's feet, and forward -- roughly chest height, angled down and out so it reads as a carried light illuminating the ground ahead rather than a beacon strapped to the forehead. This is the forward-throw SpotLight's own fixed position, not the hand -- it does not need to track the mesh the way the OmniLight below does.",
    "height": 1.3,
    "forward_offset": 0.35,
    "pitch_deg": -35.0,
    "_comment_light": "Warm (low colour temperature) on purpose -- a cool-white spotlight reads as a flashlight/sci-fi prop, not a torch. Range and angle are tuned to light the ground and nearby foliage a few paces out without turning night into day; energy is kept modest for the same reason and because a single shadow-casting light following the player at all times is the wrong place to spend the handheld's frame budget.",
    "colour": "#ffb366",
    "range_m": 9.0,
    "spot_angle_deg": 46.0,
    "energy": 3.2,
    "attenuation": 1.4,
    "shadows": false,
    "_comment_omni": "The prop's own warm point light, separate from the SpotLight throw above -- scripts/player/torch.gd keeps it synced each frame to wherever scripts/player/tool_hold.gd actually put the equipped torch mesh, so the glow reads as coming from the prop in hand. (The mesh itself, and where it sits in the hand, is items.json's `torch` entry's own `held_model`/`held_offset`/`held_rotation_deg` -- OW12 moved that data off this file's old `prop_bone`/`prop_offset`/`prop_rotation_deg` keys, retired along with the always-on kit torch.) Modest range/energy for the same D06 Compatibility-renderer reason the SpotLight stays unshadowed -- this one is unshadowed unconditionally, never toggled by `shadows` above. `flicker_amount` is a fraction of `omni_energy` the light wobbles by (0 = steady); `flicker_speed` is roughly how many wobbles per second.",
    "omni_energy": 1.6,
    "omni_range": 6.0,
    "flicker_amount": 0.35,
    "flicker_speed": 9.0
  }
}
