# Asset Ledger

Provenance for every non-original asset in the project, per `CLAUDE.md` and
`docs/TECHNICAL_START.md`.

Assets do not need to be CC0 for this private project. The rule is that nothing
ships without a row here, so attribution is a lookup rather than an archaeology
project, and so a licence audit before any public release is possible at all.

A row is required **before** the file is committed. Record the licence as found
at the time it was fetched, not as remembered.

| Asset | Creator | Source | Licence | Paid | Local path | Modifications |
|---|---|---|---|---|---|---|
| Meadows key art board | Owner-supplied (AI-generated) | Provided by owner | Owner's own | No | `docs/reference/tetherbound-meadows-keyart.png` | None |
| Input Prompts — 4 more files extracted (`xbox_lt.png`, `xbox_rt.png`, `xbox_button_start.png`, `keyboard_l.png`) | Kenney | [Input Prompts](https://kenney.nl/assets/input-prompts) (already staged raw, see the full-pack row below) | CC0 1.0 | No | `assets/ui/input_prompts/` | Owner playtest report fixes. `xbox_lt.png`/`xbox_rt.png`: combat's `quick`/`charged` verbs and build's `build_rotate_left`/`build_rotate_right` were showing the LB/RB SHOULDER icons as stand-ins for their real LT/RT trigger bindings — a documented "on-screen control instruction lies about its binding" defect (`scripts/ui/input_glyph.gd`) — now shown correctly; the real trigger art was already sitting in the raw vendor pack, unextracted. `xbox_button_start.png`/`keyboard_l.png`: prompts for the new starting-torch toggle (`torch_toggle`). Unmodified, straight copies from the already-logged raw pack, same as every other `assets/ui/input_prompts/*.png` file extracted before this row (none of which had their own ledger row until now — a pre-existing gap, not introduced here). |
| Input Prompts — 3 more files extracted (`xbox_guide.png`, `keyboard_b.png`, `keyboard_p.png`) | Kenney | [Input Prompts](https://kenney.nl/assets/input-prompts) (already staged raw, see the full-pack row below) | CC0 1.0 | No | `assets/ui/input_prompts/` | OF21 (same-context gamepad collision fix). `xbox_guide.png`: `torch_toggle`'s new dedicated gamepad glyph — `torch_toggle` moved OFF the Start/Menu button (where it collided with `backpack_drop`) onto the Guide button, the only gamepad button nothing else in the input map used at all. `keyboard_b.png`/`keyboard_p.png`: prompts for the two new world-context actions this same pass added, `build_open` (B) and `torch_place` (P). Unmodified, straight copies from the already-logged raw pack, same convention as the row above. |
| NPC bases concept board | Owner-supplied (AI-generated) | Provided by owner | Owner's own | No | `docs/art/reference/12_NPC_Bases_Reusable.png` | None. Specifies three reusable base bodies; supersedes spec §22's "one or two" per D24. |
| Tether Energy Pylon board | Owner-supplied (AI-generated) | Provided by owner | Owner's own | No | `docs/art/reference/13_Tether_Energy_Pylon.png` | None. Production brief, not mood art: **2K–3K triangle** target, human height +, and a five-part modular build (base + core module + supports ×4 + top frame + tether crystal). One of the three hero objects D24 reserves Meshy for. |
| Tether Energy Pylon — production hero object | Generated (Meshy multi-image-to-3D preview ×3, retexture on the winner) from crops of the owner's board `13_Tether_Energy_Pylon.png`; textures separated and graded in-pipeline | `tools/art_pipeline/`, task ids in `assets_raw/tether_pylon/manifest.json` and `c_tex/provenance.json` | Meshy ToS at generation date (paid credits of owner's account); derived from owner's own concept art. All Rights Reserved / proprietary, owner-licensed | Paid credits (~90) | `assets/environment/team_tether/tether_pylon.glb` + `tether_pylon_albedo.png` / `tether_pylon_albedo_dead.png` | SF33. One of the three hero objects D24 reserves Meshy for, and the first built. 3,041 triangles against the board's 2K–3K target. Candidate C won on the board's signature features (large faceted floating crystal, claw frame, lantern core). GLB is geometry-only (125KB); the 2K Meshy albedo was downscaled to 1024 and split into a live state (as textured) and a dead state (teal drained to cold slate via hue-masked grade) — per-material variants, one mesh. A baked emission-mask texture was built and then REMOVED: gl_compatibility (D01) ignores the mask and floods the whole mesh with the emission colour; the live read is albedo-carried, only the procedural conduit cables use uniform-colour emission. |
| Relay Apparatus board | Owner-supplied (AI-generated) | Provided by owner | Owner's own | No | `docs/art/reference/14_Relay_Apparatus.png` | None. Band 3. Artist note is the build spec: *"modular construction, core and rings serviceable, conductor arms and manifolds replaceable"*. Five labelled subassemblies. Serves `SE23`. |
| Legendary Tether Machine board | Owner-supplied (AI-generated) | Provided by owner | Owner's own | No | `docs/art/reference/15_Legendary_Tether_Machine.png` | None. Warden stronghold, ~15 m against its own 0–20 m scale bar. **Depicts a bound legendary inside the containment ring — the board licenses the MACHINE, not its occupant.** D23 §20 forbids new creature meshes at any credit balance, so the bound creature is an existing roster asset or VFX. |
| Terrapup/Ripplet/Galewisp/trainer turnaround crops ×17 | Derived from the owner's art pack | `tools/art_pipeline/crop_views.py` | Owner's own. Reference only; `.gdignore`d out of the export | No | `assets/creatures/tetherbound/{terrapup,ripplet,galewisp,trainer}/reference/*.png` | Cut from the four production sheets: one figure per image, shared scale, flat background. Regenerate with the script; never hand-edited. Seventeen rather than sixteen since the trainer gained a separate `head.png`, which is what the head-graft technique generates against. |
| Grandpa/Warden/Veridian Stag crops ×10 | Derived from the owner's art pack | `tools/art_pipeline/crop_views.py` | Owner's own. Reference only; `.gdignore`d out of the export | No | `assets/creatures/tetherbound/{grandpa,warden,veridian}/reference/*.png` | Cut from concept boards `05` and `06` rather than from a sheet, because these three have none. Grandpa and the Warden each carry a `head.png`; the Warden's is a ~165 px region upscaled and it is why his face is painted rather than modelled. |
| Wild roster turnaround crops ×51 | Derived from the owner's Meadows Wild Canon Pack | `tools/art_pipeline/crop_views.py` | Owner's own. Reference only; `.gdignore`d out of the export | No | `assets/creatures/tetherbound/{bramblebun,mudsnout,trailpup,meadowhart,burrowback,tuskroot,paddlenewt,mosshell,brooktail,reedwing,pipwing,duskhush,galecrest}/reference/*.png` | Cut from the four wild sheets in `docs/art/reference/wild/` — `02` Ground A (Bramblebun, Mudsnout, Trailpup), `03` Water, `04` Air, `05` Ground B (Meadowhart, Burrowback, Tuskroot, and misnamed a scale guide). Same rule as the crops above: one figure per image, shared scale, flat background, regenerate rather than hand-edit. Fifty-one and not fifty-two because Galecrest is drawn as a single wide spread-wing figure labelled FRONT and SIDE, so it yields no separate front view. `crop_views.py` also erases the sheets' pale panel-divider rules, which are faint enough to ignore on a contact sheet and quite visible enough that a generator modelled one as a plank. |
| Terrapup — production creature | Generated (Meshy multi-image-to-3D + retexture) from the owner's reference sheets; edited, rigged and animated in-pipeline | `tools/art_pipeline/`, task ids in `assets_raw/terrapup/*/provenance.json` | Meshy-generated assets: per Meshy ToS at generation date (paid/free tier of owner's account), commercial use per plan terms. Derived from owner's own concept art | Free tier credits | `assets/creatures/tetherbound/terrapup/models/creature_terrapup_lod0.glb` | Full chain in `docs/art/TERRAPUP_PRODUCTION_REPORT.md`: 2 generation rounds, 2 critique-driven mesh edits, cleanup/remesh, retexture against the 3/4 crop, own 15-bone rig, 6 procedural clips, texture grade. |
| Ripplet — production creature | Generated (Meshy) from the owner's sheet 02; edited, rigged and animated in-pipeline | `tools/art_pipeline/`, task ids in `assets_raw/ripplet/*/provenance.json` | Meshy ToS at generation date; derived from owner's own concept art | Free tier credits | `assets/creatures/tetherbound/ripplet/models/creature_ripplet_lod0.glb` | See `docs/art/RIPPLET_PRODUCTION_REPORT.md`. |
| Galewisp — production creature | Generated (Meshy) from the owner's sheet 03; edited, rigged and animated in-pipeline | `tools/art_pipeline/`, task ids in `assets_raw/galewisp/*/provenance.json` | Meshy ToS at generation date; derived from owner's own concept art | Free tier credits | `assets/creatures/tetherbound/galewisp/models/creature_galewisp_lod0.glb` | Two mesh-fix rounds (tail, ears, wingspan), glider rig, weight transfer, warm grade. |
| Trainer — production character | Generated (Meshy multi-image-to-3D for the body, a SECOND generation for the head, then retexture) from the owner's sheet 04; grafted, rigged and animated in-pipeline | `tools/art_pipeline/`, task ids in `assets_raw/trainer/*/provenance.json` | Meshy ToS at generation date; derived from owner's own concept art | Paid credits | `assets/characters/trainer/trainer_lod0.glb` | Body and head generated separately and joined by `graft_head.py` — see `docs/art/HUMANOIDS_PRODUCTION_REPORT.md`. Nine whole-figure candidates had no face at all; the head alone, at the same budget, has one. 5 procedural clips. |
| Grandpa Elias — production character | Generated (Meshy) from board `05` crops with a `06` head reference; grafted, rigged and animated in-pipeline | `tools/art_pipeline/`, task ids in `assets_raw/grandpa/*/provenance.json` | Meshy ToS at generation date; derived from owner's own concept art | Paid credits | `assets/characters/grandpa/grandpa_lod0.glb` | Same graft chain as the trainer. Body candidate chosen on a neck-down review, since the head was always going to be replaced. |
| Warden of the Meadows — production character | Generated (Meshy **text-to-3D**, then retexture) from a prompt written off board `06` | `tools/art_pipeline/`, task ids in `assets_raw/warden/*/provenance.json` | Meshy ToS at generation date; derived from owner's own concept art | Paid credits | `assets/characters/warden/warden_lod0.glb` | Text-to-3D because the board shows him arms-folded and image-to-3D reconstructs the pose, welding the forearms into one unriggable mass. Face is a painted mask, which is what the board shows. |
| Villager Female (NPC base) | Generated (Meshy multi-image-to-3D + retexture) from `docs/art/reference/12_NPC_Bases_Reusable.png`'s row; auto-rigged (Meshy, humanoid-only endpoint) and animated in-pipeline | `tools/art_pipeline/`, task ids in `assets_raw/villager_female/*/provenance.json` | Meshy ToS at generation date; derived from owner's own concept art | Paid credits | `assets/characters/villager_female/villager_female_lod0.glb` | Shipped by `NP4` as a textured, unrigged GLB with two honest remainders — a UV-seam texture smudge on one shin and an occluded twin-ponytail silhouette from the front — neither chased further per `conventions.md`'s convergence rule. `NP4-rig` rigs (Meshy auto-rig, `--height 1.75`) and animates it (`animate_humanoid.py`, same 5 procedural clips as the trainer) and installs it — the first time this base has a path into the game at all. Nothing in the live game consumes it yet; `NP1`/`NP3` still reuse the trainer/Grandpa/Warden rigs directly. **`NP7`** (Blender, no new generation): the twin-ponytail cut from the fused mesh into its own `hair_ponytail` mesh object, the resulting scalp hole patched (true-rim detection + fan-fill, filtered to loops of 10+ points to avoid spike artifacts a naive fill produced — see `ralph/DONE.md`), and the hair piece re-skinned 100% to the `Head` bone. Same file, same 6 clips, same textures — the mesh is now two draw primitives instead of one, wired into `character_model.gd::_apply_hair()`'s existing hide/show/recolour mechanism via the real `hair_ponytail` node instead of a placeholder primitive. Honest remainder: a small (~1-2px at normal camera distance, confirmed invisible in `tools/capture_village_npcs.gd`'s production frame) residual seam survives at the scalp in extreme macro close-ups from specific angles — the source mesh carries far denser pre-existing UV-seam fragmentation in the hair region than a clean single boundary loop, which is a property of the shipped Meshy retexture, not of this cut. |
| Villager Male (NPC base) | Generated (Meshy multi-image-to-3D + retexture) from `docs/art/reference/12_NPC_Bases_Reusable.png`'s row; auto-rigged (Meshy, humanoid-only endpoint) and animated in-pipeline | `tools/art_pipeline/`, task ids in `assets_raw/villager_male/*/provenance.json` | Meshy ToS at generation date; derived from owner's own concept art | Paid credits | `assets/characters/villager_male/villager_male_lod0.glb` | Same `NP4`/`NP4-rig` chain as Villager Female. `NP4`'s honest remainder: trousers render darker/colder than the reference after three texture attempts, not chased further. Rigged (`--height 1.78`), animated, installed; not yet consumed by any live NPC. |
| Team Tether Grunt (NPC base) | Generated (Meshy multi-image-to-3D + retexture) from `docs/art/reference/12_NPC_Bases_Reusable.png`'s row; auto-rigged (Meshy, humanoid-only endpoint) and animated in-pipeline | `tools/art_pipeline/`, task ids in `assets_raw/grunt/*/provenance.json` | Meshy ToS at generation date; derived from owner's own concept art | Paid credits | `assets/characters/grunt/grunt_lod0.glb` | Same `NP4`/`NP4-rig` chain; `NP4` marked this one ACCEPTABLE as-is after round 2's mask/eye-geometry fix. Rigged (`--height 1.85`, matching the Warden's), animated, installed. `NP2`'s rank badges currently ship on the Warden's rig, the only faction-appropriate body installed at the time — this is Team Tether's own base, still unconsumed by any live NPC. |
| Veridian Stag — production legendary | Generated (Meshy) from board `06`; rack rebuilt, rigged and animated in-pipeline | `tools/art_pipeline/`, task ids in `assets_raw/veridian/*/provenance.json` | Meshy ToS at generation date; derived from owner's own concept art | Paid credits | `assets/creatures/tetherbound/veridian/models/creature_veridian_lod0.glb` | Antler rack thickened and bent forward per the blind critique — the generated rack swept backward only, "which turns a crown into a windblown crest". Quadruped rig, 6 procedural clips. |
| Bramblebun — production creature | Generated (Meshy multi-image-to-3D + retexture) from the owner's Ground Sheet A; cleaned, rigged and animated in-pipeline | `tools/art_pipeline/`, task ids in `assets_raw/bramblebun/*/provenance.json` | Meshy ToS at generation date; derived from owner's own concept art | Paid credits | `assets/creatures/tetherbound/bramblebun/models/creature_bramblebun_lod0.glb` | Three turnaround views plus the 3/4 hero, three candidates. Candidate b won on the ears — broad, with the concept's visible inner cup — and on keeping the compact sitting rump the other two stretched toward a hare. Cleaned to 28k, retextured against its own concept, 15-bone quadruped rig with 0 unweighted vertices of 14,002, 6 procedural clips. Known miss: the sheet grows a thick cushion of moss, leaves and purple flowers from the shoulders back and all three candidates modelled sparse sprigs, so the retexture carries the read but the silhouette does not break the way the drawing's does. Replaced `ernie-the-duck.glb`, which the tutorial creature had been pointing at since M2. |
| Mudsnout — production creature | Generated (Meshy multi-image-to-3D + retexture) from the owner's Ground Sheet A; cleaned, rigged and animated in-pipeline | `tools/art_pipeline/`, task ids in `assets_raw/mudsnout/*/provenance.json` | Meshy ToS at generation date; derived from owner's own concept art | Paid credits | `assets/creatures/tetherbound/mudsnout/models/creature_mudsnout_lod0.glb` | New to the project entirely — D13 makes it the biome's only pre-evolution and it had no earlier stand-in. Candidate c, chosen for the bristle ridge along the spine, which is the one thing on this creature texture cannot fake, and for the roundest youngest body of the three. Candidate b had the better snout disc and lost on the ridge, on the Terrapup precedent that silhouette beats surface. Candidate a grew a flat vertical plank behind the pig in all four views: it had modelled the sheet's panel-divider rule, which `crop_views.py` now erases. |
| Trailpup — production creature | Generated (Meshy multi-image-to-3D + retexture) from the owner's Ground Sheet A; cleaned, rigged and animated in-pipeline | `tools/art_pipeline/`, task ids in `assets_raw/trailpup/*/provenance.json` | Meshy ToS at generation date; derived from owner's own concept art | Paid credits | `assets/creatures/tetherbound/trailpup/models/creature_trailpup_lod0.glb` | Candidate b, on the longest narrowest muzzle and the only clear shoulder ruff. No stone anywhere on it, which the canon pack requires: the mantle belongs to Terrapup and confusing the two is the specific failure D13 names. Replaced nothing — it simply did not exist as art before. |
| Tuskroot — production creature | Generated (Meshy multi-image-to-3D + retexture) from the owner's Ground Sheet B; cleaned, rigged and animated in-pipeline | `tools/art_pipeline/`, task ids in `assets_raw/tuskroot/*/provenance.json` | Meshy ToS at generation date; derived from owner's own concept art | Paid credits | `assets/creatures/tetherbound/tuskroot/models/creature_tuskroot_lod0.glb` | First of the R0.6 wild roster, candidate a. Where the clean-before-texture ordering bug was found and fixed: R0.5 had textured every winner's raw candidate directly, skipping `cleanup_mesh.py`'s voxel remesh, so all ten R0.5 outputs carried non-manifold geometry unsafe to rig and had to be re-cleaned and re-retextured from R0.6 onward (see `ralph/DONE.md`'s R0.6 Tuskroot entry). Three eye-guard rectangles in `grade.py` — the same eye duplicated across UV islands by the voxel remesh, not three distinct eyes. Repointed `species.json`'s pre-existing Plumberry placeholder rather than adding a new entry. |
| Meadowhart — production creature | Generated (Meshy multi-image-to-3D + retexture) from the owner's Ground Sheet B; cleaned, rigged and animated in-pipeline | `tools/art_pipeline/`, task ids in `assets_raw/meadowhart/*/provenance.json` | Meshy ToS at generation date; derived from owner's own concept art | Paid credits | `assets/creatures/tetherbound/meadowhart/models/creature_meadowhart_lod0.glb` | Second of the R0.6 wild roster, candidate a. First `species.json` entry written from scratch in this batch rather than repointing an existing placeholder. Two eye-guard rectangles. |
| Burrowback — production creature | Generated (Meshy multi-image-to-3D + retexture) from the owner's Ground Sheet B; cleaned, rigged and animated in-pipeline | `tools/art_pipeline/`, task ids in `assets_raw/burrowback/*/provenance.json` | Meshy ToS at generation date; derived from owner's own concept art | Paid credits | `assets/creatures/tetherbound/burrowback/models/creature_burrowback_lod0.glb` | Third of the R0.6 wild roster, candidate c. Only one eye could be guarded with confidence in `grade.py` — the badger's dense stone/moss camouflage pattern made a second eye unidentifiable against ordinary fur with any confidence, so only the confirmed one is guarded rather than guessing a second rectangle. |
| Paddlenewt — production creature | Generated (Meshy multi-image-to-3D + retexture) from the owner's Water Sheet; cleaned, rigged and animated in-pipeline | `tools/art_pipeline/`, task ids in `assets_raw/paddlenewt/*/provenance.json` | Meshy ToS at generation date; derived from owner's own concept art | Paid credits | `assets/creatures/tetherbound/paddlenewt/models/creature_paddlenewt_lod0.glb` | Fourth of the R0.6 wild roster and the first from the Water Sheet rather than Ground Sheet B, candidate a. That sheet has no ROLE/STRENGTHS table like the Ground trio — its `species.json` stats are read from the subtitle ("Quick Swimmer & Skirmisher") and build notes instead. Five eye-guard rectangles, the same duplicated-eye-across-UV-islands pattern Galewisp (six) and Tuskroot (three) already showed, confirmed by a full-atlas quadrant scan rather than stopping at the first eye found. R0.4's report flagged a cosmetic tail-shape defect on this winner (short/abrupt rather than the canon's long taper); documented in `species.json`'s `_comment_art` rather than sculpt-fixed, since it is not a hard fail. |
| Mosshell — production creature | Generated (Meshy multi-image-to-3D + retexture) from the owner's Water Sheet; cleaned, rigged and animated in-pipeline | `tools/art_pipeline/`, task ids in `assets_raw/mosshell/*/provenance.json` | Meshy ToS at generation date; derived from owner's own concept art | Paid credits | `assets/creatures/tetherbound/mosshell/models/creature_mosshell_lod0.glb` | Fifth of the R0.6 wild roster, candidate b, Water Sheet subtitle "Steady Tank & Shelter". Four eye-guard rectangles, same duplicated-across-UV-islands pattern; a pair of uniform amber blobs (no pupil, matching ordinary scale/wart spots) and a tan almond/slit shape (plausibly a closed eyelid, not confident enough to guard) were checked and rejected. R0.4's report flagged a topology check — a possible thin hindquarters protrusion — that could not be visually confirmed or ruled out: this container has no `libEGL.so.1`, so `turntable.py` cannot render a frame. Documented in `species.json`'s `_comment_art` for whoever next has a rendered frame to check. |
| Brooktail — production creature | Generated (Meshy multi-image-to-3D + retexture) from the owner's Water Sheet; cleaned, rigged and animated in-pipeline | `tools/art_pipeline/`, task ids in `assets_raw/brooktail/*/provenance.json` | Meshy ToS at generation date; derived from owner's own concept art | Paid credits | `assets/creatures/tetherbound/brooktail/models/creature_brooktail_lod0.glb` | Sixth and last of the R0.6 wild quadrupeds, candidate a, Water Sheet subtitle "Resourceful Diver & Helper". **The one HARD FAIL of the ten R0.4 winners**: every candidate is missing the canon's broad flat scaled paddle tail, giving a round tapering tail instead — R0.4's report itself instructs shipping it forward flagged rather than blocking or re-rolling, since a real sculpting pass is separate future work. Also the first species where `rig_quadruped.py`'s automatic weighting left vertices genuinely unweighted (35 of 14,034, 0.25%, scattered across the body rather than tail-concentrated) rather than the usual post-retexture noise being silently absorbed. Six eye-guard rectangles. Both defects documented in `species.json`'s `_comment_art` rather than fixed. |
| Galecrest — production creature | Generated (Meshy multi-image-to-3D + retexture) from the owner's Air Sheet; cleaned, rigged and animated in-pipeline | `tools/art_pipeline/`, task ids in `assets_raw/galecrest/*/provenance.json` | Meshy ToS at generation date; derived from owner's own concept art | Paid credits | `assets/creatures/tetherbound/galecrest/models/creature_galecrest_lod0.glb` | First bird species and first real use of `rig_bird.py` via `finish.py rig --kind bird`, candidate a, Air Sheet subtitle "Sky Soarer & Aerial Striker". Also fixed the bug that had been blocking all four bird species: `finish.py`'s `rig` command called `animate_quadruped.py` unconditionally after rigging, which for a bird would have silently re-detected `rig_bird.py`'s already-animated output as a glider (their bone names deliberately overlap) and overwritten its bird-specific animation with generic glider animation. `rig_bird.py` authors all six clips itself; `finish.py` now skips the redundant call for `--kind bird`. rig_report.json: 19 bones, 14,004 vertices, 6 unweighted (0.04%). Two eye-guard rectangles (a pair of glossy black hooked-beak shapes were checked and rejected — no iris ring, no pupil). Height corrected from a wrong first guess (1.85m) to R0.7's fixed 2.00m in a follow-up commit before the branch shipped. |
| Duskhush — production creature | Generated (Meshy multi-image-to-3D + retexture) from the owner's Air Sheet; cleaned, rigged and animated in-pipeline | `tools/art_pipeline/`, task ids in `assets_raw/duskhush/*/provenance.json` | Meshy ToS at generation date; derived from owner's own concept art | Paid credits | `assets/creatures/tetherbound/duskhush/models/creature_duskhush_lod0.glb` | Second bird species, candidate a, Air Sheet subtitle "Silent Watcher & Night Scout". Same `--kind bird` path proved on Galecrest, no code changes needed. rig_report.json: 19 bones, 14,006 vertices, 0 unweighted — cleaner than Galecrest's. Four eye-guard rectangles, the clearest eye signature of any species so far (gold ring, blue/teal inner ring, black pupil, white catchlight, matching the sheet's own "large gold-ringed eyes" brief); one dark round blob near a nostril checked and rejected. |
| Pipwing — production creature | Generated (Meshy multi-image-to-3D + retexture) from the owner's Air Sheet; cleaned, rigged and animated in-pipeline | `tools/art_pipeline/`, task ids in `assets_raw/pipwing/*/provenance.json` | Meshy ToS at generation date; derived from owner's own concept art | Paid credits | `assets/creatures/tetherbound/pipwing/models/creature_pipwing_lod0.glb` | Third bird species, candidate b, Air Sheet subtitle "Zippy Flier & Spotter". Same `--kind bird` path, no code changes needed for the third time. rig_report.json: 19 bones, 14,002 vertices, 0 unweighted. Four eye-guard rectangles — the sheet's "oversized teal eyes" dominate several UV islands on a creature this small; one ambiguous dark shape beside a confirmed eye checked and rejected as a likely shading/seam artifact. Smallest and shortest creature in the roster (1.20m). |
| Reedwing — production creature | Generated (Meshy multi-image-to-3D + retexture) from the owner's Water Sheet; cleaned, rigged and animated in-pipeline | `tools/art_pipeline/`, task ids in `assets_raw/reedwing/*/provenance.json` | Meshy ToS at generation date; derived from owner's own concept art | Paid credits | `assets/creatures/tetherbound/reedwing/models/creature_reedwing_lod0.glb` | Fourth and last bird species, finishing R0.6 entirely — every Meadows wild creature now has real production art. Candidate a, Water Sheet subtitle "Swift Glider & Messenger". Same `--kind bird` path, no code changes needed for the fourth time. rig_report.json: 19 bones, 14,006 vertices, 0 unweighted. Five eye-guard rectangles — a different eye style from the other three birds (dark pupil-only mass with a soft catchlight, not a bright iris ring, matching a waterfowl's eye); a beak-tip wedge and a glossy feather highlight checked and rejected. Canonically Water/Air per the roster canon but filed `type: water` in `species.json` since the schema takes one type. |
| Meadows art pack — 11 reference sheets and boards | Owner-supplied (AI-generated) | Provided by owner, commit `408c757` | Owner's own. Reference only; excluded from the export | No | `docs/art/reference/0*.png`, `1*.png` | Moved from the repository root. One byte-identical duplicate of sheet `04` deleted. Canon and precedence recorded in `docs/art/REFERENCE_CANON.md`. |
| Meadows Wild Canon Pack — 5 sheets and 4 documents | Owner-supplied (AI-generated) | Provided by owner as `Tetherbound_Meadows_Wild_Canon_Pack.zip` | Owner's own. Reference only; excluded from the export | No | `docs/art/reference/wild/*.png`, `docs/art/wild/*.md` | Unpacked, images and markdown separated. Authoritative for the twelve wild species and Tuskroot; accepted in `docs/decisions/D13`. `05_Meadows_Wild_Scale_Palette_Implementation_Guide.png` is misnamed — it is Ground Sheet B — and the file name is left as the owner wrote it, with the correction recorded in `docs/art/REFERENCE_CANON.md` and `tools/art_pipeline/views.json`. |
| Palworld screenshots ×5 | Pocketpair | Owner-supplied screenshots | Reference only, not shipped | No | `docs/reference/palworld-0*.jpg` | None |
| Trainer character (`Ranger`) | KayKit / Kay Lousberg | [KayKit Adventurers](https://kaylousberg.itch.io/kaykit-adventurers) | CC0 1.0 | No | `assets/characters/Ranger.glb` | Renamed from the pack layout. Ships with no AnimationPlayer; one is created at load and the two rigs' libraries are merged onto it (`trainer_model.gd`). |
| Trainer animation rig — General | KayKit / Kay Lousberg | [KayKit Adventurers](https://kaylousberg.itch.io/kaykit-adventurers) | CC0 1.0 | No | `assets/characters/Rig_Medium_General.glb` | Clip source only; its mesh is never instanced. |
| Trainer animation rig — MovementBasic | KayKit / Kay Lousberg | [KayKit Adventurers](https://kaylousberg.itch.io/kaykit-adventurers) | CC0 1.0 | No | `assets/characters/Rig_Medium_MovementBasic.glb` | Clip source only; its mesh is never instanced. |
| Bramblit (Triceratops) | Quaternius | [Ultimate Animated Dinosaurs](https://quaternius.com/packs/ultimateanimateddinosaurs.html) | CC0 1.0 | No | `assets/creatures/bramblit.fbx` | Renamed. Imported by Godot 4.7's native FBX path with its clips. Scaled at runtime to the gameplay collider. |
| Meadow Hopper (Frog) | Quaternius | [Animated Animals](https://quaternius.com/packs/animatedanimals.html) | CC0 1.0 | No | `assets/creatures/meadow_hopper.fbx` | Renamed. Scaled at runtime. |
| Thornback (Stegosaurus) | Quaternius | [Ultimate Animated Dinosaurs](https://quaternius.com/packs/ultimateanimateddinosaurs.html) | CC0 1.0 | No | `assets/creatures/thornback.fbx` | Renamed. Scaled at runtime. |
| Plumberry Plains Vol. 2 — 5 characters | GTB (owner-supplied) | Supplied by owner via Google Drive | Free for unlimited personal **and commercial** use. May NOT resell, redistribute or repackage the raw asset files, mint as NFTs, or train AI on them. | No | `assets/creatures/plumberry/*.glb` | Curated 5 of 10. glTF with 26 baked clips each, embedded. No skinning — transform-node animation on rigid parts — so they import and play with no retargeting. |
| Stylized Nature MegaKit — 40 models | Quaternius | [Stylized Nature MegaKit](https://quaternius.com/packs/stylizednaturemegakit.html) | CC0 1.0 | No | `assets/environment/stylized_nature/*.gltf` | Trees (4 species), bushes, ferns, grass, flowers, rocks, path stones. Textured with normal maps. `Bush_Common` ships the crimson autumn leaf texture and is re-pointed at the green one per layer (`vegetation.json` `retexture`). |
| Kloppenheim 05 Pure Sky (HDRI) | Greg Zaal / Poly Haven | [Poly Haven](https://polyhaven.com/a/kloppenheim_05_puresky) | CC0 1.0 | No | `assets/environment/sky/day.hdr` | 2K Radiance HDR. Midday, partly cloudy. Sky-only ("puresky"), so it works as a sky dome with no ground half. |
| Table Mountain 1 Pure Sky (HDRI) | Greg Zaal / Poly Haven | [Poly Haven](https://polyhaven.com/a/table_mountain_1_puresky) | CC0 1.0 | No | `assets/environment/sky/golden.hdr` | 2K Radiance HDR. Sunset, partly cloudy. Drives the `golden` time of day. |
| Nature Kit — 27 models | Kenney | [Nature Kit](https://kenney.nl/assets/nature-kit) | CC0 1.0 | No | `assets/environment/nature/*.glb` | Superseded by the Quaternius kit above; retained for the flower and log shapes. Retinted at run time by material name (`vegetation.gd`). |
| Farm Buildings — 13 models | Quaternius | [Farm Buildings](https://quaternius.com/packs/farmbuildings.html) | CC0 1.0 | No | **retired by `EV6`** (was `assets/buildings/quaternius_farm/*.obj`, removed from the tree; git history keeps it) | OBJ+MTL (flat stylized materials, no textures). Barns, silo house, chicken coop, well, windmills, water tower, two fences. Was the settlement's structures until D24 named the Medieval Village MegaKit the one civilian family and `EV6` rebuilt every structure on it. |
| Ultimate Furniture — 13 of 20 models | Quaternius | [Ultimate Furniture](https://quaternius.com/packs/ultimatefurniture.html) | CC0 1.0 | No | `assets/props/quaternius_furniture/*.obj` | OBJ+MTL. Beds, table+chairs, bookcases, closets, desk, nightstand, door — Grandpa's interior and the camp bedroll. Curated: sofas/office chair left out as too modern. `Bookcase_Books.obj`'s `mtllib` line corrected to its sibling MTL's actual filename (the pack ships it pointing at a renamed file). All three packs are authored at roughly 2× real-world scale; placement code carries the correction. |
| Survival pack — 5 of 25 models | Quaternius | [Survival Pack](https://quaternius.com/packs/survival.html) | CC0 1.0 | No | `assets/props/quaternius_survival/*.obj` | OBJ+MTL. Bonfire + its fire mesh (the campfire), backpack, axe, knife. Curated: the pack's modern items (batteries, flare gun, first-aid) left out of a medieval-rural biome. |
| Grass005 — colour, normal, roughness | ambientCG | [ambientCG Grass005](https://ambientcg.com/view?id=Grass005) | CC0 1.0 | No | `assets/environment/terrain/Grass005_*.jpg` | 2K JPG downsampled to 1024² to match the rest of the array — Terrain3D builds one Texture2DArray and rejects the whole set on a size mismatch. Base ground layer. |
| Grass004 — colour, normal, roughness | ambientCG | [ambientCG Grass004](https://ambientcg.com/view?id=Grass004) | CC0 1.0 | No | `assets/environment/terrain/Grass004_*.jpg` | 2K JPG. Was the base ground layer; kept, unused, as the dry-grass variant for a future second biome pass. |
| Ground003 — colour, normal, roughness | ambientCG | [ambientCG Ground003](https://ambientcg.com/view?id=Ground003) | CC0 1.0 | No | `assets/environment/terrain/Ground003_*.jpg` | 2K JPG. Soil/dirt layer (the hillside slope band). **`Color.jpg` locally edited by `EV4-hillside-seam-remainder-2`** (CC0 permits derivatives): the photo's own raw saturation (mean 0.45) measured ~4.5x every sibling ground texture (Ground030 0.099, Rock030 0.126) — a weedy-lawn photo with green grass tufts and generally oversaturated colour baked in across nearly its whole area, not a narrow patch. `tools/art_pipeline/desaturate_soil_texture.py` blends it toward its own local luminance (feathered, stronger pull in the green/grass-tuft hue range, milder everywhere else — same technique as Ground030's own moss fix, scaled to this photo's broader defect), taking mean saturation to 0.17. `Normal.jpg`/`Roughness.jpg` untouched. |
| Rock030 — colour, normal, roughness | ambientCG | [ambientCG Rock030](https://ambientcg.com/view?id=Rock030) | CC0 1.0 | No | `assets/environment/terrain/Rock030_*.jpg` | 2K JPG. Slope layer, blended by the auto shader. **`Color.jpg` locally edited by `EV4-hillside-seam-remainder-3`** (CC0 permits derivatives): mean value was 0.312, dark enough that this scene's grazing sun plus the material's own relief/AO crushed it toward black. `tools/art_pipeline/brighten_rock_texture.py` applies a screen-blend brightness lift (amount 0.18) in place — hue holds exactly (a uniform per-channel transform), value rises to 0.436, saturation falls to 0.073 as a side effect. **Also edited by `OF11` round 5** (same CC0 permission): four blind critique rounds accepted the rebuilt landform as sculpted but kept naming the material at close range — "a fine, even, low-contrast speckle with no strata, no cracks, no distinct rock chunks or slabs" — and the photo measured flattest of the four ground textures (value std 0.058 against Ground003's 0.101), partly because a screen blend raises darks more than lights and so reduces contrast by construction. `tools/art_pipeline/contrast_rock_texture.py` applies a large-radius local-contrast (unsharp) pass plus a gentle S-curve to LUMINANCE only, written back as one per-pixel scale on all three channels: value std 0.058 → 0.135 (+132%), mean value held at 0.427 so the brightness fix above is not undone, hue 47.01° → 46.89° and saturation 0.073 → 0.073 both unchanged. `Normal.jpg`/`Roughness.jpg` untouched. |
| Ground030 — colour, normal | ambientCG | [ambientCG Ground030](https://ambientcg.com/view?id=Ground030) | CC0 1.0 | No | `assets/environment/terrain/Ground030_*.jpg` | 1K JPG (downloaded at the 1K tier directly, matching the rest of the array at 1024²). Dirt/pebble pathway texture, already breaking into grass at its own edges. Sourced for `EV4`'s "paths as a control-map material, not a colour-map tint" fix; wired into `terrain_playground.json` as the `path` texture entry (EV4 round 5) — see `DONE.md`. **`Color.jpg` locally edited by `EV4-textures`** (CC0 permits derivatives): the photo's own moss patches measured too saturated to a blind critic; the largest connected moss regions (hue 70-170°, sat>0.20, feathered mask) are blended 85% toward their own luminance in place, taking moss saturation from ~0.36 to ~0.16 — same photo, same pebble/moss detail, just less garish. **Edited again by `EV4-textures-remainder`** (`tools/art_pipeline/reshape_moss_blobs.py`): the ~52 largest moss blobs (still round/similarly-sized after the desaturation fix) are stretched along a random per-blob axis, narrowed perpendicular, edge-roughened and about 60% given an asymmetric taper, so they read as varied streaks/wisps rather than stamped circles — a blind critic confirmed the hard-edge "decal" read is gone but still called shape variety limited ("one repeated template... resized"); accepted as a genuine partial improvement, not a full pass, per its own item text ("low priority, a finish question"). `Normal.jpg` untouched by either edit. |
| Ground037 — colour, normal | ambientCG | [ambientCG Ground037](https://ambientcg.com/view?id=Ground037) | CC0 1.0 | No | `assets/environment/terrain/Ground037_*.jpg` | 1K JPG, same tier as above. Damp, mossy forest-floor grass — bible sec8's "Deep Grass / Forest Floor" layer. Sourced alongside `Ground030`; still unused, see the `EV4-hillside-seam` entry in `BACKLOG.md` for whoever wires it in with `EV3`. |
| UI Pack (RPG Expansion) — 97 files | Kenney | [UI Pack — RPG Expansion](https://kenney.nl/assets/ui-pack-rpg-expansion) | CC0 1.0 | No | `assets_raw/vendor/kenney_ui-pack-rpg-expansion/` | Staged raw per `EV1`, not yet imported into a scene. Panels/buttons/sliders for `EV9`'s HUD rebuild. |
| UI Pack — 1343 files | Kenney | [UI Pack](https://kenney.nl/assets/ui-pack) | CC0 1.0 | No | `assets_raw/vendor/kenney_ui-pack/` | Staged raw per `EV1`. Broad source of panel corners, tabs, bars, sliders, toggles for `EV9`. |
| Input Prompts — 4752 files | Kenney | [Input Prompts](https://kenney.nl/assets/input-prompts) | CC0 1.0 | No | `assets_raw/vendor/kenney_input-prompts/` | Staged raw per `EV1`. Xbox/PlayStation/Switch/Steam Deck/keyboard+mouse/touch glyphs, to replace improvised text prompts (`EV9`). |
| Game Icons — 443 files | Kenney | [Game Icons](https://kenney.nl/assets/game-icons) | CC0 1.0 | No | `assets_raw/vendor/kenney_game-icons/` | Staged raw per `EV1`. Temporary/production icons pending Tetherbound-specific ones for Orb, creature types, bond, Tether Rift, Sigils. |
| Game Icons Expansion — 832 files | Kenney | [Game Icons Expansion](https://kenney.nl/assets/game-icons-expansion) | CC0 1.0 | No | `assets_raw/vendor/kenney_game-icons-expansion/` | Staged raw per `EV1`, companion to the base Game Icons pack above. |
| Medieval Village MegaKit — 176 models (375 files) | Quaternius | [Medieval Village MegaKit](https://quaternius.itch.io/medieval-village-megakit) | CC0 1.0 | No | `assets_raw/vendor/quaternius_medieval-village-megakit/` | Staged raw, clearing `EV1-remainder`'s itch.io block (owner downloaded and supplied via Google Drive, 2026-08-11). glTF export only — self-contained `.gltf`+`.bin`+textures per model, same format `EV2`'s Stylized Nature MegaKit already uses — FBX, OBJ and the pack's separate `Textures/` folder left out as redundant duplicates. For `EV6`'s settlement rebuild. `EV6` curated 64 of 176 models (plaster + uneven-brick wall families, round-tile roofs and gable fronts, timber corners, doors/windows/shutters, chimneys, fences, exterior borders, vines, wagon) plus the pack's 22 shared trim textures into `assets/buildings/quaternius_medieval/`; recipes in `data/config/building_prefabs.json` compose them into the settlement's prefabs. |
| Fantasy Props MegaKit — 94 models (202 files) | Quaternius | [Fantasy Props MegaKit](https://quaternius.itch.io/fantasy-props-megakit) | CC0 1.0 | No | `assets_raw/vendor/quaternius_fantasy-props-megakit/` | Staged raw, same unblock and same glTF-only rationale as the Village kit above. |
| Stylized Nature MegaKit — full pack (270 files) | Quaternius | [Stylized Nature MegaKit](https://quaternius.itch.io/stylized-nature-megakit) | CC0 1.0 | No | `assets_raw/vendor/quaternius_stylized-nature-megakit/` | Staged raw 2026-08-13, clearing `ralph/BACKLOG.md`'s standing nature-pack block on `EV2-landmark-ceiling`, `EV5-remainder-2`'s third waterside plant, and `OF12-remainder`'s ground-cover variety gap (owner downloaded the itch.io Standard-tier zip and supplied it via Google Drive, same path as the Village/Fantasy Props kits). glTF export only, same rationale as those two — FBX, OBJ and the separate `Textures/` folder left out as redundant duplicates. Superset of the curated 40-model subset already in `assets/environment/stylized_nature/*.gltf` (row above); whoever picks up the blocked items should curate further specimens (broader-canopy hero trees, waterside species, ground-cover variety) from here into that in-use directory. |
| Fantasy Props MegaKit — 10 of 94 models, in use | Quaternius | [Fantasy Props MegaKit](https://quaternius.itch.io/fantasy-props-megakit) | CC0 1.0 | No | `assets/props/quaternius_fantasy/*.gltf` | `EV7`'s first slice: `Anvil_Log`, `Workbench`, `Whetstone`, `Crate_Wooden`, `Pickaxe_Bronze` (the `work_area` cluster) and `Barrel_Apples`, `FarmCrate_Apple`, `FarmCrate_Carrot`, `Bucket_Wooden_1` (the `farmhouse_yard` cluster), plus the pack's 13 shared trim textures. Placed by `scripts/world/props.gd` from `data/config/props.json`. `bridge repair site`/`quarry station`/`trainer camp` (bible sec2 P3's other three named clusters) need geography that doesn't exist yet (a bridge, a quarry site) and are left for `EV7-remainder`. `R2.8` adds `Bed_Twin1` (the build catalogue's `creature_bed` entry) — its own trim textures (`T_Trim_Furniture_*`, `T_Trim_Metal_*`, `T_Trim_Cloth_*`) were already staged for `Workbench`, so only the model's own `.gltf`+`.bin` were new. Deliberately a different specific mesh from `assets/props/quaternius_furniture/BedTwin.obj` (a separate Quaternius pack already used for the player's own bedroll and Grandpa's bed), so a placed creature bed doesn't read as a duplicate of furniture the player already has. |
| Fantasy Props MegaKit — 5 more of 94 models, in use (`EV7-remainder`) | Quaternius | [Fantasy Props MegaKit](https://quaternius.itch.io/fantasy-props-megakit) | CC0 1.0 | No | `assets/props/quaternius_fantasy/*.gltf` | `EV7-remainder`'s two newly-buildable clusters, once `EV6-remainder` gave the world a real footbridge and route 2 (the practice-meadow path) proved startable without new geography: `Bag`, `Bench`, `Barrel` (the `trainer_camp` cluster, plus the already-curated `Crate_Wooden`) and `Rope_1`, `Axe_Bronze` (the `bridge_repair_site` cluster, plus the already-curated `Crate_Wooden`/`Bucket_Wooden_1`). No new textures — all five reuse the pack's already-staged shared trim textures (`T_Trim_Furniture_*`, `T_Trim_Props_*`, `T_Trim_Cloth_*`). `quarry_station`, the bible's fifth named cluster, is still left open: no quarry exists anywhere in the world, confirmed again here, and building one is out of scope for a prop-placement item. `EV7-clusters-fix` drops `Bench` from `trainer_camp` (see that row below) — still staged in the raw pack, no longer placed anywhere. |
| Fantasy Props MegaKit — same 5, re-placed (`EV7-clusters-fix`) | Quaternius | [Fantasy Props MegaKit](https://quaternius.itch.io/fantasy-props-megakit) | CC0 1.0 | No | `assets/props/quaternius_fantasy/*.gltf` | No new models or textures — a placement-only fix round after a genuine blind critic failed `trainer_camp` and found real composition defects in `bridge_repair_site` (`ralph/DONE.md`). `trainer_camp` drops `Bench` (a trestle table with nothing ever actually resting on it — `props.gd` places everything flush to the ground with no way to rest one prop on another) and re-spaces `Bag`/`Barrel`/`Crate_Wooden`. `bridge_repair_site` moves to hug the footbridge's real west landing and gives `Axe_Bronze` a genuine lean via two new optional placement fields on `scripts/world/props.gd`'s recipe (`pitch_deg`/`roll_deg`, `sink_m`) — see that file's own comments. |
| Fantasy Props MegaKit — 1 more of 94 models, in use (`EV6-remainder-polish`) | Quaternius | [Fantasy Props MegaKit](https://quaternius.itch.io/fantasy-props-megakit) | CC0 1.0 | No | `assets/props/quaternius_fantasy/Cabinet.gltf` | `EV6-remainder-polish`: `Cabinet` (panelled doors, metal handles) replaces the Furniture pack's `ShortCloset` in Grandpa's house — the furniture fix's own confirming blind pass named that piece "a featureless flat slab", a geometry/detail limit of the source mesh rather than a colour bug, and the swap-for-a-more-detailed-kit-piece option was the entry's own first remedy. No new textures — reuses the pack's already-staged `T_Trim_Furniture_*`/`T_Trim_Metal_*` trim maps. Real-metre scale (1.36×1.00×0.36m), placed at scale 1.0 by `grandpa_house.gd::_furnish`'s new glTF branch. |
| Medieval Village MegaKit — 5 of 176 models, in use | Quaternius | [Medieval Village MegaKit](https://quaternius.itch.io/medieval-village-megakit) | CC0 1.0 | No | `assets/buildings/quaternius_medieval/*.gltf` | `R2.6`'s first build-piece catalogue entries: `Wall_Plaster_Straight`, `Floor_UnevenBrick`, `Door_1_Flat`, `Roof_RoundTile_2x1`, `Prop_WoodenFence_Single`, plus their 12 shared trim/plaster/brick/tile textures. Same Plaster/UnevenBrick/RoundTile family `EV6` is independently curating for the settlement itself (`ralph/EV6`, in flight) — chosen to match rather than introduce a second building vocabulary under D24's one-family rule. `data/items/buildables.json` references these by path; `scripts/build/build_piece.gd` places them. |
| Kenney Future / Kenney Future Narrow fonts | Kenney | kenney.nl UI Pack (already vendored in `assets_raw/vendor/kenney_ui-pack`) | CC0 1.0 | No | `assets/ui/fonts/` | None. The UI token module (`scripts/ui/ui_tokens.gd`, D28-a) and both new Theme resources point at these. |
| Map icons — 12 files | Kenney | Game Icons + Game Icons Expansion (already vendored raw in `assets_raw/vendor/kenney_game-icons/` and `assets_raw/vendor/kenney_game-icons-expansion/`) | CC0 1.0 | No | `assets/ui/icons/map/*.png` | D33: the one icon vocabulary the minimap and full map both key landmark/marker `icon` fields into. Curated from the White/2x (100x100) silhouette variant and renamed per the landmark vocabulary: `home.png`=`home` (exact match, Grandpa's House), `flag.png`=`village` (no buildings/settlement icon exists in either pack; a flag is the closest "marked settlement" silhouette), `exit.png`=`gate` (a door-with-arrow reads as a threshold/passage), `star.png`=`camp` (no tent/campfire icon exists; a POI star is the closest generic "notable spot" marker), `trophy.png`=`stronghold` (no castle/tower icon exists; a trophy is the closest "important destination" silhouette), `barsVertical.png`=`bridge` (three vertical bars stand in for a span's support piers; no literal bridge icon exists), `cloud.png`=`cave` (no rock/cave-mouth icon exists; a cloud is the closest rounded-mass silhouette available — a placeholder, not a good match, flagged for a real replacement), `signal3.png`=`relay` (signal-strength bars, a direct fit for the relay apparatus), `contrast.png`=`rift` (a half-light/half-dark disc stands in for the Tether Rift; no portal/lightning icon exists), `diamond.png`=`objective` (exact fit, matches the HUD's own objective pointer shape), `question.png`=`question` (exact match, the undiscovered-silhouette fallback), `shoppingBasket.png`=`death_satchel` (R3.3, §22's death satchel; no bag/sack/pouch icon exists in either pack — checked by filename across both — a basket is the closest "carried loot container" silhouette). Every renamed file is otherwise byte-identical to its source PNG. |
| Modular Medieval Building Pack — 30 models (60 files) | Quaternius | [Modular Medieval Buildings](https://quaternius.com/packs/modularmedievalbuildings.html) | CC0 1.0 | No | `assets_raw/vendor/quaternius_modular-medieval-buildings/` | `BG2`: the genuine fortress kit the two staged Village/Fantasy-Props kits (both confirmed civilian-only, `docs/decisions/D28`) don't supply. Same publisher/art language as the already-staged Medieval Village MegaKit (`EV6`'s settlement), so it sits inside D24's one-village-family rule as a fortification extension of that family rather than a second one. Sourced via quaternius.com's own "Just give me the Download" button (a public Google Drive folder, not itch.io — no claim-flow block this time), OBJ+MTL export: flat per-material colours, no texture maps, matching the same no-texture convention already used for the staged Furniture/Survival Quaternius packs. FBX and Blend exports of the same 30 models also exist in the source folder and were left unstaged as redundant duplicates, same rationale `EV1-remainder` used for the Village kit's unused FBX/OBJ/Textures copies. Untextured OBJ+MTL bounding-box spot check (`Tower.obj` 1.13×4.21×1.13, `Wall.obj` 1.54×1.55×0.56, units presumed metres) reads as plausible real-world scale already, unlike the Furniture/Survival packs' documented 2x-scale quirk — worth a quick in-engine confirmation before `OF4-rebuild` commits to it, not re-verified here. Fortress-relevant filenames for `OF4-rebuild`: 13 tower variants (`Tower`, `LargeTower`, `LargeSimpleTower`, `LargeSquareTower`, `LargeSquareTowerBricks`, `PointyTower`, `SimpleTowerBricks`, `Simpletower`, `SmallSquareTower`, `SmallSquareTowerBricks`, `SmallTower`, `Watchtower`, `WatchTowerWRoof`), 7 wall variants including 2 gate/entrance pieces (`Wall`, `WallBricks`, `WallEntrance`, `WallEntranceBricks`, `TallWall`, `TallWallBricks`, `TallWallEntrance`), plus `Bridge`, `Tunnel` (archway), `Well`, `Door`, `Banner`, `Dummy`, `Target`, `TargetWithArrows`, `WindowGothic`, `WindowSquare`. All 30 models show crenellated/battlement tops in the pack's own preview render (`https://quaternius.com/assets/images/fullres/medievalbuildings.jpg`) — genuine fortification vocabulary, not civilian architecture reskinned. |
| Item + shared UI icons (remaining Kenney stand-ins) — 5 files | Kenney | Game Icons + Game Icons Expansion (already vendored raw in `assets_raw/vendor/kenney_game-icons/` and `assets_raw/vendor/kenney_game-icons-expansion/`) | CC0 1.0 | No | `assets/ui/icons/ui/orb_tier_basic.png`, `assets/ui/icons/ui/cat_survival.png`, `assets/ui/icons/ui/cat_crafting.png`, `assets/ui/icons/ui/cat_structures.png`, `assets/ui/icons/ui/cat_furniture.png` | **Supersedes part of the old "Item + shared UI icons — 23 files" row.** A blind visual review of the vertical slice's HUD called the Kenney stand-ins "web glyphs, not items" (wood read as a hamburger menu, axe as a spanner, medicine as a plus). The 18 files that review named directly — all 12 item icons plus `type_ground.png`/`type_water.png`/`type_air.png`/`bond.png`/`move_quick.png`/`move_charged.png` — were replaced by original generated art (see the `tools/gen_item_icons.py` row below) and dropped from this row entirely. These five were out of the replacement task's touched-files list and are still the original Kenney renames, so the honest per-file mapping for them stays on record here: `orb_tier_basic.png` = a second, distinct-path copy of the already-resized (now-original) `orb_basic.png` file, per the original M-G task's own brief — **note this copy is stale**: it still holds the old Kenney `coin` glyph, not the new generated orb art, since this row's files were untouched by the icon-regeneration pass. `cat_survival.png`=`flag` (expansion; a planted-flag camp marker — no campfire/tent icon exists in either pack, the same gap the map icons row already flagged for `camp`), `cat_crafting.png`=`wrench` (reused the old axe item's Kenney source silhouette before that item's own icon was replaced; wrench is a universal tools/crafting glyph, and Kenney's licence permits reuse across renamed copies same as `orb_tier_basic.png` above), `cat_structures.png`=`home` (exact match), `cat_furniture.png`=`figurine` (expansion; a small decorative object is the closest stand-in — no chair/bed icon exists). Unmodified aside from the uniform 100×100→64×64 downsample (no per-file editing, no recolouring). |
| Item + shared UI icons — 18 files, original generated art | Owner-owned (generated in-repo, no external source) | `tools/gen_item_icons.py` | Owner's own | No | `assets/ui/icons/items/wood.png`, `stone.png`, `fiber.png`, `berries.png`, `orb_basic.png`, `potion_small.png`, `axe.png`, `pickaxe.png`, `hammer.png`, `knife.png`, `fishing_rod.png`, `castle_gate_key.png`; `assets/ui/icons/ui/type_ground.png`, `type_water.png`, `type_air.png`, `bond.png`, `move_quick.png`, `move_charged.png` | Replaces this same 18-file subset of the old "Item + shared UI icons — 23 files" Kenney-stand-in row above (its remaining 5 files, still Kenney, are recorded separately, see the row directly above this one). A blind visual review called the Kenney stand-ins "web glyphs, not items" — wood was `barsHorizontal` (a hamburger-menu read), axe was the generic `wrench`, potion was a first-aid `plus`. Per the owner's spec §21, which sanctions "simple generated vector icons for game-specific concepts" (filled silhouette + small cutouts, ~2-3px equivalent stroke, 64×64 source), `tools/gen_item_icons.py` draws each icon from scratch with PIL polygons/ellipses/arcs — no text, no vendored glyph, no photographic source — at 256px supersample and downsamples to 64px with LANCZOS for antialiasing. One coherent language throughout: solid `#F2F5F2` silhouette on transparent, cutouts standing in for an ink line. Per-icon notes from the iteration pass (judged by eye at both native 64px and small-preview size, not just at a glance in isolation): `wood.png` is three log-end discs stacked in a triangle (a side-by-side pair first read as a pair of goggles/eyes); `orb_basic.png` is a sphere with two tilted crossing wrap-bands and a centre diamond, deliberately not a single flat equatorial seam so it reads as the game's own tether-orb language rather than a Poké-ball; `fiber.png` is three tied, curved strands (a concentric-ring coil first read as a target, and a three-loop knot first read as a flower); `axe.png` is a double-bladed (labrys-style) wedge on a haft, mass on both sides of the handle, after a one-sided wedge kept reading as a flag/pennant no matter how its edge was shaped; `pickaxe.png` is a horizontal double-pointed spindle crossing the haft, after a single tapered crescent read as an umbrella and a chevron read as an arrow; `hammer.png` is a plain centred rectangular head, after an off-centre attachment with a dividing line read as a flag; `fishing_rod.png` is a tapered rod polygon (grip to a fine tip) with the grip cap anchored on the rod itself, after an arc+separate-ellipse version left the grip looking like a detached floating ball. `stone.png`, `berries.png`, `potion_small.png`, `knife.png`, `castle_gate_key.png`, `type_ground.png`, `type_air.png`, `bond.png`, `move_quick.png` and `move_charged.png` read correctly on the first pass and needed no rework; `type_water.png`'s highlight cutout was moved from a centred dot (read as a pupil) to a small rim shine. Re-run with `python3 tools/gen_item_icons.py` from the repo root; it overwrites these 18 files in place, same filenames, so `items.json`'s `icon` paths and the UI's direct-by-name lookups are untouched. |
| `orb_greater.png` item icon | Owner-owned (generated in-repo, no external source) | `tools/gen_item_icons.py` | Owner's own | No | `assets/ui/icons/items/orb_greater.png` | R4.9, the orb tier ladder. Extends the same `icon_orb_basic()` silhouette (sphere, two crossing tether-bands, centre gem) with one added outer ring cutout, so the second tier reads as "the same family, reinforced" rather than an unrelated icon — same coherent language the row above this one documents, added to it rather than starting a new one. |
| EV9 HUD status icons — 4 files, plus a lettering style board | Owner-directed (generated via ChatGPT image generation, owner's own account) | Provided by owner via file upload, 2026-08-13 | ChatGPT/OpenAI image-generation ToS at generation date; owner-directed | No | `assets/ui/icons/hud/hp_heart.png`, `stamina_bolt.png`, `creatures_paw.png`, `orb_capture.png`; board at `docs/art/reference/ev9_display_lettering_style_guide.png` | Clears part of `EV9`'s "still open" remainder (`ralph/BACKLOG.md`) — icon glyphs for HP/STA/Creatures/Orbs and a branded display-font reference, flagged there as needing an owner-supplied board before picking one, per `CLAUDE.md`'s asset-generation rule. All 4 icons are real RGBA PNGs with a genuine alpha channel (verified: `getextrema()` returns `(0, 255)`, not a flat opaque background) — confirmed by compositing each over the live HUD panel colour `#17242B` (`scripts/ui/ui_tokens.gd`'s `BG_PANEL`) before acceptance, not just eyeballed on a white canvas. `hp_heart.png`≈`#43C983`/`stamina_bolt.png`≈`#E2A354` (both match `UITokens.HP_GREEN`/`STAMINA_ORANGE` closely); `creatures_paw.png`≈`#F1F4F1` (matches `TEXT_PRIMARY` `#F2F5F2`, correct per spec — reads pale-on-white but strong on the dark panel, confirmed by the same composite check); `orb_capture.png` is full-colour (gold/teal-blue gem, not a single flat tint). Two rejected drafts along the way, not staged: a first heart came back as a hollow ring with a jagged raster-mask inner edge (not a filled shape); a first orb came back as a near-exact two-tone Poké Ball (belt line, centre button) — both re-prompted. `stamina_bolt.png` also went through one revision (an initial draft carried four extra decorative "motion spark" flecks that broke the single-silhouette read at HUD scale; removed). **Known style gap, deliberately not resolved here:** `hp_heart.png`/`stamina_bolt.png`/`orb_capture.png` share a glossy, gradient-shaded rendering language (chosen and confirmed by the owner after a flagged alternative), while `creatures_paw.png` is a flat matte silhouette from an earlier round — left as a genuine mismatch for `EV9`'s eventual blind-judge pass or `EV10`'s cohesion pass to weigh in on, not silently reconciled by this entry. **2026-08-14: three of four wired in** — `hp_heart`/`stamina_bolt`/`creatures_paw` now mount on `scripts/ui/playground_hud.gd`'s HP bar, stamina arc and creature block respectively, confirmed by direct render (`tools/capture_exploration_hud.gd`). `orb_capture` still has no mount point — no orb-count panel exists anywhere in the current HUD, checked directly rather than assumed — and the lettering style board still has nowhere to apply, since no title/logo screen exists in the game to carry a "TETHERBOUND" wordmark. Both remain open in `BACKLOG.md`'s `EV9` entry. |
| Modular Medieval Building Pack — 21 of 30 models staged, 12 in use | Quaternius | [Modular Medieval Buildings](https://quaternius.com/packs/modularmedievalbuildings.html) | CC0 1.0 | No | `assets/buildings/quaternius_castle/*.obj` | `OF4-rebuild`: curated into `data/config/building_prefabs.json`'s `castle` prefab (113 module placements from 12 distinct pieces), replacing `landmark.gd`'s procedural silhouette. In the recipe: `WallBricks`, `TallWallBricks`, `WallEntranceBricks`, `TallWallEntrance` (walls/gate), `LargeSquareTowerBricks`, `SmallSquareTowerBricks`, `PointyTower`, `LargeTower`, `LargeSimpleTower`, `SmallSquareTower`, `SimpleTowerBricks` (towers), `Banner`. Staged but unused (curated in, tried or kept as options, not placed): `Tower`, `Wall`, `WallEntrance`, `TallWall`, `LargeSquareTower`, `Watchtower`, `WatchTowerWRoof`, `SmallTower`, `Simpletower` — the plain (non-`Bricks`) counterparts of pieces used in their brick-textured form, plus two lookout-tower variants not needed once the four corner towers already gave real height variation. Never staged at all: `Bridge`, `Tunnel`, `Well`, `Door`, `Dummy`, `Target`/`TargetWithArrows`, `WindowGothic`/`WindowSquare` — out of scope for an exterior-only, non-enterable landmark. Loaded as bare `Mesh` resources exactly like the Furniture/Survival packs (`grandpa_house.gd::_furnish`'s pattern) — `building_prefabs.gd` gained an `.obj` fallback alongside its existing `.gltf` path, since this kit (unlike the Medieval Village kit already in this file) has no glTF export. Confirmed in-engine: real-world scale, no correction needed, matching `BG2`'s spot check. One genuine gap found only once loaded: every material in the kit's own MTLs (`LightRock`, `DarkRock`, `Black`, `Celing`, `LightWood`, `Banner`, all numbered variants) exports at the same placeholder grey `Kd 0.64 0.64 0.64` — confirmed directly against the OBJ/MTL text and via a headless material dump, not a rendering bug. Given real per-material colour, not a texture, so `building_prefabs.json`'s existing `retint`-by-surface-name mechanism (already used for `MI_WindowGlass` above) supplies stone/timber/iron/heraldic tones per material-name slot instead. Also found (round 3 of `OF4-rebuild`'s critique pass): `WallEntranceBricks`' and `TallWallEntrance`'s arch cuts sit in different Z-planes (not a matched stacking pair). **Superseded by `OF4-gate-arch` (2026-08-13), which found the real cause and MODIFIED these staged files** — the two entrance modules do carry the same arch profile (identical 15-gon outer plate, opening x=±0.445, apex y=0.98); the differing bounding boxes are only because `WallEntranceBricks`' whole mesh is authored 0.153m off-origin in z. The jagged opening was a **triangulation** defect: this pack authors each wall face as one large n-gon, and on the entrance modules that n-gon is a concave 15/16-sided loop wrapping the doorway, which Godot's OBJ importer turns into a triangle FAN from the first vertex — correct only for convex polygons, so it filled a lopsided wedge of the archway back in with solid geometry. All 21 staged `.obj` files were therefore ear-clipped into triangles in place by `tools/retriangulate_obj.py` (only `f` lines rewritten, re-emitting the same `v/vt/vn` index triples; vertex/UV/normal data and MTL material names byte-identical, so the `retint` keys are unaffected). 304 concave faces across the kit were being mis-fanned, not just the two entrance pieces — tower windows and crenellations were silently affected too. The staged copies now differ from `assets_raw/vendor/`'s pristine originals in exactly that one way; re-run the script after any re-stage. |
| UI audio cues — 9 files | Generated in-repo (`tools/audio/gen_ui_cues.py`, a Python `wave`/`struct` synthesizer script) | Written for this task; not fetched from any third party | CC0-equivalent, owner-owned | No | `assets/ui/audio/*.wav` | Spec §20's small coherent UI audio set: `ui_focus`, `ui_accept`, `ui_cancel`, `ui_tab`, `ui_error`, `aim_enter`, `build_snap`, `build_place`, `capture_success`. 44100 Hz 16-bit mono WAV, short and subtle (25–250 ms, gain trimmed toward -12..-18 dB) — sine/square tones and a filtered-noise sweep, no samples from any vendored pack. `assets_raw/vendor/kenney_ui-pack/Sounds/` (already vendored, row above, CC0) was checked first and has six clicks/switches/taps but no error, rising-aim, or arpeggio-style cue, and mixing recorded clicks with synthesized tones for the rest would read as two different sound languages in one small set — synthesizing the whole set from one script keeps it coherent and gives exact control over the spec'd frequencies/durations. Wired this pass by `scripts/ui/audio_cues.gd` into `game_menu.gd`, `build_menu.gd`, `build_placer.gd` only; `combat_hud.gd`/`capture_reticle.gd`/`playground_hud.gd`/`throw_preview.gd` are owned by another agent this pass and are a known follow-up (`aim_enter` and `capture_success` exist as files but are not yet played by anything). |
| Stylized Nature MegaKit — 2 more of 270, in use (`OF12-remainder` (b)) | Quaternius | [Stylized Nature MegaKit](https://quaternius.itch.io/stylized-nature-megakit) | CC0 1.0 | No | `assets/environment/stylized_nature/Grass_Wide_Short.gltf`, `Grass_Wide_Tall.gltf` | Curated from the full pack (row above) into `data/config/vegetation.json`'s `grass` layer, giving it a second species pair alongside `Grass_Common_Short/Tall`. Genuinely broader footprint (1.04–1.16m vs Common's 0.64–0.90m, measured from each glTF's own `POSITION` bounds), same shared `Grass`/`Grass.png` material as every other model in the layer, so no new texture staged. Not yet rendered or blind-judged — config/asset only. |
| Stylized Nature MegaKit — 1 more of 270, in use (`EV2-landmark-ceiling`) | Quaternius | [Stylized Nature MegaKit](https://quaternius.itch.io/stylized-nature-megakit) | CC0 1.0 | No | `assets/environment/stylized_nature/CherryBlossom_3.gltf`, `Leaves_CherryBlossom_C.png` (bark reuses the already-staged `Bark_NormalTree*.png`) | Replaces `TwistedTree_1` in `vegetation.json`'s `grove` layer (the "broad oak" hero-tree slot). Measured every tree-shaped model in the full pack by reading each glTF's own `POSITION` bounds directly: `CherryBlossom_3` (16.59w × 14.49h × 13.03d, width/height 1.15) is the only form in the whole pack genuinely wider than tall, clearing the bar a blind critic named against all 3 `TwistedTree` forms. A swap, not a 4th slot — `ENVIRONMENT_AND_UI_BIBLE.md` sec10 caps hero trees at 2-3 forms. **2026-08-14: rendered and blind-verified — found and fixed a real bug, plus a confirmed honest limit.** The "retextured green" claim above was wrong when written: the swap was keyed to material name `Leaves_TwistedTree`, but `CherryBlossom_3`'s own leaf material is named `Leaves_CherryBlossom` (confirmed by reading the glTF JSON directly) — so it shipped rendering its native pink/purple blossom colour, caught by a dedicated close-up render (`tools/_capture_grove_closeup.gd`). Fixed in `vegetation.json` by adding matching `Leaves_CherryBlossom` retexture/retint/variant_retint entries; re-rendered and confirmed green. A focused blind pass on the fixed frame then answered the forking-trunk question this row's own text flagged as unconfirmable: the trunk visibly leans/curves (a real win) but does not fork — no candidate in the pack has separate trunk/branch nodes to do so. See `BLOCKED.md`'s open `EV2-landmark-ceiling` entry for the resulting owner question. |
| Stylized Nature MegaKit — 1 more of 270, in use (`EV5-remainder-2`) | Quaternius | [Stylized Nature MegaKit](https://quaternius.itch.io/stylized-nature-megakit) | CC0 1.0 | No | `assets/environment/stylized_nature/Grass_Wheat.gltf` | Curated into `data/config/water.json`'s `marginals` band as the third waterside plant species. No literal sedge/cattail model exists anywhere in the 270-file pack (searched by name and by inspecting candidate glTF mesh/material/image refs); `Grass_Wheat` is the closest available read — a tall, narrow, dense blade-cluster mesh (1.11×1.78×1.01m, 1530 verts) with a distinct seed-head silhouette, genuinely different from `Plant_1_Big`'s broad arching leaf (2.89×3.76×3.13m, 360 verts). Reuses the already-staged `Grass.png` texture (a plain gradient strip fully controlled by the band's own tint), so no new texture file. **2026-08-14: rendered and blind-verified, partial.** A blind pass on `tools/capture_water.gd`'s four frames counted three distinct waterside forms overall (enough to not read as one species repeated) but did not separately identify `Grass_Wheat` as a fourth distinguishable form from the pre-existing reed — plausibly not in frame at any of the four fixed viewpoints (only 8 clumps of 3 across the whole shoreline) or too close in shared tint to read apart at this render distance. See `BACKLOG.md`'s `EV5-remainder-2` entry. |

**Most of the build is CC0 1.0 — but not all of it, and this ledger previously
claimed otherwise.** Corrected 2026-08-12: the Meshy-generated creatures and
characters (Ripplet, Galewisp, Grandpa Elias, and the rest of the roster
generated the same way — see their own rows above) are **not** CC0; they
carry Meshy's ToS at generation date, derived from the owner's own concept
art, and are treated as **All Rights Reserved / proprietary, owner-licensed**
for this project. The **Plumberry Plains Vol. 2** pack (also above) is not
CC0 either — its own row already states its real terms (free for personal and
commercial use, no resale/redistribution/repackaging, no AI training), also
treated as proprietary/owner-licensed rather than open. Everything else in
this table genuinely is CC0 1.0, and for that portion nothing here carries a
legal attribution obligation — though credits should still name Quaternius,
Kay Lousberg, Kenney and ambientCG; it costs a line and it is how these packs
keep existing.

The creature models remain stand-ins for bespoke art (`docs/decisions/D10`), and
the blind critic has said so in two consecutive reviews (`docs/reviews/MA-01`,
`MA-02`). They are in the build because a rigged, animated, correctly-scaled
stand-in proves the systems; they are not the answer to the owner's bar.

## Retired

Kept for audit history. These files are no longer in the repository, but they
were committed at some point and a licence audit should be able to see that.

| Asset | Creator | Source | Licence | Retired because | Was at |
|---|---|---|---|---|---|
| Trainer character (`character-a`) | Kenney | [Blocky Characters](https://kenney.nl/assets/blocky-characters) | CC0 1.0 | Minecraft-proportioned blocky figure in a different art language from the creatures beside it — the critic's lead complaint in `MA-01`. | `assets/characters/trainer.glb` |
| Trainer textures | Kenney | [Blocky Characters](https://kenney.nl/assets/blocky-characters) | CC0 1.0 | Went with the model above. | `assets/characters/Textures/` |
| Meadow Hopper (rabbit) | Google (Poly) | [Poly Pizza](https://poly.pizza/m/dyeBDJxhDwP) | CC-BY 3.0 | Unrigged static scan; could not animate, and CC-BY added an attribution obligation the rest of the roster does not carry. | `assets/creatures/meadow_hopper.glb` |
| Thornback (boar) | Google (Poly) | [Poly Pizza](https://poly.pizza/m/57fSWum6F1P) | CC-BY 3.0 | Unrigged static scan. | `assets/creatures/thornback.glb` |
| Bramblit (fox) | Google (Poly) | [Poly Pizza](https://poly.pizza/m/10u8FYPC5Br) | CC-BY 3.0 | Unrigged static scan. | `assets/creatures/bramblit.glb` |

## Rules

- `docs/` carries a `.gdignore`, so nothing in it is imported by Godot or
  included in an export. The reference images are documentation, not game
  content, and the Windows export preset excludes them explicitly.
- Never assume an asset is redistributable. "Free to download" is not a licence.
- Prefer assets with animations appropriate to their role over assets that look
  better in a still.
- Test scale and materials in-engine before committing to a roster. A pack that
  looks cohesive on a store page can fall apart under one directional light.
- If the game is ever distributed publicly, audit this table and purchase or
  replace anything that needs it.
