# Gate A bootstrap note

Temporary infrastructure only. `tools/gate_a_apply.py` plus `.github/workflows/gate-a-apply.yml` let the ChatGPT GitHub connector apply asserted textual patches on `claude/gate-a-integrated` and dispatch the repository's existing Godot CI. Remove these two helper files after Gate A lands; they do not define gameplay behavior or replace the canonical Gate A prompts.
