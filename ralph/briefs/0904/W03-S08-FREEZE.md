# Lane W03-S08-FREEZE — S08-22 pins solid at (-164.12, -9.13, 4334.56) for 45,000 frames (Phase 0.3 / CL-H14)

Branch: `ralph/W03-S08-FREEZE-0904`. Model tier: root-cause investigation.

**Why.** Band 4 has no evidence because the crossing → Ironwood Grove leg freezes to the centimetre on two independent runs from the same seed (t≈933.8). Read `docs/FINISH_THE_MEADOWS.md` §0.3, closure plan row CL-H14, and `ralph/reports/G3-HARNESS-0904/REPORT.md` §4. Already ruled out, with probes committed: not the walker (`tools/gate_f/probe_ironwood_approach.gd` arrives in 10,792/12,000 frames), not a CarveFailsafe volume (`probe_carve_failsafe_at.gd`). A cold-teleport probe found no ground under the point while a walked body passed it — a Terrain3D streaming/collision hypothesis, explicitly untested. **Fails if** closed by moving the waypoint.

**Owns:** new `tools/gate_f/probe_s08_freeze_*.gd`, `tools/gate_f/probe_ironwood_approach.gd`, `tests/helpers/stick_navigator.gd` only if the cause is provably the navigator (it was exonerated in isolation, so be suspicious of yourself), and — if the cause is the world — the responsible script under `scripts/world/` (likely candidates: Terrain3D collision radius handling in `playground_world.gd`, `world_perimeter.gd`, river/water volumes in `scripts/world/river.gd`, `carve_failsafe`), with a regression test. Do not touch `S08.json`.

**Method.** Reproduce the freeze from the S08 synthetic entry at the cheapest possible cost: skip to step S08-22 (or teleport-then-settle at a point 40 m before the pin and walk the last stretch with the same navigator call), and log per frame: position, velocity, `is_on_floor`, the collision normal(s) and collider names from `get_slide_collision`, the terrain height under the body, the camera position (Terrain3D collision streams around the camera — is the camera far from the body at t≈933?), and the navigator's state. Compare the frozen run's log with the isolated probe's log at the same coordinates — the difference is the cause. Consider: the segment's earlier steps left the camera/rig in a different state than the isolated probe (mounted creature? dismount residue? a follower body wedged against the player? a save/load between steps that re-placed the body before collision existed?). Fix the mechanism, add a test or probe that reproduces the old freeze and shows the fix, then run S08 from its synthetic entry through S08-22 and at least 10 steps past it in logic mode (`tools/gate_f/run_segment.sh`; see `docs/GATE3_EXECUTION_PLAN.md` §4b for the freeze-record trap).

**Acceptance.** S08-22 arrives; the root cause is written in `ralph/reports/W03-S08-FREEZE-0904/REPORT.md`; CL-H14 rows in `docs/CURRENT_STATE.md` and `docs/GATE2_GATE3_CLOSURE_PLAN.md` rewritten with the evidence. If after ~3 hours the cause is still open, checkpoint the probes and the differential logs as committed evidence and report precisely what is ruled out.
