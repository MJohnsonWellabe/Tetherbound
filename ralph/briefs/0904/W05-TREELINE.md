# Lane W05-TREELINE — the tree-lines that read as "one lollipop, repeated" (Phase 1.2), and the one bake window

Branch: `ralph/W05-TREELINE-0904`. Model tier: visual judgement + bake.

**Why.** Two blind judges independently ranked this the top residual visual gap. It was wrongly blocked for weeks by the claim that widening the `trees` layer's corridor-wide `scale_min`/`scale_max` re-rolls the whole corridor's RNG — checked against `scripts/world/scatter_rules.gd::_place_one()` on 2026-09-04: it does not (same draws, same order, only sizes change). Raising an anchor's `count` or adding a per-layer `band_scale` DOES re-roll; avoid those. Read `docs/FINISH_THE_MEADOWS.md` §1.2, closure plan CL-B2 (the tree/terrain bake inputs: grow trees to 12–18 m against the 1.80 m trainer, fix trunk-to-height ratio, ≥3× scale variance per family — grow, never shrink), `docs/specs/BAND1_COMPOSITION_PLAN.md` §3, and `docs/VISUAL_BIBLE.md` §4/§4a.

**You are the only lane allowed to touch bake inputs this round.** Owns: `data/config/vegetation.json` (corridor-wide layers), `data/config/bands/*/vegetation.json`, `data/scatter/playground/` (the re-bake, committed in the SAME commit as the config), `tools/_capture_*` scripts you need, `ralph/reports/W05-TREELINE-0904/`. Do not touch `terrain_playground.json` or `data/terrain/` (a terrain re-bake is 25–40 min and no terrain input is in scope). Do not touch `scatter_rules.gd`.

**Do.** 1) Measure the current tree height distribution against the trainer from a real frame or from the bake (`tools/perf_render_stats.gd`, the bake reader). 2) Widen the `trees` layer's `scale_min`/`scale_max` corridor-wide (and per family where a family has its own layer) so the tallest trees reach 12–18 m and the spread is ≥3×; keep the perf proxy at `band1_open` ≤ 7,500 draws / ≤ 12.0 M primitives (`tools/perf_render_stats.gd`, measure before and after). 3) Re-bake scatter: `godot --headless --path . --script scripts/world/bake_playground_scatter.gd` (check the script's own header for the exact invocation); `tests/test_scatter_perf_budget.gd::test_playground_bake_is_committed_and_fresh` must pass; also `test_scatter_rules.gd`, `test_veg_corridor.gd`, `test_band_vegetation.gd`. 4) Render the stands the judges named — `place2-the-rise`, `place5-bridge-approach`, `comp7-pond-reveal`, `comp8-bridge-rim` via `tools/_capture_band1_composition.gd` / `_capture_band1_places.gd` — before and after, same stands, one contact sheet each. 5) Code-blind judge on the after sheet (do not name what changed), asking specifically whether tree-line silhouette variety reads as improved on those stands, and for any new defect (floating trees, trunks through roads, scale disagreement with the trainer). Iterate at most twice.

**Acceptance.** Judge names silhouette variety improved on the named stands with no new scale defect; perf proxy within budget; bake fresh; `docs/VISUAL_BIBLE.md` §4a gap list and `docs/CURRENT_STATE.md` updated. Report with before/after numbers.
