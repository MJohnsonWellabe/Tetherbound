# Lane W06-FINALE — cut the endgame dialogue hard, put the legendary inside the machine, let the garrison withdraw (Phase 1.3 / CL-W7, CL-O8, CL-G5)

Branch: `ralph/W06-FINALE-0904`. Model tier: writing + staging + visual judgement.

**Three items, one file family (the finale).**

1. **The endgame dialogue is paragraphs** (owner: "the dialogue at the end needs to be cut way down. it was paragraphs with the warden. then paragraphs on the legendary thing"). Measured: `data/dialogue/stronghold.json` is 5,343 characters; `stronghold_warden_challenge` is 8 lines / 1,547 chars, averaging 193 a line, peaking at 379. Cut every conversation in that file to short lines a player reads in one glance on a 1280×800 handheld (target: no line over ~110 characters, the challenge under ~350 characters total, the file under ~2,000). **What must survive:** the Warden believes separation prevents chaos; he confirms the readout rather than denying it; he does not recant when he loses (`docs/specs/MEADOWS_PROGRESSION_SPEC.md` §33 and prompt 69). Say it in a fraction of the words; let the freeing sequence carry its weight visually. Keep every `effect`/flag/`battle:` hook intact. **Portraits:** while you are in this file, re-point each line's `portrait` to its speaker using this exact contract (another lane is producing the files; the names are fixed): the Warden → `res://assets/ui/portraits/warden.png`, grunts → `grunt.png`, officers → `officer.png`, captains → `captain.png`, the player → `trainer.png`, villagers → `villager_male.png` / `villager_female.png`, Grandpa → `grandpa.png`. Do not create the PNGs.
2. **The legendary inside the machine** (owner: "The legendary should be in the machine not in a ring outside the machine"). `scripts/world/stronghold_climax.gd`, `data/config/stronghold_climax.json`, `assets/environment/team_tether/tether_machine.glb` (D49: the machine was generated without its prisoner so the creature can be staged inside it). Stage the legendary inside the machine's chamber/cage volume, visible through the machine's structure, so the reveal reads "the creature is the power source"; the freeing sequence moves it out. Fit by measured mesh bounds, never by transform guesses (D49's lesson).
3. **The Hall's garrison never withdraws** (CL-G5): `scripts/world/stronghold_occupation.gd` places braziers, work-lamps and the checkpoint camp once and never reacts to `legendary_freed`. On that flag (and on load with it set), the garrison dressing goes dark/withdraws so "the world looked changed because of what I did" holds.

**Owns:** `data/dialogue/stronghold.json`, `data/config/{stronghold_climax,stronghold_occupation}.json`, `scripts/world/{stronghold_climax,stronghold_occupation}.gd`, the finale tests (`tests/smoke_gate_e_finale.gd`, `smoke_stronghold*.gd`, `smoke_finale_persistence.gd`, `test_band_dialogue.gd`, any spoiler/dialogue-length test you add), `tools/_capture_stronghold_*.gd`, `ralph/reports/W06-FINALE-0904/`. Do not touch `stronghold.gd`, `meadow_healing.gd`, `rift_collapse.gd`, or any other dialogue file.

**Verify.** Add a test that asserts the length budget on `stronghold.json` (real data, not source grep) and the three canon beats are present as flags/lines; run `smoke_gate_e_finale`, `smoke_stronghold`, `smoke_stronghold_reload`, `smoke_finale_persistence`, `test_band_dialogue`; grep the logs for `^ERROR:`. Render the climax chamber before/after (one xvfb capture each; the stronghold captures under `tools/` show how the chamber is reached) and put the after frame to a code-blind judge asking whether the creature reads as inside/part of the machine and whether the garrison's withdrawal is visible on the exterior stand; commit the sheet and verdict.

**Acceptance.** All four smokes green first attempt; the length test green and seen red on the old file; the judge names the creature as staged inside the machine. Update `docs/CURRENT_STATE.md` rows CL-W7/CL-O8/CL-G5.
