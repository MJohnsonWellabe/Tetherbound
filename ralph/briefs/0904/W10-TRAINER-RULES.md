# Lane W10-TRAINER-RULES — the Challenge button that never disappears, no leaving a trainer fight, and the level gate that refuses in character (CL-W5a, CL-W5b, CL-W4)

Branch: `ralph/W10-TRAINER-RULES-0904`. Model tier: implementation.

Read `docs/owner/OWNER_DIRECTIVES_2026-09-04-B.md` (the amendment A-1, A-2, A-4 win), closure plan rows CL-W4/CL-W5, `scripts/world/trainer_npc.gd`, `scripts/world/encounter_director.gd::can_challenge()`, `scripts/combat/combat_manager.gd::_flee_pressed()`.

1. **CL-W5(a), confirmed defect.** `trainer_npc.gd::_prompt_for()` is unconditional: a beaten trainer still advertises "Challenge %s". After a win the prompt must change to a non-challenge wording. **Trap:** the prompt must never contain "talk" or "choose" — `tests/smoke_opening.gd` finds Grandpa and the starters by those substrings. Use e.g. "Speak with %s" / "Greet %s" (check `prompts()` config in `data/config/` for the existing key style and add a `beaten` key). Test: real `TrainerNpc` fixture, set the defeat flag, assert the prompt text; seen red first.
2. **CL-W5(b), no leaving a trainer fight.** `_flee_pressed()` stays for wild fights; for a fight whose opponent is trainer-owned it is refused with an honest HUD line (`push_world_message`, e.g. "You can't walk away from a challenge.") and the disengage glyph is hidden from the combat legend for trainer fights. The tournament's post-loss retry is untouched. Total-party faint remains the only other exit (the death satchel already handles it — verify with `smoke_trainer_battle` and a faint-out test). Test: real `CombatManager` fixture, wild → flee works; trainer → refused, message pushed.
3. **CL-W4, the diegetic level gate — mechanism only, no gates authored yet.** Add an optional `min_level` (or `challenge_level`) per trainer spec; when the player's highest-level creature (define it: highest in party — record the choice in `docs/decisions/`) is below it, `can_challenge()` is false for a NEW fifth reason and the trainer plays a `too_low` conversation/line instead of the fight — the owner's own wording as the default line: "You're too low level. I'll crush you and send you crying to Grandpa." A too-low player must hear the taunt, not the already-defeated line (the dark-features T1 note in `trainer_npc.gd`). The tracked objective should be able to surface the remedy ("Train to level N") — if `quest_log.gd` can carry a hint cheaply, add it; otherwise put the level in the taunt. Do NOT set `min_level` on any shipped trainer (density lands first — `docs/FINISH_THE_MEADOWS.md` "The dependency to state plainly"); add one line to `docs/specs/MEADOWS_PROGRESSION_SPEC.md` § South Bridge describing the level condition as a diegetic refusal, per closure plan §7.

**Owns:** `scripts/world/trainer_npc.gd`, `scripts/world/encounter_director.gd` (the `can_challenge` reasons only), `scripts/combat/combat_manager.gd` (`_flee_pressed()` and the legend glyph only — another lane adds a VFX hook elsewhere in the file), `data/config/` prompt/trainer schema files, `data/dialogue/` only to add a shared `trainer_too_low` conversation, `docs/specs/MEADOWS_PROGRESSION_SPEC.md` (one paragraph), tests: `test_trainer_npc*.gd`, `test_trainers_data.gd`, `test_encounter_*.gd`, `smoke_trainer_battle.gd`, `smoke_trainer_no_usable_ally.gd`, `smoke_opening.gd`, `smoke_tournament_bracket.gd`. `ralph/reports/W10-TRAINER-RULES-0904/`.

**Acceptance.** All named tests/smokes green first attempt; each new behaviour has a test seen red; `docs/CURRENT_STATE.md` rows updated.
