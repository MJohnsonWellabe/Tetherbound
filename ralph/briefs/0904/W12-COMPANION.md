# Lane W12-COMPANION — companion presence: the deployed creature visibly feels like a companion (addendum §E / owner directive C §5)

Branch: `ralph/W12-COMPANION-0904`. Model tier: design + implementation + visual judgement.

Read `docs/FINISH_THE_MEADOWS_ADDENDUM_2026-09-04.md` §E and `docs/owner/OWNER_DIRECTIVES_2026-09-04-C.md` §5 in full, then `scripts/creatures/follower_creature.gd`, `creature_animator.gd`, `creature_body.gd`, `creature_condition.gd`, the bond model in `bond_milestones.gd`, and `docs/GAMEPLAY_SYSTEMS.md` §Creature care. Inventory what animations the installed creature rigs actually have (`assets/creatures/`; `tools/_capture_creature_animation_world.gd` and `tools/_bones.gd` help) — the layer must use what exists.

**Build a small reusable contextual-reaction layer** for the active/deployed creature: `scripts/creatures/companion_presence.gd`, data-driven by `data/config/companion_presence.json`. States, each with a cooldown and a context guard so they never fire during combat, aiming, dialogue, menus, riding, or while an interact prompt is live: acknowledgment (turns toward / approaches the player after the player stands still N s, or on recall/deploy); post-victory reaction (on a won fight's result beat: a bounce/roar/happy loop for ~2 s); hurt/tired (below 30 % HP or exhausted: slower gait, head-low idle, occasional flinch); camp/rest (near a lit campfire or bed: lies down/idles differently); care response (on feed/heal/revive from the satchel: a short reaction toward the player); bond scaling (higher bond → shorter acknowledgment delay, warmer post-victory reaction; read `bond_nodes()`). Prefer animation names that exist; where a rig lacks one, use procedural motion (a hop, a head turn via bone pose, a scale-pulse) so every species gets every state. Add a hook point for a future `bond_milestone` event (a progression feed is being built by another lane on `Game`; do not build one — poll `party.revision` / bond counters for now and document the hook).

**Owns:** `scripts/creatures/companion_presence.gd` (new), `data/config/companion_presence.json` (new), `scripts/creatures/follower_creature.gd` (attach/tick the layer; keep the diff minimal), `creature_animator.gd` only for a generic "play if exists" helper, ONE hook line where fights resolve (`combat_manager.gd` — another lane edits `_flee_pressed()` and VFX hooks; keep yours to a single call at the result beat) and ONE where feed/heal applies (`scripts/ui/tab_backpack.gd`), `tests/test_companion_presence.gd` (real fixture: each state fires on its trigger, respects its cooldown, and is suppressed in combat/dialogue — seen red first), `tools/_capture_companion_*.gd`, `ralph/reports/W12-COMPANION-0904/`. Do not touch `creature_body.gd` materials, `combat_hud.gd`, or `party_strip.gd`.

**Verify.** `smoke_creature_control`, `smoke_combat`, `smoke_catching`, `smoke_gate_a_rest_torch`, `smoke_riding`, `smoke_gate_b_continuous` (core prefix) green. Capture three moments under xvfb (acknowledgment near the trainer, hurt state, camp state) into one sheet; code-blind judge asks whether the creature reads as reacting to the situation rather than looping a generic idle. Record the addendum §E "done when" evidence honestly.

**Acceptance.** Tests green; smokes green; the judge names at least one moment as clearly contextual; no reaction ever interrupts combat, traversal, prompts or camera. Record any small design calls in `docs/decisions/`.
