# Lane W14-RIDING — riding is unfinished three ways (CL-O3)

Branch: `ralph/W14-RIDING-0904`. Model tier: implementation + one render.

Owner, on hardware: "When you ride your person didn't show up on the creature." "You can't sprint or jump when riding." "Nothing that is rideable should come with a saddle on it. You have to build the saddle and put it on then it visually appears." Read `docs/owner/OWNER_PLAYTEST_2026-09-04.md` OP-0904-3, closure plan CL-O3, D48 (riding is a world verb and costs no stamina), `scripts/world/riding_controller.gd`, `scripts/player/player_controller.gd`, `scripts/player/trainer_model.gd`, `creature_body.gd`'s mount offset, `tests/smoke_riding.gd`, `docs/GAMEPLAY_SYSTEMS.md` §Riding. PR #39 (`05e57a09`) already attaches the generated saddle mesh (`assets/props/riding_saddle/riding_saddle.glb`) around `mount()`/`dismount()` — verify that rule holds (no rideable species carries a saddle at spawn; the saddle appears only once the `saddle` item is built and fitted) and fix if it does not.

**Do.** 1) The rider is visible on the mount: the trainer model reparents/attaches to the mount point, seated (a riding pose — use an existing animation if the rig has one, else a procedural seated pose: hips down, knees bent, hands forward), scaled correctly against the mount, and returns to its normal parent on dismount; the camera rig follows. 2) Sprint and jump while mounted: the mount sprints (faster gait, no player stamina cost per D48 — decide whether the creature's own stamina is spent and record it in `docs/decisions/`) and jumps (a real hop that clears the low fences/rocks a walking player can vault; the rider stays seated through it). 3) Saddle rule verified with a test: spawn each rideable species (Terrapup, Tuskroot, Burrowback — check `data/creatures/species.json` for the `rideable` flag; do not change which species are rideable, a design contract lane owns that) and assert no saddle node exists before fitting and one exists after. 4) `smoke_riding.gd` extended to cover mounted sprint, mounted jump, rider visibility (a real node-presence check on the mount) and dismount restoration.

**Owns:** `scripts/world/riding_controller.gd`, `scripts/player/player_controller.gd` (the mounted-input branch only), `scripts/player/trainer_model.gd` (attach/pose), `data/config/riding.json` (tunables; create if absent), `tests/smoke_riding.gd`, `tests/test_riding_*.gd` (new), `tools/_capture_riding.gd` (new), `ralph/reports/W14-RIDING-0904/`. Do not touch `creature_body.gd` beyond reading the mount point (if a mount-point fix is needed there, apply the minimal one-line change and flag it), and stay out of the camera-rig conversation push-in another lane is building (`camera_rig.gd` — only touch it if mounted following is broken, and say so).

**Verify.** `smoke_riding`, `smoke_creature_control`, `smoke_gate_b_continuous` (core), `smoke_playground` green; capture one xvfb frame of a mounted Terrapup with the rider seated and the saddle visible, and one before fitting with no saddle; a code-blind judge confirms the rider reads as riding (not floating/clipping) and scale agrees with the 1.80 m trainer. Commit the sheet.

**Acceptance.** All three owner items closed with tests seen red first; `docs/CURRENT_STATE.md` CL-O3 rewritten.
