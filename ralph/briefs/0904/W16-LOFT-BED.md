# Lane W16-LOFT-BED — Grandpa's loft bed does not work, and the wake beat is submerged in the bed (CL-G12 + the P1 wake row)

Branch: `ralph/W16-LOFT-BED-0904`. Model tier: implementation + one render.

Owner: "I've never been able to sleep in the loft bed." and "at the beginning of the game, you are submerged in the bed rather than on it." The passing smoke (`smoke_gate_b_continuous` sleeps in that bed) is the finding, not a contradiction: the harness reaches the bed by a path the player cannot. Read `docs/CURRENT_STATE.md` §3 rows P1 (loft bed; submerged wake), closure plan CL-G12 and §7, `ralph/reports/OPENING-BED-0903/` (the earlier fix moved a floating backpack into the bed and nobody checked the hips against the mattress), `scripts/story/sequence_director.gd` (wake beat), the house interior script (`scripts/world/` — grep `loft`, `player_bed`, `grandpa_house`), `scripts/world/player_bed.gd`, `scripts/world/interaction_arbiter.gd` / `prompt_arbiter.gd`, `tools/_capture_bed_*.gd`, `tools/gate_f/probe_bed_rest_sequence.gd`.

**Do.** 1) Drive a REAL body from the house's ground floor up the loft stair to the bed with stick input only (model on `tools/gate_f/probe_inn_exit_clearance.gd`), and log at every step: the arbiter's winning prompt, the bed interactable's `can_offer` state and reach radius, the body's position vs the bed's interact volume, whether the stair is climbable at all with plain `move_and_slide`. Find the gap (expect one of: the prompt is won by something else — a door, Grandpa, the chest; the interact volume sits under the mattress where a standing body cannot enter; the stair's collision is too steep; the bed is only "offered" in a story state the player has already left). 2) Fix it in the world/interact path, not the harness; add a probe-backed test that a body walking the stair gets the bed prompt and can sleep. 3) The wake beat: render the frame (`tools/_capture_bed_wake.gd`), measure hip height against the mattress plane, fix the placement/pose so the body lies ON the bed, re-render; judge blind.

**Owns:** the house interior script(s), `scripts/world/player_bed.gd`, `scripts/story/sequence_director.gd` (the wake pose/placement only), `data/config` for the house/bed, `tools/gate_f/probe_loft_bed_*.gd` (new), `tools/_capture_bed_wake.gd`, tests: `smoke_gate_a_rest_torch.gd`, `smoke_opening.gd`, `smoke_gate_a_opening_segment.gd`, a new `tests/test_loft_bed_reachable.gd`, `ralph/reports/W16-LOFT-BED-0904/`. Do not touch the arbiter's general rules unless the defect is provably there (then minimal, with a test, flagged).

**Acceptance.** The probe sleeps in the loft bed from a stick-driven climb; the wake frame shows the body on the mattress (judge confirms); the three smokes green; `docs/CURRENT_STATE.md` both rows rewritten with the mechanism.
