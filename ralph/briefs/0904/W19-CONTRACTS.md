# Lane W19-CONTRACTS — the three design contracts (C1 rideable roster/fly/teleport, C2 task feed, C3 village replan), camping necessity, and two decisions

Branch: `ralph/W19-CONTRACTS-0904`. Model tier: design writing (docs only; no code, no Godot needed).

Read `docs/FINISH_THE_MEADOWS.md` Phase 2a in full, `docs/owner/OWNER_PLAYTEST_2026-09-04.md` (OP-0904-1, -4, -6, -9), `docs/owner/OWNER_DIRECTIVES_2026-09-04-B.md` (D-0904B-2, -3, amendment), closure plan §2.G, `docs/GAME_VISION.md`, `docs/specs/MEADOWS_PROGRESSION_SPEC.md`, `docs/specs/MEADOWS_MACRO_LAYOUT.md`, `docs/specs/GATE3_ENCOUNTER_CONTRACTS.md` (the shape a contract should have — file ownership, fails-if rows, tests to pin), D23, D46, D48, D50, D55, D56, and the code each contract must respect: `scripts/world/severed_spokes.gd`, `tether_sigil.gd`, `south_bridge.gd`/`gated_crossing.gd`, `tether_relay.gd` + `data/config/relay_site.json`/`tether_relay.json`, `quest_log.gd` + `data/progression/objectives.json`, `map_state.gd`, `data/config/village.json`, `village_npcs.json`, band 1 `props.json`/`harvest.json`, `data/creatures/species.json` (`rideable`), `riding_controller.gd`.

**Write, as `docs/specs/`:**
1. `C1_RIDEABLE_ROSTER_FLY_TELEPORT.md` — Burrowback, Tuskroot and Terrapup rideable (Terrapup from midway through the chapter); the other two starters get fly and teleport learned well after the Meadows: which gets which and why, how the choice is made legible at the starter pick and reinforced in-chapter (Grandpa's line, the Team screen, a visible "will learn" tag), what each starter gives INSIDE the Meadows so no choice is punished, and the hard limits that keep the chapter's physical gates intact (nothing flies over a locked spoke, the Sigil gate or the South Bridge — the abilities are simply not granted here). Nothing of Biome 2 is built. Include the file-level implementation slices and tests.
2. `C2_TASK_FEED.md` — "things pop up on the map and tell you to go do them": the mechanic (a surfaced task with a map pin, a tracked counter, a reward, a completion beat), built around the two authored instances: the **relay shutdown chain** (beat the grunts at each relay station → that relay can be turned off → story-carried, with `relay_disabled` also healing that relay's stations per V-5/CL-E12) and the **beat-every-trainer quest** (31 trainers, tracked, a reward at milestones), plus 3–5 more "like that" that fit existing content (alphas — CL-W1 already pins them; the Sigils; harvest a rare node set; a camping chain). How the feed coexists with the MAIN STORY objective card and the quest log; controller-first UI; save fields; what fires the pop-up. Implementation slices with owned files and tests.
3. `C3_VILLAGE_REPLAN.md` — houses along a road not a circle; a berry field, a tree grove, a stone area as named places; five villagers maximum with the other 14 resited out into the chapter (name where each goes and what they do there — this is also density); what the tournament ground and Grandpa's house keep; the opening's smoke constraints (`smoke_opening` finds Grandpa and the starters by prompt substrings; the fence corner past TrailGate; the gather route), the terrain/bake implications (any `terrain_playground.json` change is a 25–40 min terrain re-bake; say what needs one), and a staged plan so the opening never breaks.
4. `C4_CAMPING_NECESSARY.md` — "Camping isn't necessary. It needs to be necessary." within CLAUDE.md's satiety hard rule (no harsher hunger, no starvation): necessity from attrition, distance, night, and recovery scarcity — HP/condition that only rest restores past a floor, revive scarcity, the relay/ridge distances, night dangers; the numbers to tune and the tests. **Fails if** the proposal is a faster satiety drain.
5. Two decision records in `docs/decisions/`: (a) Burrowback's palette — the 1.5:1 grass-separation bar vs its grey-olive rock-armour identity (`ralph/reports/G3-CREATURE-COLOUR-0904/REPORT.md`): decide (recommended: keep the identity, meet legibility by rim/contact-shadow and value rather than hue, and state the accepted ratio); (b) the level-gate placement rule — which trainers/crossings carry a `min_level` once density has landed, and the number for the South Bridge relative to the tournament exit level.
6. Edit `docs/FINISH_THE_MEADOWS.md` Phase 2a to point at the four contracts and `docs/00_START_HERE.md`'s authoritative table with one row for `docs/specs/C*.md`.

**Owns:** the six files above and the two edits. Nothing else. No code. Each contract must name owned files, tests to pin, a *fails if* per section, and must not invent a hard-rule violation. Report at `ralph/reports/W19-CONTRACTS-0904/REPORT.md`.
