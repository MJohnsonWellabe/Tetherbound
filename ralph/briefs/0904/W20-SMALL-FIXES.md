# Lane W20-SMALL-FIXES — the cheap, owned, disjoint items that are generating false readings (closure plan §6.2)

Branch: `ralph/W20-SMALL-FIXES-0904`. Model tier: implementation.

Each item below is small and has an exact location; read the closure plan row for each before touching it. Commit each item separately with its id in the message.

1. **CL-E12** — on `relay_disabled`, run `meadow_healing` for the relay's own three drain stations only (a station filter on the existing mechanism in `scripts/world/meadow_healing.gd`, fired from `scripts/world/tether_relay.gd`). Test: real flag set → the three stations' ground state changes, no others. Evidence: a before/after frame from the `06-relay-standing` stand (`tools/` has the relay capture; one xvfb run) showing the ground change inside the site radius; commit one sheet.
2. **CL-G6** — the Riding Saddle costs Ironwood: `data/config/recipes_rootstone.json` `saddle` — the file's own `_comment_ironwood` says exactly what to change. Update `test_recipes*.gd` / `test_crafting*.gd` expectations.
3. **CL-E1** — Oreth's C-3 profile is not authored: `data/config/bands/band3_the_river_lock/trainers.json` `captain_riverwatch` — Mosshell WALL, Brooktail CURRENT per `docs/specs/GATE3_ENCOUNTER_CONTRACTS.md` §4.2, plus the `band_split_baseline` fixture mirror in the same commit (read `tests/test_band_content.gd`'s rules). **CL-E2** facing: fix Oreth's stale `facing_deg` (his own comment flags it). Do not touch band 3 `props.json` (another lane places his post).
4. **CL-D1, CL-D2, CL-D6, CL-D7** — the stale sentences: `data/config/chapter_curve.json:36` ("no trainers of its own"); `docs/prompts/64` and `66` "empty spawn data" notes; `docs/GATE3_EXECUTION_PLAN.md` §4's answered questions; `docs/acceptance/MEADOWS_EXIT_CRITERION.md` B2/B4 numbers. **CL-D4** — the "waiting on the re-bake" `_why` strings in band 2 `vegetation.json` — SKIP this one (another lane owns vegetation this round); note it in the report.
5. **CL-H5** — the freeze-record trap: `tools/gate_f/run_segment.sh` writes the lane declaration itself in logic mode so `_freeze_display_claim()` finds it; do not edit the tracked 2026-08-27 record. **CL-H4** — `S02-60` route-row threshold (900 of a segment that wrote 565): re-derive from the real play clock at 2 Hz (`tools/gate_f/segments/S02.json`). Do not touch S06–S09 (another lane).
6. **CL-H8** — decide `S06-30`'s invented workbench beat with one live probe (Band 2 has no crafting site): if the `build_catalogue` context genuinely fails to release after a workbench interact, that is a game defect — report it with the reproduction and do not fix; otherwise remove the beat from `S06.json` (coordinate: the fight re-scripting lane also edits S06.json — touch ONLY step S06-30 and say so).
7. **CL-E10** — `tools/_probe_band5_approach.gd`'s `03-mid-route` shot faces away from the Hall; fix the aim.

**Owns:** exactly the files named above, plus `ralph/reports/W20-SMALL-FIXES-0904/`. Tests per item; `smoke_relay`, `smoke_relay_station`, `test_trainers_data`, `test_band_content`, `test_chapter_curve`, `test_gate_f_rig` green. Report with one line per item: done / not done and why.
