# Lane W21-HARNESS-FIGHTS — every Gate 3 segment fights blind (CL-H1), and dialogue→combat handoffs by press count (CL-H2, CL-H7)

Branch: `ralph/W21-HARNESS-FIGHTS-0904`. Model tier: implementation (harness scripts).

Read closure plan rows CL-H1, CL-H2, CL-H7; `tools/gate_f/SEGMENT_SCHEMA.md` (the `fight_until_resolved` step and the named failure mode); how 2.8 rewrote S04's rounds (`tools/gate_f/segments/S04.json`: greet → predicate → greet → `fight_until_resolved`); `ralph/reports/G3-BAND2-0903/`, `G3-BAND3-0903/`, `G3-BAND4-0903/`, `G3-BAND5-0903-S09/` for how each segment derailed (Dorn, Hess, the wild Meadowhart, Corr's `narrative_modal`).

**Do.** In `S06.json`, `S07.json`, `S08.json`, `S09.json`: replace every `press combat_quick, times: N` fight with `fight_until_resolved`; before each trainer challenge add a party-health assert and, after any faint, a switch or a revive **by item identity** (never slot); replace fixed press counts on every `battle:`-effect conversation with advance-by-predicate. Fix `S07-26`'s region assert (`the_long_water` is asserted ~700 m from the region). Leave `S06-30` alone (another lane decides it) and do not move any waypoint (another lane is root-causing the S08-22 freeze; do not touch S08-22's coordinates). Then run each segment in logic mode from its synthetic entry (`build_s06_entry_synthetic.gd` … `S09`; `run_segment.sh`; the freeze-record trap in `docs/GATE3_EXECUTION_PLAN.md` §4b — another lane is fixing it in `run_segment.sh`; use the documented workaround meanwhile) far enough to show the first two fights of each resolve fairly, and record P/F counts. Expect S08 to still freeze at S08-22 and the input-context flip at Oreth/Vance/Riverwatch (two other lanes); record where each run stops, do not work around them.

**Owns:** `tools/gate_f/segments/S06.json` (except step S06-30), `S07.json`, `S08.json` (except S08-22's coordinates), `S09.json`, the `S0NC.json` capture twins if they carry the same steps, `tests/test_gate_f_segments*.gd` (schema tests; add one that fails if any Gate 3 segment contains a bare `combat_quick` press block), `ralph/reports/W21-HARNESS-FIGHTS-0904/`. Do not touch `operator_harness.gd`, `run_segment.sh`, or `stick_navigator.gd`.

**Acceptance.** Zero `press combat_quick, times` blocks in S06–S09; the schema test seen red on the old files; each segment's first fights resolve with the party alive in a logic run; report the exact step each segment now stops at and why.
