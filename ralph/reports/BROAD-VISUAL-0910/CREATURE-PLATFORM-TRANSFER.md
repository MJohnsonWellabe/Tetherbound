# Creature platform-transfer fix

Status: focused behavioral regression and exact native catalogue recapture passed.

The exact native catalogue trace reproduced the Glass Field obstruction on physics frame 3. `Named_blackwater_elder` began at `(-150, 64.385, 4460)` with authored `home=(-150, 61.009, 4460)`, zero velocity and `is_on_floor=true`, 1.799m above the trainer. After the trainer moved to Glass Field, the elder jumped 611.328m to `(-310, 69.064, 5050)` while its velocity remained zero and its home stayed at Fallen Giant. No explicit wild relocation has that signature.

The trainer and creature are both `CharacterBody3D` bodies on the default collision layer. The elder had settled on the trainer capsule, so Godot classified the trainer as its moving floor and applied the trainer's teleport delta during `move_and_slide()`. Creature bodies now set `platform_floor_layers=0` in `_ready()`. This leaves ordinary collision and floor detection intact; it only removes inherited platform displacement. No `AnimatableBody3D` or authored moving-platform requirement for creatures exists in the repository.

`tests/smoke_creature_platform_transfer.gd` uses the actual scripted Mosshock body on a trainer capsule. The explicit negative control restores the engine-default platform layer mask after production `_ready()` and reproduced a 128.062m carry. The fixed mode held planar displacement to 0.000m. Both first attempts exited 0 without errors: `creature-platform-negative-control-first` (03:51:30–03:51:33) and `creature-platform-fixed-first` (03:51:42–03:51:45).

The exact native catalogue regression `stormwood-platform-fixed-basal-first` then captured six rows: two Rodline controls plus the Fallen Giant and Glass Field day/night pairs. It exited 0 without errors in 64 seconds (03:52:09–03:53:13) and recorded no discontinuity: the elder stayed at `(-150, ~64, 4460)`, 611.311m from the Glass Field trainer. The Glass Field day frame under `shots/catalogue/stormwood/broad-platform-basal01` shows clear trainer/front terrain with the indigo Voltarach alpha visible at left. That capture also contains the concurrent basal-grass candidate, but the trace's unchanged elder position isolates the platform-transfer fix. Ordinary creature crowding remains a separate placement issue.

## Stormheart ordinary-cluster clearance

The separate Stormheart obstruction was ordinary `dynamo_cluster_24`, not the named elder. Its former 40m spawn disc at `(-116, 5460)` covered both the terminal road at `x=-100` and the Stormheart destination; the baseline manifest placed `Wild_tanglevolt_284525011_1` only 2.68m from the catalogue player pose. The candidate moves only this cluster centre to `(-170, 5405)`, preserving its id, stable order, count, radius, phase tables and derived levels while leaving it visible near Stormheart.

The focused envelope test derives both active phase identities and installed fitted render footprints, then includes the authored spawn radius, production wander radius and 5.2m exploration camera arm. Its geometry assertion passed in `stormheart-cluster-envelope-first`; the run's only failure exposed an incorrect test expectation. I had called the calm derived base level 41, but `StormwoodEncounterCatalogue.wild_config("calm")` actually derives and pins level 40 from the phase-qualified seed. The corrected invariant is calm Tanglevolt level 40 and surge Sparkit level 42. The earlier level-41 claim was wrong; it was not a placement change or a production instance override.

The production seating probe `stormheart-cluster-native-seat-first` exited 0 without engine errors. It found exactly two cluster members on terrain: `(-156.581, 111.550, 5410.479)` with 0.009m seat delta and `(-203.764, 109.742, 5410.625)` with 0.023m seat delta. Their road distances were 56.581m and 103.764m, their Stormheart distances were 82.122m and 119.551m, and terrain was the only collider within 5m. This establishes supported ground and critical-space clearance for the two actual calm bodies; a fresh rendered Stormheart comparison remains pending.

The corrected focused run `stormheart-derived-level-envelope-first` passed 2 tests / 20 assertions without errors (04:03:39–04:03:42). The native OpenGL catalogue pair `broad-stormheart-clearance01` also completed 2/2 day/night frames without errors in 57 seconds (04:00:28–04:01:25). Those frames prove the former creature occlusion is gone, while exposing a separate landmark-composition problem: the exact catalogue destination is beneath the giant tree's deck/ramp structure, so its broad wood underside dominates the view. The world boot also contained the held basal-grass candidate, so this pair is evidence for body clearance rather than an isolated environment comparison. A production-input southern-road approach capture is prepared to judge the landmark from ordinary play before changing its geometry.
