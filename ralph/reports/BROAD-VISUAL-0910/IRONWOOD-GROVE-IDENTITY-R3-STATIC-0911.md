# Ironwood Grove identity R3 — static candidate

Status: authored for native production capture and independent visual review; no Godot, render, bake, or headless process was run in this lane.

The R2 production frames were readable and correctly scaled, but remained POLISH because the road did not resolve into a named old-growth destination, the five-tree age ladder existed mainly in numbers, and the supposed crafting glade contained no visual craft story.

R3 keeps all five harvest nodes at their existing coordinates with their existing `ironwood` item, amount, prompt, model family, and interaction. A separate collisionless presentation layer adds:

- 27 ground-following root flares whose radii increase from young through mature to elder trees;
- a split-bough crown threshold 24–34 metres ahead of the existing real-route arrival, plus a second shared crown between the elder pair;
- the installed Quaternius workbench, log anvil, axe and pickaxe, three cut timbers, a chopping stump, a tool rack, and seven inset timber-round path marks in the existing crafting clearing;
- three warm local lights, each at or below 7.6 metres range and 1.2 energy.

No added node owns collision, a prompt, an item, a reward, an encounter, progression state, or terrain. The existing arrival lens is retained unchanged. The R3 harness keeps the same four production-world compositions and camera values as R2 and changes only its evidence output directory, so any visible gain must come from authored world content rather than camera restaging.

Required next evidence:

1. Run the focused tests after the coordinator's current native-render lane is idle.
2. Run one canonical scatter bake if the retained R2 sightline clearing is not already represented by the served bake.
3. Capture all eight R3 production frames.
4. Have a code-blind reviewer grade road reveal, old-growth identity, age hierarchy, worked-glade story, day/night readability, finish, and player scale.
