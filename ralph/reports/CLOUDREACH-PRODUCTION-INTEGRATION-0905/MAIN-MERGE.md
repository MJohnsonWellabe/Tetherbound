# Main reconciliation checkpoint — 2026-09-05

Branch codex/cloudreach-cliffs merges origin/main 2cd711eb1, retaining its prior
checkpoint e002853ea and reference imports 92fda325e. No reset/rebase or merge to
main. The owner's newer conflict rule is binding: irreconcilable overlaps keep
Cloudreach's implementation, with displacement disclosed. This reconciliation
preserves its behavior alongside main's shipped shared systems.

## Resolutions

- W13 static sequence-numbered feed and D76 unordered bonds remain canonical;
  Cloudreach retains owned-only Fly route credit, reset/load safety, one banner,
  reward receipts, captain identity and combat-to-relay HUD handoff. Obsolete
  branch presenter/overlay removed. See UI-MAIN-MERGE.md for actual smoke paths.
- Pickup setup retains count, duplicate guard and realm identity. Meadows saves
  retain cache:<placement>; Cloudreach retains cache:cloudreach:<placement>.
- W04 portraits are matched to the installed Cloudreach cast/traveler bodies;
  Aila retains her distinct owner-reference image. No shipped prop art was altered.
- W12 companion, W09 VFX, W23 difficulty, route-strip and authored pickup/density
  changes remain integrated. Older balance numbers do not certify the new damage
  multiplier; a fresh combat probe is required.
- Windows CRLF caused false-stale terrain/scatter hashes and source-pattern test
  failures. Canonical LF hashing preserves historical Linux bake fingerprints.
  No terrain/scatter payload or manifest was rewritten. New regression controls
  check LF/CRLF equivalence and continued detection of real content changes.

## Checks run on this integrated source

- Headless editor import: exit 0, no ERROR or SCRIPT ERROR.
- Selected units: 352 tests / 33,703 assertions, three failures in stale chapter
  fixtures that omitted three required shrine vanes. Corrected isolated chapter
  suite: 8 tests / 115 assertions / 0 failed, including denial before vanes.
- HUD lifecycle 34/34; production integration 64/64; progression feed lifecycle
  14/14; physical Fly runtime 36/36. All exit 0 without engine/script errors.
- Terrain/scatter committed-bake freshness: 2 tests / 2 assertions / 0 failed.
- Meadows smoke_playground: exit 0, smoke OK after the hash fix. Read distinct
  errors: the pre-existing headless alpha material-null error remains; this is
  documented in AGENT_WORKFLOW.md and not presented as an error-free boot.
  The pre-fix attempt was stopped while computing the unnecessary scatter fallback.
- Full unit suite is running. Its initial Windows CRLF failures are recorded and
  corrected source is staged; follow-up selectors and final totals are pending.
- Cached diff against origin/main passes whitespace checks. Inherited main report
  whitespace is not silently modified to make a first-parent diff look clean.

## Still open, not a completed visual/gameplay pass

Round-4 visual source and continuous harness work remain a separate active batch.
The continuous replay now crosses the former Maela blocker, but the full chapter
has not passed. Twelve real round-4 views and actual airborne Fly evidence exist;
the independent review, sustained performance, payoff/HUD captures and combat
remeasurement remain open. Final visual acceptance belongs to external ChatGPT.
