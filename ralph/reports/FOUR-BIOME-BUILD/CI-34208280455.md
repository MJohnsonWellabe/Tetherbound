# CI 34208280455 — terminal success with a retry finding

Submitted SHA: `23b7d4acd935b508d53769a71e434857de1de41c`.
GitHub's terminal result is success: 26 passing jobs, three skipped. Root
retrieved the completed job logs individually. Export and two optional
known-red jobs were skipped; no export or complete campaign claim follows.

## Confirmed repairs

- MP2: all six smokes attempt 1/1. Livewire samples the unchanged window at
  345 and 348 ms and refuses subsequent real early actions without authority
  advancement. Alpha and Swim-Stone late-join also pass.
- MP4: hosted Tamsin and the other five smokes pass attempt 1/1.
- All seven multiplayer shards pass first-attempt. MP3's peer-death smoke
  deliberately kills peer 1 and requires coordinator exit 2: its printed FAIL
  is the asserted negative control, not a hidden failed smoke.
- All four unit shards pass: 715/112576, 755/65405, 738/28888 and 682/279142
  tests/assertions, respectively, zero failed.
- Regions: real-ledger solo rewards (eight checks), lightning cleanup and
  detached-opponent cleanup all pass, including the new native-error scans.
  Ordinary Stormwood-to-Water gate path also passes.
- Core playground passes attempt 1/1. Its native error set contains the
  previously documented alpha material-null error; it is not zero-error proof.
- Gate B core and gate-evidence checks pass first-attempt. These preserve
  their documented fixtures and scope; they do not prove full fresh-save play.

## Still open: aggression retry

Combat shard job `102003435270` reports `smoke_aggression.gd` failed attempt
1/2 at 09:21:14 UTC: the player stood **116.1 m** from Galecrest for 900
frames, and no ambush began. Attempt 2/2 passed at 09:23:32 UTC.

The smoke's `_walk_towards()` returns no success result, and both callers
continue even when its 4,000-frame approach budget expires. The failed log
therefore does not establish that the player ever entered notice range. It
also does not reveal the physical cause of the failed approach. Diagnose
movement and strengthen that precondition without weakening the ambush or
peaceful-creature checks. Per the workflow, a retry is a finding, not a clean
pass: this run cannot be presented as first-attempt-green throughout.

Prepared diagnostic strengthening makes `_walk_towards()` return a checked
result, requires actual 3D proximity before either patience observation, preserves
a spontaneous fight during approach for the existing opponent checks, and
records movement/target/velocity/locomotion/ground/wall samples every 400 frames.
An inconclusive peaceful prefix now stops instead of contaminating the ambush
half. No walk budget, patience budget, aggression rule or peaceful assertion is
weakened. Parser check passes; this is not yet a runtime repair of the unknown
approach failure. The diagnostic is queued behind the live Stormwood route.

Other passing job groups: vegetation corridor, scatter rules, harvest,
terrain/scatter freshness, UI/build, owner regressions and input/core verbs.
Some fixture tests still emit native detached-node or teardown errors; a
successful assertion summary is not a blanket claim that engine logs are clean.
