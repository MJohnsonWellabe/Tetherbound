# CI 34213024669 — terminal failure, Livewire observation window

Submitted head: `0ddc10269955bfbe16389ab8c2e6916708c64d06`, PR #80.
GitHub tests merge `111252ea87094cfa975e59fa4eebb92402e5cc3d` against main
`f6b79a6b32983d3b60f7333d8b44706736701474`.

This batch contains the separated aggression fixtures, strict approach checks,
Salt Crown ordinary spawn placement and Reedhaven tool-contract correction.
Run is now terminal: 25 successful jobs, one failed job, three skipped jobs.
Local documentation and subsequent
implementation commits are not covered by this run.

## Completed jobs inspected so far

| Job | ID | Evidence |
|---|---|---|
| changes | 102018222272 | Completed successfully; checkout identifies the exact tested merge above and code jobs actually launched. Not a documentation-only skip. |
| terrain bake freshness | 102018738757 | 1 test, 1 assertion, 0 failures. |
| scatter bake freshness | 102018738791 | 1 test, 1 assertion, 0 failures. |
| net-smoke discovery | 102018738877 | Completed successfully and launched all seven multiplayer shards. |
| unit shard 1 | 102018739034 | 708 tests, 112187 assertions, 0 failures. Native output includes unscoped-flag fixture errors and renderer/resource teardown leaks; this is not a clean-native-log claim. |
| unit shard 2 | 102018738826 | Reports 767 tests, 65890 assertions, 0 failures, but three map-legibility methods abort on missing constants before their assertions. This is a missing proof, not a passed map contract. |
| unit shard 3 | 102018738944 | 719 tests, 28734 assertions, 0 failures; native fixture errors remain in output. |
| unit shard 4 | 102018738818 | 698 tests, 279216 assertions, 0 failures, including both new Reedhaven tests. Native detached-tree/material errors and resource leaks remain. |
| combat shard | 102018738840 | Every smoke passes on its first attempt. Aggression specifically records peaceful 2.2 m/900 frames and Galecrest self-initiation at 8.8 m. Other combat fixtures still emit existing native errors; no whole-job clean-native claim. |
| multiplayer shard 2 | 102019345930 | Failed Livewire's released-baseline timing-window check: host sampled 119 ms before deadline against required 180–350 ms. The released action's authoritative refusal and unchanged action sequence both pass. All five subsequent smokes pass first-attempt. This is not a shard timeout. |

Logs for each completed job above were retrieved independently. The two optional
known-red jobs and export are skipped, not passed.

## Remaining terminal job review

| Job | ID | Evidence |
|---|---|---|
| scatter rules | 102018738725 | 38 tests / 1019854 assertions / 0 failed. |
| vegetation corridor | 102018738811 | 9 tests / 1537510 assertions / 0 failed. |
| harvest | 102018738856 | 30 tests / 799078 assertions / 0 failed; detached-tree native errors remain. |
| UI/build | 102018738781 | Successful smoke verdicts; no actual retry-failure line found. |
| owner regressions | 102018738870 | Successful smokes, including catch/save and trainer camera; resource leaks remain. |
| Gate B core | 102018739014 | Successful job; continuous fixture is not whole-campaign acceptance. |
| regions | 102018738879 | Successful; hosted rewards and Crown heartstone assertions pass; other fixtures emit resource leaks. |
| core verbs | 102018738920 | Successful; native material/teardown errors remain. |
| gate evidence | 102018739006 | Successful including finale; native teardown/material errors remain. |
| multiplayer 1 | 102019345981 | Successful; save ownership and actual launch/join checks pass. |
| multiplayer 3 | 102019345877 | Successful; mounted swimming/resource/ownership checks pass. |
| multiplayer 4 | 102019345948 | Successful; human swimming and separated-island bodies pass. |
| multiplayer 5 | 102019345893 | Successful; actual session and host/client save ownership pass. |
| multiplayer 6 | 102019346003 | Successful; distinct creature owners and proxies pass. |
| multiplayer 7 | 102019346277 | GitHub reports success; two log retrievals timed out. Log review remains outstanding, not silently treated as inspected. |
| solo regression | 102023309874 | Successful dependency fence only; echo job adds no new runtime proof. |

The previous run's green retry did not establish the aggression repair. This
run now passes aggression on its first attempt, corroborating the local behavior
proof. The local second-scene velocity warning is tracked in
`AGGRESSION-STAGING-WARNING.md`; it is not hidden by this CI status.

The Livewire timing failure requires investigation rather than a blind retry.
Earlier host-physics sampling passed locally and in CI, but this run proves it
can still miss the narrow observation window. The measured 119 ms is new
evidence; no cooldown duration, refusal assertion or timing bound has been
relaxed. Overall workflow failed. Host-local diagnostic telemetry now records
entry remaining time, sample count, elapsed time and largest sample gap so the
next measured run can distinguish a late command from a stalled physics frame.
Parser check passes (`root-livewire-timing-telemetry-20260908.log`); telemetry
is not a fix or a new runtime pass. Production authority is unchanged.

## Map test false-green finding

The current full map lacks `CANVAS_OUTLINE_SIZE` and `MARKER_KNOCKBACK`.
Three tests assign those missing values directly to typed locals, raising a
script error before any failed assertion is registered. The isolated local
baseline reproduces three errors yet exits 0 (13 tests, 21 assertions).
Added type guards preserve every existing legibility threshold and now yield
three explicit failures, exit 1, without script errors (13 tests, 24 assertions).
Local repair `8f0584cda` restores the real drawing contract and passes 29 tests /
95 assertions across legibility, fog, realm view and zoom. It is outside this
CI head. First/second-open rendered proof and code-blind judgment remain pending;
no fake unused constants or relaxed thresholds were added.
Logs: `%TEMP%/root-map-legibility-baseline.log` and
`%TEMP%/root-map-legibility-failclosed.log`.
