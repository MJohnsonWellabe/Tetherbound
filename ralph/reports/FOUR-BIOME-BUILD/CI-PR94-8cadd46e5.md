# PR94 8cadd46e5 CI audit — complete

Run 34373161169, exact head `8cadd46e5ace78293163ce053eab2def7cacf931`, completed successfully on **attempt 1 at 2026-09-09 16:22:28 UTC**. REST confirms this PR94 head against landed main c3a7eac5a; an earlier connector snapshot was stale. **29 jobs: 26 success, three skips** (two known-red jobs and PR export). Every executed full job log is retained: **26 logs / 9,792,650 bytes**. No executed failed-attempt line or second-attempt group was found. This audit identifies no new blocker to landing the isolated PR94 candidate.

Artifact root `.artifacts/pr94-8cadd46e-ci/` retains exact run metadata, all job/step metadata, every executed full raw log, normalized diagnostic comparison and invocation/unit scan. This is the finalized-death/telemetry candidate on the landed protocol repair. Its earlier telemetry environment failure and later new-native-error finding remain failures in the prior receipts; neither is relabeled by this run. No rerun, local Godot invocation or remote mutation is part of this review. The pending statements below are historical checkpoints.

Final coverage: all **38 discovered multiplayer smokes executed once**, distributed 6/6/6/6/6/7/1 across seven shards; all passed. The new real-ENet finalized-death case in job 102541924501 kept the ordinary revival window, finalized one death, removed the remote fighter on host authority, rejected late snapshot combat restart, awarded no defeat flag/reward, and kept the host in Meadows. Its section contains no engine error. Water Alpha and remaining realm/hosted cases also pass first invocation. Node release-tag checks pass **7/7**, and the opening segment passes **1/1** with the exact actionable Bramblebun at 3.03 m.

Diagnostic comparison against landed protocol evidence: units retain exactly 218 normalized baseline engine diagnostic lines. Broader smoke differences are existing three/four-resource shutdown counts. The intentional `smoke_net_peer_death` case still emits its known two process-liveness errors; these moved from prior shard 2 to current shard 3 because adding the 38th smoke reshuffled assignment. Across all network jobs, that normalized error multiset remains unchanged. It is not a new native error and is not represented as zero-error coverage. The previously discovered receive-map despawn error is absent from this run. Game 108's four declared negative errors remain explicit; native 81/77/82 and new 37/15 sections are clean. Full-world occupied-host rebuild and broader commercial/biome acceptance remain outside the scoped protocol/death repair.

Twelve-job checkpoint: changes, discovery, both bakes, all four units, harvest, gate-B core, gate evidence and combat have succeeded with full raw logs retained. Units total **3,129 tests / 487,903 assertions / zero failed**; the normalized 218 unit engine diagnostic lines match the landed protocol baseline exactly. No executed failed-attempt line or retry group in these logs. Remaining native/world jobs are pending; this is not final acceptance.

Twenty-one-job checkpoint: all non-network jobs are now successful (PR export skipped); full logs retained, with five network shards still running. Regions job 102539976768 proves **finalized-death 37 checks / zero failures** and **telemetry 15 checks / zero failures / 916 ms**, explicit `/tmp/stormwood-combat-telemetry.json` output environment and no engine errors in either section. The same job preserves Game 108 checks with its exact four declared negative errors, plus native baseline 81, cancellation 77 and latejoin 82 clean checks. Network shards 4 and 7 pass first invocation, including realm return/host travel. Discovery lists the expected 38 net smokes. Remaining full-job comparison differences are existing resource shutdown count variations, not a new diagnostic class.
