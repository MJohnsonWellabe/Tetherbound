# PR96 first-head CI finding

Head39917123258dfa32216aecb95264eccd7747d66a, run34354734726, attempt1.
Unit shard3 job102477077569 failed its runtime texture compression policy check
for `assets/characters/trainer/trainer_teal_accent.png.import`: mode0 instead of
the required fully reimported mode2. The job completed645 tests/26,672 assertions,
one failed. Complete386,594-character raw log is retained at
`.artifacts/arlo-pr-ci/job-102477077569.log`.

This is a real missing import-policy correction, not an intermittent failure.
Root applied the existing policy tool to this sidecar only and imported it,
then repeated production texture binding and actual image capture successfully.
No CI retry of this unchanged head was requested. The corrected head81504019d
superseded it. Final29 jobs:11 success,1 failure,15 cancelled,2 configured skips.
All25 jobs that executed steps have complete raw logs retained and scanned
(8,732,489 stored bytes). Solo fence/export were cancelled before any steps;
they have no executed logs or acceptance credit. The cancelled network shards
are incomplete, not passing.

Baseline comparison uses mainab5314's exact matching job names and full logs.
The failed unit3's native error signatures/counts match main exactly. Unit2 has
13 retained resources and2 texture RIDs versus main's12/1, consistent with the
additional cached character texture; this is a disclosed shutdown-count change,
not a new runtime error class. Gate-A and owner-regression shutdown counts vary
within already observed classes. No new `recv_nodes` or script-error class was
found in executed logs. The first-head regions job completed the four aim cases
19/8/5/8 and the character binding probe7 before cancellation, but that does not
rescue the failed texture-policy check. This head remains unaccepted.

Final job inventory and comparison are retained at
`.artifacts/arlo-pr-ci/first-head-jobs.json` and
`first-head-baseline-comparison.json`.
