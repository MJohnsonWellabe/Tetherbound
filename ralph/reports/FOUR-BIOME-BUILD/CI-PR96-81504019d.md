# PR96 corrected-head CI — accepted

Head `81504019dddab7178f2d95d9dc02946e2f411782`, run34355901748,
attempt1, success. Started13:15:10 UTC and completed13:35:15 UTC on2026-09-09
(20m05s). All29 jobs accounted:26 successful executed jobs and3 configured skips
(two optional known-red gates and PR-only export skip). The earlier failed
head399171232 and cancelled work remain recorded in CI-PR96-399171232.md.

All26 executed raw logs were retained and inspected,9,206,042 stored bytes at
`.artifacts/pr96-81504019-ci/`. `run.json`, `jobs.json` and
`baseline-comparison.json` preserve the run/head, job and native-error audit.

- Four unit shards:3,076 tests/487,614 assertions, zero failed. The corrected
  runtime texture policy passes in shard3.
- Seven network shards:37 named smokes, each first and only invocation, all
  complete. Solo regression fence passes.
- Real character binding/cache/finish probe:7 checks, failures empty.
- Existing native earned-input cases:19/8/5/8 checks, failures empty.
- All other required biome, combat, owner, scatter/terrain, UI and evidence jobs
  completed successfully; no cancelled or skipped code job is credited.

## Complete raw-log disposition

Existing unit negative-path errors, material-null messages and shutdown resource
warnings remain. Matching mainab5314 job logs establish the same classes. Unit2
retains13 resources and2 texture RIDs versus main12/1, consistent with the extra
cached character texture; this is explicitly a changed shutdown count, not a
claim that the whole suite is raw-error-free. Other shutdown count variations
remain in already observed classes.

Network shard5 retains old disappearing-trainer/cached-node/invalid-delta errors
already present in main's same smoke; dynamic peer identifiers differ. Shard2's
two process-cleanup errors likewise match main after the PID is accounted for.
No new unauthorized-despawn `recv_nodes` condition or SCRIPT ERROR was found.
These baseline defects are separate from the held new client-protocol work.

The code change is Arlo's bounded cloth derivative, explicit isolated body
texture and required VRAM import. Independent image-only review accepted both
the initial and compressed paired render, with modest-contrast/night-context
limits disclosed. PR96 landed as `1eef3df5a774e4fe4a2ba27751df24def522e2b7`;
root verified reviewed-head ancestry and identical tree
`f1d05163519b35f72d00cb770b4b4ab4877720ce`. Main CI `34358304662`, Release
`34358304734` and literal latest-tag verification remain pending. It does not accept
the held biome/creature candidates or establish a new campaign path.
