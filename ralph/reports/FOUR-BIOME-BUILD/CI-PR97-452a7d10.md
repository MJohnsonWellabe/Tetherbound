# PR97 first exact-head CI — retained failure

Head `452a7d10e8c20ecb30868defaa3df88664f60615`, base main
`1eef3df5a774e4fe4a2ba27751df24def522e2b7`. CI `34362371531`, attempt1,
2026-09-09 14:15:07–14:34:02 UTC, **failure**. No rerun was requested.

29 jobs:24 success,1 failure,4 skips (two configured known-red jobs, export and
solo aggregate fence). All25 executed jobs' complete raw logs are retained in
`.artifacts/pr97-452a7d10-ci/job-<id>.log`,9,378,746 bytes. Final job metadata is
`final-jobs.json`; error comparison against main1eef is `partial-comparison.json`.

All four unit shards passed:3,116 tests,487,808 assertions,0 failed. Newly added
test files reshuffle shard membership, so unit error comparison uses the aggregate,
not the same numbered shard. There are217 raw engine error headers against218 in
main's aggregate; the existing negative-control/material-null classes remain.
The19-resources-at-exit count is new as an exact count, not a new error class.

All seven network shards passed their first invocations, including the default
production Water Alpha path. The deliberate peer-kill negative control retains
its expected coordinator failure and two OS process-cleanup errors. No new
unauthorized-despawn or missing receiver error appears in these network logs.

The new region lifecycle step failed. Its uploaded artifact
`realm-transition-34362371531-1` (artifact10108610860) was downloaded unchanged to
`lifecycle-first.zip` and extracted under `lifecycle-first/`. Individual receipts:

| Case | Result | Elapsed |
|---|---|---|
| Actual Game orchestration |108 checks pass; exactly4 expected injected errors |1.571710s |
| Native baseline |All29/29/23 assertions report pass, then staying stderr emits8 inactive-ENet errors; wrapper correctly fails and kills owned peers |3.093513s |
| Native cancellation |77 checks pass, all three processes exit0, raw error scan clear |3.259726s |
| Native late join |82 checks pass, all three processes exit0, raw error scan clear |4.556185s |

These cancellation and late-join passes exercise the final snapshot-revision ACK
code on this exact head, unlike the earlier Windows late-join candidate. They do
not turn the failed baseline or whole CI green.

Diagnosis: the baseline probe stops its physics/coordinator but leaves native
automatic visibility running during terminal flush after deliberately disconnecting
Session's normal disconnect handlers. The host can exit first, leaving the staying
peer's four bodies/eight synchronizers calling get_unique_id on inactive ENet.
The eight errors match that inventory. A fixture-only post-proof quiesce and owned
transport detach is being verified; no production protocol change is inferred from
this failure and no error suppression is permitted.

Other region steps continued and passed. Their existing unscoped-flag negative
controls and shutdown resource errors remain visible. Combat, core, gate and
owner smokes also vary in existing3/4-resources-at-exit counts; these are retained,
not described as error-free. No new SCRIPT ERROR was found. PR97 remains draft;
this first head is not approved for landing.
