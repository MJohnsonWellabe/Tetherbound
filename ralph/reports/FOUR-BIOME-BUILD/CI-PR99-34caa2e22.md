# PR99 combined-head CI audit — complete

Run 34374823501 at exact head `34caa2e22c69eaa1cdbeee9903b0c8c85024e6ab` completed successfully on **attempt 1, 2026-09-09 16:34:47 UTC**. **29 jobs: 26 success / three skips** (two known-red jobs and PR export). All 26 executed full raw logs are retained under `.artifacts/pr99-34caa2e2-ci/`, **9,414,654 bytes**, alongside metadata/comparison/invocation scans. No executed failed attempt or automatic retry was found. The narrow motion delta is stacked on reviewed PR94. Root independently verified its tree remains identical when merged against actual landed main 1d6c15c46. Superseded initial run 34373237730 is retained separately by the integration owner and is not credited as a complete pass.

All 38 multiplayer smokes pass first invocation. Regions retain Game 108 with its exact four declared negative errors; native 81/77/82, finalized-death 37 and telemetry 15 (917 ms) all pass clean. Node checks pass 7/7. Opening passes 1/1 with actionable Bramblebun at 3.18 m. Units add exactly five animation tests / 28 assertions, with aggregate normalized 218 diagnostics unchanged. All five new animation tests are explicitly present in unit shard 3 job 102547096021.

Reviewer disposition: no concrete motion-source blocker established; the limited change is supported by the completed tests and prior scoped image verdict. This is **not** a zero-error Water Alpha claim or broad commercial/biome acceptance. The new shutdown occurrence below remains unresolved and preserved; no rerun erased it. Earlier pending statements below are historical checkpoints.

## Water Alpha shutdown occurrence

Job 102549187623, after host participant removal, live-target removal and capture-lock cleanup all pass, emits at 16:32:02.992 UTC: `WARNING: 9 ObjectDB instances were leaked at exit` at `cleanup (core/object/object.cpp:2535)`, followed by `ERROR: 4 resources still in use at exit` at `clear (core/io/resource.cpp:822)`. `ALL CHECKS PASSED` follows. No resource identities or GDScript backtrace are provided. It is a shutdown occurrence, not a receive-map or inactive-ENet failure.

Matching Water Alpha sections in PR94 job 102541924635, main c3 job 102538608623 and PR97 3104 job 102530606160 each have one ObjectDB warning at the same cleanup location and no resource error. Therefore this occurrence is new to the matching smoke, even though the resource-exit class exists elsewhere. Region Stormwood arches' two-resource occurrence separately matches prior main c3. Other differences are established two/three/four-resource count variations; intentional peer-death retains its two baseline process-liveness errors.

Source reach review: Alpha extends WildCreature and uses the shared animator, so shared reset/playback changes can execute. This fixture summons `water_mosshell`, with Aquaryn as Alpha; neither opts into Torrentoad's contact metadata. The new animator state stores only string/float values. The delta adds no allocation, callback, signal connection, timer, or retained Resource reference; its Animation lookups are local. The unit child/capture helpers are not invoked by net5. No causal retention path was found in this delta, but the non-verbose log cannot identify or prove ownership of the leaked resources.

Sixteen-job checkpoint: all four unit shards, changes/discovery, bakes, scatter/vegetation/harvest, combat/owner/UI/core and multiplayer shard 7 succeeded. Units total **3,134 tests / 487,931 assertions / zero failures**, including all five new animation tests (+5 tests/+28 assertions versus PR94). Normalized aggregate unit diagnostics remain exactly 218 baseline lines. Other differences so far are existing resource-shutdown count variation. No executed failure/retry found. Remaining world/network jobs must finish before acceptance.
