# PR #90 exit-head CI verdict — run 34292244664

Reviewed 2026-09-09 against repository `MJohnsonWellabe/Tetherbound`.

## Verdict

Run `34292244664` verifies exact PR #90 exit head
`452a4d63cc33030703612d71d7a2e390d4d8a995` on branch
`codex/four-biome-wave6`. GitHub reports pull-request attempt 1, started
2026-09-08 23:48:06 UTC and completed successfully at 2026-09-09 00:09:50 UTC:
21m44s wall time. The PR base recorded by GitHub is unchanged main
`65267c4bd935d80b2e073799caeffc81b913952c`.

All 29 workflow jobs were inspected. Twenty-six executed jobs concluded success.
The three non-executed jobs were the configured `verify-continuous-core-known-red`,
`verify-gate-b-full-known-red`, and `export` skips. Every executed job's steps and
decoded log were inspected; all executed steps concluded success except the four
cache-path `Install Godot` steps, which were expected skips because the cached editor
was used. Raw decoded logs are local under `.artifacts/ci-34292244664/` (26 files).

This is a terminal green workflow, but it is **not first-attempt-clean smoke evidence**.
Of 86 distinct wrapped smokes, 85 passed on attempt 1. `smoke_traversal.gd` failed
attempt 1 because the open Sigil Gate walk reached only 7.0 m past the gate, then
passed unchanged on attempt 2 of its configured three-attempt allowance. This retry is
a finding and remains disclosed; the run was not rerun and workflow attempt stays 1.

## Executed coverage

- Four unit shards passed 3,062 tests and 487,412 assertions, zero failures:
  shard 1 802/292,975; shard 2 752/134,803; shard 3 610/34,818; shard 4 898/24,816.
- The four direct unit-style regression jobs passed 79 tests and 3,356,444 assertions:
  scatter freshness 1/1, terrain freshness 1/1, vegetation corridor 9/1,537,510,
  harvest 30/799,078, and scatter rules 38/1,019,854. (The two freshness checks are
  counted separately, hence five summaries across four regression categories.)
- All seven multiplayer shards passed. Discovery found the complete 36 two-peer smoke
  files; every selected net smoke reached its `PASSED` receipt. The peer-death smoke's
  killed-peer exit 2 is its deliberate negative control, followed by a passing verdict.
- Gate A UI/build, regions, core verbs, Gate B core, combat, owner regressions, gate
  evidence, bake freshness, vegetation/harvest/scatter rules, net discovery, and the
  final solo-regression fence all concluded success.

## Log review findings

The retry above is the only observed executed-smoke failure. Logs also contain expected
negative-control diagnostics: malformed JSON/settings/save versions; unknown species,
conversation and unscoped flags; starter-adoption refusal; a deliberately killed net
peer; save snapshot/commit refusals; and missing-node/invalid-packet messages exercised
by multiplayer teardown. These are followed by their tests' passing receipts.

Distinct native cleanup/fixture messages remain visible despite green steps: ObjectDB
leak warnings (1, 3, 6, 8, 10, 39, 73 and 85 objects), resources still in use at exit
(counts including 1–6, 12 and 15), a dummy-renderer PagedAllocator page-in-use error,
null `material` and `data.tree` fixture errors, Terrain3D texture no-mipmap warnings,
the known Cloudreach candy surface-placement warning, and the deprecated physics-
interpolation call. Nothing in this run proves those messages repaired. No script
compile failure, job failure, cancellation, workflow rerun, or second workflow attempt
was found.

## Credit boundary

This run verifies only the exact exit head. It does not prove that this head merged to
main, does not replace main's own push CI, and does not establish a fresh opening-to-
Tidewake campaign, the Stage C6 58-location day/night audit, visual acceptance, Ally
performance, or resolution of the rendered allocation crash.
