# Earned Meadows departure — Wave 1 source audit

2026-09-08. Scope: the retained post-tournament party, in the existing Meadows scene, through the real Cloudreach boundary. Source audit only; no world run, production changes, scene reload, or acceptance claim. Prompt 78's playable-first contract governs; old Gate F scenarios supply route clues, not permission to inject their starting saves.

## Shortest authored causal route

| Boundary | Required ordinary action and receipt | Current authority |
|---|---|---|
| South Bridge | Follow the lower Meadows road, try the actual closed gate, let its guardian walk over, complete his challenge dialogue, defeat his two creatures, spend the awarded bridge key through the production gate, physically cross. | `scripts/world/south_bridge.gd:189,239,305`; `scripts/world/gated_crossing.gd:232,237,243,415`; `data/config/bands/band1_lower_meadows/trainers.json`, `south_bridge_grunt` (Mudsnout 10, Burrowback 12). |
| Old Quarry / Burrow Warrens | Gather real rootstone with the carried pickaxe; enter the authored Warrens chambers and defeat the actual Burrowback guardian (14). Observe `warrens_cleared` and its earned rewards. This is a named required chapter objective, even if a later physical bridge does not independently check that flag. | `data/progression/objectives.json:143`; `data/config/burrow_warrens.json`, `guardian`/`clear`; live `burrow_warrens.gd:6737` marker, `:6745` guardian. |
| Relay / Mill | Follow the river approach, fight Captain Vance, talk to freed Sela, disable the console, then interact with the mill gate using Sela's gear and cross its real span. Record `relay_captain_defeated`, `captive_rescued`, `relay_disabled`, `mill_crossing_restored`. | `data/progression/objectives.json:151–181`; `data/config/bands/band3_the_river_lock/trainers.json`; `data/dialogue/relay.json`, `relay_captive_freed`; `scripts/world/tether_relay.gd:911`; `scripts/world/mill_crossing.gd:20`. |
| Saddle / riding | Tournament victory earns `recipe_saddle`. Gather rootstone, then Band 4 ironwood and remaining wood/fiber; use actual crafting UI for frame then saddle. Inspect the actual five's rideability. The live Ripplet/three Bramblebun/Mudsnout roster has no rider: catch a real Meadowhart and complete the actual full-belt replacement ceremony before selecting/deploying it, using its riding offer, moving while mounted and dismounting through input. | `data/recipes/recipes_rootstone.json:42,54,67`; `data/creatures/species.json:75,155,206,346`; `data/config/bands/band4_upper_meadows_ironwood/harvest.json`; `scripts/world/riding_controller.gd:242,258,326,789,859`. |
| Three captains / Sigil Gate | Defeat riverwatch Oreth, field Halder, ridge Vess; keep their actual sigil items until the gate consumes them. Follow the physical approach to the Sigil Gate and interact. Ascending southward order avoids the old S08 field→riverwatch backtrack. | `data/config/bands/band4_upper_meadows_ironwood/trainers.json`; `scripts/world/playground_world.gd:219,221,1708`; `hall_approach_open`. |
| Meadows Hall | Walk the real causeway and successive chambers. Win patrol, courtyard and elite; each opens the next physical passage. Use actual recovery interactions/carried remedies if needed. Fight Warden Aldis's five creatures (18/18/19/19/20). | `data/config/stronghold.json:164–239`; `scripts/world/stronghold.gd:5258,5272,5288,5829`; Warden reward in `data/config/bands/band5_stronghold_approach/trainers.json`. |
| Machine / roster | Walk through the opened Warden passage to the machine's actual prompt. Complete the production chamber/free/join dialogue sequence, then the real five-creature ceremony. Both keeping and declining the offered legendary are valid; prove `legendary_settled`, not mandatory `legendary_joined`. Complete the machinery-failure dialogue. | `scripts/world/stronghold_climax.gd:144,692,732,920,950,966`; `data/config/stronghold_climax.json`. |
| Aftermath / Cloudreach | Complete a real post-victory acknowledgement conversation for `meadows_acknowledged`, then walk the rebuilt storm-road span. Wait for actual Cloudreach arrival and retained state before invoking `cloudreach_live_segment.gd`. | `data/progression/objectives.json:230`; `data/dialogue/meadows_freed.json`; `scripts/world/rift_crossing.gd:381,403,407,413`; `autoload/game_state.gd:1410`. |

The relay's pickets and optional roadside trainers are real authored preparation and reward sources; they are not all independent physical lock prerequisites in their trainer rows. Do not invent grants to replace their value if the actually earned party cannot beat the next mandatory battle. The first bridge itself raises the combat ladder from tournament level 5 to opponents 10/12; source review cannot establish that the current earned party/resources can win it. A failed real fight is evidence to plan ordinary preparation, not a reason to weaken combat or seed levels.

Saddle totals are **8 rootstone, 2 ironwood, 6 wood, 10 fiber** across both recipes. Ordinary axe/pickaxe suffice; reinforcement is an optional durability investment, not a second tool identity. The current saddle is deliberately early-Band-4 gated. **Live roster correction:** `.artifacts/wave1-fresh-team.log:225` proves Ripplet, three Bramblebun and Mudsnout, not the Terrapup assumed in this report's initial draft. None of those three species has a `rideable` block in the current species catalogue; `creature_species.gd::is_rideable` therefore returns false. Meadowhart's block has `can_carry=true` and `requires_item=saddle`. Unless the actually carried roster has acquired another rideable by then, retain the S08 real Meadowhart catch and complete the ordinary five-slot replacement ceremony through UI. Record the outgoing member and newly accepted Meadowhart as a deliberate roster change, keep ownership at five, and update subsequent identity receipts to that actual result. The current `fresh_opening_segment.gd::catch_existing` deliberately refuses a full party, so it is **not** a usable full-belt catch/ceremony entrypoint without a bounded extension that proves the real pending-catch UI and replacement. No direct `party.add`, removal, hidden sixth slot, or granted saddle/creature. Riding does not itself unlock subsequent story gates, but this route should demonstrate its earned traversal use.

Warden victory grants the Cloudreach entitlement flag and Meadows heart reward before machine completion. The crossing additionally waits for `legendary_freed`; it does **not** require joining the legendary. `Game.can_enter_realm` checks the configured key **flag** (`autoload/game_state.gd:1410–1417`), so it is not an item that disappears after use. `RiftCrossing` sets the return-route unlock flag and invokes realm travel in its production collision callback; the harness must never call `enter_realm` itself.

## Current route geometry must win over old scenarios

* `SouthBridge.near_point`, `far_point`, and `depth_past_crossing` derive the actual carve frame. These provide both navigation targets and a signed physical crossing receipt. The real gate's `Interactable` is the target, not a remembered grunt position.
* Warrens has `marker("entrance")`, chamber markers, `marker("guardian")`, and `guardian()`. S06's old coordinates are not a safe substitute for the rebuilt site's markers and authored passages.
* Hall exposes `marker`, `has_marker`, `route`, `trainers_node`, and `recovery_point`. The current site is `[8,7560]`; the Warden's real marker is around `[8,7658.2]`, not his old trainer fallback position. Resolve actual markers rather than duplicating transforms. The machine is in the west legendary chamber, reached through its actual opened passage.
* The storm-road crossing now derives from the carve centered at `[-34.08,7535.62]` in `terrain_playground.json`, close to Hall. Use `near_anchor()` / `far_anchor()` and `span_ready()`, not the old village-scale spoke coordinates.
* **Aftermath placement caveat:** `village_npcs.json:221–234` still describes and places the storm-road traveller at `[184.2,52.6]`. A source search found no remapping in `village_npcs.gd`. Therefore a near-Hall acknowledgement NPC must not be assumed. The shortest verified authored acknowledgement location still needs to be selected from actual live NPC placements; the traveller may require a village-length backtrack. This is a bounded route/data mismatch to verify, not permission to set the objective flag or silently omit it.

## Reusable input seams and disqualifying setup

Gate F S05 = bridge; S06 = Quarry/Warrens; S07 = relay/mill; S08 = ironwood/saddle/captains; S09 = Sigil approach; S10a = Hall; S10b = Warden/machine/ceremony. Each wrapper starts with wipe/seed/boot from a prior exit and later saves an exit; `run_chain.sh` starts another process for the next segment. **Do not execute these wrappers inside the continuous claim.** S10c/d/e's older broad backtrack evidence is not itself a Cloudreach-entry requirement.

Small useful seams:

* `tests/helpers/stick_navigator.gd` supplies real collision-aware movement. Drive its `step()` from the live continuation so incidental combat can be observed and handled, rather than holding a navigation wait through a fight.
* `tools/gate_f/operator_harness.gd:2851,2881` resolve movement and moving entities; `:3254` validates the actual interaction provider; `:4026` advances dialogue with injected controls; `:6013` drives sticks. These are extraction references, not a reason to instantiate the fixture-owning runner.
* Gate F `_step_fight` at `:2681` injects quick/switch input, but its completion permits already-present `until_flag` and it does not supply modern strike navigation. Reuse `cloudreach_live_segment.gd::CampaignPilot` and the exact admission / observed round defeats pattern in `meadows_earned_tournament_segment.gd:87–126` instead.
* `meadows_earned_team_segment.gd::care_existing` provides one actual Satchel spend. Reuse for carried potions/revives/berries; a missing supply is a route requirement, not a direct heal.
* `gate_a_material_route.gd::_harvest_node` and actual craft UI input can support rootstone/ironwood/saddle. Never invoke `Game.craft`, inventory grants, tool reinforcement, or hotbar assignment directly from the continuation.

Do not reuse fixture entrypoints from `smoke_south_bridge_challenge.gd` (ensured party, poses, injected defeat/key), `smoke_riding_saddle.gd` (spawn/setup/saddle flags), `smoke_stronghold.gd` (route flags/poses), or `smoke_meadows_realm_handoff.gd` (granted handoff/load). Gate F force-aim writes rig yaw/pitch. The base combat pilot also has direct yaw/switch shortcuts; the new CampaignPilot overrides are required. Inherited Gate B direct feeding/healing/dialogue starts and arena teleport remain forbidden; the earned helpers deliberately override their reachable paths.

## Minimal implementation sequence

1. Add one live-context SouthBridge helper, owned inputs only, existing party identity receipt, observed actual trainer ID and two real victories, durable defeat/open flags and signed far-bank physical receipt. No boot/save entrypoint and no standalone fixture smoke. This is the first missing boundary.
2. Add a Quarry/Warrens live segment using its current room markers, real resource collection and guardian victory. Extend care/preparation only in response to observed actual stock/health needs.
3. Add relay→mill, then early Band 4 saddle/riding and captains. Reuse a small exact-provider trainer admission routine rather than copying the whole Gate F runner. Each boundary hands the same scene/Game/party onward.
4. Add Hall progression and the ceremony using current markers. Record an explicit roster choice in the route contract and validate that exact resulting five; do not pretend both branches preserve the same IDs.
5. Resolve the real acknowledgement location, walk to RiftCrossing, then hand the naturally loaded destination to the already extracted Cloudreach segment. The game owns its travel autosave and scene transition; the harness owns neither a reload nor a second fresh-start setup.

No end-to-end pass is claimed by this report. Parser/unit contracts can establish refusal and receipt logic; each authored segment still requires its actual input run, followed by the one uninterrupted fresh-save composition.

## Full-belt Meadowhart path — follow-up source audit

2026-09-08, after exact-head batch `8cd81b422e466a3918f471344750ee7edd15e883`. Report-only work; no helper, gameplay edit or world run. This section resolves the actual Ripplet/three Bramblebun/Mudsnout roster's riding obstacle.

**The intended production path is catch first, then decide which creature goes free.** There is no ordinary release-first control in the current Team screen. `autoload/party.gd:73–75` identifies removal as the ceremony's operation; the only `remove_at` caller under `scripts/ui/` is `tab_creatures.gd:1784`. Normal Team row activation outside the ceremony reorders/selects (`tab_creatures.gd:1369–1399`); it cannot make an empty slot in advance. Calling party removal, setting `pending_catch`, or invoking a farewell callback directly would manufacture the prerequisite.

### Production causal chain and exact controls

1. **Find and fight a real Meadowhart.** Species ID is `meadowhart`; do not select by a possibly renamed display label. It is already authored just beyond South Bridge: Band 2 `spawns.json:32–44`, order `5`, two bodies around `[40,0,1400]`, radius 16. Band 4 also has order `4` at `[60,0,6230]`, radius 20. These are search anchors, not spawn or pose instructions. Read `EncounterDirector.wild_creatures()`, choose a living visible actual body, walk to its exact `_engageable()` offer and verify `CombatManager.enemy_body()` after the press. Catching the early Band 2 herd is an available authored preparation path; saddle use still waits for ironwood.
2. **Earn capture without changing the five.** Real chip/aim/throw inputs must produce a successful catch. `encounter_director.gd:3311–3348` writes a single `Game.pending_catch` when the party is full; it leaves the five owned members intact and refuses a second pending catch. `Game._watch_pending_catch` (`autoload/game_state.gd:1062–1067`) then opens the `creatures` menu tab automatically after combat. Read the captured pending instance, species and exact existing IDs; do not set them. The pending display row is not an owned sixth slot.
3. **Choose an existing member to release.** `tab_creatures.gd:1641–1674` enters `_release_stage == "choose"`, holds menu input and focuses `_pending_button` (extended index `PARTY.MAX_CREATURES`, currently 5). Its five owned Buttons are `_rows[0..4]`. Read actual focus and drive `ui_up`/`ui_down` to the deliberately selected outgoing instance's current row. The focus fence wraps between first, last and pending rows (`:1858`), so use observed focus rather than an assumed fixed number of presses. `ui_accept` invokes the row's production Button handler (`:1369–1375`) and opens the farewell question. Choosing pending index 5 would reject the Meadowhart, not obtain a rider.
4. **Make the explicit irreversible confirmation.** The `confirm` stage initially focuses `_farewell_keep`, labelled **Keep them**. Read `_release_target` and the outgoing instance again; party IDs and pending identity must still be unchanged. Press `ui_down` to the actual `_farewell_release` Button, labelled **Let them go**, then `ui_accept`. Button wiring is at `:764–779`; focus fencing at `:1877`; state transition at `:1762–1800`. `menu_cancel` or accepting **Keep them** merely returns to choosing. Neither completes the replacement.
5. **Verify the real replacement and close the goodbye.** Production removes the selected member and **appends** pending to the resulting four (`:1784–1796`), then clears pending and enters `done`. Expected ordered IDs are therefore `before` with exactly the chosen ID removed, followed by the captured Meadowhart ID. Do not expect an in-place replacement at the old index: remaining rows shift and the newcomer lands at index 4. Verify size five throughout observable frames, released ID absent, other four unchanged/in order, pending null, newcomer present, and `done`. The focused **Back to the belt** (`_farewell_done`) takes `ui_accept` to `_end_release` (`:1828`); then `menu_cancel` closes the ordinary menu and world input must resume. This actual result becomes the next segment's retained-party baseline.

The route should accept an explicitly selected outgoing **instance**, then find its current row. Three Bramblebun species do not make the three individuals interchangeable; identifying a slot only by species could silently release a different earned creature. This audit does not choose which member the campaign gives up.

### Reuse boundaries

`fresh_opening_segment.gd::catch_existing` is intentionally **not** a full-belt API: lines 61–64 refuse five, and lines 66–74 require `before + 1`. Removing that check alone would also leave an impossible six-owned completion assertion. A bounded future capture-to-ceremony entrypoint can bind the same live context, retain exact admission checks, use the pure `_fight_until_catchable` and overridden `_catch_with_real_throws` (`fresh_opening_segment.gd:106`), then wait for real capture/pending/Team-choose state instead of party growth. `_live_catch_finished` (`:183`) already reads actual catch results/outcome and does not itself require growth. Preserve its earned orb inventory, HP, deadlines and enemy AI; do not fall back to the base opening helper's HP/inventory fixtures.

`tests/smoke_release.gd:252–309` documents the actual confirm/done controls and expected appended newcomer. **Its whole helper is unsuitable:** it fills the belt and writes `pending_catch` (`:94,110`) and directly `grab_focus()`s the outgoing row (`:254`). Only its input press primitive (`:351`) and receipt logic are reusable references; the continuation must navigate actual focus with input. `meadows_earned_team_segment.gd::_tap` is another existing event-input primitive. Neither stage needs a new generic UI framework or direct `Button.pressed.emit`, `_on_row`, `_do_release`, `menu.open`, or `hold_input` calls.

Old S08 is also not ceremony evidence: steps S08-41–45 chip/throw/wait and assert only `party_size >= 5`, then proceed to build/craft. They do not resolve a pending full-belt catch. The future continuous stage must prove the production ceremony explicitly rather than reuse that assertion.

### Paid saddle and mount prerequisites

Recipe IDs are `saddle_frame` and `saddle`; item IDs are `rootstone`, `ironwood`, `wood`, `fiber`, `saddle_frame`, `saddle`. Only `saddle` requires `recipe_saddle`, earned by `tournament_final_oskar`. Frame costs 5 rootstone/4 wood/2 fiber; saddle costs that frame plus 3 rootstone/2 ironwood/2 wood/8 fiber. Snapshot both recipes' actual costs and carried quantities before their respective presses, then require exact deductions and one output. Opening an actual camp/anvil `CraftInteractable` creates the production `craft_panel.gd`. Read its `_recipe_ids` and `_rows`; navigate `ui_up`/`ui_down` by recipe identity and use `ui_accept`. Button wiring at `craft_panel.gd:363` invokes `_craft` (`:458`), which alone calls Game crafting. The continuation must not invoke either callback itself.

S08's statement that Band 4 has no crafting site is stale. Current `band4_upper_meadows_ironwood/props.json:10–15,184–189` authors real camp crafting prompts at `[-238.03,6475.08]` and `[273.4,5654.9]`. `rest_point.gd:108–118` creates `CraftInteractable` by default unless `craft=false`; neither of these blocks disables it. Therefore a new paid workbench is not intrinsically required for saddle crafting. Choose the actual reachable authored prompt along the route; if building instead, retain the real paid placement/material proof.

After replacement and crafting, use party-cycle input to select the actual captured Meadowhart, deploy through `creature_recall` if necessary, and stow the hammer through its real hotbar input. `riding_controller.gd:242–269` requires an owned active living visible body and a carried `saddle`; `:789–839` suppresses mounting while the hammer is out and gives mounting lower priority than nearby ordinary interactions. Walk until this exact controller owns **Ride Meadowhart** (or its nickname) as an actionable offer, then Interact. `interaction_activate` at `:859` calls production `mount`; this fits the saddle automatically (`:326–355,665–676`) and earns `saddle_fitted_meadowhart`. The saddle remains carried, not consumed per mount. Read actual `is_mounted`, mounted body identity and movement under stick input; Interact on the high-priority **Dismount** offer ends the ride. No direct `mount`, `fit_saddle`, flag write, carrier assignment or camera pose change belongs in the continuation.

### Dorn is authored preparation, not a physical/objective prerequisite

The exact trainer ID is **`quarry_picket_dorn`**, not `quarry_dorn` (the latter appears in dialogue IDs). `data/config/bands/band2_stone_and_root/trainers.json:8–41` places him at `[315,1668]` by the approach road, with Bramblebun 9 / Burrowback 9, challenge `quarry_dorn_challenge`, defeat flag `defeated_quarry_dorn`, and real rewards 30 coins plus two `potion_small`. He has no `gate_fight`, minimum-level or key-reward field. A targeted search of current `scripts/` and `data/` finds the defeat flag only in that trainer row and its scope declaration, with no physical gate, harvest gate, Warrens entry or progression-objective reader. His trainer ID additionally appears in the reward/curve catalogues; those are authored pacing, not passage locks.

The actual main objective after `south_bridge_open` is `clear_the_burrow_warrens` → `warrens_cleared` (`objectives.json:143–149`). Quarry rootstone nodes (`band2_stone_and_root/harvest.json:7–65`) have item/amount/position and no Dorn flag gate; `items.json:173–178` requires the carried pickaxe. `old_quarry.gd` builds the foundations/hardware, not a Dorn-controlled door. Therefore S06's Dorn-before-quarry sequence is a sensible ordinary source of combat growth and two earned potions, but it must not be described as a mandatory unlock or blindly copied as one. If actual party health/strength calls for that encounter, drive his exact NPC challenge and real fight; otherwise a bounded Quarry segment can legitimately gather rootstone and proceed toward the required Warrens without synthesizing `defeated_quarry_dorn`. Keep the named encounter present/fightable under the content floor; absence of a hard gate does not make a missing Dorn acceptable.

## Earned Quarry / Warrens continuation — source complete, runtime unproved

2026-09-08 bounded implementation: `tests/helpers/meadows_earned_warrens_segment.gd` exposes `run(tree, current_world, Game)` after the preceding earned South Bridge segment. It requires the actual open bridge, durable guardian defeat, physical far-bank depth, the same live Meadows scene and five unique owned instance identities. It never boots, reloads, grants prerequisites or moves an actor by assignment. No driver or production change is included.

The route reads `terrain_playground.json`'s current `band2_stone_and_root` spine, walks to the nearest authored quarry join, then gathers the live rootstone nodes from `band2_stone_and_root/harvest.json` in authored order. **There are seven current deposits**, not just the five described in the older header: orders 12–16 plus second-seam orders 2020/2021 at `[381,1824]` and `[413,1820]`. Each gather observes the actual node/amount, equips the carried pickaxe through its real hotbar input, confirms the exact harvest prompt activation, and requires the production catalogue's full harvest-yield inventory delta. `QuarryInput` reuses only the existing material helper's input/observation methods, mapping rootstone to its stone/pickaxe equip seam. Missing stock, missing/broken tools, a refused prompt or underpayment fails; no replacement resource is spawned.

After the quarry, the helper follows the remaining Band2 spine to the authored `warren_undertrail`, approaches the current rotated `marker("entrance")` from outside along the entrance-to-mouth axis and the configured apron length, and walks the shortest **ungated** passage graph: mouth → hall → den. The cave's supplied marker Y is preserved; projecting room targets to `ground_height_at` would aim at the bank above the cave. The optional gated vault branch and old S06 room coordinates are excluded. Dorn remains ordinary optional preparation, not an invented unlock.

Care reuses the earned-team Satchel/party-cycle seam with the actual retained party and carried remedies. Wild encounters are piloted through `CampaignPilot._act`, one decision at a time under the existing **7200 actual physics-frame** wild budget, including a post-check that rejects a final press crossing the deadline. Incidental actual wild encounters are observable road events; unexpected trainer admission, actual loss or missing usable ally fails. The exact live `WarrenGuardian` body/creature must match current Burrowback level 14 configuration. Aggressive admission during room travel is observed, and explicit Engage verifies the exact offered body before pressing and the admitted body afterward.

Guardian receipts open on the actual CombatManager `entered` signal. The killing `hit_landed` event precedes production victory XP (`combat_manager.gd:1305–1310`), so it captures the active and surviving **instance IDs**, avoiding `last_xp_award`'s duplicate-label keys. Immediately after the one observed guardian win, before more walking or incidental XP, the helper requires HP zero, real landed strikes, `warrens_cleared` and `is_cleared()`, exactly **90 coins, 5 rootstone, 2 Greater Orbs, 1 Revive and 1 Hide Vest**, plus the configured **140 XP per surviving member** in addition to that member's actual ordinary victory share. XP is compared cumulatively across level rollover; fainted members must receive neither payout. Preowned stock, a flag alone, missing/duplicate rewards and replaced identities fail. It then walks the same passages back through the live entrance to the outside approach and requires the retained five in the original world.

Focused validation: `test_meadows_earned_warrens_segment.gd` **8 tests / 82 assertions / 0 failures**; engine log `%TEMP%/meadows-earned-warrens-tests-20260908.log`. Helper parser also passed. Early focused runs caught an incorrectly named test assertion and the stale five-deposit expectation; both were corrected without gameplay or limit changes. **No world run has been performed or claimed.** Actual trail/cave collision traversal, guardian balance against the earned party, remedy sufficiency and the uninterrupted integration remain pending the parent's serialized RAM slot. These tests establish route selection and strict receipt/refusal arithmetic, not playability or completion of the chapter.

## Fresh-team boundary diagnosis — observed 293.92-second prefix failure

`.artifacts/wave1-fresh-key-nav-camp.log:261–264` records the second failed team approach: the real opening/key/village path had passed, the team had actually reached five and won one ordinary training fight, then the 3600-frame approach to `Wild_mudsnout_1_2` ended at player `(-37.72714,2.680571,4.499945)` and wild `(-54.17064,5.498205,23.00343)`. World input was free and locomotion enabled. The logged last slide contacts named Terrain, so those names alone did not identify the obstacle.

The bounded geometry audit loaded only the local baked height-map resource. Its sampled heights matched current `playground_heightfield.gd`; the slope around the terminal position is approximately **12.7 degrees**, below Player's 45-degree walkable limit. A small native HeightMapShape3D patch built from those baked samples, the production Player locomotion script with its standard 0.4m-radius/1.8m capsule, and a plain camera basis reached the target's 5.5m vicinity in **257 frames** under both a freshly reset existing navigator and straight physical-stick input. Both ended at `(-50.52888,5.425577,18.90151)`. This diagnostic contains setup poses and a reduced scene; it is **not** a campaign run or proof of full-world traversal. Log: `%TEMP%/earned-team-nav-geometry-20260908.log`; diagnostic source `.artifacts/probe-earned-team-nav.gd` is uncommitted evidence tooling. Its initial edge-sampling index warning was corrected before the final clean measurement.

The actual route error is the village boundary: the stopped player is inside the current polygon, the chosen Mudsnout outside. Polygon vertex 11 is `(-38,6)`. Production `village_boundary.gd::_build_corner_guards` builds `FenceCornerGuard_11` with 1.1m half-width; adding the Player capsule's 0.4m radius puts the blocked south-side centre at **z=4.5**, matching the recorded stop to 0.0001m. The direct bearing to the wild intersects that solid guard. Opening `road_gate_open` opens the authored leaves; it does not remove this fence or its corners. The existing straight target chase had no village-exit route and could remain committed against this boundary. This geometry identifies the wrong crossing; it does not reconstruct every unlogged navigator decision during the failed 3600 frames.

The bounded team-helper repair derives inside/outside classification and gate approaches from current `village_boundary.json`. It considers only the actual live `road_gate.gd` nodes whose `flag_id` is `road_gate_open`, `_open` is true and real `_shape` collision is disabled. For the recorded approach it selects **PondGate `(-21,21)`**, walks the inside approach, actual leaf centre and outside approach, then continues toward the same latched wild. Each approach offset derives from the authored gate orientation and clearing distance. The lead-in and departure segments must not cross another fence edge. Tests prove the only crossing is the actual PondGate opening, and exercise the reverse direction. Missing/closed leaves or unsupported same-side chords through the polygon fail closed.

All gate waypoints share the original **3600 total approach frames**; no retry, extra catch, replacement target, larger distance or relaxed admission assertion was introduced. Actual creature identity remains checked before pressing and after admission, and an encounter interrupting an unfinished gate crossing fails. Additional failure telemetry records contact paths/normals, player velocity, input vector and navigator detour state. No production terrain, collision, navigator or gameplay file changed.

Final focused validation: `test_meadows_earned_team_segment.gd` **11 tests / 85 assertions / 0 failures**, `%TEMP%/meadows-earned-team-boundary-tests-20260908.log`; helper parser passed. The gate guard unit instantiates the production road-gate script with a test-owned collision shape to verify its actual field contract; it does **not** build or operate a world gate. The full fresh prefix has not been rerun for this change, and the repaired approach remains unproved in the complete world until the parent's serialized runtime.

### 2026-09-08 — earned Relay → Mill continuation and reproduced gantry defect

`tests/helpers/meadows_earned_relay_segment.gd::run(tree, current_world, Game)` now continues the same live Meadows world after the earned Warrens clear. It retains the five creature instance identities and rejects preexisting captain/rescue/relay/Mill flags, a preowned bridge gear, or an already-open crossing. It reuses only the existing Warrens helper's ordinary movement, wild-combat, Satchel care and physical-input seams; it never enters that helper's run/setup. No actor pose, HP, condition, XP, inventory, progression, dialogue-state or production callback is assigned by the new continuation.

The route derives the current Undertrail exit, remaining Band2 trail, Band3 road and Relay approach loop from `terrain_playground.json`, then uses the live relay's `world_of()` transform to enter its authored arch. That arch has no locked leaf or picket prerequisite. Hess, Orrin and Dell therefore remain optional; their old S07 fixture sequence is not imported. The captain is the actual `Trainers.body_for("relay_captain")`, admitted by the real prompt and completed challenge dialogue. Its three exact current opponents are Galecrest 11, Duskhush 11 and Tuskroot 12. One input decision at a time remains under the existing 9000-physics-frame trainer deadline. Actual admissions, killing hits and wins must match all three; the immediate receipt requires 60 coin, 3 Greater Orbs, 1 Revive and each retained creature's exact earned XP. XP is accumulated per killing-hit active/surviving identity before production awards it, including the final 120 bonus only for surviving members. No gear is attributed to this captain.

The actual `RelayNPCs/Sela/Interactable` must finish `relay_captive_freed`. Production's third dialogue line grants exactly one `mill_bridge_gear` and records `captive_rescued`; both are required. The continuation then walks the authored ramp, gantry centre, shared deck edge and pad before physically activating `TetherRelay/ApparatusSeam/Console/Interactable`. It verifies `relay_disabled`, the live disabled state, and the actual lit-conduit count changing from positive to zero. The deck legs watch every physics frame for a drop more than the existing 0.6m arrival tolerance below deck height, so a fall and later recovery cannot count as supported traversal. The route descends the same ramp, exits through the arch, rejoins the authored Relay loop and approaches Old Mill Crossing on its near bank. Its exact gate input must consume the one earned gear, open the real crossing and record `mill_crossing_restored`; ordinary walking must then reach the authored channel's far bank with the same five identities.

**Reproduced production traversal blocker and minimal repair.** `tether_relay.gd::_build_decks` previously used `atan2(_u.x, _u.y)` for a box whose config width means site-s and depth means site-t. That maps local box X to negative site-t and box Z to site-s, rotating the rectangular gantry footprint by 90 degrees. Actual built native collider corners were s=[-3.6,-0.4], t=[-15,-7], leaving the authored ramp head 1.9m from the gantry and the pad another 2.4m away. The prior relay smoke independently placed the player on each deck and tested ramp peak height, so it did not prove the connecting walk.

A bounded native scene built the production decks/ramps and an 80m sampled heightfield patch, then used the production Player controller and physical stick input from one initial fixture placement before the ramp. Before the repair it reached the ramp, fell during the gantry leg to Y=5.5213, and exhausted that leg's unchanged 600-frame deadline. Parent-authorized repair changes only deck yaw to `atan2(-_u.y, _u.x)`, aligning local X with site-s and local Z with site-t. The actual gantry footprint becomes s=[-6,2], t=[-12.6,-9.4], touching the pad at s=2. The identical continuous input route then reached foot/ramp/gantry/seam/pad in 21/157/42/48/12 frames. Every supported deck leg stayed above Y=10.00074. Dimensions, deck height, slope, physics settings, materials and config remain unchanged.

Durable regression `tests/smoke_relay_deck_walk.gd` reproduces that bounded native route with one initial diagnostic pose and no intermediate relocation; each deck leg checks its minimum supported Y as well as actual floor contact at arrival. It is **fixture collision evidence, not an earned chapter or complete-world pass**. The new focused contract suite passed **6 tests / 61 assertions / 0 failures**, with no engine errors, in `%TEMP%/meadows-earned-relay-tests-20260908.log`. The native smoke passed with exit 0 and no engine errors in `%TEMP%/relay-deck-walk-smoke-20260908.log`; the before/after diagnostic logs are `%TEMP%/earned-relay-decks-before-20260908.log` and `%TEMP%/earned-relay-decks-after-20260908.log`. The helper's parser passed. An earlier focused run exposed mixed string/dictionary dialogue lines in the test reader; it was fixed before the final clean suite.

**Still unproved:** the complete earned Warrens→Relay→Mill world run, the carried team's captain readiness/stock, incidental encounter navigation, and actual world console/Mill interaction. No full-world slot, retry, reset, seed, grant, reload or hidden preparation was used. The driver is not modified or integrated by this agent; the parent owns that next composition step and CI registration of the bounded native smoke.
