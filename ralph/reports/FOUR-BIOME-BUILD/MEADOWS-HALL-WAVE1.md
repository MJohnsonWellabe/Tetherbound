# Meadows earned Sigils and Hall — Wave 1

## Scope established before implementation, 2026-09-08

Current production progression is: restored Mill crossing → defeat `captain_riverwatch`, `captain_field`, `captain_ridge` → carry their real `river_sigil`, `field_sigil`, `ridge_sigil` → interact with the real `SigilGate`, spending all three and recording `hall_approach_open` → physically enter Meadows Hall → defeat `stronghold_patrol` in `outer_works`, `stronghold_courtyard` in `courtyard`, and `stronghold_elite` in `tether_approach` → walk the opened passage into `warden_arena`.

Sources: `data/progression/objectives.json:175–209`, `scripts/world/playground_world.gd:221–222,1713–1720`, `data/config/stronghold.json::passages/gauntlet`, and `scripts/world/stronghold.gd:5258–5289`. Riverwatch's actual trainer is in the Band3 catalogue, although it belongs to the Upper Meadows captain objective; field and ridge are in Band4. Each Hall passage uses its preceding trainer's `gated_by_flag`, with the final legendary-chamber passage gated separately by `defeated_warden`.

No saddle, rideable creature, mount, capture, craft or Dorn flag is a production prerequisite for those gates. The prior fresh five lack a rider, and an earned saddle/mount route remains separate, unimplemented preparation; this Hall segment must not claim that experience demonstrated. If a required capture/craft prerequisite is encountered in current production, stop and report it instead of granting or bypassing it.

Planned bounded helper: same live world/Game and retained five after the completed Relay/Mill helper; current authored roads and live trainer/Stronghold markers; actual care with carried remedies; exact trainer prompt/dialogue admissions, ordinary piloted combat, configured rewards; real gate consumption and physical passage receipts. Stop after the elite and actual Warden-arena entry. Warden battle, machine, legendary roster choice and aftermath are outside this segment. No driver/shared/production edits, commits, or full-world tests in this task.

## Implemented bounded continuation

`tests/helpers/meadows_earned_hall_segment.gd::run(tree, world, Game)` is implemented. It requires the completed Relay/Mill flags, an actually open Mill crossing, zero remaining bridge gear, and the player on the Mill's physical far bank. The actual five creature instance IDs become the retained baseline. Preowned Sigils, already-defeated captains, an already-open Hall approach, missing/open Hall shutters, or a defeated Warden fail before traversal.

The road is derived from the current Band3/4/5 spine, starting at the Mill road's far-side point `[-152,4235]`. The actual captain bodies lie on it in ascending order: Riverwatch `[-100,4350]`, Field `[170,5590]`, Ridge `[-280,6460]`. Riverwatch is correctly read from the merged production trainer catalogue despite residing in Band3's data file. The next gate lies at the live `SigilGate` transform; near/far approach points follow its actual rotated forward axis and must lie on opposite sides of the adjoining road points. The real Interact/dialogue sequence must spend exactly one each of all three Sigils, record `hall_approach_open`, disable the actual leaf collision, and physically carry the player across its plane. It cannot count a preowned/duplicate/unspent key or flag alone.

Every required trainer is challenged through its exact live provider and completed authored conversation. The inherited Relay killing-hit observer captures ordinary per-opponent XP and survival by creature instance before the production award; a named-trainer admission override verifies the exact trainer ID, species, level and opponent order. The required battle then uses one ordinary CampaignPilot input decision at a time under the existing **9000 physics-frame** trainer budget. All expected opponents must be admitted, killed with actual hits, and won; each current configured item/coin payout and per-creature XP delta must match exactly. Care uses only the existing real Satchel/remedy/party-selection input seam with carried stock. A loss, insufficient usable stock, wrong admission or deadline ends the segment.

After gate passage, movement leaves the terrain road at the actual Hall entrance/ramp marker; room, trainer and passage positions come from live Hall nodes and markers. The approach retains `smoke_stronghold.gd`'s **950-frame** entrance budget. The three Hall passage crossings retain its **600 total physics frames each**, shared by returning from the trainer to the room axis, reaching the actual shutter plane, and arriving in the next room. They do not get a fresh full budget per waypoint. The real shutter mesh must be visible with enabled collision before its guard, then hidden with disabled collision after the actual victory. Missing shutter objects fail closed (the production `door_is_open()` query alone treats missing doors as open, so it cannot establish this claim). Walking on the Hall floor uses the inherited continuous supported-height guard; falling and recovering cannot count as supported room traversal.

Successful completion is `defeated_stronghold_elite` plus ordinary arrival at the actual `warden_arena` marker, the same ordered five identities, the same Meadows world/Game, no active fight, no `defeated_warden`, and the actual Warden-to-legendary shutter still closed. This is explicitly an **on-foot** route. It neither earns nor claims riding, a saddle, a replacement catch, the Warden victory, or the ending.

## Focused validation and limits

`tests/test_meadows_earned_hall_segment.gd`: **8 tests / 72 assertions / 0 failures**, with no engine errors, `%TEMP%/meadows-earned-hall-tests-20260908.log`. Tests cover the real road/captain ordering; missing or mismatched guard/passage prerequisites; exact Sigil reward/spend including duplicate/missing/unspent negatives; live rotated gate geometry; shutter fixture mesh/collision fields including absent shapes; one shared room-hop deadline; and exact named/opponent admission with wrong-trainer and duplicate-opponent negatives. Shutter objects in that unit are fixtures matching production's fields, not an operated Hall world. A first test run used a sub-ULP absolute-distance tolerance at world z=7400; the new geometry test now permits one float32 ULP (0.0005m), while gameplay arrival and traversal bounds remain unchanged. Final helper parser is clean.

**No full-world run was performed.** Actual captain/gauntlet combat readiness with the carried roster, paid remedy sufficiency, long-distance navigation, Hall entrance/corridor passage and gate interaction remain unproved until the parent's serialized uninterrupted runtime. No production file, shared helper, Relay file, driver, save, branch state or gameplay configuration was edited by this task. No commit or push was made. The parent owns integration and the next runtime slot.
