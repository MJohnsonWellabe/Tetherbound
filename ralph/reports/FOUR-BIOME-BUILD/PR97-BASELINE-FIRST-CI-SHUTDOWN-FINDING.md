# PR97 first CI baseline failure — fixture shutdown diagnosis

CI34362371531, exact head452a7d10e. First attempt retained; no rerun. Downloaded artifacts: `.artifacts/pr97-452a7d10-ci/lifecycle-first/`. Baseline receipt fails after3.093513 seconds on staying native stderr. All three baseline stdout results reach host29/departing29/staying23, but eight native errors invalidate acceptance. Other exact-head cases: actual Game108 with its four declared errors, cancellation77 and latejoin82 clean. This report does not relabel the baseline as passed.

All eight staying errors are `The multiplayer instance isn't currently active` at ENet get_unique_id, cpp438. Host/departing baseline stderr are empty. The fixture terminal function stops its own physics and coordinator, disconnects normal Session disconnect handlers and waits0.3s, while native synchronizer internal callbacks remain active and the ENet object remains attached. Host can finish its timer before a receiving client. That client then retains an inactive ENet reference during the terminal window. Four staying bodies each have State and Admission synchronizers, matching eight callbacks/errors. Separate stdout/stderr do not provide a precise interleaved timestamp, so the report identifies the causal source path rather than claiming captured per-error timing.

Exact engine source supports the mechanism: synchronizer internal idle invokes visibility updates; replication's visibility callback queries the unique peer ID; disconnection status processing clears replication state while the disconnected peer object can remain assigned. The fixture suppresses normal Session teardown, which normally releases that peer. Sources: [synchronizer](https://raw.githubusercontent.com/godotengine/godot/5b4e0cb0f/modules/multiplayer/multiplayer_synchronizer.cpp), [replication visibility](https://raw.githubusercontent.com/godotengine/godot/5b4e0cb0f/modules/multiplayer/scene_replication_interface.cpp), [SceneMultiplayer status](https://raw.githubusercontent.com/godotengine/godot/5b4e0cb0f/modules/multiplayer/scene_multiplayer.cpp).

## Approved bounded correction

Only `tools/probe_realm_transition_adapter_peer.gd` changes. All protocol assertions and automatic native cadence remain live through the proof. In post-assertion terminal cleanup, enumerate actual bodies/synchronizers and stop their automatic visibility callbacks, record exact count, then retain the existing notification flush and explicitly detach/close the fixture's own transport before quit. Expected terminal synchronizer counts: baseline host12/departing4/staying8; cancellation all12; latejoin host14/departing4/latejoin10. A mismatch produces a failure without changing the existing81/77/82 assertion counts. Raw error scanning is unchanged; none of the eight errors is allow-listed.

This is a fixture lifecycle repair, not a production policy change. No Godot validation was run while the character lane held the lease. Root reviews this source before a new changed-head attempt; the original CI failure remains retained.

## Corrected source validation

After lease release, one candidate per unchanged mode passed: baseline81 in9.145587s, cancellation77 in8.049225s, latejoin82 in11.009490s. Artifacts `.artifacts/realm-terminal-baseline-v1/`, `realm-terminal-cancel-v1/`, `realm-terminal-latejoin-v1/`. All18 raw logs clear of ERROR, SCRIPT ERROR and WARNING; all launcher exits0. Exact terminal synchronizer counts matched baseline12/4/8, cancellation12/12/12, latejoin14/4/10. Max system commit55%, max process count260. Global Godot count0 on immediate lease release. No further run; original failed CI is retained, and a new exact-head CI remains required.
Final adapter SHA256: 2C7FF163DFF10070FE3F5BDB5E61B7BF9E9482DB4C7F31648A538382443FB294
