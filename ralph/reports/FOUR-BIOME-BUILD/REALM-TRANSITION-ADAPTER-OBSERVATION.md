# Adapter observation — drain stall identified

2026-09-09. One root-authorized observation only, eight seconds after coordinated begin. No acceptance credit. Artifacts and exact source hashes: `.artifacts/realm-transition-adapter-20260909-observe1/`. Total launcher interval 15.428074 seconds. All three final state dumps were captured, then owned launcher/engine trees stopped. Follow-up CIM inspection found zero Godot processes. All six role logs contain zero native errors, script errors, warnings or fixture failures.

Host final state: round 2, phase `draining`, all three installation acknowledgments, 12 directed channel acknowledgments, no missing fences. Departing final state: `despawn_fence=true`, Trainer receipt/body inventory 2/2, Creature 2/2, Item 0/0. Staying also retains 2/2 per active kind; host has three bodies per kind. The blocker is actual receiver inventory, not request grant or either quorum.

The exact engine revision `5b4e0cb0f` explains this pattern. `modules/multiplayer/scene_replication_interface.cpp::_update_spawn_visibility(0, ...)` first evaluates synchronizer visibility for peer zero. If true, its public fast path skips per-peer visibility checks. `_update_sync_visibility` similarly ORs the zero-peer predicate with each specific peer predicate. The production adapter's `refresh_visibility()` calls `update_visibility(0)`, while its scoped predicates allow observer zero for unaffected owners. Thus their bodies stay admitted at the departing receiver. Mover-owned bodies deny zero through the owner match and are removed, matching the observed one pair removed at each receiver. The earlier passing protocol fixture called `update_visibility(actual_target_id)` explicitly.

Proposed bounded correction, pending review: enumerate actual connected peer IDs for the adapter's all-observer refresh, preserving explicit-observer calls and all policy predicates. This also refreshes owner-state visibility per real observer instead of treating zero as a universally visible recipient. No behavior correction or further candidate run was included in this observation.

Root subsequently approved that correction and its necessary automatic-update safeguard. Engine synchronizers default to idle visibility updates, whose internal callback also calls `update_visibility(0)`; the earlier passing toy explicitly disabled this automatic mode for both synchronizers. The production correction preserves native cadence and defines scoped zero-peer permission as the conjunction of permissions for actual connected recipients. If any scoped recipient is denied, zero returns false so the engine checks each recipient. Existing baseline filters remain separate and unchanged. Empty recipient sets remain public; the aggregate never invokes its predicate with zero. Phase refresh additionally emits explicit real-recipient updates. Tests were added for these semantics and the native fixture records emitted observer IDs, but the corrected source has not yet run while the character lane imports assets.

Engine children were correctly identified: host 6152 peak 143.558594 MB, departing 13444 peak 143.839844 MB, staying 5984 peak 143.281250 MB. Console launchers were 18016/20768/21380, approximately 7.25 MB each. Engine exit codes are unavailable in the receipt after process-tree termination; terminal absence was verified separately. System memory peak 79.672475% used, minimum free 1589.855469 MB, below the 90%/400 MB stop thresholds throughout.

Game remains unconnected. This diagnosis provides no real traversal, rollback, initial-join or host compatibility acceptance.
