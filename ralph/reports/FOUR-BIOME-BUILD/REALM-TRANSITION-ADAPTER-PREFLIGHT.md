# Adapter contract preflight after two retained failures

2026-09-09. Root directed a contract review rather than another immediate native run. Corrected fixture parse and a **pre-network 18-check inspection** passed, exit0, with no native or script errors. No ENet connection was created. Neither earlier failure is relabeled a pass.

The `preflight` mode reads `data/config/realm_hearts.json` and verifies both `meadows` and canonical `water` through the actual Game Realm Hearts service, including existing scene resources. It checks the mounted `/root/Game/Session/RealmTransition` path; actual Session/coordinator argument and return types; all three source spawner paths and empty registered inventories; trainer and creature factory authority before tree entry (State owner20, Admission owner1); and absence of any destination receiver.

The preflight explicitly prints `Game entry flag=realm_key_water can_enter_target=false`. This fixture starts with a fresh Game and tests the transport component directly, after snapshot readiness; it does not invoke `Game.enter_realm` and earns no entry-key or full travel acceptance. Its Session registry bootstrap supplies all three actual ENet identities before the coordinator request. The upcoming real Water smoke must take the actual router and its seeded gameplay requirements.

Review also corrected a proof-quality defect: earlier request/reply methods were on the persistent probe hub. They now reside on the source world's `Producer`, which is actually deleted after drain, so their path has the lifetime being tested. Continuous position, reliable rotation and reliable body-presentation producers run through actual synchronizers and the production scoped gate. The staying peer never constructs a target world. Trainer and creature target bodies retain immutable host-origin metadata.

Limitations remain explicit: procedural shell creation and encounter internals are tiny seams. The probe checks prior scene reply delivery and retained participant membership, not the actual combat manager's complete catch, reward or fight progression. Initial/late join and reconnect are not exercised by this pre-snapshotted native setup. A separate focused director contract test passed **1 test/7 assertions** for initial/reconnect snapshot denial followed by resumed deployment; that stubbed test is not an actual Session handshake proof.

Remaining native risks: real two-round fence progression, actual receiver drain before subtree removal, destination admission with absent staying-target path, continuing movement, and raw engine shutdown errors. Subsequent Game cancellation/rollback, late scoped join ACK, ordinary host/solo compatibility, default Water route and exact-head full CI remain required. No third multi-peer candidate has been launched at this checkpoint.
