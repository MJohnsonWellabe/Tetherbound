# Native actual-Game cancellation — first attempt failed

2026-09-09. Approved one candidate with tiny actual Session/coordinator/spawners and actual Game entry. `.artifacts/realm-transition-cancel-native-v1/` retains every role stdout/stderr and process/resource receipt. Invocation: `tools/run_realm_transition_adapter.ps1 -ArtifactName realm-transition-cancel-native-v1 -Port 39729 -Cancellation`. A prior read-only preflight passed 18 checks. No world run or retry.

**Failure after 12.322355 seconds.** The first captured raw error was departing peer `Node not found: "AdapterWater/TrainerSpawner" (relative to "/root")`. Its raw stderr contains ten errors: missing TrainerSpawner and CreatureSpawner paths, each followed by null node, missing cache ID and null spawner/on_spawn_receive; then two `!pinfo.recv_nodes.has(net_id)` / ERR_UNAUTHORIZED / on_despawn_receive errors. Host and staying raw stderr are clear. Any fixture success markers are invalid as acceptance because these native errors occurred. The runner stopped owned processes after detecting the first raw error; all Godot processes were terminal on release. Root was notified immediately so the queued import could proceed.

The fixture extends the real coordinator only to inject a cancellation error immediately after its unmodified `_check_local_drain` has sent the real empty-inventory receipt. It does not fabricate a host outcome, drain count, admission or readiness. Mover calls actual Game.enter_realm with its tiny real source as current_scene. The log records drained=true while source trainer/creature receipts and actual body parents are all empty, then the host's real recovery_required settlement. Actual Game retained the source root, observed source readiness, and only then six actual source-body factories ran. Local token/pins/overlay cleanup checks completed. These useful observations identify where the remaining failure occurs; they do not establish a clean cancellation path.

## Source diagnosis and bounded proposal

`_retarget` replaces `tx.to` with the source before calling Session membership reversal. The fixture correctly queues old destination bodies for deletion on the membership event. During that still-live interval, the prior target is no longer named by admission/outgoing transaction gates. Origin policy exempts the body's owner, so those abandoned Water bodies can become visible to the mover even though it never created a Water receiver. An automatic/phase refresh can enqueue their spawn before queued deletion, explaining the missing target path followed by unauthorized despawn. This causal explanation is based on source plus matching raw errors; no instrumented engine ordering beyond those logs is claimed.

Proposed minimal repair for root review: before overwriting the target, retain a receiver denial for that abandoned realm using the existing token/generation history lifecycle; distribute it with the transition snapshot and retain it through completion until that receiver explicitly readies the realm in a later crossing, or disconnect/reset. Do not create a dummy target, sleep for deletion, or suppress errors. Cover pending abandoned-target body visibility and stale readiness in focused tests before considering one corrected candidate. No production repair or rerun is included in this first-failure receipt.

Resource guard receipt: maximum system commit 52%, maximum global process count 257. The resource ceiling did not cause this failure.
