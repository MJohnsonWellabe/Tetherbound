# Corrected native actual-Game cancellation — pass

2026-09-09. Root approved one changed-source candidate after the preserved ten-error first attempt. The correction retains abandoned-target receiver history before retarget/membership reversal and rejects duplicate or stale retargets. No dummy target, delayed deletion, error suppression, deadline weakening or world run. Invocation: `tools/run_realm_transition_adapter.ps1 -ArtifactName realm-transition-cancel-native-v2 -Port 39739 -Cancellation`.

**77 checks passed**: host 22, departing 32, staying 23, including 54 preflight checks. Elapsed **8.6059023 seconds**. All three launcher exits 0. All six role stdout/stderr logs contain zero ERROR, SCRIPT ERROR or WARNING. The first failed candidate remains a failure; this is the first candidate after the abandoned-target source correction, not an unchanged retry. Artifacts: `.artifacts/realm-transition-cancel-native-v2/`.

The actual mover Game.enter_realm calls the real Session coordinator. The fixture injects only cancellation after unmodified real drain logic confirms empty actual body parents and empty engine receipt inventories, before the host loading install is consumed. It does not inject the outcome or admission. The real host returns recovery_required. Actual Game retains its original source root and observes source readiness; six actual trainer/creature factories then respawn bodies. Assertions verify host membership restored to source, transaction/pins/local state cleared, owned overlay dismissed, and all six source trainer/creature bodies restored. The staying peer still has no Water target path and receives host motion; the host still receives staying motion and reliable body presentation. All these assertions are accompanied by clean native logs.

Focused tests before the candidate passed **22 tests / 105 assertions**, raw log clear, in `.artifacts/realm-transition-cancel-v2/unit.log`. This covers admission/outgoing/scene RPC denial for still-live abandoned target bodies, retention after completion, exact future readiness, unrelated baseline, duplicate and stale retargets. The shell accidentally printed the following no-match `rg` exit code as the Godot exit, so this receipt relies on the explicit test summary and raw log and does not claim a separately captured unit process exit code.

Resource receipt: max system commit **52%**, max global process count **257**. Actual engine peaks: host PID14176 **143.50 MB**, departing PID13932 **143.89 MB**, staying PID4764 **144.14 MB**. Child exit-code handles were unavailable; all children were terminal and global Godot count was independently verified as zero. All recorded source hashes matched after execution. Lease was explicitly released to root and the queued character fixture immediately after terminal inspection.

Limits: this proves an injected cancellation at the real drain event, not a naturally elapsed 120-second network timeout. The actual Game recovery path and native protocol now compose in a tiny authored world; the coordinator's baseline fixture remains pre-snapshotted. Scoped native latejoin, broader host compatibility/occupied-world defect and exact-head full CI remain open. The prior default Water production pass was on the pre-cancellation-correction snapshot and is retained with its original scope; it is not relabeled as validation of this newer source.
