# Scoped latejoin receiver implementation and first focused evidence

2026-09-09. Approved six-file scope: origins/coordinator, their tests, existing native adapter peer fixture and PowerShell runner. No Session rewrite, world run, commit or push.

Scoped origin rows now require explicit receiver membership; unknown transport identities are denied immediately. Host/immutable owner and untagged-body baseline are preserved. Joining receiver generations remain denied until matching actual realm readiness. New cohorts inherit pending generations. Disconnect removes membership; sequence tombstones and matching-generation deltas reject stale pending/readiness messages. Ready messages do not overwrite full origin rows.

The first policy ACK still releases the ordinary Session snapshot and peer_joined. A separate same-permit receiver-ready phase requires actual snapshot_ready and the existing realm/spawner readiness check. Host validates live permit, phase and registered realm. Policy snapshot refresh preserves the original generation/deadline, avoiding artificial revision churn between simultaneous joiners. The original 120-second deadline spans both stages. Expiry explicitly cleans coordinator membership and invokes existing Session.kick; existing session-end/title behavior supplies recovery instead of indefinitely retaining a joined invisible receiver. No new UI architecture.

First focused run passed 27 tests/141 assertions, exit0, raw clear. A static follow-up identified generation churn on policy refresh; correction plus focused regression passed **28 tests/147 assertions**, exit0, raw clear. Both logs retained in `.artifacts/realm-transition-latejoin-unit-v1/`. Fixture preflight passed **18 checks**, exit0. At that preflight checkpoint native acceptance was pending. The subsequent first native candidate passed82; the final revision-ACK follow-up passed29 tests/154 assertions. See REALM-TRANSITION-LATEJOIN-NATIVE-FIRST-ATTEMPT.md for exact source boundaries.

Invocation contract remains `tools/probe_realm_transition_adapter.gd -- ROLE PORT latejoin`. Roles are host, departing, latejoin. Start host and departing; only after exact host stdout marker **ADAPTER LATEJOIN READY** start the third process. Marker follows actual completed departure with live scoped origin bodies. The latejoin role uses actual Session.join/hello/policy/snapshot. It asserts the pending generation exists and matches installed origin rows when snapshot_applied fires. Host peer_joined then creates an extra real scoped source Item cohort while receiver readiness is pending, exercising deny inheritance and actual admission after snapshot/readiness. Host and existing Water mover remain active.

The approved native candidate subsequently ran after static checks, with internal60s/external90s guard, actual engine descendants, isolated profiles, 90% system commit/400 process ceilings, raw-error stop and preserved first attempt. Preflight does not claim a real handshake; the initial host/mover remain explicitly pre-snapshotted.
