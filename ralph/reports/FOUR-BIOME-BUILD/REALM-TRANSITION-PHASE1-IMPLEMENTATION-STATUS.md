# Phase1 implementation receipt — partial, unlanded

2026-09-09. Shared staging workspace; no lane commit, push or branch change. `Game.enter_realm` is integrated as an unshipped client-only draft. Native component, tiny Game control flow and the first default production Water Alpha route pass; broader multiplayer and CI gates remain open.

## Implemented draft

- Production trainer/deployed-creature adapter: host-owned empty AdmissionSync, preserved host trainer baseline, remote-owner fallback, scoped state gate, and actual engine spawn/despawn receipt inventory.
- Persistent Session coordinator: two rounds of reliable channel0/channel1 fences. Every participating receiver first acknowledges the phase installation, so a fence on another channel cannot arrive before that receiver knows its token. Sender fences have separate sender/receiver/channel identities.
- Mover's committed catch/result is allowed to settle before the first fences; new actions close. Remaining players keep movement and encounter participation. Prior replies remain open until the second fence round. Body presentation/landing and director scene-bound sends have recipient gates.
- Shared trainer XP application can use the persistent coordinator when the original director receiver is retired; the same party eligibility/message code is used by both paths. Ordinary persistent ledger and SleepVote traffic stays outside the scene gates.
- Receiver history survives a completed departure. Successful readiness compares the exact captured deny and session epoch; stale completion cannot reopen a newer departure. Disconnect and session reset remove the identity.
- Actual authored spawners are enumerated; unsupported nonempty kinds refuse departure. Empty authored spawners are observed. Source/target pins protect the active finite client transaction.

## Verification so far

- Adapter unit first source had two `SCRIPT ERROR`s from calling the C++-only `is_visible_to` method. Runner exit0 and its summary were misleading; that attempt is a failure. Corrected production baseline predicates passed 4 tests/12 assertions, with no raw errors.
- Receiver-history tests passed their first native run: 4 tests/14 assertions, no raw errors.
- First coordinator parse found an inferred Dictionary-derived `needed` count without a type. Explicit `int` correction parsed cleanly.
- Mounted Session/coordinator and shared director latest parse checks passed. Coordinator gate tests passed first run: 5 tests/22 assertions, no raw errors.
- Origin tests passed first run 4/19; persistent reward tests passed first run 2/6; updated coordinator tests passed 5/22, all without raw errors.
- The first native production adapter candidate failed before protocol checks on the pre-existing client timeout lookup for a relayed peer. All roles were stopped at the first raw error and retained. See [first-attempt receipt](REALM-TRANSITION-ADAPTER-FIRST-ATTEMPT.md). No real Water route, full CI or host regression claim yet.
- The narrow physical-peer timeout guard passed 2 tests/8 assertions. The second native candidate reached live replication but refused the fixture's invalid `water_archipelago` target. Its six raw logs had no engine/script errors; it remains a failure. See [second-attempt receipt](REALM-TRANSITION-ADAPTER-SECOND-ATTEMPT.md).
- Canonical target `water` and production API/path contracts passed an 18-check pre-network preflight. An actual director test of the snapshot-ready gate passed 1 test/7 assertions; it does not prove a native join handshake.
- The third candidate timed out at the host deadline after 23.340 seconds. Initial replication and the earlier scene reply succeeded; drain and admission were not established. All six logs had zero engine/script errors. System memory peaked at 79.115% used; console-launcher peaks are not engine peaks. See [third-attempt receipt](REALM-TRANSITION-ADAPTER-THIRD-ATTEMPT.md).
- Added diagnostics distinguish pending/grant, installation quorum, directed fence quorum, and received/body inventory. A separate 3.236-second pre-network check passed 18/18 and confirmed the coordinator can process. At that earlier checkpoint no fourth candidate had run and the blocking gate was unidentified; the later observation and correction below supersede that checkpoint.
- A subsequent authorized observation-only run identified the blocked gate: both fence rounds complete, but four bodies remain at the departing receiver. Exact engine source shows `update_visibility(0)` takes a public fast path when the zero-peer predicate is true, bypassing the intended recipient denial. The prior passing toy used actual recipient IDs. At that observation checkpoint a minimal adapter correction was proposed; no candidate acceptance was claimed until the subsequent corrected run below. See [observation receipt](REALM-TRANSITION-ADAPTER-OBSERVATION.md).
- The approved correction preserves automatic cadence with aggregate-public scoped permission and explicit per-peer phase refresh. Focused tests passed 12/52, preflight 18/18. The first corrected native candidate then passed 81 checks in 7.521 seconds, all roles exit0, all raw logs clear. Actual despawn/empty inventory, admission, retained motion and automatic-update regression checks passed. See [fourth-attempt receipt](REALM-TRANSITION-ADAPTER-FOURTH-ATTEMPT.md) for limits; this is component acceptance only.
- Game integration's first pure run passed18 tests/91 assertions. The first tiny Game fixture failed its source-install precondition and was retained. Corrected setup then passed73 live control-flow assertions in3.928 seconds, exit0. Exactly four declared injected-negative ERROR messages, no unexpected errors/scripts/warnings. Covers awaited cancellation, source identity, rollback readiness/admission, recovery action layout, client saver0, host save refusal and host/solo flow. See [Game second-attempt receipt](REALM-TRANSITION-GAME-SECOND-ATTEMPT.md); its coordinator seam is recorded, not native transport.
- The unchanged default production Water Alpha smoke passed its first attempt:28 checks,129.630 seconds, coordinator exit0, zero raw errors/script errors/failures. Known warnings:2 deprecated interpolation +30 terrain mipmaps. Source hashes unchanged, system commit max75%, process count max260, Godot0 after terminal. See [production receipt](REALM-TRANSITION-WATER-ALPHA-PRODUCTION.md) for actual join/router/remote-combat/disconnect coverage and explicit limits.

## Remaining before shipping

- Root approved the scoped origin-body receiver-denial extension; its focused matrix and corrected component-native fixture pass. The fixture has no dummy target receiver on the staying peer; full lifecycle and real-world acceptance remain open.
- Finish origin payload lifecycle, rollback/readiness sequencing and cancellation validation, including actual disconnect while a round awaits installation/fence acknowledgments.
- The identified visibility dependency is corrected and proven in the component fixture. Keep all prior failures and raw logs. Game is now integrated; the production adapter's lifecycle complexity remains subject to integration review before shipping.
- The default Water route has now passed under root's exclusive lease. Broader retained-fight, scoped late-join, native rollback/return/host regression evidence and full exact-head CI remain open before shipping.

- A subsequent static audit found an unhandled begin-client cancellation race before the overlay exists. The passing Water route does not exercise this failure path. See [failure-path audit](REALM-TRANSITION-BEGIN-FAILURE-AUDIT.md); correction and focused evidence are required.
