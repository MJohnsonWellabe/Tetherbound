# Hosted Tamsin failure — CI 34201147829

PR80 head c5012fff0d4286db42b76de34386ab37efcd3d1c; job 101981005779.
Terminal run: 25 jobs pass, three skip, multiplayer shard 4 fails. This is
not a shard-budget problem and has not been fixed by a retry.

## Retrieved evidence

Root read the job log and downloaded artifact 10046569743. Local evidence is
`.artifacts/mp4-34201147829/net-stormwood_hosted_trainers-20260908T075543Z/`.

- 07:57:12 UTC: remote Tamsin admission succeeds; host remains in Meadows.
- Forged and stale-request checks report unchanged HP/record.
- 07:57:23: host round-0 HP fixture is set to 1.
- 07:57:24: host follower is 0.40 m from the requested stand.
- 07:57:27: smoke reports injecting real quick input.
- 07:59:42: authored round 0 does not advance to round 1.
- The smoke nevertheless attempts round 1, then exceeds its own watchdog.
  Other shard smokes run afterward; the job terminates at 08:09:44.

Peer 1 logs two `strike_intent refused: unavailable` messages, without action
IDs/timestamps. Their attribution to forged, replayed or finishing input is
unproven. NET_RUN records the round failure but not per-strike geometry/verdicts.

## Diagnostic changes, not a claimed repair

- Require the production sender action counter to advance after the real quick
  input, rather than treating successful injection alone as attack submission.
- Expose the existing read-only host geometry helper for the quick slot as well
  as charged, and print geometry and host cooldown/action state before input.
- Check that raw action 703 was actually accepted before testing its replay.
- Print host state and finish on a missing attack or round transition; do not
  obscure the first failure with a doomed round-1 setup and secondary timeout.

No damage, timing, range, facing, admission or assertion tolerance is relaxed.
Godot check-only passes; hosted-combat unit suite passes 5 tests / 14 assertions.
An instrumented two-peer runtime reproduction remains required. No cause is
assigned from the two generic refusal messages alone.

## Local instrumented reproduction — 2026-09-08

At 16dd36323, the unchanged setup plus diagnostics completed exit 0 on Windows
Godot 4.7. Coordinator log:
`C:/Users/mattj/AppData/Local/Temp/tamsin-instrumented-20260908.log`;
peer logs and NET_RUN: `.artifacts/tamsin-instrumented-20260908/`.

- Raw703 acceptance is confirmed on the host, then its replay changes nothing.
- Round0 pre-input host geometry connects: 1.2501m / 9m range, 0.215 degrees
  / 13-degree half-cone; the host cooldown deadline had elapsed.
- Production sender counter advances 703 -> 705 during the input/settle window;
  the host advances to its second authored opponent.
- Round1 pre-input geometry connects at 2.865 degrees; counter advances 0 -> 2.
  Both peers receive the defeat flag, completed-trainer retry is refused without
  a second reward, and final world hashes agree. NET_RUN has no failures/fatal.
- Peer logs still contain old-scene synchronizer/node-cache errors during realm
  travel. There is no new SCRIPT ERROR, but this is not a zero-engine-error run.

This run does not reproduce the Linux CI defect and is **not** a demonstrated
repair. Do not count a local rerun as proof that the failing hosted job is fixed.
The next CI batch must retain these diagnostics so a recurrence distinguishes
missing submission, host refusal and accepted geometry miss.

## Stronger finishing-input precondition

The smoke now waits, bounded to 180 host probes, for the actual quick profile
to connect before injecting the finishing input. Position convergence alone
was insufficient evidence for the existing claim of synchronized strike geometry.
This uses the same read-only condition already required for Livewire's charged
test and preserves all production range/facing/cooldown rules. It is confined
to Tamsin's finishing-input loop, not the staging helper shared with Livewire.
Parser validation passes. This stricter precondition is newer than the local
runtime above and still needs CI runtime verification; it does not establish
that geometry caused the original Linux failure.
