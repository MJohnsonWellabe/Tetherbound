# Earned late Water composition — source and focused checks

2026-09-08. New helper `tests/helpers/water_earned_late_segment.gd` extracts the ordinary route after the setup boundary in `tests/smoke_water_continuous.gd`. It never loads or executes that SceneTree wrapper. There is no fresh campaign or full-world completion claim here.

## Caller contract and endpoint

Call `setup(tree, world, player, camera)`, then `await run_from_mount(preparation.result().swimmer, terminal_abort_callback)`. The preparation lane owns actual five-slot capture/farewell, earned materials and paid saddle receipts. Its revised endpoint is mounted at Tidal's arrival-side spine point, after clearing the workbench. This helper observes the same actual CreatureInstance, compatible swimmer data, Stone/recipe, carried saddle, full five distinct ordered identities and no pending ceremony. It does not manufacture a payment receipt from inventory presence; the caller must retain the preceding preparation receipt.

The helper rides the authored Tidal spine and departure, crosses to Salt Crown, charts the onward route, crosses to Sluice, defeats Bex, operates west control, uses the creature bed and overnight rest, follows the ascent/departure spine, defeats Calder, operates east control and verifies the combined flag/barrier removal. It crosses to Veilfall, rests, hikes to and defeats Venn, physically retraces the spine for ordinary recovery, returns to the waterfall, activates the entry, operates intake/return controls, traverses the interior, defeats Nerissa and releases the Guardian tether.

Its successful endpoint requires `water_tether_disabled` and `water_guardian_freed`, unchanged five identities and no pending catch. Claimed/settled/currents flags must remain absent. It never activates the Guardian invitation. The parent-owned ending helper owns that next ceremony.

## Inputs and bounds

Mount/dismount uses the exact actual RidingController arbiter winner and verifies its activation signal after an Interact press. Prompt activation, dialogue, recall, party selection, bed row selection, resting and combat all use input. No direct summon, dismount, prompt callback, dialogue advance, body orientation, roster, inventory, HP, progression or pose writes occur.

The ordinary Shellwatch camp implementation is retained locally with deadline checks: identify the actual active member, focus its exact bed row, assign through UI, advance exactly one day, verify full recovery, cycle back to that same recovered member and deploy through recall input. Thus a five-member belt cannot silently heal the wrong row or deploy the next selected member.

Original limits remain: mounted polyline legs `max(900, distance * 45)` with 2.5 m arrival; crossing anchor 1600 frames / 1.8 m; land legs `max(1200, horizontal distance * 65)` / original tolerances; prompt approaches eight angles; trainer entry 15 seconds, fight 180 seconds, dialogue 80 steps; dock/control/flag waits retain 180/240-frame bounds. Fights and input edges explicitly use normal 1x/60 Hz with prior clock restoration. There are no added retries or enlarged ceilings. The new Tidal departure legs use the existing mounted budget formula.

The unchanged 50-minute total deadline is observed on physics frames. A live run requires a caller-owned abort callback. On expiry, the helper records failure, releases held inputs and calls that owner once. No helper quits the caller's SceneTree. Its local DeadlineNavigator preserves the shared navigator's original recovery, walked-frame and held-frame accounting while also exiting on the total deadline. The parent callback supplies the strict terminal boundary even while another bounded helper is awaited.

## Validation

- Parser check: exit 0, no engine errors. `.artifacts/water-earned-late-parse-engine.log`.
- Focused suite: **6 tests, 45 assertions, 0 failed**, exit 0, no ERROR/SCRIPT ERROR/WARNING entries. `.artifacts/water-earned-late-focused.log` and `.artifacts/water-earned-late-focused-engine.log`.
- Focused checks use actual spawned CreatureInstance identities, replacement/reordering/duplicate/four/six-holder negatives, deadline abort exactly once and real held-action release, nested expired navigation without player/physics access, missing live receipt refusal, actual authored opponent counts (2/3/3/4) and mounted-route data. A supplementary source guard excludes fixture loading and gameplay bypasses.
- Focused APPDATA: `%TEMP%/tetherbound-water-earned-late-unit`. No full-world load, import, save reuse or campaign advancement was performed by these checks.

## Runtime limits and first risks to observe

No repaired or unchanged full-world run was launched. The original synthetic late wrapper does not validate the actual caught swimmer's level, stamina, combat strength or five-member camp lifecycle. Those remain actual campaign obligations.

The added Tidal departure approach is unproved. Its authored spine point 3 is Aquaryn's original spawn. `water_alpha.gd` disables the body collision in the build-time already-completed branch, but its same-live `_on_alpha_exit` only clears `_local_fight`; `_begin_local` does not populate inherited `_engaged_with`, which the ordinary director's cleanup checks. Therefore a still-colliding resolved Alpha is a concrete source risk at that waypoint, not a demonstrated failure in this new route. No terrain, placement, collision suppression, teleport or reload was added to evade it. The first campaign attempt must retain the actual collision/position evidence if this approach blocks.

Completed dock prompts disable themselves in `water_dock_actions.gd::_process`, supporting ordinary remount after chart/east-control completion. That source contract is not a runtime claim that all competing providers have cleared. A provider mismatch fails closed and retains the live winner diagnostic.

Only these new helper/test/report files belong to this implementation. Production, the synthetic wrapper, the parent driver and the parent ending helper remain outside this edit ownership.
