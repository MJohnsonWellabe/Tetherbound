# Earned Water swimmer preparation — Wave 3

Source/focused evidence only. No full world or earned campaign run was launched.

`tests/helpers/water_earned_swimmer_preparation_segment.gd` accepts the actual ready
post-Iona Water world, earned personal Stone/recipe, free controls and five distinct
live party identities. It selects the lowest-level member of a duplicate species
(stable earlier slot on ties); it stops when all five species are unique. It does
not invent permission to release a unique companion.

It counts current inventory against the production saddle recipe and visits the
nearest needed, unclaimed authored Tidal harvest row. Authored `approach_from`
points are honored. Existing Reedhaven harvest utilities retain their tool,
exact-yield and durable-receipt checks, original per-leg walking budget,
eight approach stances, 45 residency frames and 180 receipt frames. There is one
finite pass over authored rows; failed approaches stop, with no substitute retry.
Registration metadata is not mistaken for residency: actual node/yield checks own
acceptance. Paths from Iona to those supplies have not been walked by this helper.

The target is an actual visible living resident normal wild, with production
ground-site metadata, no named-encounter claim, a compatible swim-mount species,
and zero water depth at its body. Nearest current candidate wins. Missing resident
candidates stop without spawning or rerolling. The existing exact director/provider
Engage driver keeps the original 2600-frame first-target budget and 180-frame
admission settle. `water_earned_swimmer_segment.replace_swimmer` handles physical
weakening/throw/farewell with its existing bounds and exact retained identities.
Its same instance then performs the paid workbench craft. The new result exposes
observed material and saddle counts as `paid_saddle_receipt` after that helper's
exact payment checks pass.

After gathering and before selecting the wild, the existing Shellwatch deployment
utility selects/deploys an available ally. If it is wounded, the existing ordinary
Tidal camp bed/rest sequence runs once: exact member focus, actual bed assignment,
one-night receipt, full recovered HP, ordinary cycle and redeployment. Its original
15-second rest completion deadline and input/selection bounds are retained. Missing
available allies and failed recovery stop; there is no HP or resting-state write.

Ordinary bound controller buttons select the actual captured CreatureInstance,
deploy if needed, and activate the exact actionable RidingController provider.
World button edges match the proved process-frame alignment used by the earned
Meadows party selector, with the same three/five physics tick hold/release. The
whole live wrapper uses 1x/60 Hz and restores both clocks on the single exit path.
The endpoint is **mounted near the authored Tidal arrival-side spine[0]**, not the
departure anchor. Root review found that the workbench priority 0 beats Ride's -1
even when the follower is already within the old radius-plus-2.5 m approach
tolerance. One physical leg leaves camp before selecting/deploying the swimmer.
The target is existing spine[0] (535.497, 1352.510 XZ), with runtime ground sampling
and the unchanged 1.3 m walking tolerance. It clears the workbench, rest and bed
prompts even at that tolerance. Spine[1] is only about 3 m from the bed (whose
actual local prompt offset is z+0.7 m and radius 2.6 m), so that nearer point lacks
the same tolerance margin. A focused production-arbiter control confirms the old
workbench/Ride priority conflict; actual new clearance/follower movement is unproved.
The result exposes the actual swimmer and live world/player/camera/director/
manager/riding handles for the late traversal helper. Capture may leave a swimmer
weak; this helper makes no stamina, HP or survivability guarantee for the caught
mount. Its own ordinary recovery may become an actual later prerequisite.

Validation: parser clean; focused `--only=water_earned_swimmer_preparation_segment`
passed 5 tests / 26 assertions / 0 failures. Tests cover duplicate-only farewell
policy, real RefCounted identities (negative IDs are valid), carried surplus and
actual authored resource selection, refusal of missing live entry, authored prompt
clearance, and the production priority conflict. Logs:
`%TEMP%/wave3-swimmer-clearance-focus-console.log` and
`%TEMP%/wave3-swimmer-clearance-focus-engine.log`. No Godot errors/warnings.
This is not runtime proof of harvest access, catch success, paid craft or mounting.

## Remaining late suffix extraction

The old `smoke_water_continuous.gd` has fixture writes before departure (new game,
realm/character/world IDs, save/catalogue/flags, level-60 Aquaryn, party and saddle,
world creation, player position and direct summon/mount). None can enter the earned
path. Later methods also call direct dismount, summon, interaction activation,
dialogue advance and ally facing; those require physical controller replacements.

Required route/order: Tidal departure → sheltered Salt Crown crossing → dry
landing/spine/chart (`water_dock_salt_crown_landing_charted`) → sheltered Sluice
crossing → Bex → west control → ordinary camp → Calder → east control → actual
both-controls barrier disappearance → sheltered Veilfall crossing → camp/Venn →
actual cave entry and interior containment → ordinary creature recall → intake
pump → return sluice wheel → Nerissa's four-creature roster → guardian tether.
The current catch lane owns the new late traversal helper, ending with
`water_tether_disabled` and `water_guardian_freed` before the invitation.

Final ceremony remains separate: exact `WaterVeilfall._guardian_prompt` → actual
pending `water_abyssal_guardian` durable claim → explicit GUI farewell. Declining
the newcomer preserves the five and is supported by production transaction index
5. Await pending null, personal `water_capture_receipt:<claim-id>` and world
`water_guardian_settled`, `water_currents_restored`, `realm_relic_water_earned`.
Do not apply the normal-wild append receipt to this durable guardian claim: its
decoded pending identity and replacement-in-selected-slot contract differ.

Parent review adds care for the caught newcomer itself before clearance/Ride:
physical catch necessarily left it weakened, so the same existing Tidal bed,
rest, reselection and redeployment flow runs for that exact swimmer when wounded.
This is separate from preparing the old catch fighter. No state is repaired
through an API. The single authored clearance leg follows care, preserving
its original navigation limit. Combined local suites pass15tests/101asserts
under `.artifacts/wave4-water-full-composed-focus-engine.log`, zero engine/script
errors. Actual capture/craft/care/mount route remains runtime-unproved.
