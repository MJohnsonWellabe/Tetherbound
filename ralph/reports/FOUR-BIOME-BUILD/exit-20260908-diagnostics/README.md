# Preserved diagnostic source

These are copies of local scratch tools used during the exit session. They are not
registered tests or the requested complete Stage C6 survey. Copy the five files back
to `.artifacts/` when intentionally reproducing them; the PowerShell wrappers refer
to that directory and the original machine's installed Godot path. Choose fresh log
and APPDATA profile names before running: the wrappers refuse profile reuse. Never
run against the owner's save directory. Do not run full Terrain3D worlds concurrently.

- `probe_actual_realm_load.gd`: actual solo Meadows→Cloudreach timing, explicitly
  bypassing the progression gate. Not earned campaign evidence.
- `probe_cloudreach_route_cost.gd`: one authored ridge construction timing and
  mesh/collision digest. No full-world load or full-route equivalence claim.
- `wave6_cloudreach_visual.gd`: supplemental bounded capture, not58 destinations.
  Its last render crashed in production ground-cover allocation; see the handoff.
- The two PowerShell wrappers retain original commands and owner-save guards.

Raw logs/profiles/images remain local and are named in
`docs/HANDOFF_FOUR_BIOME_2026-09-08_OWNER_STOP.md`. No successful result should be
inferred merely from a diagnostic source file being present.
