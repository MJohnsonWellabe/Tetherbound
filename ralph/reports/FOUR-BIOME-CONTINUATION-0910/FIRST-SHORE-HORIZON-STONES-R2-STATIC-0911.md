# First Shore Horizon Stones — R2 static recovery candidate

## R1 production disposition

`FAIL`. Fresh R1 production frames invalidated the static expectation. The west `rock_tallA` source normalized into an enormous low tan slab that dominated every composition. The east `rock_largeC` remained an angular stack, the open aperture never formed, and night reduced both to black masses with one tiny visible practical.

## Narrow R2 correction

- Remove both failed nature-rock sources from this location.
- Use the installed `Rock_Medium_3`, `Rock_Medium_1`, and `Rock_Medium_2` family for a coherent material/silhouette vocabulary.
- Fit each rock to an explicit final XYZ box rather than uniform height scaling. The uprights are capped at 2.35 m and 2.15 m wide, 1.75 m and 1.65 m deep, and 6.4 m and 5.35 m tall. The sighting stone remains separately readable at 1.65 x 1.9 x 1.35 m.
- Pull the uprights to measured offsets that retain at least 8 m of open aperture between their authored bounds.
- Reduce lean to two degrees so neither upright rotates into a low slab profile.
- Keep two inward warm base practicals, raised modestly to 1.02 energy / 8.5 m range so both inner edges can retain form at night. No broad fill or whole-object emission is added.

The canonical viewpoint, horizon heading, distant islands, route clearance, terrain, vegetation, encounters, pickups, progression, and collision remain unchanged.

## Static evidence

`tests/test_water_first_shore_horizon_stones.gd` now verifies:

- all three source assets exist;
- every stone declares and realizes explicit final XYZ bounds;
- no stone exceeds the narrow width/depth cap that would permit another giant slab;
- the two uprights retain an eight-metre open aperture;
- the low sighting stone and tall uprights retain hierarchy;
- terrain contacts are finite, lights remain bounded, and the presentation adds zero collision;
- the production mount and authoritative landmark remain unchanged.

`tools/capture_water_first_shore_horizon_stones.gd` now targets the fresh `FIRST-SHORE-HORIZON-STONES-R2` output directory. It is parse-checked only in this non-rendering lane.

## Expected disposition

`POLISH` candidate, pending fresh R2 production frames. R2 directly removes the two observed source-shape failures and makes the aperture numerically enforceable. It is not claimed `PASS`; material response, vegetation overlap, and actual night edge separation still require production-frame judgment.
