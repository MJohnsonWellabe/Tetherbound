# First Shore Welcome Beacon R2 — static recovery candidate

## Categorical defect

The latest strict Water catalogue judgment is stale for this location: its canonical
day/night pair predates the runtime `FirstShoreWelcomeBeacon` mount and therefore shows
an empty bank. Current production source already builds the retained arch, banners,
lanterns and crown practical, and the September 10 continuation recorded a clean 4/4
untracked production run. The categorical FAIL is consequently a missing current
receipt, not evidence that the present landmark node is absent.

The retained frame still exposes one local composition gap: at roughly 20 m the arch
has no approach cue, and at night its small crown practical is the only focal plane.
Biome-wide stiff cover, bank materials and distant density remain real Water gaps but
are intentionally outside this location-specific lane.

## Bounded R2 change

Two collision-free torch cairns now form a staggered approach between the canonical
viewpoint `(35.707,98.104)` and arch `(16,98)`. They reuse installed
`Rock_Medium_2/3` and the existing built torch scene. Unequal `0.92/1.05 m` stone bases,
`2.05/2.2 m` torches and different yaws prevent a duplicated-post read. Each owns a
small warm practical at energy `0.72`, range `6.5 m`; the retained crown remains energy
`1.65`, range `11 m` after a prior stronger-crown comparison showed no meaningful gain.

Marker centres `(17,94)` and `(24,102)` sit between the evidence stand and destination.
Their conservative 1.2 m footprints retain more than 8 m beyond both four-metre route
edges. They add no collision, interaction, progression, pickup, encounter, terrain,
camera, vegetation or route change. The existing two arch-pier colliders and measured
two-metre throat remain the site's only physics.

## Expected disposition and evidence

Expected disposition is **POLISH**, not PASS. The current authored arch already closes
the stale “no beacon” defect; the approach pair should add sufficient navigation and
night hierarchy to clear the categorical floor. A hard pass would still require a
fresh judge to accept the whole shore frame despite coarse bank/ground treatment and
limited creature emphasis.

`tools/capture_water_first_shore_welcome_beacon.gd` owns a fresh four-frame output
directory with canonical and oblique approach day/night pairs, production grounding,
player-displacement rejection, required-node checks and an explicit fixture disclosure.
No renderer was started for this static lane.

Focused static validation:

- `test_water_first_shore_welcome_site.gd`: 2 tests, 36 assertions, zero failures.
- `capture_water_first_shore_welcome_beacon.gd`: Godot `--check-only`, exit 0.
- `water_first_shore_welcome_site.json`: PowerShell JSON parse successful.
- Owned diff: `git diff --check`, no whitespace errors.
