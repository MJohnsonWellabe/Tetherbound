# Meadows relay visual pass R6 — 2026-09-11

## Scope

Bounded follow-up to R5's honest `POLISH`. Trusted full-resolution evidence
showed two remaining defects: the broad fitted gate still read as a near-black
monolith, and the newly visible undercroft source clipped to a stark white orb.

## Candidate

- Mount two substantial installed castle-kit Banner standards on the solid
  gate piers, outside the 6.8m opening. Each oxblood cloth has an explicit
  timber pole and crossbar immediately proud of the masonry; these are
  presentation-only children and introduce no collider.
- Reduce only the three warm source meshes from 12cm / 2.2 emission to
  7cm / 0.65 emission and shift the undercroft colour warmer. Omni energy and
  range remain unchanged, so the useful bounded pools survive while the source
  retains amber colour instead of clipping white.
- Preserve the tighter R5 apparatus composition and correct its surface-seat
  guard to compare against live ground at the ordinary body's settled XZ.
- Traversal, arch opening, colliders, progression, console, relay state and
  encounter placement remain unchanged.

## Validation and evidence

Static preflight: config parses as JSON and `git diff --check` is clean.

Focused Godot validation: 2 tests, 82 assertions, 0 failed, exit 0.

Trusted Windows OpenGL production capture:

- `ralph/reports/FOUR-BIOME-CONTINUATION-0910/RELAY-IDENTITY-R6/`
- 8/8 frames written, 0 failures, exit 0.
- PNG timestamps 06:22:06–06:22:19 on 2026-09-11.
- Manifest `complete=true`; production Meadows scene, ordinary player, live
  terrain/scatter/props/trainers/relay state, authored clock frozen only after
  application, HUD and independent submersion overlay hidden.

Full-resolution review: the paired oxblood standards and their timber brackets
materially divide the dark gate mass in daylight while preserving an obvious
open arch and guard sightline. Both remain legible at night. The undercroft
source is now a small amber point over a bounded warm pool rather than a clipped
white orb. Standing/road retain the scaffolded structural depth, and the close
apparatus view proves the complete hero without rail screening.

Honest disposition: `PASS` on the named-location bar. No material visual or
evidence blocker remains in the eight trusted frames.
