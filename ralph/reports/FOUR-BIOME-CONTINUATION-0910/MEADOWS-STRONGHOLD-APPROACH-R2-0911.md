# Meadows Stronghold Approach hierarchy R2 — 2026-09-11

## Scope and production-frame audit

Follow-up to `7c6ff8b9b`, whose trusted evidence honestly retained `POLISH`.
The 560m seam view remained a broad pylon field with a necessarily tiny Hall;
the sequential occupation beats improved travel but did not author the first
place where the Hall can honestly dominate by angular size. HUD and stale quest
copy also competed with every proof frame, and the night route had only one
mid-band fire.

The full visible system audit found healthy live dirt trail/ground cover,
Meadows ecology, off-route harvest nodes, critical/side consumables, and the
final waystop's complete rest/craft economy. Those systems are preserved rather
than padded with duplicate resources. The Hall itself is outside this pass.

## Candidate

- Add `hallward_overlook` at the first final-bend view where the road and Hall
  align: paired installed castle standards on the two verges, paired physical
  metal torch sources, short outward fence wings, and an asymmetric weapon/
  supply read.
- Keep every new prop collider at least 7.5m from the real Band 5 polyline; the
  centre route remains empty and no terrain/collision/Hall geometry changes.
- Add one 13m canopy-only aperture around that overlook. Grass, flowers, path
  stones and terrain remain visible, preserving the authored meadow-to-Hall
  transition rather than creating a bald staging pad.
- Replace the prior seven-frame proof with four paired day/night route views:
  outer-watch arrival, road drop, processional reveal, and final Hall overlook.
  The production scene, ordinary player, live scatter/props/harvestables and
  encounters remain; only HUD/SubmersionOverlay are hidden and the authored
  day/night clock is applied then frozen.

## Validation and evidence

Static preflight: both edited JSON files parse and `git diff --check` is clean.

The consolidated authoritative Meadows scatter bake completed against the
accepted Long Water R6 state and this Band 5 aperture: **256 regions, 823,726
kept placements, 3,474 drained, 29,741,427 bytes**, fingerprint
`6043170113397801`. Exact generated scope is the playground manifest plus 256
region binaries; no other scatter realm changed. The combined band-merge,
fingerprint, freshness/performance, and Stronghold identity gate passed **18
tests / 340 assertions / 0 failed**. The final Stronghold-only rerun passed **4
tests / 102 assertions / 0 failed**.

Production evidence:
`ralph/reports/FOUR-BIOME-CONTINUATION-0910/STRONGHOLD-APPROACH-HIERARCHY-R2/`.
The trusted Windows OpenGL rerun wrote **8/8 frames, 0 failures**, from
07:34:07 through 07:34:27 local time. Its first two attempts correctly rejected
the first day frame while the far Terrain3D collision ring was still streaming;
the retained harness now prewarms each stand, re-seats at the same authored XZ
on the live local surface, and guards the settled position before capture.

## Honest disposition

**POLISH, not PASS.** The paired route views now prove a real sequence: distant
watch occupation and conduit, an enclosed woodland road-drop, an unmistakable
faction threshold, then the Hall and its occupied ramp at useful angular size.
The final view is the strongest frame and the new verge props do not obstruct
the road. Night retains the teal conduit and small warm pools instead of losing
every wayfinding cue.

The remaining blockers are visible in this same evidence. The 560m arrival is
still necessarily field-heavy and the Hall remains tiny there. The road-drop
pair is crowded by very large ordinary Galecrest bodies at both frame edges.
The final transition's drained ground and repeated arrow boards are
prototype-bright against the otherwise pastoral route, especially by day, and
night loses much of the dirt road and threshold structure outside the small
local pools.
The installed `Torch_Metal` meshes have no material named `Fire`; `props.gd`
therefore logs two warnings even though the attached campfire overlay supplies
the small warm pools. In the final day frame the paired standards read more as
small verge pennants than as a strong foreground threshold, and the Hall is
still very dark at night. The Hall-dominant day composition and readable
occupation sequence make the candidate worth keeping; these remaining defects
are why this bounded hierarchy gain is not a commercial PASS.
