# JUDGE-4 derived frames

**None of these are new captures.** There is no Godot binary in the JUDGE-4
container, so nothing could be re-shot. Every file here is derived from frames
already committed on `origin/ralph/T1-HALL-REBUILD` and `origin/ralph/T1-VARIANTS`,
and is included only so the defects in `ralph/reports/JUDGE-4-2026-08-30.md`
can be checked without rebuilding the crops.

| File | Source | Why |
|---|---|---|
| `derived-H01-hall-at-400m-3x.png` | `T1-HALL-REBUILD/shots/H-01-approach-400.png`, crop (430,290)–(900,450), 3× nearest | Q1-D2. The Hall at 400m beside the grey panels and the tether pylons, at a size where the 9-luma sky contrast is visible. |
| `derived-H02b-hall-and-grey-panels-2x.png` | `T1-HALL-REBUILD/shots/H-02b-sigil-gate-raised.png`, crop (180,200)–(960,500), 2× nearest | Q1-D1. All four untextured quads and their size relative to the building. Also shows the ungrassed hill (§0-a). |
| `derived-variants-day-wide-sheet.png` | the four `*-day-wide.png` frames stacked | Q2. Whole-queue day read in one image, in the order Nightburrow / Stormtrail / Riftfrill / Ashtusk, each with the 1.80m trainer as ruler. |
| `derived-variants-night-wide-sheet.png` | the four `*-night-wide.png` frames stacked | Q2. Same order at night. Row 4 is where Ashtusk's ember tusks resolve; rows 2 and 3 are where Stormtrail and Riftfrill do not.

The contact-sheet stacks are the artefact the skill actually asks for — seeing
the queue at once is what makes "one creature recoloured" visible, and it is
what the committed evidence did not provide.
