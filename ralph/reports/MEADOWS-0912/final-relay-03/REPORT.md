# Tether Relay — independent final-relay-03 judgment

**OWNER-0912 Tier 2 #12 verdict: FAIL / not yet accepted.**

The manifest is complete (`complete: true`, 10/10 expected PNGs, no capture
failures), and I inspected every frame at its native 1280 × 800 resolution. The
large-scale redesign and installed hero apparatus are successful. This now reads
as a fortified, multi-level field installation rather than a toy relay. The set
does not close the full complaint, however: the planned route/console proof shows
only the back of a hanging standard, ordinary arrival views do not communicate a
continuous route from the road to the console, and substantial undercroft space
still collapses to black at night.

## Strict requirement verdicts

| Requirement | Verdict | Frame evidence |
|---|---|---|
| Massing and scale | **PASS** | `01-relay-approach-day/night` establishes a player-dwarfing gatehouse, while `02-relay-standing-*` and `04-relay-road-*` show a broad two-level deck, multiple full-height supports, perimeter pylons, and trainers for scale. The apparatus rises well above the deck and reads as the compound's purpose. |
| Material finish | **PARTIAL** | The hero machine has layered, weathered surfaces in `02-*` and especially `03-relay-apparatus-*`; masonry, timber braces, deck edging, and the cyan pylon/cable family are materially clearer than the blockout-like support treatment called out in `final-relay-01`. The enormous gate facade in `01-*` still repeats coarse dark blocks with little value breakup, and its near-black standards read as blank slabs. `05-*` exposes a very flat black hanging panel at point-blank range, with almost no readable cloth body in either lighting state. |
| Apparatus hierarchy | **PASS** | `02-*`, `03-*`, and `04-*` consistently make the installed weathered apparatus the dominant upper-deck subject. The clean foreground boiler that competed with it in `final-relay-01` is no longer present in this evidence composition. Pylons and teal cables extend the machine into a readable field network without surpassing it. |
| Console readability | **FAIL** | A small cyan-faced box is visible beside the apparatus in `01-*`, `02-*`, and `04-*`, but at ordinary distance it reads as a sign/screen rather than an unmistakable interaction station. The specifically planned `05-relay-route-console-day/night` pair contains no visible console or route at all: both frames are almost entirely a close view of the back of a hanging standard against masonry. A named evidence view missing its subject cannot close this gate. |
| Ordinary road/ramp route | **PARTIAL** | `01-*` clearly invites the player through the large central arch, and `02-*`/`04-*` show the lower arch plus narrow stairs at the deck edges. No frame demonstrates one continuous road-to-stair/ramp-to-console read. From the approach, the console is only a tiny cyan rectangle beyond the arch; from `04-*`, the right stair is visible but its relationship to that console is ambiguous. The much closer `05-*` pair then loses the route entirely behind the standard. |
| Undercroft — day | **PARTIAL** | `02-relay-standing-day` and `04-relay-road-day` show a real service level with columns, timber scaffold, a bag/crates, masonry apertures, and a warm maintenance opening. The large left bay nevertheless remains a nearly featureless black floor/void even in daylight, weakening spatial clarity beneath the successful upper deck. |
| Undercroft — night | **FAIL** | The warm opening and bounded practical remain readable in `02-relay-standing-night`, `03-relay-apparatus-night`, and `04-relay-road-night`, with no broad light blowout. They illuminate only the small right-hand aperture. Most of the left and central undercroft compresses to black, obscuring floor continuity, supports, and the player's likely circulation space. |
| Day/night identity | **PARTIAL** | The compound silhouette, apparatus, crystals, and cable network survive both states across `01-*` through `04-*`. Night preserves a restrained cyan/warm hierarchy, but the undercroft loss and blank black standards are materially worse after dark. The `05-*` pair is readable as wall/wood/cloth in both states but is evidence of the wrong subject. |

## Frame-level findings

- `01-relay-approach-day/night`: strongest proof of monumental entry scale and a
  clear arch threshold. The apparatus and cyan console face are only glimpsed
  through the opening. Broad blank wall mass and near-black hangings dominate.
- `02-relay-standing-day/night`: strongest overall site hierarchy. The apparatus,
  upper deck, pylons, right stair, warm service arch, and trainers all relate at
  useful scale. The left undercroft bay remains unreadably black.
- `03-relay-apparatus-day/night`: strongest apparatus and installed-material
  evidence. Timber trim and stone deck support the weathered hero successfully;
  the bounded warm source helps the lower wall, but not the wider undercroft.
- `04-relay-road-day/night`: best ordinary-distance circulation evidence. The
  lower arch and right stair are visible, but the stair-to-console destination is
  not explicit and the dark lower floor has no continuous visual lead.
- `05-relay-route-console-day/night`: invalid as console/route acceptance. The
  camera is pressed into the hanging standard, which occludes the entire planned
  subject. It also reveals the standard as a nearly planar black rectangle.

## Required reproof

Retain the current massing, apparatus placement, improved support materials, and
restrained cyan/warm lighting. Recapture an ordinary player-height sequence that
shows, without teleport-camera ambiguity, the road or arch arrival, the actual
walkable stair/ramp, the upper-deck landing, and the console as one readable
destination. Replace the failed `05-*` framing with an unobstructed player-distance
console three-quarter in day and night. The night reproof must also expose enough
floor edge, columns, and service detail in the left/central undercroft to judge it
as traversable space rather than a black void; do not solve that by blowing out the
apparatus or the field pylons.
