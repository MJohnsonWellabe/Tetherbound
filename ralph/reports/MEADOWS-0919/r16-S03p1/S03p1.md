# S03p1 — S03 phase 1/3: Village tutorial ladder: Tam's tools -> team of five -> training -> gathering -> camp + creature bed -> sleep -> feed

### preflight — capture not required
- segment declares no captures and no continuous record
- predicted cost 152 s over 116792 frames at 0.0005 s/frame (re-priced after each boot)

### S03-01 — how this segment is entered
- expected: a note event carries this observation into events.jsonl
- actual: Section B: this segment's entry save is `S02-exit`. The three steps below are the handoff's own mechanics: the live save directory is emptied first so the title's Load list has exactly one non-empty slot and auto-focuses it (`title_screen.gd` focuses the FIRST non-empty slot), then slot 4 is seeded from the run directory, then the load is driven through the production title-screen Load path by synthetic input. Section B names the seed and the Load path; the wipe is not named there and is recorded here as an added mechanical step, needed because an autosave left in slot 0 would take the auto-focus and the segment would load the wrong state.
- events: t=0.25
- verdict: PASS

### S03-02 — empty the live save directory
- expected: every slot file is gone, so the seeded slot 4 is the only thing the title's Load list can offer
- actual: save directory D:/tetherbound/closeout-profiles/gate-f-run-20260919-meadows-r16/Roaming/Godot/app_userdata/Tetherbound/saves does not exist yet; nothing to wipe
- events: t=0.25
- verdict: PASS

### S03-03 — seed slot 4 from the previous segment's exit save
- expected: Section B: 'The next segment boots fresh, restores that file into `user://`, and loads it through the production title-screen Load path'
- actual: seeded slot 4 from D:\tetherbound\owner-kickoff-closeout-r5\ralph\reports\gate-f-run-20260919-meadows-r16/S02/saves/S02-exit.json (4190889 bytes)
- events: t=0.25
- verdict: PASS

### S03-04 — boot the real title screen
- expected: the real title comes up with Start New Game focused and Load Game enabled, because a save now exists
- actual: booted title in 520 ms (30 settle frames); re-priced at boot:title: 268198 physics waits at 0.01440 s, 13340 process waits at 0.00050 s and 0.0 fixed seconds = 3868 s against 14399 s remaining
- events: t=0.77
- verdict: PASS

### S03-05 — the title focuses something
- expected: the title focuses its first button; without focus a controller cannot reach Load Game at all
- actual: focus_owner=@Button@33 focus_text=Start New Game
- events: t=0.77
- verdict: PASS

### S03-06 — move focus to Load Game
- expected: d-pad down moves focus from Start New Game to Load Game
- actual: 1 x ui_down moved focus 'Start New Game' (@Button@33) -> 'Load Game' (@Button@34)
- events: t=0.80
- verdict: PASS

### S03-07 — open the slot list
- expected: Section B: 'driven with synthetic input -- this is itself save/load coverage'. Load Game opens the slot list and auto-focuses the first non-empty slot, which is the seeded slot 4
- actual: pressed ui_accept x1 (tap, 1 frames each) on the default device, resolved to JoyBtn:0 [unchecked against input_contexts.json: input_context 'title' is not in input_contexts.json]
- events: t=0.88
- verdict: PASS

### S03-08 — load the seeded slot
- expected: the slot loads and the title transitions into the Meadows; section I.4 records the load duration
- actual: pressed ui_accept x1 (tap, 1 frames each) on the default device, resolved to JoyBtn:0 [unchecked against input_contexts.json: input_context 'title' is not in input_contexts.json]
- events: t=1.75
- verdict: PASS

### S03-09 — let the loaded world stand up
- expected: The loaded Meadows has a live Player and returns world input before the next party/deployment action. Observe readiness rather than waiting a fixed duration after synchronous CPU scene construction already completed.
- actual: input_context=world (wanted world) [true after 0 physics frames]
- events: t=1.75
- verdict: PASS
- observation: Native R8/R9 emitted world input at this step around play t=1.7 after several minutes of wall-clock loading; the former 180-second physics wait then stood idle in an already ready world. Retain the same worst-case 10800-frame readiness budget without adding artificial idle playtime.

### S03-09-health — choose an available creature if the saved lead fainted
- expected: The real party-cycle input skips fainted and resting creatures. A healthy saved lead is preserved; a fainted lead is replaced by the next available earned party member before the existing recall input deploys it.
- actual: pressed party_cycle x1 (tap, 1 frames each) on the default device, resolved to JoyBtn:9
- events: t=2.33
- verdict: PASS

### S03-09a — deploy the active creature after load
- expected: RIG-13 (extending RIG-11 to the segments RIG-11's own fix never reached): a load restores the party and deploys nothing -- encounter_director.gd::can_challenge() returns false on a null/undeployed ally before checking whether a trainer is already beaten, so trainer_npc.gd falls back to its 'defeated' conversation line instead of the 'challenge' line that leads to combat. No trainer fight downstream of a load -- including this segment's Bryn practice match -- can start without this. creature_recall (RB) summons the active creature so this segment's fights can actually start.
- actual: pressed creature_recall x1 (tap, 1 frames each) on the default device, resolved to JoyBtn:10
- events: t=2.85
- verdict: PASS

### S03-10 — the load restored the village arrival
- expected: S02 ended at the village center; the load has to bring the player back there, not to a fresh spawn
- actual: 2.3 m from (10, -10), wanted within 12.0
- events: t=2.85
- verdict: PASS

### S03-11 — the party travelled
- expected: the starter and the first catch: two of five
- actual: party size 3 (wanted >= 2)
- events: t=2.85
- verdict: PASS
- observation: RIG-15: equals -> min. Catching is probabilistic; a script's single throw can miss on a real, non-buggy roll, and a team that ends up BIGGER than the milestone is still a team that reached it. `equals` failed both directions for no reason a real player's outcome should fail on.

### S03-12 — the ladder's next rung is tracked
- expected: Section E.5 objective 6/27 `village_tools`, 'Meet Tam in the village and take his tools.'
- actual: tracked objective id=tam_tools_given text=Meet Tam in the village and take his tools. (wanted village_tools = flag_id tam_tools_given) [matched on entry id -> flag_id]
- events: t=2.85
- verdict: PASS

### S03-13 — Section H cadence for this segment
- expected: a note event carries this observation into events.jsonl
- actual: Section H: this segment is outside the mandatory 0.5 Hz list, so it 'runs at 0.1 Hz (one frame per 10 s) plus event-forced frames, to keep the record continuous without drowning the run directory'. The vocabulary has no background-recorder action; recorded here as a gap.
- events: t=2.85
- verdict: PASS

### S03-14 — open the Map tab
- expected: Section E.5: 'full-map usefulness (open Map tab, zoom in/out with LT/RT, verify pan/centering behavior and zoom persistence across close/reopen)'. The `map` shortcut opens the shell straight onto the tab
- actual: map opened the shell: context world -> menu_map, focus on '' (@Control@65964)
- events: t=3.35
- verdict: PASS

### S03-15 — the Map tab is up
- expected: input_context names the live tab: menu_map
- actual: input_context=menu_map (wanted menu_map)
- events: t=3.35
- verdict: PASS

### S03-16 — capture GF-18-MAP-01
- expected: Section G: Map tab at S03 (fresh-ish save) -- what a new player's map actually shows
- actual: DELEGATED GF-18-MAP-01 to capture lane S03C (this is the logic lane; it takes no frames)
- events: t=3.35
- verdict: DELEGATED

### S03-17 — zoom the map in
- expected: Section E.5: zoom in/out with LT/RT
- actual: pressed map_zoom_in x3 (tap, 1 frames each) on the default device, resolved to JoyAxis:5:1.0
- events: t=6.75
- verdict: PASS

### S03-18 — zoom the map back out
- expected: Section E.5: zoom in/out with LT/RT
- actual: pressed map_zoom_out x3 (tap, 1 frames each) on the default device, resolved to JoyAxis:4:1.0
- events: t=10.05
- verdict: PASS

### S03-19 — close the shell
- expected: B closes the shell and hands the world back
- actual: physical shell close: {"ok":true,"presses":1,"settle_frames":1,"why":""}
- events: t=10.27
- verdict: PASS

### S03-20 — reopen the Map tab
- expected: Section E.5: 'zoom persistence across close/reopen' -- the level the map reopens at is the finding
- actual: map opened the shell: context world -> menu_map, focus on '' (@Control@65984)
- events: t=10.77
- verdict: PASS

### S03-21 — close the shell again
- expected: B closes the shell and hands the world back
- actual: physical shell close: {"ok":true,"presses":1,"settle_frames":1,"why":""}
- events: t=11.00
- verdict: PASS

### S03-22 — Section E.5 nav note -- village_tools
- expected: a note event carries this observation into events.jsonl
- actual: Section E.5 navigation record, objective 6/27 `village_tools`. Wording: 'Meet Tam in the village and take his tools.' It names WHAT (Tam, tools) and a WHERE only as specific as 'in the village'; it does not say which building or which side. Player-visible information: the fresh-save reveal shows the village and its roads, and GF-18-MAP-01 is the record of what that map actually offers -- whether Tam is a pin at all. Route decision: cross the village square westward along the spine, because the objective names no landmark and the square is the only place the map marks. Reasoned from GF-18-MAP-01 and GF-03-VILLAGE-01 (S02). Operator: record time-to-route-decision and whether the map named Tam.
- events: t=11.00
- verdict: PASS

### S03-23 — walk to Tam
- expected: Reach the live Tam at his current workshop bay; stop inside the NPC prompt radius rather than the retired village coordinate.
- actual: walked 21.2 m to Tam (node name; selected /root/MeadowsPlayground/VillageNPCs/Tam) in 265 walking frames (0 held)
- events: t=15.43
- verdict: PASS
- observation: answer_prompts turned ON for this journey walk. TRADE RECORDED EXPLICITLY: the schema says this flag must stay off in any segment whose subject is whether something blocks travel, so these segments can no longer evidence 'a narrative modal blocked the player from travelling'. That finding is already captured in full, un-answered, in S02-superseded-2/3/4 and S02/BLOCKER.md -- three press counts, identical 7201-frame holds. What it buys is everything downstream: measured in S03 and S04, an unanswered modal at a walk's end left input_context='narrative_modal' where the next step expected combat, so no fight started, no catch resolved, the team never reached three, and every tournament entry check then failed on team size. Without this the journey produces no combat, catching, building or progression evidence at all.

### S03-24 — talk to Tam
- expected: the ladder's rung: 'take his tools'
- actual: pressed `interact` on "[img=36x36]res://assets/ui/input_prompts/xbox_button_x.png[/img]   Greet Tam" (provider 'Interactable'): context world -> narrative_modal
- events: t=16.78
- verdict: PASS

### S03-25 — hear him out
- expected: his conversation gives the axe and pickaxe; advances by predicate (CD-3), not a guessed press count
- actual: advanced 5 line(s) over 5 press(es) of interact; DialoguePanel closed, context 'narrative_modal' -> 'world'
- events: t=19.67
- verdict: PASS
- observation: Fixed 2026-09-02: this used a blind press{times:16}, the exact CD-3 shape this vocabulary has advance_dialogue_until_closed for -- a fixed count is wrong in both directions, and every run this session left input_context still narrative_modal at S03-25w immediately after (diagnostic, RIG-25/T2-GATEF-RUN6), meaning 16 presses under-ran this conversation every single time. advance_dialogue_until_closed reads the panel and presses until it actually closes, the same fix S03-54 already uses for its own conversation.

### S03-26 — Tam's tools were given
- expected: `tam_tools_given` is the rung's own contract flag
- actual: flag tam_tools_given set
- events: t=19.67
- verdict: PASS

### S03-27 — capture GF-03-VILLAGE-02
- expected: Section G: street-level walk mid-S03, between Tam/Oskar/Mira -- street scale, NPC presence
- actual: DELEGATED GF-03-VILLAGE-02 to capture lane S03C (this is the logic lane; it takes no frames)
- events: t=19.67
- verdict: DELEGATED

### S03-28 — the chain advanced
- expected: Section E.5 objective 7/27 `tournament_build_team`
- actual: tracked objective id=tournament_team_ready text=Build your full team of five for the village tournament. (wanted tournament_build_team = flag_id tournament_team_ready) [matched on entry id -> flag_id]
- events: t=19.67
- verdict: PASS

### S03-29 — Section E.5 nav note -- tournament_build_team
- expected: a note event carries this observation into events.jsonl
- actual: Section E.5 navigation record, objective 7/27 `tournament_build_team`. Wording: 'Catch and raise a team for the village tournament.' It names WHAT and no WHERE at all -- there is nowhere named to go. Player-visible information: the minimap and the world itself; wild creatures are visible individuals on the meadow south-east of the village (the practice meadow the opening already used). Route decision: return to the practice meadow because it is the only ground the player has already seen wild creatures on. Reasoned from GF-18-MAP-01 and GF-03-VILLAGE-02. Operator: record whether an objective that names no place left the player uncertain, and for how long.
- events: t=19.67
- verdict: PASS

### S03-25w — the world owns input before this walk
- expected: no narrative modal is holding locomotion when this walk starts
- actual: input_context=world (wanted world)
- events: t=19.67
- verdict: PASS
- observation: T2-GATEF-RUN6 modal-holds-locomotion audit. A walk that begins while a narrative modal still owns input does not fail as 'a modal is open' -- it fails as 'did not reach', many steps and sometimes many minutes later, which is how X04 lost its whole combat block to 3601 held frames. This assert is diagnostic and changes no behaviour: it turns that silent cascade into one named cause at the point of origin. RIG-25's rule, applied to narrative modals rather than to shop panels.

### S03-30 — walk out to the practice meadow
- expected: `data/config/tournament.json`'s `entry.min_party_size` is 3: 'a team, not a favourite'
- actual: walked 51.1 m to (30, -40) in 702 walking frames (0 held)
- events: t=31.38
- verdict: PASS
- observation: answer_prompts turned ON for this journey walk. TRADE RECORDED EXPLICITLY: the schema says this flag must stay off in any segment whose subject is whether something blocks travel, so these segments can no longer evidence 'a narrative modal blocked the player from travelling'. That finding is already captured in full, un-answered, in S02-superseded-2/3/4 and S02/BLOCKER.md -- three press counts, identical 7201-frame holds. What it buys is everything downstream: measured in S03 and S04, an unanswered modal at a walk's end left input_context='narrative_modal' where the next step expected combat, so no fight started, no catch resolved, the team never reached three, and every tournament entry check then failed on team size. Without this the journey produces no combat, catching, building or progression evidence at all.

### S03-31 — rescan for points of interest
- expected: Section F's POI set for the dead-travel meter
- actual: 1378 points of interest in the tree
- events: t=31.38
- verdict: PASS

### S03-31a — why the ladder still tries up to 10, but should not need to
- expected: a note event carries this observation into events.jsonl
- actual: The user asked directly why this scripts 10 numbered attempts when only 3 new catches are needed (party starts at 2, S03-39 wants 5). 10 was sized as pure headroom against two problems that are now fixed or greatly reduced: the ally-species engage-gap (fixed, operator_harness.gd::_find_entity) that used to eat several attempts before ever reaching a fight, and a shallow, guessed chip-down that left real catch odds far lower than catching.json's curve actually allows (fixed, chip_to_floor + throw_until_caught -- computed to each fight's own real floor, then thrown at until caught or out of tries). With those fixed, a real single-attempt sample landed a catch on the FIRST throw. Rather than guess a smaller number and risk under-provisioning on a bad RNG run, every numbered attempt's move_to_entity/interact_with pair now carries skip_if party_size>=5 (operator_harness.gd, added for this) -- once the team actually reaches five, every remaining attempt SKIPS in a few frames instead of walking, fighting and throwing for nothing. 10 stays as the ceiling for a genuinely unlucky run; it should rarely if ever be reached now.
- events: t=31.38
- verdict: PASS

### S03-32a — walk to a live bramblebun (attempt 1)
- expected: RIG-16: tracks a live bramblebun's own position every frame instead of a fixed cluster-centre coordinate. Arrival also requires the intended living wild creature to own the actual actionable Engage prompt; bounded physical repositioning shares the existing walking budget.
- actual: walked 3.5 m to bramblebun (species_id, nearest of 76 (Wild_bramblebun_0_1, Wild_bramblebun_0_2, Wild_bramblebun_0_3, Wild_bramblebun_1003_1, Wild_bramblebun_1003_2, Wild_bramblebun_1006_1, ...); selected /root/MeadowsPlayground/Wild_bramblebun_0_3) in 42 walking frames (0 held)
- events: t=32.10
- verdict: PASS
- observation: RIG-F5/GAME-F3. `within: 4.5` is what handed the interact button to a harvest node: prompt_arbiter.gd is 'highest priority first, then NEAREST', engage and gather are both priority 0, and from 4.5 m out a node at half a metre wins every time. Four of the twenty authored village nodes sit inside the practice cluster's own 15 m disc, deliberately -- harvest.json's own _why_gate_b_three_beds put them on 'the practice-path loop the first day already walks' -- so the collision is structural and the fix is to stand at the creature, which is what a player does. 2.0 rather than 1.8: RIG-F5 measured a bramblebun 0.22 m up a slope failing a 1.8 m close_3d check at 1.81 m in 3D, and named 2.0 as the floor.

### S03-32a2 — challenge it (attempt 1)
- expected: After prompt settling, require the actual Engage provider and the exact living wild selected by the preceding verified approach before pressing. A competing gather prompt or another wild is not this encounter.
- actual: pressed `interact` on "[img=36x36]res://assets/ui/input_prompts/xbox_button_x.png[/img]   Engage Bramblebun" (provider 'EncounterDirector'): context world -> combat
- events: t=35.08
- verdict: PASS

### S03-33a — let the fight stage
- expected: the combat camera acquires both combatants
- actual: waited 180 physics frames
- events: t=38.08
- verdict: PASS

### S03-35a — trim it with quick attacks (do not kill it)
- expected: the target is WEAKENED as close to zero HP as this fight will safely bear -- not defeated, a defeated wild cannot be caught -- computed from the fight's own real per-hit damage rather than a guessed hit count or a guessed HP-fraction floor. Once the earned party already has five members, verify that live condition and issue no further attack input.
- actual: 11 x combat_quick: enemy hp 10.6/93.7 (11.3%), hits dealt [9.4, 0.0, 8.4, 8.5, 9.9, 9.0, 8.9, 9.5, 8.8, 5.0, 5.8], largest 9.9
- events: t=45.60
- verdict: PASS
- observation: RIG, 2026-08-31: combat_quick x20 KILLED a 106 HP target, so a fixed times: 3 (S02's own proven opener) replaced it -- correctly avoiding a kill, but measured directly (gate-f-run-20260902T053310Z-s03enginefix) landing anywhere from 57% to 75% of max HP depending on the creature's own defence, the FLAT part of hp_factor's curve. A fixed enemy_hp_fraction 0.3 floor (this step's own immediately-prior version) fixed that but was still a guessed number, not a computed one -- called out directly by the owner in this session, 2026-09-02 ('if 5 presses took 70 health you could do another 2 without killing it. why wouldnt you? ... you can surely calculate what needs to be done'). chip_to_floor (operator_harness.gd, added for this) reads the live enemy hp before and after every swing and learns the actual per-hit damage this fight is dealing -- combat_math.gd::rolled_damage()'s only per-swing randomness against a fixed target is its +/-10% variance roll, so one real hit already bounds every later one -- and refuses the next swing only once the largest hit seen so far, scaled by safety_factor (1.25, past the 1.1/0.9=1.222 worst-case roll swing), would not survive. No fixed floor anywhere in this step; it chips exactly as far as the specific creature in front of it allows.

### S03-36a — aim and throw until caught, or out of tries (attempt 1)
- expected: the target is caught, or every allotted throw against it is spent -- a missed throw does not end the fight (catching.json's own cooldown exists so a failed catch can be re-thrown once the wobble clears; combat_manager.gd re-engages the same target on a miss), so this keeps throwing at the SAME chipped-down target instead of treating one orb as the whole attempt.
- actual: caught on throw 1 of 4 (observed launch 1 (reticle confirmed on body after 39 tracked frame(s): eligible): caught)
- events: t=54.13
- verdict: PASS
- observation: Owner directive, this session: "we should chip down then throw orbs til we catch." The ladder used to spend exactly one orb per numbered attempt (arm aim, track, throw once, wait) and then move on regardless of outcome -- discarding real, intended-to-be-retried catch chances the moment a single throw missed, even though nothing about the game ends the fight on a miss. throw_until_caught (operator_harness.gd, added for this) re-arms the aim, re-tracks the live target and throws again, up to max_throws times, stopping the instant the party actually grows.

### S03-32a2w — wait for the world to own input before this walk
- expected: no narrative modal or combat is holding locomotion when this walk starts
- actual: input_context=world (wanted world) [true after 5 physics frames]
- events: t=54.22
- verdict: PASS
- observation: T2-GATEF-RUN6 modal-holds-locomotion audit, upgraded per FINDING-CAPSTONE3-S03-CATCH-LOOP-STALL-2026-09-01. This step used to be a one-shot `assert`, diagnostic only: it recorded that the previous fight/catch had not finished unwinding but did not wait for it, so attempts b-j walked straight into 'the interaction arbiter is DISABLED... input_context combat' every time. `wait_until` polls the same check instead of sampling it once, so the next attempt's walk starts only once combat has actually closed and the world owns input again -- CD-3's rule applied to the retry ladder's own seam, not just to a single wait.

### S03-32ar1 — open the Satchel to check for a fainted party member
- expected: game_menu.gd::select() grabs tab_backpack.gd::first_focus() (_buttons[0]) on open, so focus starts at slot 0 (orb_basic) every time.
- actual: inventory opened the shell: context world -> menu_backpack, focus on '' (@Button@66472)
- events: t=54.53
- verdict: PASS

### S03-32ar2 — focus the Revive draught
- expected: T2-GATEF-RUN5/GAME-9/RIG-24 (S03-56d/S03-56f) already proved fixed-offset focus_move fragile in this same bag once acquisitions or spends shift the grid: a positional "3 cells right of slot 0" assumed a fresh save's fill order, and this ladder's own telemetry (gate-f-run-20260901T220548Z-s03fix attempts 3-4) shows it silently landing on the wrong item once earlier presses in this same recovery block had already shifted the grid, so every recovery reported PASS while reviving nothing and party size stayed capped at 2 (FINDING-S03-CATCH-LOOP-REVIVE-SLOT-2026-09-02). focus_item reads the cursor and sends real ui_left/ui_right the way a player reads the bag, regardless of where Revive actually sits.
- actual: focus_item 'revive': cursor on cell 5 after 5 move(s) (from cell 0)
- events: t=55.53
- verdict: PASS

### S03-32ar3 — try to use it on whoever needs it
- expected: tab_backpack.gd::_read_use(): opens the target picker if any party member is fainted, or safely refuses ('Nobody needs reviving.') with no state change otherwise -- live-verified in both directions (tools/gate_f/probe_revive_menu_flow.gd), which is what makes this safe to run every time regardless of party state.
- actual: pressed interact x1 (tap, 1 frames each) on the default device, resolved to JoyBtn:2
- events: t=55.80
- verdict: PASS

### S03-32ar4 — confirm the target if a picker opened
- expected: If the picker opened, focus already sits on the only eligible (fainted) row; this confirms it, clearing `fainted` and consuming one Revive. If nobody was fainted, this press instead picks the Revive stack up off the grid (Godot's ordinary carry gesture) -- non-destructive, corrected by the next step.
- actual: pressed ui_accept x1 (tap, 1 frames each) on the default device, resolved to JoyBtn:0
- events: t=55.97
- verdict: PASS

### S03-32ar5 — put back an accidental pick-up, or close the shell
- expected: Observe the live Satchel and close any remaining picker, held stack or grid using mapped B only while the shell owns it; already-closed world requires zero input.
- actual: physical shell close: {"ok":true,"presses":1,"settle_frames":1,"why":""}
- events: t=56.23
- verdict: PASS

### S03-32ar6 — close the shell for real
- expected: Observe the live Satchel and close any remaining picker, held stack or grid using mapped B only while the shell owns it; already-closed world requires zero input.
- actual: physical shell close: {"ok":true,"presses":0,"settle_frames":0,"why":""}
- events: t=56.23
- verdict: PASS

### S03-32ar7 — select a healthy earned pilot before the next fight
- expected: Read the actual earned roster and use mapped LB only as needed to deploy its living, non-resting member with greatest current HP; verify the actual companion is visible before continuing. Revive recovery remains before this step.
- actual: physical world pilot selection: {"no_input":true,"ok":true,"physics_frames":0,"presses":0,"process_frames":0,"selected_index":2,"why":"healthiest earned creature and actual companion verified; 0 physical LB taps"}
- events: t=56.23
- verdict: PASS
- observation: R8 selected a newly caught 15 HP Bramblebun with one blind cycle while 79 HP and 58 HP allies were available; it fainted during encounter staging. Select by live HP with bounded physical cycling, without health or active-index writes.

### S03-32ar8 — let the party swap finish
- expected: encounter_director.gd::summon_active_creature() is async; the newly-active creature's body needs a few frames to stand up before the next engage is attempted.
- actual: waited 60 physics frames
- events: t=57.23
- verdict: PASS

### S03-32ar9 — wait for the world to own input before attempt 2
- expected: Both branches above converge on a closed shell and a settled party swap; the world must have input back before the next attempt's walk starts.
- actual: input_context=world (wanted world) [true after 0 physics frames]
- events: t=57.23
- verdict: PASS

### S03-32b — walk to a live bramblebun (attempt 2)
- expected: RIG-16: tracks a live bramblebun's own position every frame instead of a fixed cluster-centre coordinate. Arrival also requires the intended living wild creature to own the actual actionable Engage prompt; bounded physical repositioning shares the existing walking budget.
- actual: walked 10.6 m to bramblebun (species_id, #2 nearest of 75 (Wild_bramblebun_0_1, Wild_bramblebun_0_2, Wild_bramblebun_1003_1, Wild_bramblebun_1003_2, Wild_bramblebun_1006_1, Wild_bramblebun_1006_2, ...); selected /root/MeadowsPlayground/Wild_bramblebun_0_2) in 130 walking frames (0 held)
- events: t=59.42
- verdict: PASS
- observation: RIG-F5/GAME-F3. `within: 4.5` is what handed the interact button to a harvest node: prompt_arbiter.gd is 'highest priority first, then NEAREST', engage and gather are both priority 0, and from 4.5 m out a node at half a metre wins every time. Four of the twenty authored village nodes sit inside the practice cluster's own 15 m disc, deliberately -- harvest.json's own _why_gate_b_three_beds put them on 'the practice-path loop the first day already walks' -- so the collision is structural and the fix is to stand at the creature, which is what a player does. 2.0 rather than 1.8: RIG-F5 measured a bramblebun 0.22 m up a slope failing a 1.8 m close_3d check at 1.81 m in 3D, and named 2.0 as the floor.

### S03-32b2 — challenge it (attempt 2)
- expected: After prompt settling, require the actual Engage provider and the exact living wild selected by the preceding verified approach before pressing. A competing gather prompt or another wild is not this encounter.
- actual: pressed `interact` on "[img=36x36]res://assets/ui/input_prompts/xbox_button_x.png[/img]   Engage Bramblebun" (provider 'EncounterDirector'): context world -> combat
- events: t=62.52
- verdict: PASS

### S03-33b — let the fight stage
- expected: the combat camera acquires both combatants
- actual: waited 180 physics frames
- events: t=65.52
- verdict: PASS

### S03-35b — trim it with quick attacks (do not kill it)
- expected: the target is WEAKENED as close to zero HP as this fight will safely bear -- not defeated, a defeated wild cannot be caught -- computed from the fight's own real per-hit damage rather than a guessed hit count or a guessed HP-fraction floor. Once the earned party already has five members, verify that live condition and issue no further attack input.
- actual: 11 x combat_quick: enemy hp 16.0/106.2 (15.0%), hits dealt [9.2, 8.5, 9.8, 8.6, 9.7, 9.2, 9.3, 9.2, 5.8, 5.5, 5.6], largest 9.8
- events: t=72.97
- verdict: PASS
- observation: RIG, 2026-08-31: combat_quick x20 KILLED a 106 HP target, so a fixed times: 3 (S02's own proven opener) replaced it -- correctly avoiding a kill, but measured directly (gate-f-run-20260902T053310Z-s03enginefix) landing anywhere from 57% to 75% of max HP depending on the creature's own defence, the FLAT part of hp_factor's curve. A fixed enemy_hp_fraction 0.3 floor (this step's own immediately-prior version) fixed that but was still a guessed number, not a computed one -- called out directly by the owner in this session, 2026-09-02 ('if 5 presses took 70 health you could do another 2 without killing it. why wouldnt you? ... you can surely calculate what needs to be done'). chip_to_floor (operator_harness.gd, added for this) reads the live enemy hp before and after every swing and learns the actual per-hit damage this fight is dealing -- combat_math.gd::rolled_damage()'s only per-swing randomness against a fixed target is its +/-10% variance roll, so one real hit already bounds every later one -- and refuses the next swing only once the largest hit seen so far, scaled by safety_factor (1.25, past the 1.1/0.9=1.222 worst-case roll swing), would not survive. No fixed floor anywhere in this step; it chips exactly as far as the specific creature in front of it allows.

### S03-36b — aim and throw until caught, or out of tries (attempt 2)
- expected: the target is caught, or every allotted throw against it is spent -- a missed throw does not end the fight (catching.json's own cooldown exists so a failed catch can be re-thrown once the wobble clears; combat_manager.gd re-engages the same target on a miss), so this keeps throwing at the SAME chipped-down target instead of treating one orb as the whole attempt.
- actual: caught on throw 1 of 4 (observed launch 1 (reticle confirmed on body after 7 tracked frame(s): eligible): caught)
- events: t=80.85
- verdict: PASS
- observation: Owner directive, this session: "we should chip down then throw orbs til we catch." The ladder used to spend exactly one orb per numbered attempt (arm aim, track, throw once, wait) and then move on regardless of outcome -- discarding real, intended-to-be-retried catch chances the moment a single throw missed, even though nothing about the game ends the fight on a miss. throw_until_caught (operator_harness.gd, added for this) re-arms the aim, re-tracks the live target and throws again, up to max_throws times, stopping the instant the party actually grows.

### S03-32b2w — wait for the world to own input before this walk
- expected: no narrative modal or combat is holding locomotion when this walk starts
- actual: input_context=world (wanted world) [true after 5 physics frames]
- events: t=80.93
- verdict: PASS
- observation: T2-GATEF-RUN6 modal-holds-locomotion audit, upgraded per FINDING-CAPSTONE3-S03-CATCH-LOOP-STALL-2026-09-01. This step used to be a one-shot `assert`, diagnostic only: it recorded that the previous fight/catch had not finished unwinding but did not wait for it, so attempts b-j walked straight into 'the interaction arbiter is DISABLED... input_context combat' every time. `wait_until` polls the same check instead of sampling it once, so the next attempt's walk starts only once combat has actually closed and the world owns input again -- CD-3's rule applied to the retry ladder's own seam, not just to a single wait.

### S03-32br1 — open the Satchel to check for a fainted party member
- expected: game_menu.gd::select() grabs tab_backpack.gd::first_focus() (_buttons[0]) on open, so focus starts at slot 0 (orb_basic) every time.
- actual: inventory opened the shell: context world -> menu_backpack, focus on '' (@Button@66848)
- events: t=81.18
- verdict: PASS

### S03-32br2 — focus the Revive draught
- expected: T2-GATEF-RUN5/GAME-9/RIG-24 (S03-56d/S03-56f) already proved fixed-offset focus_move fragile in this same bag once acquisitions or spends shift the grid: a positional "3 cells right of slot 0" assumed a fresh save's fill order, and this ladder's own telemetry (gate-f-run-20260901T220548Z-s03fix attempts 3-4) shows it silently landing on the wrong item once earlier presses in this same recovery block had already shifted the grid, so every recovery reported PASS while reviving nothing and party size stayed capped at 2 (FINDING-S03-CATCH-LOOP-REVIVE-SLOT-2026-09-02). focus_item reads the cursor and sends real ui_left/ui_right the way a player reads the bag, regardless of where Revive actually sits.
- actual: focus_item 'revive': cursor on cell 5 after 5 move(s) (from cell 0)
- events: t=82.23
- verdict: PASS

### S03-32br3 — try to use it on whoever needs it
- expected: tab_backpack.gd::_read_use(): opens the target picker if any party member is fainted, or safely refuses ('Nobody needs reviving.') with no state change otherwise -- live-verified in both directions (tools/gate_f/probe_revive_menu_flow.gd), which is what makes this safe to run every time regardless of party state.
- actual: pressed interact x1 (tap, 1 frames each) on the default device, resolved to JoyBtn:2
- events: t=82.52
- verdict: PASS

### S03-32br4 — confirm the target if a picker opened
- expected: If the picker opened, focus already sits on the only eligible (fainted) row; this confirms it, clearing `fainted` and consuming one Revive. If nobody was fainted, this press instead picks the Revive stack up off the grid (Godot's ordinary carry gesture) -- non-destructive, corrected by the next step.
- actual: pressed ui_accept x1 (tap, 1 frames each) on the default device, resolved to JoyBtn:0
- events: t=82.72
- verdict: PASS

### S03-32br5 — put back an accidental pick-up, or close the shell
- expected: Observe the live Satchel and close any remaining picker, held stack or grid using mapped B only while the shell owns it; already-closed world requires zero input.
- actual: physical shell close: {"ok":true,"presses":2,"settle_frames":2,"why":""}
- events: t=83.02
- verdict: PASS

### S03-32br6 — close the shell for real
- expected: Observe the live Satchel and close any remaining picker, held stack or grid using mapped B only while the shell owns it; already-closed world requires zero input.
- actual: physical shell close: {"ok":true,"presses":0,"settle_frames":0,"why":""}
- events: t=83.02
- verdict: PASS

### S03-32br7 — select a healthy earned pilot before the next fight
- expected: Read the actual earned roster and use mapped LB only as needed to deploy its living, non-resting member with greatest current HP; verify the actual companion is visible before continuing. Revive recovery remains before this step.
- actual: physical world pilot selection: {"no_input":false,"ok":true,"physics_frames":4,"presses":2,"process_frames":4,"selected_index":4,"why":"healthiest earned creature and actual companion verified; 2 physical LB taps"}
- events: t=83.43
- verdict: PASS
- observation: R8 selected a newly caught 15 HP Bramblebun with one blind cycle while 79 HP and 58 HP allies were available; it fainted during encounter staging. Select by live HP with bounded physical cycling, without health or active-index writes.

### S03-32br8 — let the party swap finish
- expected: encounter_director.gd::summon_active_creature() is async; the newly-active creature's body needs a few frames to stand up before the next engage is attempted.
- actual: waited 60 physics frames
- events: t=84.43
- verdict: PASS

### S03-32br9 — wait for the world to own input before attempt 3
- expected: Both branches above converge on a closed shell and a settled party swap; the world must have input back before the next attempt's walk starts.
- actual: input_context=world (wanted world) [true after 0 physics frames]
- events: t=84.43
- verdict: PASS

### S03-32c — walk to a live bramblebun (attempt 3)
- expected: RIG-16: tracks a live bramblebun's own position every frame instead of a fixed cluster-centre coordinate. Arrival also requires the intended living wild creature to own the actual actionable Engage prompt; bounded physical repositioning shares the existing walking budget.
- actual: CONDITION-SATISFIED move_to_entity: authored skip_if verified from live state; no action input issued. predicate={"check":"party_size","min":5.0}; readback=party size 5 (wanted >= 5)
- events: t=84.43
- verdict: PASS
- observation: RIG-F5/GAME-F3. `within: 4.5` is what handed the interact button to a harvest node: prompt_arbiter.gd is 'highest priority first, then NEAREST', engage and gather are both priority 0, and from 4.5 m out a node at half a metre wins every time. Four of the twenty authored village nodes sit inside the practice cluster's own 15 m disc, deliberately -- harvest.json's own _why_gate_b_three_beds put them on 'the practice-path loop the first day already walks' -- so the collision is structural and the fix is to stand at the creature, which is what a player does. 2.0 rather than 1.8: RIG-F5 measured a bramblebun 0.22 m up a slope failing a 1.8 m close_3d check at 1.81 m in 3D, and named 2.0 as the floor.

### S03-32c2 — challenge it (attempt 3)
- expected: After prompt settling, require the actual Engage provider and the exact living wild selected by the preceding verified approach before pressing. A competing gather prompt or another wild is not this encounter.
- actual: CONDITION-SATISFIED interact_with: authored skip_if verified from live state; no action input issued. predicate={"check":"party_size","min":5.0}; readback=party size 5 (wanted >= 5)
- events: t=84.43
- verdict: PASS

### S03-33c — let the fight stage
- expected: the combat camera acquires both combatants
- actual: waited 180 physics frames
- events: t=87.43
- verdict: PASS

### S03-35c — trim it with quick attacks (do not kill it)
- expected: the target is WEAKENED as close to zero HP as this fight will safely bear -- not defeated, a defeated wild cannot be caught -- computed from the fight's own real per-hit damage rather than a guessed hit count or a guessed HP-fraction floor. Once the earned party already has five members, verify that live condition and issue no further attack input.
- actual: CONDITION-SATISFIED chip_to_floor: authored skip_if verified from live state; no action input issued. predicate={"check":"party_size","min":5.0}; readback=party size 5 (wanted >= 5)
- events: t=87.43
- verdict: PASS
- observation: RIG, 2026-08-31: combat_quick x20 KILLED a 106 HP target, so a fixed times: 3 (S02's own proven opener) replaced it -- correctly avoiding a kill, but measured directly (gate-f-run-20260902T053310Z-s03enginefix) landing anywhere from 57% to 75% of max HP depending on the creature's own defence, the FLAT part of hp_factor's curve. A fixed enemy_hp_fraction 0.3 floor (this step's own immediately-prior version) fixed that but was still a guessed number, not a computed one -- called out directly by the owner in this session, 2026-09-02 ('if 5 presses took 70 health you could do another 2 without killing it. why wouldnt you? ... you can surely calculate what needs to be done'). chip_to_floor (operator_harness.gd, added for this) reads the live enemy hp before and after every swing and learns the actual per-hit damage this fight is dealing -- combat_math.gd::rolled_damage()'s only per-swing randomness against a fixed target is its +/-10% variance roll, so one real hit already bounds every later one -- and refuses the next swing only once the largest hit seen so far, scaled by safety_factor (1.25, past the 1.1/0.9=1.222 worst-case roll swing), would not survive. No fixed floor anywhere in this step; it chips exactly as far as the specific creature in front of it allows.

### S03-36c — aim and throw until caught, or out of tries (attempt 3)
- expected: the target is caught, or every allotted throw against it is spent -- a missed throw does not end the fight (catching.json's own cooldown exists so a failed catch can be re-thrown once the wobble clears; combat_manager.gd re-engages the same target on a miss), so this keeps throwing at the SAME chipped-down target instead of treating one orb as the whole attempt.
- actual: CONDITION-SATISFIED throw_until_caught: authored skip_if verified from live state; no action input issued. predicate={"check":"combat_running","equals":false}; readback=combat_running=false (wanted false)
- events: t=87.43
- verdict: PASS
- observation: Owner directive, this session: "we should chip down then throw orbs til we catch." The ladder used to spend exactly one orb per numbered attempt (arm aim, track, throw once, wait) and then move on regardless of outcome -- discarding real, intended-to-be-retried catch chances the moment a single throw missed, even though nothing about the game ends the fight on a miss. throw_until_caught (operator_harness.gd, added for this) re-arms the aim, re-tracks the live target and throws again, up to max_throws times, stopping the instant the party actually grows.

### S03-32c2w — wait for the world to own input before this walk
- expected: no narrative modal or combat is holding locomotion when this walk starts
- actual: input_context=world (wanted world) [true after 0 physics frames]
- events: t=87.43
- verdict: PASS
- observation: T2-GATEF-RUN6 modal-holds-locomotion audit, upgraded per FINDING-CAPSTONE3-S03-CATCH-LOOP-STALL-2026-09-01. This step used to be a one-shot `assert`, diagnostic only: it recorded that the previous fight/catch had not finished unwinding but did not wait for it, so attempts b-j walked straight into 'the interaction arbiter is DISABLED... input_context combat' every time. `wait_until` polls the same check instead of sampling it once, so the next attempt's walk starts only once combat has actually closed and the world owns input again -- CD-3's rule applied to the retry ladder's own seam, not just to a single wait.

### S03-32cr1 — open the Satchel to check for a fainted party member
- expected: game_menu.gd::select() grabs tab_backpack.gd::first_focus() (_buttons[0]) on open, so focus starts at slot 0 (orb_basic) every time.
- actual: inventory opened the shell: context world -> menu_backpack, focus on '' (@Button@67074)
- events: t=87.72
- verdict: PASS

### S03-32cr2 — focus the Revive draught
- expected: T2-GATEF-RUN5/GAME-9/RIG-24 (S03-56d/S03-56f) already proved fixed-offset focus_move fragile in this same bag once acquisitions or spends shift the grid: a positional "3 cells right of slot 0" assumed a fresh save's fill order, and this ladder's own telemetry (gate-f-run-20260901T220548Z-s03fix attempts 3-4) shows it silently landing on the wrong item once earlier presses in this same recovery block had already shifted the grid, so every recovery reported PASS while reviving nothing and party size stayed capped at 2 (FINDING-S03-CATCH-LOOP-REVIVE-SLOT-2026-09-02). focus_item reads the cursor and sends real ui_left/ui_right the way a player reads the bag, regardless of where Revive actually sits.
- actual: focus_item 'revive': cursor on cell 5 after 5 move(s) (from cell 0)
- events: t=88.78
- verdict: PASS

### S03-32cr3 — try to use it on whoever needs it
- expected: tab_backpack.gd::_read_use(): opens the target picker if any party member is fainted, or safely refuses ('Nobody needs reviving.') with no state change otherwise -- live-verified in both directions (tools/gate_f/probe_revive_menu_flow.gd), which is what makes this safe to run every time regardless of party state.
- actual: pressed interact x1 (tap, 1 frames each) on the default device, resolved to JoyBtn:2
- events: t=89.08
- verdict: PASS

### S03-32cr4 — confirm the target if a picker opened
- expected: If the picker opened, focus already sits on the only eligible (fainted) row; this confirms it, clearing `fainted` and consuming one Revive. If nobody was fainted, this press instead picks the Revive stack up off the grid (Godot's ordinary carry gesture) -- non-destructive, corrected by the next step.
- actual: pressed ui_accept x1 (tap, 1 frames each) on the default device, resolved to JoyBtn:0
- events: t=89.30
- verdict: PASS

### S03-32cr5 — put back an accidental pick-up, or close the shell
- expected: Observe the live Satchel and close any remaining picker, held stack or grid using mapped B only while the shell owns it; already-closed world requires zero input.
- actual: physical shell close: {"ok":true,"presses":2,"settle_frames":2,"why":""}
- events: t=89.58
- verdict: PASS

### S03-32cr6 — close the shell for real
- expected: Observe the live Satchel and close any remaining picker, held stack or grid using mapped B only while the shell owns it; already-closed world requires zero input.
- actual: physical shell close: {"ok":true,"presses":0,"settle_frames":0,"why":""}
- events: t=89.58
- verdict: PASS

### S03-32cr7 — select a healthy earned pilot before the next fight
- expected: Read the actual earned roster and use mapped LB only as needed to deploy its living, non-resting member with greatest current HP; verify the actual companion is visible before continuing. Revive recovery remains before this step.
- actual: physical world pilot selection: {"no_input":true,"ok":true,"physics_frames":0,"presses":0,"process_frames":0,"selected_index":4,"why":"healthiest earned creature and actual companion verified; 0 physical LB taps"}
- events: t=89.58
- verdict: PASS
- observation: R8 selected a newly caught 15 HP Bramblebun with one blind cycle while 79 HP and 58 HP allies were available; it fainted during encounter staging. Select by live HP with bounded physical cycling, without health or active-index writes.

### S03-32cr8 — let the party swap finish
- expected: encounter_director.gd::summon_active_creature() is async; the newly-active creature's body needs a few frames to stand up before the next engage is attempted.
- actual: waited 60 physics frames
- events: t=90.58
- verdict: PASS

### S03-32cr9 — wait for the world to own input before attempt 4
- expected: Both branches above converge on a closed shell and a settled party swap; the world must have input back before the next attempt's walk starts.
- actual: input_context=world (wanted world) [true after 0 physics frames]
- events: t=90.58
- verdict: PASS

### S03-32d — walk to a live bramblebun (attempt 4)
- expected: RIG-16: tracks a live bramblebun's own position every frame instead of a fixed cluster-centre coordinate. Arrival also requires the intended living wild creature to own the actual actionable Engage prompt; bounded physical repositioning shares the existing walking budget.
- actual: CONDITION-SATISFIED move_to_entity: authored skip_if verified from live state; no action input issued. predicate={"check":"party_size","min":5.0}; readback=party size 5 (wanted >= 5)
- events: t=90.58
- verdict: PASS
- observation: RIG-F5/GAME-F3. `within: 4.5` is what handed the interact button to a harvest node: prompt_arbiter.gd is 'highest priority first, then NEAREST', engage and gather are both priority 0, and from 4.5 m out a node at half a metre wins every time. Four of the twenty authored village nodes sit inside the practice cluster's own 15 m disc, deliberately -- harvest.json's own _why_gate_b_three_beds put them on 'the practice-path loop the first day already walks' -- so the collision is structural and the fix is to stand at the creature, which is what a player does. 2.0 rather than 1.8: RIG-F5 measured a bramblebun 0.22 m up a slope failing a 1.8 m close_3d check at 1.81 m in 3D, and named 2.0 as the floor.

### S03-32d2 — challenge it (attempt 4)
- expected: After prompt settling, require the actual Engage provider and the exact living wild selected by the preceding verified approach before pressing. A competing gather prompt or another wild is not this encounter.
- actual: CONDITION-SATISFIED interact_with: authored skip_if verified from live state; no action input issued. predicate={"check":"party_size","min":5.0}; readback=party size 5 (wanted >= 5)
- events: t=90.58
- verdict: PASS

### S03-33d — let the fight stage
- expected: the combat camera acquires both combatants
- actual: waited 180 physics frames
- events: t=93.58
- verdict: PASS

### S03-35d — trim it with quick attacks (do not kill it)
- expected: the target is WEAKENED as close to zero HP as this fight will safely bear -- not defeated, a defeated wild cannot be caught -- computed from the fight's own real per-hit damage rather than a guessed hit count or a guessed HP-fraction floor. Once the earned party already has five members, verify that live condition and issue no further attack input.
- actual: CONDITION-SATISFIED chip_to_floor: authored skip_if verified from live state; no action input issued. predicate={"check":"party_size","min":5.0}; readback=party size 5 (wanted >= 5)
- events: t=93.58
- verdict: PASS
- observation: RIG, 2026-08-31: combat_quick x20 KILLED a 106 HP target, so a fixed times: 3 (S02's own proven opener) replaced it -- correctly avoiding a kill, but measured directly (gate-f-run-20260902T053310Z-s03enginefix) landing anywhere from 57% to 75% of max HP depending on the creature's own defence, the FLAT part of hp_factor's curve. A fixed enemy_hp_fraction 0.3 floor (this step's own immediately-prior version) fixed that but was still a guessed number, not a computed one -- called out directly by the owner in this session, 2026-09-02 ('if 5 presses took 70 health you could do another 2 without killing it. why wouldnt you? ... you can surely calculate what needs to be done'). chip_to_floor (operator_harness.gd, added for this) reads the live enemy hp before and after every swing and learns the actual per-hit damage this fight is dealing -- combat_math.gd::rolled_damage()'s only per-swing randomness against a fixed target is its +/-10% variance roll, so one real hit already bounds every later one -- and refuses the next swing only once the largest hit seen so far, scaled by safety_factor (1.25, past the 1.1/0.9=1.222 worst-case roll swing), would not survive. No fixed floor anywhere in this step; it chips exactly as far as the specific creature in front of it allows.

### S03-36d — aim and throw until caught, or out of tries (attempt 4)
- expected: the target is caught, or every allotted throw against it is spent -- a missed throw does not end the fight (catching.json's own cooldown exists so a failed catch can be re-thrown once the wobble clears; combat_manager.gd re-engages the same target on a miss), so this keeps throwing at the SAME chipped-down target instead of treating one orb as the whole attempt.
- actual: CONDITION-SATISFIED throw_until_caught: authored skip_if verified from live state; no action input issued. predicate={"check":"combat_running","equals":false}; readback=combat_running=false (wanted false)
- events: t=93.58
- verdict: PASS
- observation: Owner directive, this session: "we should chip down then throw orbs til we catch." The ladder used to spend exactly one orb per numbered attempt (arm aim, track, throw once, wait) and then move on regardless of outcome -- discarding real, intended-to-be-retried catch chances the moment a single throw missed, even though nothing about the game ends the fight on a miss. throw_until_caught (operator_harness.gd, added for this) re-arms the aim, re-tracks the live target and throws again, up to max_throws times, stopping the instant the party actually grows.

### S03-32d2w — wait for the world to own input before this walk
- expected: no narrative modal or combat is holding locomotion when this walk starts
- actual: input_context=world (wanted world) [true after 0 physics frames]
- events: t=93.58
- verdict: PASS
- observation: T2-GATEF-RUN6 modal-holds-locomotion audit, upgraded per FINDING-CAPSTONE3-S03-CATCH-LOOP-STALL-2026-09-01. This step used to be a one-shot `assert`, diagnostic only: it recorded that the previous fight/catch had not finished unwinding but did not wait for it, so attempts b-j walked straight into 'the interaction arbiter is DISABLED... input_context combat' every time. `wait_until` polls the same check instead of sampling it once, so the next attempt's walk starts only once combat has actually closed and the world owns input again -- CD-3's rule applied to the retry ladder's own seam, not just to a single wait.

### S03-32dr1 — open the Satchel to check for a fainted party member
- expected: game_menu.gd::select() grabs tab_backpack.gd::first_focus() (_buttons[0]) on open, so focus starts at slot 0 (orb_basic) every time.
- actual: inventory opened the shell: context world -> menu_backpack, focus on '' (@Button@67297)
- events: t=93.87
- verdict: PASS

### S03-32dr2 — focus the Revive draught
- expected: T2-GATEF-RUN5/GAME-9/RIG-24 (S03-56d/S03-56f) already proved fixed-offset focus_move fragile in this same bag once acquisitions or spends shift the grid: a positional "3 cells right of slot 0" assumed a fresh save's fill order, and this ladder's own telemetry (gate-f-run-20260901T220548Z-s03fix attempts 3-4) shows it silently landing on the wrong item once earlier presses in this same recovery block had already shifted the grid, so every recovery reported PASS while reviving nothing and party size stayed capped at 2 (FINDING-S03-CATCH-LOOP-REVIVE-SLOT-2026-09-02). focus_item reads the cursor and sends real ui_left/ui_right the way a player reads the bag, regardless of where Revive actually sits.
- actual: focus_item 'revive': cursor on cell 5 after 5 move(s) (from cell 0)
- events: t=94.92
- verdict: PASS

### S03-32dr3 — try to use it on whoever needs it
- expected: tab_backpack.gd::_read_use(): opens the target picker if any party member is fainted, or safely refuses ('Nobody needs reviving.') with no state change otherwise -- live-verified in both directions (tools/gate_f/probe_revive_menu_flow.gd), which is what makes this safe to run every time regardless of party state.
- actual: pressed interact x1 (tap, 1 frames each) on the default device, resolved to JoyBtn:2
- events: t=95.23
- verdict: PASS

### S03-32dr4 — confirm the target if a picker opened
- expected: If the picker opened, focus already sits on the only eligible (fainted) row; this confirms it, clearing `fainted` and consuming one Revive. If nobody was fainted, this press instead picks the Revive stack up off the grid (Godot's ordinary carry gesture) -- non-destructive, corrected by the next step.
- actual: pressed ui_accept x1 (tap, 1 frames each) on the default device, resolved to JoyBtn:0
- events: t=95.43
- verdict: PASS

### S03-32dr5 — put back an accidental pick-up, or close the shell
- expected: Observe the live Satchel and close any remaining picker, held stack or grid using mapped B only while the shell owns it; already-closed world requires zero input.
- actual: physical shell close: {"ok":true,"presses":2,"settle_frames":2,"why":""}
- events: t=95.73
- verdict: PASS

### S03-32dr6 — close the shell for real
- expected: Observe the live Satchel and close any remaining picker, held stack or grid using mapped B only while the shell owns it; already-closed world requires zero input.
- actual: physical shell close: {"ok":true,"presses":0,"settle_frames":0,"why":""}
- events: t=95.73
- verdict: PASS

### S03-32dr7 — select a healthy earned pilot before the next fight
- expected: Read the actual earned roster and use mapped LB only as needed to deploy its living, non-resting member with greatest current HP; verify the actual companion is visible before continuing. Revive recovery remains before this step.
- actual: physical world pilot selection: {"no_input":false,"ok":true,"physics_frames":4,"presses":2,"process_frames":4,"selected_index":1,"why":"healthiest earned creature and actual companion verified; 2 physical LB taps"}
- events: t=96.18
- verdict: PASS
- observation: R8 selected a newly caught 15 HP Bramblebun with one blind cycle while 79 HP and 58 HP allies were available; it fainted during encounter staging. Select by live HP with bounded physical cycling, without health or active-index writes.

### S03-32dr8 — let the party swap finish
- expected: encounter_director.gd::summon_active_creature() is async; the newly-active creature's body needs a few frames to stand up before the next engage is attempted.
- actual: waited 60 physics frames
- events: t=97.18
- verdict: PASS

### S03-32dr9 — wait for the world to own input before attempt 5
- expected: Both branches above converge on a closed shell and a settled party swap; the world must have input back before the next attempt's walk starts.
- actual: input_context=world (wanted world) [true after 0 physics frames]
- events: t=97.18
- verdict: PASS

### S03-32e — walk to a live bramblebun (attempt 5)
- expected: RIG-16: tracks a live bramblebun's own position every frame instead of a fixed cluster-centre coordinate. Arrival also requires the intended living wild creature to own the actual actionable Engage prompt; bounded physical repositioning shares the existing walking budget.
- actual: CONDITION-SATISFIED move_to_entity: authored skip_if verified from live state; no action input issued. predicate={"check":"party_size","min":5.0}; readback=party size 5 (wanted >= 5)
- events: t=97.18
- verdict: PASS
- observation: RIG-F5/GAME-F3. `within: 4.5` is what handed the interact button to a harvest node: prompt_arbiter.gd is 'highest priority first, then NEAREST', engage and gather are both priority 0, and from 4.5 m out a node at half a metre wins every time. Four of the twenty authored village nodes sit inside the practice cluster's own 15 m disc, deliberately -- harvest.json's own _why_gate_b_three_beds put them on 'the practice-path loop the first day already walks' -- so the collision is structural and the fix is to stand at the creature, which is what a player does. 2.0 rather than 1.8: RIG-F5 measured a bramblebun 0.22 m up a slope failing a 1.8 m close_3d check at 1.81 m in 3D, and named 2.0 as the floor.

### S03-32e2 — challenge it (attempt 5)
- expected: After prompt settling, require the actual Engage provider and the exact living wild selected by the preceding verified approach before pressing. A competing gather prompt or another wild is not this encounter.
- actual: CONDITION-SATISFIED interact_with: authored skip_if verified from live state; no action input issued. predicate={"check":"party_size","min":5.0}; readback=party size 5 (wanted >= 5)
- events: t=97.18
- verdict: PASS

### S03-33e — let the fight stage
- expected: the combat camera acquires both combatants
- actual: waited 180 physics frames
- events: t=100.18
- verdict: PASS

### S03-35e — trim it with quick attacks (do not kill it)
- expected: the target is WEAKENED as close to zero HP as this fight will safely bear -- not defeated, a defeated wild cannot be caught -- computed from the fight's own real per-hit damage rather than a guessed hit count or a guessed HP-fraction floor. Once the earned party already has five members, verify that live condition and issue no further attack input.
- actual: CONDITION-SATISFIED chip_to_floor: authored skip_if verified from live state; no action input issued. predicate={"check":"party_size","min":5.0}; readback=party size 5 (wanted >= 5)
- events: t=100.18
- verdict: PASS
- observation: RIG, 2026-08-31: combat_quick x20 KILLED a 106 HP target, so a fixed times: 3 (S02's own proven opener) replaced it -- correctly avoiding a kill, but measured directly (gate-f-run-20260902T053310Z-s03enginefix) landing anywhere from 57% to 75% of max HP depending on the creature's own defence, the FLAT part of hp_factor's curve. A fixed enemy_hp_fraction 0.3 floor (this step's own immediately-prior version) fixed that but was still a guessed number, not a computed one -- called out directly by the owner in this session, 2026-09-02 ('if 5 presses took 70 health you could do another 2 without killing it. why wouldnt you? ... you can surely calculate what needs to be done'). chip_to_floor (operator_harness.gd, added for this) reads the live enemy hp before and after every swing and learns the actual per-hit damage this fight is dealing -- combat_math.gd::rolled_damage()'s only per-swing randomness against a fixed target is its +/-10% variance roll, so one real hit already bounds every later one -- and refuses the next swing only once the largest hit seen so far, scaled by safety_factor (1.25, past the 1.1/0.9=1.222 worst-case roll swing), would not survive. No fixed floor anywhere in this step; it chips exactly as far as the specific creature in front of it allows.

### S03-36e — aim and throw until caught, or out of tries (attempt 5)
- expected: the target is caught, or every allotted throw against it is spent -- a missed throw does not end the fight (catching.json's own cooldown exists so a failed catch can be re-thrown once the wobble clears; combat_manager.gd re-engages the same target on a miss), so this keeps throwing at the SAME chipped-down target instead of treating one orb as the whole attempt.
- actual: CONDITION-SATISFIED throw_until_caught: authored skip_if verified from live state; no action input issued. predicate={"check":"combat_running","equals":false}; readback=combat_running=false (wanted false)
- events: t=100.18
- verdict: PASS
- observation: Owner directive, this session: "we should chip down then throw orbs til we catch." The ladder used to spend exactly one orb per numbered attempt (arm aim, track, throw once, wait) and then move on regardless of outcome -- discarding real, intended-to-be-retried catch chances the moment a single throw missed, even though nothing about the game ends the fight on a miss. throw_until_caught (operator_harness.gd, added for this) re-arms the aim, re-tracks the live target and throws again, up to max_throws times, stopping the instant the party actually grows.

### S03-32e2w — wait for the world to own input before this walk
- expected: no narrative modal or combat is holding locomotion when this walk starts
- actual: input_context=world (wanted world) [true after 0 physics frames]
- events: t=100.18
- verdict: PASS
- observation: T2-GATEF-RUN6 modal-holds-locomotion audit, upgraded per FINDING-CAPSTONE3-S03-CATCH-LOOP-STALL-2026-09-01. This step used to be a one-shot `assert`, diagnostic only: it recorded that the previous fight/catch had not finished unwinding but did not wait for it, so attempts b-j walked straight into 'the interaction arbiter is DISABLED... input_context combat' every time. `wait_until` polls the same check instead of sampling it once, so the next attempt's walk starts only once combat has actually closed and the world owns input again -- CD-3's rule applied to the retry ladder's own seam, not just to a single wait.

### S03-32er1 — open the Satchel to check for a fainted party member
- expected: game_menu.gd::select() grabs tab_backpack.gd::first_focus() (_buttons[0]) on open, so focus starts at slot 0 (orb_basic) every time.
- actual: inventory opened the shell: context world -> menu_backpack, focus on '' (@Button@67523)
- events: t=100.45
- verdict: PASS

### S03-32er2 — focus the Revive draught
- expected: T2-GATEF-RUN5/GAME-9/RIG-24 (S03-56d/S03-56f) already proved fixed-offset focus_move fragile in this same bag once acquisitions or spends shift the grid: a positional "3 cells right of slot 0" assumed a fresh save's fill order, and this ladder's own telemetry (gate-f-run-20260901T220548Z-s03fix attempts 3-4) shows it silently landing on the wrong item once earlier presses in this same recovery block had already shifted the grid, so every recovery reported PASS while reviving nothing and party size stayed capped at 2 (FINDING-S03-CATCH-LOOP-REVIVE-SLOT-2026-09-02). focus_item reads the cursor and sends real ui_left/ui_right the way a player reads the bag, regardless of where Revive actually sits.
- actual: focus_item 'revive': cursor on cell 5 after 5 move(s) (from cell 0)
- events: t=101.48
- verdict: PASS

### S03-32er3 — try to use it on whoever needs it
- expected: tab_backpack.gd::_read_use(): opens the target picker if any party member is fainted, or safely refuses ('Nobody needs reviving.') with no state change otherwise -- live-verified in both directions (tools/gate_f/probe_revive_menu_flow.gd), which is what makes this safe to run every time regardless of party state.
- actual: pressed interact x1 (tap, 1 frames each) on the default device, resolved to JoyBtn:2
- events: t=101.80
- verdict: PASS

### S03-32er4 — confirm the target if a picker opened
- expected: If the picker opened, focus already sits on the only eligible (fainted) row; this confirms it, clearing `fainted` and consuming one Revive. If nobody was fainted, this press instead picks the Revive stack up off the grid (Godot's ordinary carry gesture) -- non-destructive, corrected by the next step.
- actual: pressed ui_accept x1 (tap, 1 frames each) on the default device, resolved to JoyBtn:0
- events: t=102.00
- verdict: PASS

### S03-32er5 — put back an accidental pick-up, or close the shell
- expected: Observe the live Satchel and close any remaining picker, held stack or grid using mapped B only while the shell owns it; already-closed world requires zero input.
- actual: physical shell close: {"ok":true,"presses":2,"settle_frames":2,"why":""}
- events: t=102.30
- verdict: PASS

### S03-32er6 — close the shell for real
- expected: Observe the live Satchel and close any remaining picker, held stack or grid using mapped B only while the shell owns it; already-closed world requires zero input.
- actual: physical shell close: {"ok":true,"presses":0,"settle_frames":0,"why":""}
- events: t=102.30
- verdict: PASS

### S03-32er7 — select a healthy earned pilot before the next fight
- expected: Read the actual earned roster and use mapped LB only as needed to deploy its living, non-resting member with greatest current HP; verify the actual companion is visible before continuing. Revive recovery remains before this step.
- actual: physical world pilot selection: {"no_input":true,"ok":true,"physics_frames":0,"presses":0,"process_frames":0,"selected_index":1,"why":"healthiest earned creature and actual companion verified; 0 physical LB taps"}
- events: t=102.30
- verdict: PASS
- observation: R8 selected a newly caught 15 HP Bramblebun with one blind cycle while 79 HP and 58 HP allies were available; it fainted during encounter staging. Select by live HP with bounded physical cycling, without health or active-index writes.

### S03-32er8 — let the party swap finish
- expected: encounter_director.gd::summon_active_creature() is async; the newly-active creature's body needs a few frames to stand up before the next engage is attempted.
- actual: waited 60 physics frames
- events: t=103.30
- verdict: PASS

### S03-32er9 — wait for the world to own input before attempt 6
- expected: Both branches above converge on a closed shell and a settled party swap; the world must have input back before the next attempt's walk starts.
- actual: input_context=world (wanted world) [true after 0 physics frames]
- events: t=103.30
- verdict: PASS

### S03-32f — walk to a live bramblebun (attempt 6)
- expected: RIG-16: tracks a live bramblebun's own position every frame instead of a fixed cluster-centre coordinate. Arrival also requires the intended living wild creature to own the actual actionable Engage prompt; bounded physical repositioning shares the existing walking budget.
- actual: CONDITION-SATISFIED move_to_entity: authored skip_if verified from live state; no action input issued. predicate={"check":"party_size","min":5.0}; readback=party size 5 (wanted >= 5)
- events: t=103.30
- verdict: PASS
- observation: RIG-F5/GAME-F3. `within: 4.5` is what handed the interact button to a harvest node: prompt_arbiter.gd is 'highest priority first, then NEAREST', engage and gather are both priority 0, and from 4.5 m out a node at half a metre wins every time. Four of the twenty authored village nodes sit inside the practice cluster's own 15 m disc, deliberately -- harvest.json's own _why_gate_b_three_beds put them on 'the practice-path loop the first day already walks' -- so the collision is structural and the fix is to stand at the creature, which is what a player does. 2.0 rather than 1.8: RIG-F5 measured a bramblebun 0.22 m up a slope failing a 1.8 m close_3d check at 1.81 m in 3D, and named 2.0 as the floor.

### S03-32f2 — challenge it (attempt 6)
- expected: After prompt settling, require the actual Engage provider and the exact living wild selected by the preceding verified approach before pressing. A competing gather prompt or another wild is not this encounter.
- actual: CONDITION-SATISFIED interact_with: authored skip_if verified from live state; no action input issued. predicate={"check":"party_size","min":5.0}; readback=party size 5 (wanted >= 5)
- events: t=103.30
- verdict: PASS

### S03-33f — let the fight stage
- expected: the combat camera acquires both combatants
- actual: waited 180 physics frames
- events: t=106.30
- verdict: PASS

### S03-35f — trim it with quick attacks (do not kill it)
- expected: the target is WEAKENED as close to zero HP as this fight will safely bear -- not defeated, a defeated wild cannot be caught -- computed from the fight's own real per-hit damage rather than a guessed hit count or a guessed HP-fraction floor. Once the earned party already has five members, verify that live condition and issue no further attack input.
- actual: CONDITION-SATISFIED chip_to_floor: authored skip_if verified from live state; no action input issued. predicate={"check":"party_size","min":5.0}; readback=party size 5 (wanted >= 5)
- events: t=106.30
- verdict: PASS
- observation: RIG, 2026-08-31: combat_quick x20 KILLED a 106 HP target, so a fixed times: 3 (S02's own proven opener) replaced it -- correctly avoiding a kill, but measured directly (gate-f-run-20260902T053310Z-s03enginefix) landing anywhere from 57% to 75% of max HP depending on the creature's own defence, the FLAT part of hp_factor's curve. A fixed enemy_hp_fraction 0.3 floor (this step's own immediately-prior version) fixed that but was still a guessed number, not a computed one -- called out directly by the owner in this session, 2026-09-02 ('if 5 presses took 70 health you could do another 2 without killing it. why wouldnt you? ... you can surely calculate what needs to be done'). chip_to_floor (operator_harness.gd, added for this) reads the live enemy hp before and after every swing and learns the actual per-hit damage this fight is dealing -- combat_math.gd::rolled_damage()'s only per-swing randomness against a fixed target is its +/-10% variance roll, so one real hit already bounds every later one -- and refuses the next swing only once the largest hit seen so far, scaled by safety_factor (1.25, past the 1.1/0.9=1.222 worst-case roll swing), would not survive. No fixed floor anywhere in this step; it chips exactly as far as the specific creature in front of it allows.

### S03-36f — aim and throw until caught, or out of tries (attempt 6)
- expected: the target is caught, or every allotted throw against it is spent -- a missed throw does not end the fight (catching.json's own cooldown exists so a failed catch can be re-thrown once the wobble clears; combat_manager.gd re-engages the same target on a miss), so this keeps throwing at the SAME chipped-down target instead of treating one orb as the whole attempt.
- actual: CONDITION-SATISFIED throw_until_caught: authored skip_if verified from live state; no action input issued. predicate={"check":"combat_running","equals":false}; readback=combat_running=false (wanted false)
- events: t=106.30
- verdict: PASS
- observation: Owner directive, this session: "we should chip down then throw orbs til we catch." The ladder used to spend exactly one orb per numbered attempt (arm aim, track, throw once, wait) and then move on regardless of outcome -- discarding real, intended-to-be-retried catch chances the moment a single throw missed, even though nothing about the game ends the fight on a miss. throw_until_caught (operator_harness.gd, added for this) re-arms the aim, re-tracks the live target and throws again, up to max_throws times, stopping the instant the party actually grows.

### S03-32f2w — wait for the world to own input before this walk
- expected: no narrative modal or combat is holding locomotion when this walk starts
- actual: input_context=world (wanted world) [true after 0 physics frames]
- events: t=106.30
- verdict: PASS
- observation: T2-GATEF-RUN6 modal-holds-locomotion audit, upgraded per FINDING-CAPSTONE3-S03-CATCH-LOOP-STALL-2026-09-01. This step used to be a one-shot `assert`, diagnostic only: it recorded that the previous fight/catch had not finished unwinding but did not wait for it, so attempts b-j walked straight into 'the interaction arbiter is DISABLED... input_context combat' every time. `wait_until` polls the same check instead of sampling it once, so the next attempt's walk starts only once combat has actually closed and the world owns input again -- CD-3's rule applied to the retry ladder's own seam, not just to a single wait.

### S03-32fr1 — open the Satchel to check for a fainted party member
- expected: game_menu.gd::select() grabs tab_backpack.gd::first_focus() (_buttons[0]) on open, so focus starts at slot 0 (orb_basic) every time.
- actual: inventory opened the shell: context world -> menu_backpack, focus on '' (@Button@67746)
- events: t=106.60
- verdict: PASS

### S03-32fr2 — focus the Revive draught
- expected: T2-GATEF-RUN5/GAME-9/RIG-24 (S03-56d/S03-56f) already proved fixed-offset focus_move fragile in this same bag once acquisitions or spends shift the grid: a positional "3 cells right of slot 0" assumed a fresh save's fill order, and this ladder's own telemetry (gate-f-run-20260901T220548Z-s03fix attempts 3-4) shows it silently landing on the wrong item once earlier presses in this same recovery block had already shifted the grid, so every recovery reported PASS while reviving nothing and party size stayed capped at 2 (FINDING-S03-CATCH-LOOP-REVIVE-SLOT-2026-09-02). focus_item reads the cursor and sends real ui_left/ui_right the way a player reads the bag, regardless of where Revive actually sits.
- actual: focus_item 'revive': cursor on cell 5 after 5 move(s) (from cell 0)
- events: t=107.65
- verdict: PASS

### S03-32fr3 — try to use it on whoever needs it
- expected: tab_backpack.gd::_read_use(): opens the target picker if any party member is fainted, or safely refuses ('Nobody needs reviving.') with no state change otherwise -- live-verified in both directions (tools/gate_f/probe_revive_menu_flow.gd), which is what makes this safe to run every time regardless of party state.
- actual: pressed interact x1 (tap, 1 frames each) on the default device, resolved to JoyBtn:2
- events: t=107.95
- verdict: PASS

### S03-32fr4 — confirm the target if a picker opened
- expected: If the picker opened, focus already sits on the only eligible (fainted) row; this confirms it, clearing `fainted` and consuming one Revive. If nobody was fainted, this press instead picks the Revive stack up off the grid (Godot's ordinary carry gesture) -- non-destructive, corrected by the next step.
- actual: pressed ui_accept x1 (tap, 1 frames each) on the default device, resolved to JoyBtn:0
- events: t=108.17
- verdict: PASS

### S03-32fr5 — put back an accidental pick-up, or close the shell
- expected: Observe the live Satchel and close any remaining picker, held stack or grid using mapped B only while the shell owns it; already-closed world requires zero input.
- actual: physical shell close: {"ok":true,"presses":2,"settle_frames":2,"why":""}
- events: t=108.47
- verdict: PASS

### S03-32fr6 — close the shell for real
- expected: Observe the live Satchel and close any remaining picker, held stack or grid using mapped B only while the shell owns it; already-closed world requires zero input.
- actual: physical shell close: {"ok":true,"presses":0,"settle_frames":0,"why":""}
- events: t=108.47
- verdict: PASS

### S03-32fr7 — select a healthy earned pilot before the next fight
- expected: Read the actual earned roster and use mapped LB only as needed to deploy its living, non-resting member with greatest current HP; verify the actual companion is visible before continuing. Revive recovery remains before this step.
- actual: physical world pilot selection: {"no_input":true,"ok":true,"physics_frames":0,"presses":0,"process_frames":0,"selected_index":1,"why":"healthiest earned creature and actual companion verified; 0 physical LB taps"}
- events: t=108.47
- verdict: PASS
- observation: R8 selected a newly caught 15 HP Bramblebun with one blind cycle while 79 HP and 58 HP allies were available; it fainted during encounter staging. Select by live HP with bounded physical cycling, without health or active-index writes.

### S03-32fr8 — let the party swap finish
- expected: encounter_director.gd::summon_active_creature() is async; the newly-active creature's body needs a few frames to stand up before the next engage is attempted.
- actual: waited 60 physics frames
- events: t=109.47
- verdict: PASS

### S03-32fr9 — wait for the world to own input before attempt 7
- expected: Both branches above converge on a closed shell and a settled party swap; the world must have input back before the next attempt's walk starts.
- actual: input_context=world (wanted world) [true after 0 physics frames]
- events: t=109.47
- verdict: PASS

### S03-32g — walk to a live bramblebun (attempt 7)
- expected: RIG-16: tracks a live bramblebun's own position every frame instead of a fixed cluster-centre coordinate. Arrival also requires the intended living wild creature to own the actual actionable Engage prompt; bounded physical repositioning shares the existing walking budget.
- actual: CONDITION-SATISFIED move_to_entity: authored skip_if verified from live state; no action input issued. predicate={"check":"party_size","min":5.0}; readback=party size 5 (wanted >= 5)
- events: t=109.47
- verdict: PASS
- observation: RIG-F5/GAME-F3. `within: 4.5` is what handed the interact button to a harvest node: prompt_arbiter.gd is 'highest priority first, then NEAREST', engage and gather are both priority 0, and from 4.5 m out a node at half a metre wins every time. Four of the twenty authored village nodes sit inside the practice cluster's own 15 m disc, deliberately -- harvest.json's own _why_gate_b_three_beds put them on 'the practice-path loop the first day already walks' -- so the collision is structural and the fix is to stand at the creature, which is what a player does. 2.0 rather than 1.8: RIG-F5 measured a bramblebun 0.22 m up a slope failing a 1.8 m close_3d check at 1.81 m in 3D, and named 2.0 as the floor.

### S03-32g2 — challenge it (attempt 7)
- expected: After prompt settling, require the actual Engage provider and the exact living wild selected by the preceding verified approach before pressing. A competing gather prompt or another wild is not this encounter.
- actual: CONDITION-SATISFIED interact_with: authored skip_if verified from live state; no action input issued. predicate={"check":"party_size","min":5.0}; readback=party size 5 (wanted >= 5)
- events: t=109.47
- verdict: PASS

### S03-33g — let the fight stage
- expected: the combat camera acquires both combatants
- actual: waited 180 physics frames
- events: t=112.47
- verdict: PASS

### S03-35g — trim it with quick attacks (do not kill it)
- expected: the target is WEAKENED as close to zero HP as this fight will safely bear -- not defeated, a defeated wild cannot be caught -- computed from the fight's own real per-hit damage rather than a guessed hit count or a guessed HP-fraction floor. Once the earned party already has five members, verify that live condition and issue no further attack input.
- actual: CONDITION-SATISFIED chip_to_floor: authored skip_if verified from live state; no action input issued. predicate={"check":"party_size","min":5.0}; readback=party size 5 (wanted >= 5)
- events: t=112.47
- verdict: PASS
- observation: RIG, 2026-08-31: combat_quick x20 KILLED a 106 HP target, so a fixed times: 3 (S02's own proven opener) replaced it -- correctly avoiding a kill, but measured directly (gate-f-run-20260902T053310Z-s03enginefix) landing anywhere from 57% to 75% of max HP depending on the creature's own defence, the FLAT part of hp_factor's curve. A fixed enemy_hp_fraction 0.3 floor (this step's own immediately-prior version) fixed that but was still a guessed number, not a computed one -- called out directly by the owner in this session, 2026-09-02 ('if 5 presses took 70 health you could do another 2 without killing it. why wouldnt you? ... you can surely calculate what needs to be done'). chip_to_floor (operator_harness.gd, added for this) reads the live enemy hp before and after every swing and learns the actual per-hit damage this fight is dealing -- combat_math.gd::rolled_damage()'s only per-swing randomness against a fixed target is its +/-10% variance roll, so one real hit already bounds every later one -- and refuses the next swing only once the largest hit seen so far, scaled by safety_factor (1.25, past the 1.1/0.9=1.222 worst-case roll swing), would not survive. No fixed floor anywhere in this step; it chips exactly as far as the specific creature in front of it allows.

### S03-36g — aim and throw until caught, or out of tries (attempt 7)
- expected: the target is caught, or every allotted throw against it is spent -- a missed throw does not end the fight (catching.json's own cooldown exists so a failed catch can be re-thrown once the wobble clears; combat_manager.gd re-engages the same target on a miss), so this keeps throwing at the SAME chipped-down target instead of treating one orb as the whole attempt.
- actual: CONDITION-SATISFIED throw_until_caught: authored skip_if verified from live state; no action input issued. predicate={"check":"combat_running","equals":false}; readback=combat_running=false (wanted false)
- events: t=112.47
- verdict: PASS
- observation: Owner directive, this session: "we should chip down then throw orbs til we catch." The ladder used to spend exactly one orb per numbered attempt (arm aim, track, throw once, wait) and then move on regardless of outcome -- discarding real, intended-to-be-retried catch chances the moment a single throw missed, even though nothing about the game ends the fight on a miss. throw_until_caught (operator_harness.gd, added for this) re-arms the aim, re-tracks the live target and throws again, up to max_throws times, stopping the instant the party actually grows.

### S03-32g2w — wait for the world to own input before this walk
- expected: no narrative modal or combat is holding locomotion when this walk starts
- actual: input_context=world (wanted world) [true after 0 physics frames]
- events: t=112.47
- verdict: PASS
- observation: T2-GATEF-RUN6 modal-holds-locomotion audit, upgraded per FINDING-CAPSTONE3-S03-CATCH-LOOP-STALL-2026-09-01. This step used to be a one-shot `assert`, diagnostic only: it recorded that the previous fight/catch had not finished unwinding but did not wait for it, so attempts b-j walked straight into 'the interaction arbiter is DISABLED... input_context combat' every time. `wait_until` polls the same check instead of sampling it once, so the next attempt's walk starts only once combat has actually closed and the world owns input again -- CD-3's rule applied to the retry ladder's own seam, not just to a single wait.

### S03-32gr1 — open the Satchel to check for a fainted party member
- expected: game_menu.gd::select() grabs tab_backpack.gd::first_focus() (_buttons[0]) on open, so focus starts at slot 0 (orb_basic) every time.
- actual: inventory opened the shell: context world -> menu_backpack, focus on '' (@Button@67969)
- events: t=112.73
- verdict: PASS

### S03-32gr2 — focus the Revive draught
- expected: T2-GATEF-RUN5/GAME-9/RIG-24 (S03-56d/S03-56f) already proved fixed-offset focus_move fragile in this same bag once acquisitions or spends shift the grid: a positional "3 cells right of slot 0" assumed a fresh save's fill order, and this ladder's own telemetry (gate-f-run-20260901T220548Z-s03fix attempts 3-4) shows it silently landing on the wrong item once earlier presses in this same recovery block had already shifted the grid, so every recovery reported PASS while reviving nothing and party size stayed capped at 2 (FINDING-S03-CATCH-LOOP-REVIVE-SLOT-2026-09-02). focus_item reads the cursor and sends real ui_left/ui_right the way a player reads the bag, regardless of where Revive actually sits.
- actual: focus_item 'revive': cursor on cell 5 after 5 move(s) (from cell 0)
- events: t=113.80
- verdict: PASS

### S03-32gr3 — try to use it on whoever needs it
- expected: tab_backpack.gd::_read_use(): opens the target picker if any party member is fainted, or safely refuses ('Nobody needs reviving.') with no state change otherwise -- live-verified in both directions (tools/gate_f/probe_revive_menu_flow.gd), which is what makes this safe to run every time regardless of party state.
- actual: pressed interact x1 (tap, 1 frames each) on the default device, resolved to JoyBtn:2
- events: t=114.12
- verdict: PASS

### S03-32gr4 — confirm the target if a picker opened
- expected: If the picker opened, focus already sits on the only eligible (fainted) row; this confirms it, clearing `fainted` and consuming one Revive. If nobody was fainted, this press instead picks the Revive stack up off the grid (Godot's ordinary carry gesture) -- non-destructive, corrected by the next step.
- actual: pressed ui_accept x1 (tap, 1 frames each) on the default device, resolved to JoyBtn:0
- events: t=114.32
- verdict: PASS

### S03-32gr5 — put back an accidental pick-up, or close the shell
- expected: Observe the live Satchel and close any remaining picker, held stack or grid using mapped B only while the shell owns it; already-closed world requires zero input.
- actual: physical shell close: {"ok":true,"presses":2,"settle_frames":2,"why":""}
- events: t=114.62
- verdict: PASS

### S03-32gr6 — close the shell for real
- expected: Observe the live Satchel and close any remaining picker, held stack or grid using mapped B only while the shell owns it; already-closed world requires zero input.
- actual: physical shell close: {"ok":true,"presses":0,"settle_frames":0,"why":""}
- events: t=114.62
- verdict: PASS

### S03-32gr7 — select a healthy earned pilot before the next fight
- expected: Read the actual earned roster and use mapped LB only as needed to deploy its living, non-resting member with greatest current HP; verify the actual companion is visible before continuing. Revive recovery remains before this step.
- actual: physical world pilot selection: {"no_input":true,"ok":true,"physics_frames":0,"presses":0,"process_frames":0,"selected_index":1,"why":"healthiest earned creature and actual companion verified; 0 physical LB taps"}
- events: t=114.62
- verdict: PASS
- observation: R8 selected a newly caught 15 HP Bramblebun with one blind cycle while 79 HP and 58 HP allies were available; it fainted during encounter staging. Select by live HP with bounded physical cycling, without health or active-index writes.

### S03-32gr8 — let the party swap finish
- expected: encounter_director.gd::summon_active_creature() is async; the newly-active creature's body needs a few frames to stand up before the next engage is attempted.
- actual: waited 60 physics frames
- events: t=115.62
- verdict: PASS

### S03-32gr9 — wait for the world to own input before attempt 8
- expected: Both branches above converge on a closed shell and a settled party swap; the world must have input back before the next attempt's walk starts.
- actual: input_context=world (wanted world) [true after 0 physics frames]
- events: t=115.62
- verdict: PASS

### S03-32h — walk to a live bramblebun (attempt 8)
- expected: RIG-16: tracks a live bramblebun's own position every frame instead of a fixed cluster-centre coordinate. Arrival also requires the intended living wild creature to own the actual actionable Engage prompt; bounded physical repositioning shares the existing walking budget.
- actual: CONDITION-SATISFIED move_to_entity: authored skip_if verified from live state; no action input issued. predicate={"check":"party_size","min":5.0}; readback=party size 5 (wanted >= 5)
- events: t=115.62
- verdict: PASS
- observation: RIG-F5/GAME-F3. `within: 4.5` is what handed the interact button to a harvest node: prompt_arbiter.gd is 'highest priority first, then NEAREST', engage and gather are both priority 0, and from 4.5 m out a node at half a metre wins every time. Four of the twenty authored village nodes sit inside the practice cluster's own 15 m disc, deliberately -- harvest.json's own _why_gate_b_three_beds put them on 'the practice-path loop the first day already walks' -- so the collision is structural and the fix is to stand at the creature, which is what a player does. 2.0 rather than 1.8: RIG-F5 measured a bramblebun 0.22 m up a slope failing a 1.8 m close_3d check at 1.81 m in 3D, and named 2.0 as the floor.

### S03-32h2 — challenge it (attempt 8)
- expected: After prompt settling, require the actual Engage provider and the exact living wild selected by the preceding verified approach before pressing. A competing gather prompt or another wild is not this encounter.
- actual: CONDITION-SATISFIED interact_with: authored skip_if verified from live state; no action input issued. predicate={"check":"party_size","min":5.0}; readback=party size 5 (wanted >= 5)
- events: t=115.62
- verdict: PASS

### S03-33h — let the fight stage
- expected: the combat camera acquires both combatants
- actual: waited 180 physics frames
- events: t=118.62
- verdict: PASS

### S03-35h — trim it with quick attacks (do not kill it)
- expected: the target is WEAKENED as close to zero HP as this fight will safely bear -- not defeated, a defeated wild cannot be caught -- computed from the fight's own real per-hit damage rather than a guessed hit count or a guessed HP-fraction floor. Once the earned party already has five members, verify that live condition and issue no further attack input.
- actual: CONDITION-SATISFIED chip_to_floor: authored skip_if verified from live state; no action input issued. predicate={"check":"party_size","min":5.0}; readback=party size 5 (wanted >= 5)
- events: t=118.62
- verdict: PASS
- observation: RIG, 2026-08-31: combat_quick x20 KILLED a 106 HP target, so a fixed times: 3 (S02's own proven opener) replaced it -- correctly avoiding a kill, but measured directly (gate-f-run-20260902T053310Z-s03enginefix) landing anywhere from 57% to 75% of max HP depending on the creature's own defence, the FLAT part of hp_factor's curve. A fixed enemy_hp_fraction 0.3 floor (this step's own immediately-prior version) fixed that but was still a guessed number, not a computed one -- called out directly by the owner in this session, 2026-09-02 ('if 5 presses took 70 health you could do another 2 without killing it. why wouldnt you? ... you can surely calculate what needs to be done'). chip_to_floor (operator_harness.gd, added for this) reads the live enemy hp before and after every swing and learns the actual per-hit damage this fight is dealing -- combat_math.gd::rolled_damage()'s only per-swing randomness against a fixed target is its +/-10% variance roll, so one real hit already bounds every later one -- and refuses the next swing only once the largest hit seen so far, scaled by safety_factor (1.25, past the 1.1/0.9=1.222 worst-case roll swing), would not survive. No fixed floor anywhere in this step; it chips exactly as far as the specific creature in front of it allows.

### S03-36h — aim and throw until caught, or out of tries (attempt 8)
- expected: the target is caught, or every allotted throw against it is spent -- a missed throw does not end the fight (catching.json's own cooldown exists so a failed catch can be re-thrown once the wobble clears; combat_manager.gd re-engages the same target on a miss), so this keeps throwing at the SAME chipped-down target instead of treating one orb as the whole attempt.
- actual: CONDITION-SATISFIED throw_until_caught: authored skip_if verified from live state; no action input issued. predicate={"check":"combat_running","equals":false}; readback=combat_running=false (wanted false)
- events: t=118.62
- verdict: PASS
- observation: Owner directive, this session: "we should chip down then throw orbs til we catch." The ladder used to spend exactly one orb per numbered attempt (arm aim, track, throw once, wait) and then move on regardless of outcome -- discarding real, intended-to-be-retried catch chances the moment a single throw missed, even though nothing about the game ends the fight on a miss. throw_until_caught (operator_harness.gd, added for this) re-arms the aim, re-tracks the live target and throws again, up to max_throws times, stopping the instant the party actually grows.

### S03-32h2w — wait for the world to own input before this walk
- expected: no narrative modal or combat is holding locomotion when this walk starts
- actual: input_context=world (wanted world) [true after 0 physics frames]
- events: t=118.62
- verdict: PASS
- observation: T2-GATEF-RUN6 modal-holds-locomotion audit, upgraded per FINDING-CAPSTONE3-S03-CATCH-LOOP-STALL-2026-09-01. This step used to be a one-shot `assert`, diagnostic only: it recorded that the previous fight/catch had not finished unwinding but did not wait for it, so attempts b-j walked straight into 'the interaction arbiter is DISABLED... input_context combat' every time. `wait_until` polls the same check instead of sampling it once, so the next attempt's walk starts only once combat has actually closed and the world owns input again -- CD-3's rule applied to the retry ladder's own seam, not just to a single wait.

### S03-32hr1 — open the Satchel to check for a fainted party member
- expected: game_menu.gd::select() grabs tab_backpack.gd::first_focus() (_buttons[0]) on open, so focus starts at slot 0 (orb_basic) every time.
- actual: inventory opened the shell: context world -> menu_backpack, focus on '' (@Button@68192)
- events: t=118.83
- verdict: PASS

### S03-32hr2 — focus the Revive draught
- expected: T2-GATEF-RUN5/GAME-9/RIG-24 (S03-56d/S03-56f) already proved fixed-offset focus_move fragile in this same bag once acquisitions or spends shift the grid: a positional "3 cells right of slot 0" assumed a fresh save's fill order, and this ladder's own telemetry (gate-f-run-20260901T220548Z-s03fix attempts 3-4) shows it silently landing on the wrong item once earlier presses in this same recovery block had already shifted the grid, so every recovery reported PASS while reviving nothing and party size stayed capped at 2 (FINDING-S03-CATCH-LOOP-REVIVE-SLOT-2026-09-02). focus_item reads the cursor and sends real ui_left/ui_right the way a player reads the bag, regardless of where Revive actually sits.
- actual: focus_item 'revive': cursor on cell 5 after 5 move(s) (from cell 0)
- events: t=119.87
- verdict: PASS

### S03-32hr3 — try to use it on whoever needs it
- expected: tab_backpack.gd::_read_use(): opens the target picker if any party member is fainted, or safely refuses ('Nobody needs reviving.') with no state change otherwise -- live-verified in both directions (tools/gate_f/probe_revive_menu_flow.gd), which is what makes this safe to run every time regardless of party state.
- actual: pressed interact x1 (tap, 1 frames each) on the default device, resolved to JoyBtn:2
- events: t=120.17
- verdict: PASS

### S03-32hr4 — confirm the target if a picker opened
- expected: If the picker opened, focus already sits on the only eligible (fainted) row; this confirms it, clearing `fainted` and consuming one Revive. If nobody was fainted, this press instead picks the Revive stack up off the grid (Godot's ordinary carry gesture) -- non-destructive, corrected by the next step.
- actual: pressed ui_accept x1 (tap, 1 frames each) on the default device, resolved to JoyBtn:0
- events: t=120.38
- verdict: PASS

### S03-32hr5 — put back an accidental pick-up, or close the shell
- expected: Observe the live Satchel and close any remaining picker, held stack or grid using mapped B only while the shell owns it; already-closed world requires zero input.
- actual: physical shell close: {"ok":true,"presses":2,"settle_frames":2,"why":""}
- events: t=120.70
- verdict: PASS

### S03-32hr6 — close the shell for real
- expected: Observe the live Satchel and close any remaining picker, held stack or grid using mapped B only while the shell owns it; already-closed world requires zero input.
- actual: physical shell close: {"ok":true,"presses":0,"settle_frames":0,"why":""}
- events: t=120.70
- verdict: PASS

### S03-32hr7 — select a healthy earned pilot before the next fight
- expected: Read the actual earned roster and use mapped LB only as needed to deploy its living, non-resting member with greatest current HP; verify the actual companion is visible before continuing. Revive recovery remains before this step.
- actual: physical world pilot selection: {"no_input":true,"ok":true,"physics_frames":0,"presses":0,"process_frames":0,"selected_index":1,"why":"healthiest earned creature and actual companion verified; 0 physical LB taps"}
- events: t=120.70
- verdict: PASS
- observation: R8 selected a newly caught 15 HP Bramblebun with one blind cycle while 79 HP and 58 HP allies were available; it fainted during encounter staging. Select by live HP with bounded physical cycling, without health or active-index writes.

### S03-32hr8 — let the party swap finish
- expected: encounter_director.gd::summon_active_creature() is async; the newly-active creature's body needs a few frames to stand up before the next engage is attempted.
- actual: waited 60 physics frames
- events: t=121.70
- verdict: PASS

### S03-32hr9 — wait for the world to own input before attempt 9
- expected: Both branches above converge on a closed shell and a settled party swap; the world must have input back before the next attempt's walk starts.
- actual: input_context=world (wanted world) [true after 0 physics frames]
- events: t=121.70
- verdict: PASS

### S03-32i — walk to a live bramblebun (attempt 9)
- expected: RIG-16: tracks a live bramblebun's own position every frame instead of a fixed cluster-centre coordinate. Arrival also requires the intended living wild creature to own the actual actionable Engage prompt; bounded physical repositioning shares the existing walking budget.
- actual: CONDITION-SATISFIED move_to_entity: authored skip_if verified from live state; no action input issued. predicate={"check":"party_size","min":5.0}; readback=party size 5 (wanted >= 5)
- events: t=121.70
- verdict: PASS
- observation: RIG-F5/GAME-F3. `within: 4.5` is what handed the interact button to a harvest node: prompt_arbiter.gd is 'highest priority first, then NEAREST', engage and gather are both priority 0, and from 4.5 m out a node at half a metre wins every time. Four of the twenty authored village nodes sit inside the practice cluster's own 15 m disc, deliberately -- harvest.json's own _why_gate_b_three_beds put them on 'the practice-path loop the first day already walks' -- so the collision is structural and the fix is to stand at the creature, which is what a player does. 2.0 rather than 1.8: RIG-F5 measured a bramblebun 0.22 m up a slope failing a 1.8 m close_3d check at 1.81 m in 3D, and named 2.0 as the floor.

### S03-32i2 — challenge it (attempt 9)
- expected: After prompt settling, require the actual Engage provider and the exact living wild selected by the preceding verified approach before pressing. A competing gather prompt or another wild is not this encounter.
- actual: CONDITION-SATISFIED interact_with: authored skip_if verified from live state; no action input issued. predicate={"check":"party_size","min":5.0}; readback=party size 5 (wanted >= 5)
- events: t=121.70
- verdict: PASS

### S03-33i — let the fight stage
- expected: the combat camera acquires both combatants
- actual: waited 180 physics frames
- events: t=124.70
- verdict: PASS

### S03-35i — trim it with quick attacks (do not kill it)
- expected: the target is WEAKENED as close to zero HP as this fight will safely bear -- not defeated, a defeated wild cannot be caught -- computed from the fight's own real per-hit damage rather than a guessed hit count or a guessed HP-fraction floor. Once the earned party already has five members, verify that live condition and issue no further attack input.
- actual: CONDITION-SATISFIED chip_to_floor: authored skip_if verified from live state; no action input issued. predicate={"check":"party_size","min":5.0}; readback=party size 5 (wanted >= 5)
- events: t=124.70
- verdict: PASS
- observation: RIG, 2026-08-31: combat_quick x20 KILLED a 106 HP target, so a fixed times: 3 (S02's own proven opener) replaced it -- correctly avoiding a kill, but measured directly (gate-f-run-20260902T053310Z-s03enginefix) landing anywhere from 57% to 75% of max HP depending on the creature's own defence, the FLAT part of hp_factor's curve. A fixed enemy_hp_fraction 0.3 floor (this step's own immediately-prior version) fixed that but was still a guessed number, not a computed one -- called out directly by the owner in this session, 2026-09-02 ('if 5 presses took 70 health you could do another 2 without killing it. why wouldnt you? ... you can surely calculate what needs to be done'). chip_to_floor (operator_harness.gd, added for this) reads the live enemy hp before and after every swing and learns the actual per-hit damage this fight is dealing -- combat_math.gd::rolled_damage()'s only per-swing randomness against a fixed target is its +/-10% variance roll, so one real hit already bounds every later one -- and refuses the next swing only once the largest hit seen so far, scaled by safety_factor (1.25, past the 1.1/0.9=1.222 worst-case roll swing), would not survive. No fixed floor anywhere in this step; it chips exactly as far as the specific creature in front of it allows.

### S03-36i — aim and throw until caught, or out of tries (attempt 9)
- expected: the target is caught, or every allotted throw against it is spent -- a missed throw does not end the fight (catching.json's own cooldown exists so a failed catch can be re-thrown once the wobble clears; combat_manager.gd re-engages the same target on a miss), so this keeps throwing at the SAME chipped-down target instead of treating one orb as the whole attempt.
- actual: CONDITION-SATISFIED throw_until_caught: authored skip_if verified from live state; no action input issued. predicate={"check":"combat_running","equals":false}; readback=combat_running=false (wanted false)
- events: t=124.70
- verdict: PASS
- observation: Owner directive, this session: "we should chip down then throw orbs til we catch." The ladder used to spend exactly one orb per numbered attempt (arm aim, track, throw once, wait) and then move on regardless of outcome -- discarding real, intended-to-be-retried catch chances the moment a single throw missed, even though nothing about the game ends the fight on a miss. throw_until_caught (operator_harness.gd, added for this) re-arms the aim, re-tracks the live target and throws again, up to max_throws times, stopping the instant the party actually grows.

### S03-32i2w — wait for the world to own input before this walk
- expected: no narrative modal or combat is holding locomotion when this walk starts
- actual: input_context=world (wanted world) [true after 0 physics frames]
- events: t=124.70
- verdict: PASS
- observation: T2-GATEF-RUN6 modal-holds-locomotion audit, upgraded per FINDING-CAPSTONE3-S03-CATCH-LOOP-STALL-2026-09-01. This step used to be a one-shot `assert`, diagnostic only: it recorded that the previous fight/catch had not finished unwinding but did not wait for it, so attempts b-j walked straight into 'the interaction arbiter is DISABLED... input_context combat' every time. `wait_until` polls the same check instead of sampling it once, so the next attempt's walk starts only once combat has actually closed and the world owns input again -- CD-3's rule applied to the retry ladder's own seam, not just to a single wait.

### S03-32ir1 — open the Satchel to check for a fainted party member
- expected: game_menu.gd::select() grabs tab_backpack.gd::first_focus() (_buttons[0]) on open, so focus starts at slot 0 (orb_basic) every time.
- actual: inventory opened the shell: context world -> menu_backpack, focus on '' (@Button@68415)
- events: t=125.00
- verdict: PASS

### S03-32ir2 — focus the Revive draught
- expected: T2-GATEF-RUN5/GAME-9/RIG-24 (S03-56d/S03-56f) already proved fixed-offset focus_move fragile in this same bag once acquisitions or spends shift the grid: a positional "3 cells right of slot 0" assumed a fresh save's fill order, and this ladder's own telemetry (gate-f-run-20260901T220548Z-s03fix attempts 3-4) shows it silently landing on the wrong item once earlier presses in this same recovery block had already shifted the grid, so every recovery reported PASS while reviving nothing and party size stayed capped at 2 (FINDING-S03-CATCH-LOOP-REVIVE-SLOT-2026-09-02). focus_item reads the cursor and sends real ui_left/ui_right the way a player reads the bag, regardless of where Revive actually sits.
- actual: focus_item 'revive': cursor on cell 5 after 5 move(s) (from cell 0)
- events: t=126.05
- verdict: PASS

### S03-32ir3 — try to use it on whoever needs it
- expected: tab_backpack.gd::_read_use(): opens the target picker if any party member is fainted, or safely refuses ('Nobody needs reviving.') with no state change otherwise -- live-verified in both directions (tools/gate_f/probe_revive_menu_flow.gd), which is what makes this safe to run every time regardless of party state.
- actual: pressed interact x1 (tap, 1 frames each) on the default device, resolved to JoyBtn:2
- events: t=126.33
- verdict: PASS

### S03-32ir4 — confirm the target if a picker opened
- expected: If the picker opened, focus already sits on the only eligible (fainted) row; this confirms it, clearing `fainted` and consuming one Revive. If nobody was fainted, this press instead picks the Revive stack up off the grid (Godot's ordinary carry gesture) -- non-destructive, corrected by the next step.
- actual: pressed ui_accept x1 (tap, 1 frames each) on the default device, resolved to JoyBtn:0
- events: t=126.55
- verdict: PASS

### S03-32ir5 — put back an accidental pick-up, or close the shell
- expected: Observe the live Satchel and close any remaining picker, held stack or grid using mapped B only while the shell owns it; already-closed world requires zero input.
- actual: physical shell close: {"ok":true,"presses":2,"settle_frames":2,"why":""}
- events: t=126.85
- verdict: PASS

### S03-32ir6 — close the shell for real
- expected: Observe the live Satchel and close any remaining picker, held stack or grid using mapped B only while the shell owns it; already-closed world requires zero input.
- actual: physical shell close: {"ok":true,"presses":0,"settle_frames":0,"why":""}
- events: t=126.85
- verdict: PASS

### S03-32ir7 — select a healthy earned pilot before the next fight
- expected: Read the actual earned roster and use mapped LB only as needed to deploy its living, non-resting member with greatest current HP; verify the actual companion is visible before continuing. Revive recovery remains before this step.
- actual: physical world pilot selection: {"no_input":true,"ok":true,"physics_frames":0,"presses":0,"process_frames":0,"selected_index":1,"why":"healthiest earned creature and actual companion verified; 0 physical LB taps"}
- events: t=126.85
- verdict: PASS
- observation: R8 selected a newly caught 15 HP Bramblebun with one blind cycle while 79 HP and 58 HP allies were available; it fainted during encounter staging. Select by live HP with bounded physical cycling, without health or active-index writes.

### S03-32ir8 — let the party swap finish
- expected: encounter_director.gd::summon_active_creature() is async; the newly-active creature's body needs a few frames to stand up before the next engage is attempted.
- actual: waited 60 physics frames
- events: t=127.85
- verdict: PASS

### S03-32ir9 — wait for the world to own input before attempt 10
- expected: Both branches above converge on a closed shell and a settled party swap; the world must have input back before the next attempt's walk starts.
- actual: input_context=world (wanted world) [true after 0 physics frames]
- events: t=127.85
- verdict: PASS

### S03-32j — walk to a live bramblebun (attempt 10)
- expected: RIG-16: tracks a live bramblebun's own position every frame instead of a fixed cluster-centre coordinate. Arrival also requires the intended living wild creature to own the actual actionable Engage prompt; bounded physical repositioning shares the existing walking budget.
- actual: CONDITION-SATISFIED move_to_entity: authored skip_if verified from live state; no action input issued. predicate={"check":"party_size","min":5.0}; readback=party size 5 (wanted >= 5)
- events: t=127.85
- verdict: PASS
- observation: RIG-F5/GAME-F3. `within: 4.5` is what handed the interact button to a harvest node: prompt_arbiter.gd is 'highest priority first, then NEAREST', engage and gather are both priority 0, and from 4.5 m out a node at half a metre wins every time. Four of the twenty authored village nodes sit inside the practice cluster's own 15 m disc, deliberately -- harvest.json's own _why_gate_b_three_beds put them on 'the practice-path loop the first day already walks' -- so the collision is structural and the fix is to stand at the creature, which is what a player does. 2.0 rather than 1.8: RIG-F5 measured a bramblebun 0.22 m up a slope failing a 1.8 m close_3d check at 1.81 m in 3D, and named 2.0 as the floor.

### S03-32j2 — challenge it (attempt 10)
- expected: After prompt settling, require the actual Engage provider and the exact living wild selected by the preceding verified approach before pressing. A competing gather prompt or another wild is not this encounter.
- actual: CONDITION-SATISFIED interact_with: authored skip_if verified from live state; no action input issued. predicate={"check":"party_size","min":5.0}; readback=party size 5 (wanted >= 5)
- events: t=127.85
- verdict: PASS

### S03-33j — let the fight stage
- expected: the combat camera acquires both combatants
- actual: waited 180 physics frames
- events: t=130.85
- verdict: PASS

### S03-35j — trim it with quick attacks (do not kill it)
- expected: the target is WEAKENED as close to zero HP as this fight will safely bear -- not defeated, a defeated wild cannot be caught -- computed from the fight's own real per-hit damage rather than a guessed hit count or a guessed HP-fraction floor. Once the earned party already has five members, verify that live condition and issue no further attack input.
- actual: CONDITION-SATISFIED chip_to_floor: authored skip_if verified from live state; no action input issued. predicate={"check":"party_size","min":5.0}; readback=party size 5 (wanted >= 5)
- events: t=130.85
- verdict: PASS
- observation: RIG, 2026-08-31: combat_quick x20 KILLED a 106 HP target, so a fixed times: 3 (S02's own proven opener) replaced it -- correctly avoiding a kill, but measured directly (gate-f-run-20260902T053310Z-s03enginefix) landing anywhere from 57% to 75% of max HP depending on the creature's own defence, the FLAT part of hp_factor's curve. A fixed enemy_hp_fraction 0.3 floor (this step's own immediately-prior version) fixed that but was still a guessed number, not a computed one -- called out directly by the owner in this session, 2026-09-02 ('if 5 presses took 70 health you could do another 2 without killing it. why wouldnt you? ... you can surely calculate what needs to be done'). chip_to_floor (operator_harness.gd, added for this) reads the live enemy hp before and after every swing and learns the actual per-hit damage this fight is dealing -- combat_math.gd::rolled_damage()'s only per-swing randomness against a fixed target is its +/-10% variance roll, so one real hit already bounds every later one -- and refuses the next swing only once the largest hit seen so far, scaled by safety_factor (1.25, past the 1.1/0.9=1.222 worst-case roll swing), would not survive. No fixed floor anywhere in this step; it chips exactly as far as the specific creature in front of it allows.

### S03-36j — aim and throw until caught, or out of tries (attempt 10)
- expected: the target is caught, or every allotted throw against it is spent -- a missed throw does not end the fight (catching.json's own cooldown exists so a failed catch can be re-thrown once the wobble clears; combat_manager.gd re-engages the same target on a miss), so this keeps throwing at the SAME chipped-down target instead of treating one orb as the whole attempt.
- actual: CONDITION-SATISFIED throw_until_caught: authored skip_if verified from live state; no action input issued. predicate={"check":"combat_running","equals":false}; readback=combat_running=false (wanted false)
- events: t=130.85
- verdict: PASS
- observation: Owner directive, this session: "we should chip down then throw orbs til we catch." The ladder used to spend exactly one orb per numbered attempt (arm aim, track, throw once, wait) and then move on regardless of outcome -- discarding real, intended-to-be-retried catch chances the moment a single throw missed, even though nothing about the game ends the fight on a miss. throw_until_caught (operator_harness.gd, added for this) re-arms the aim, re-tracks the live target and throws again, up to max_throws times, stopping the instant the party actually grows.

### S03-39 — the team is five
- expected: data/config/tournament.json entry.min_party_size is 5 and objectives.json's tournament_build_team says 'Build your full team of five for the village tournament.' The old `min: 3` predates FIRST-HOUR-FUN-REBUILD. Five is also the hard ownership cap (autoload/party.gd MAX_CREATURES), so this is the complete permanent team, never storage or a reserve.
- actual: party size 5 (wanted >= 5)
- events: t=130.85
- verdict: PASS
- observation: RIG-15: equals -> min. Catching is probabilistic; a script's single throw can miss on a real, non-buggy roll, and a team that ends up BIGGER than the milestone is still a team that reached it. `equals` failed both directions for no reason a real player's outcome should fail on.

### S03-39a — T2-BUILDPLACE: a SEVERE, previously-undiscovered GAME defect
- expected: a note event carries this observation into events.jsonl
- actual: FINDING-T2-BUILDPLACE-2026-08-30.md, round 2 (the loud flag): scripts/combat/encounter_director.gd::interaction_offer() (line ~1037) returns a priority-100, distance-0 "<ally> is out of the fight." statement UNCONDITIONALLY whenever the tracked `_ally` has fainted -- with NO proximity gate, checked BEFORE _engageable(). prompt_arbiter.gd ranks priority before distance always ("distance at all and still has to beat a creature nine metres away"), so this one line outranks EVERY OTHER INTERACTION IN THE WORLD -- not just re-engaging a wild creature, but every village NPC greeting, every harvest node, every creature bed's own rest prompt -- for as long as `_ally` stays non-null and fainted, which (per RIG-13's own finding) only a fresh save/load clears, never gameplay on its own. Live-confirmed this run: after the catch loop's own real, non-buggy, RIG-15-acknowledged possible outcome (a starter fainting on a real throw), S03-53's interact_with(Mira) FAILed with the live prompt reading exactly "Ripplet is out of the fight." -- not a wrong-prompt bug at Mira, a WORLD-WIDE interaction lockout. A real player whose only starter faints during these exact opening catch attempts cannot catch a replacement (catching is ALSO interaction_offer()-gated and _engageable() is checked AFTER the blanket clause), cannot talk to any NPC, cannot gather, and cannot reach a creature bed's rest prompt -- for the rest of that play session, with no in-game affordance suggesting a save+reload is the only way out. This reads as a genuine, previously-undocumented SOFT-LOCK risk in the opening tutorial and is reported as such, loudly, per this lane's own brief. Out of this lane's ownership (scripts/combat/**, actively edited by T3-TYPECHART) and NOT fixed here. The workaround below is rig-side only, so this validation run's own exit save is not itself proof the game is safe for a real player.
- events: t=130.85
- verdict: PASS

### S03-39b — select a healthy earned pilot before the next fight
- expected: Read the actual earned roster and use mapped LB only as needed to deploy its living, non-resting member with greatest current HP; verify the actual companion is visible before continuing. Revive recovery remains before this step.
- actual: physical world pilot selection: {"no_input":true,"ok":true,"physics_frames":0,"presses":0,"process_frames":0,"selected_index":1,"why":"healthiest earned creature and actual companion verified; 0 physical LB taps"}
- events: t=130.85
- verdict: PASS
- observation: R8 selected a newly caught 15 HP Bramblebun with one blind cycle while 79 HP and 58 HP allies were available; it fainted during encounter staging. Select by live HP with bounded physical cycling, without health or active-index writes.

### S03-39c — let the party swap finish
- expected: encounter_director.gd::summon_active_creature() is async (awaits _spawn_ally_body's ground placement); the newly-active creature's body needs a few frames to actually stand up before Bryn's challenge prompt is read.
- actual: waited 60 physics frames
- events: t=131.85
- verdict: PASS

### S03-39d — T2-BUILDPLACE ROUND 3: party_cycle alone is not enough when the party never grew past one
- expected: a note event carries this observation into events.jsonl
- actual: Two full replays of this round's own fix both left party_size==1 at S03-39 with the sole starter fainted -- the catch loop's own real, RIG-15-acknowledged possible outcome (10 real throws, all missed or the wild creature's own retaliation across 10 fights wore the one deployed attacker down), reproduced identically both times against this segment's own deterministic scripted input against S02-exit's seed. party.gd::cycle_active() (S03-39b) cannot help here: there is no second party member to cycle to. data/items/items.json's `revive` item (creature_instance.gd::revive(), granted x2 in the opening satchel per tools/gate_f/probe_tool_equip_sequence.gd's own read of a real S02-exit.json) is the game's own designed answer to a fainted creature outside a bed -- 'the only thing that raises a creature back up once it's fainted' (its own blurb). S03-39e..S03-39j below use it through the real Satchel UI (scripts/ui/tab_backpack.gd), the same way a real player would, live-verified end to end (tools/gate_f/probe_revive_menu_flow.gd) in BOTH directions before being added here: pressing `interact` on the Revive slot when nobody is fainted safely refuses ('Nobody needs reviving.', no state change) and a stray `ui_accept` after it merely PICKS UP the stack (Godot's ordinary grid pick-up-then-place gesture) rather than doing anything destructive; pressing `interact` when the sole party member IS fainted opens the target picker with focus already on the only eligible row, and `ui_accept` confirms it, clearing `fainted` on the SAME creature instance `_ally` points to (immediate, no dismiss/resummon needed) and consuming one Revive. The probe also confirmed the exact unconditional 4-press close sequence below (`interact`, `ui_accept`, `menu_cancel`, `menu_cancel`) reaches a closed shell in BOTH branches with no leftover held/targeting state, which is what makes this safe to run every time regardless of party state, with no conditional the step-script format cannot express anyway.
- events: t=131.85
- verdict: PASS

### S03-39e — open the Satchel
- expected: game_menu.gd::select() always grabs tab_backpack.gd::first_focus() (_buttons[0]) on open, so focus starts at slot 0 (orb_basic) every time regardless of any earlier open/close -- confirmed live, this does not disturb S03-56d's own later 'focus_move right x4' assumption.
- actual: inventory opened the shell: context world -> menu_backpack, focus on '' (@Button@68638)
- events: t=132.12
- verdict: PASS

### S03-39f — focus the Revive draught
- expected: the satchel cursor lands on whichever cell currently holds the Revive stack
- actual: focus_item 'revive': cursor on cell 5 after 5 move(s) (from cell 0)
- events: t=133.13
- verdict: PASS
- observation: HARNESS-HYGIENE-0903. Was `focus_move {right, times: 3}`, a positional guess that Revive sits exactly 3 cells right of slot 0 in the satchel's acquisition-order fill -- the same bug class already fixed for the berries-focus step below (see S03-56d's own note and FINDING-S03-105-HOME-MATERIALS-ROOT-CAUSE-2026-09-02.md) and for equip_tool's hotbar lookup. `focus_item` reads the live cursor and `satchel_slot_of('revive')` instead of assuming a fill order that upstream catching/spending can move.

### S03-39g — try to use it on whoever needs it
- expected: tab_backpack.gd::_read_use() (USE_ACTION = "interact"): opens the target picker if any party member is fainted (`_any_eligible_target`), or refuses harmlessly ('Nobody needs reviving.') with no state change otherwise. Either way this press alone changes nothing that could be lost.
- actual: pressed interact x1 (tap, 1 frames each) on the default device, resolved to JoyBtn:2
- events: t=133.43
- verdict: PASS

### S03-39h — confirm the target if a picker opened
- expected: If the picker opened, focus already sits on the only eligible (fainted) row (_first_eligible_target_row()), and this is the SAME button the grid's own pick-up-then-place uses (Button.pressed) to confirm it -- `_on_target_row()` calls creature.revive(0.5) and consumes one Revive. If the picker did NOT open (nobody fainted), this press instead picks the Revive stack up off the grid (Godot's ordinary carry gesture) -- live-confirmed non-destructive, corrected by S03-39i below.
- actual: pressed ui_accept x1 (tap, 1 frames each) on the default device, resolved to JoyBtn:0
- events: t=133.65
- verdict: PASS

### S03-39i — put back an accidental pick-up, or close the shell
- expected: Observe the live Satchel and close any remaining picker, held stack or grid using mapped B only while the shell owns it; already-closed world requires zero input.
- actual: physical shell close: {"ok":true,"presses":2,"settle_frames":2,"why":""}
- events: t=133.95
- verdict: PASS

### S03-39j — close the shell for real
- expected: Observe the live Satchel and close any remaining picker, held stack or grid using mapped B only while the shell owns it; already-closed world requires zero input.
- actual: physical shell close: {"ok":true,"presses":0,"settle_frames":0,"why":""}
- events: t=133.95
- verdict: PASS

### S03-39k — the world owns input again
- expected: Both branches of S03-39e..S03-39j converge on a closed shell; the world must have input back before Bryn's challenge can be attempted.
- actual: input_context=world (wanted world)
- events: t=133.95
- verdict: PASS

### S03p1-phase-save-00 — open the pause shell
- expected: Explicit phase-added production save seam, derived from S03-265; handoff artifact S03p1-exit.json.
- actual: map opened the shell: context world -> menu_map, focus on '' (@Control@68802)
- events: t=134.45
- verdict: PASS
- observation: Opened via the map shortcut rather than a bare game_menu press. The pause shell REOPENS ON THE LAST TAB USED, so a fixed press count from an assumed backpack start lands on the wrong tab in any segment that opens the menu more than once. Measured in S03: the shell reopened on `menu_map` and 4x menu_tab_right landed on `menu_settings` instead of `menu_build`, failing nine consecutive build steps. `map` is one of only two tabs with a shortcut in data/config/menu.json, so it is the only deterministic starting point available.

### S03p1-phase-save-01 — cycle right to the Save tab
- expected: Explicit phase-added production save seam, derived from S03-266; handoff artifact S03p1-exit.json.
- actual: selected save after 4 controller RB presses; route ["menu_map", "menu_quest_log", "menu_build", "menu_players"]
- events: t=135.20
- verdict: PASS
- observation: 2026-09-15: supersedes the historical fixed-count navigation. Observe real controller tab transitions until the named tab, accounting for Players and conditional Skills visibility; no tab index or press count is assumed.

### S03p1-phase-save-02 — the Save tab is up
- expected: Explicit phase-added production save seam, derived from S03-267; handoff artifact S03p1-exit.json.
- actual: input_context=menu_save (wanted menu_save)
- events: t=135.20
- verdict: PASS

### S03p1-phase-save-03 — move focus to slot 4's Save button
- expected: Explicit phase-added production save seam, derived from S03-268; handoff artifact S03p1-exit.json.
- actual: 4 x ui_down moved focus 'Save' (@Button@68853) -> 'Save' (@Button@68873)
- events: t=136.50
- verdict: PASS

### S03p1-phase-save-04 — press Save
- expected: Explicit phase-added production save seam, derived from S03-269; handoff artifact S03p1-exit.json.
- actual: pressed ui_accept x1 (tap, 1 frames each) on the default device, resolved to JoyBtn:0 [unchecked against input_contexts.json: input_context 'menu_save' is not in input_contexts.json]
- events: t=137.23
- verdict: PASS

### S03p1-phase-save-05 — give the write a moment
- expected: Explicit phase-added production save seam, derived from S03-270; handoff artifact S03p1-exit.json.
- actual: waited 120 physics frames
- events: t=139.23
- verdict: PASS

### S03p1-phase-save-06 — copy slot 4 out into the run directory
- expected: Explicit phase-added production save seam, derived from S03-271; handoff artifact S03p1-exit.json.
- actual: slot 4 copied to saves/S03p1-exit.json (4193310 bytes)
- events: t=139.23
- verdict: PASS

### S03p1-phase-save-07 — close the shell
- expected: Explicit phase-added production save seam, derived from S03-272; handoff artifact S03p1-exit.json.
- actual: physical shell close: {"ok":true,"presses":1,"settle_frames":1,"why":""}
- events: t=139.50
- verdict: PASS
