# Guardian (legendary) offer layout (X03; unblocks the F14 offer visual verdict)

`tools/capture_water_guardian_confirm.gd` (Tidewake lane's production capture:
the real Water world, the Deep Watcher's chamber prompt, the real game menu),
1280x720, opengl3 under xvfb.

- `before_*.jpg`: main fe07d0d2 (the same tool's committed frames from batch 4).
- `after_*.jpg`: ralph/ui-guardian-offer-b, which also carries
  ralph/ui-red-rule-b and ralph/ui-creature-preview-b.
- `_sheet_before_after.jpg`: the offer, the decline step, and the pending
  decline result, side by side.

Staging: as in the tool header (belt of four Mosshells, player placed at the
chamber prompt, one save refusal to hold the pending-decline wording).
