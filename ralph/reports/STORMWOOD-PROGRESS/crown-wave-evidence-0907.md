# Preserved Crown integration wave

Base main: `4562268dee581d2f2ce167a4670bbf752f139310`. Branch: `ralph/stormwood-dynamo-0907`. This wave is not a complete chapter or an accepted visual pass.

Root independently ran `smoke_stormwood_arches.gd` against the actual normal BuildPlacer, Game inventory, ledger, Session dispatcher, CharacterBody3D and Area3D fixture. It proves exact Crown-grade and ordinary costs, raised Still Grove → Crown → return travel, dismantling and blocked-arrival refusal. Log: `D:/CodexWork/stormwood-built-crown-journey-root.log`, PASS, exit 0. Five ObjectDB/two resource shutdown warnings remain. This is a single-host flat fixture, not an ENet or walked forest proof.

The new heartstone fixture proves early refusal, valid shared chapter dispatch, immediate disabled repeat interaction and saved Crown/Rootgate flags: `D:/CodexWork/stormwood-heartstone-final-root.log`, ten assertions, zero failures, exit 0. Its prerequisites are explicitly fixture state; it does not prove Wen, the guardian or the route. The initial fixture lacked the world's simulation flag; a production boolean inference error and delayed prompt refresh were also corrected before the successful run.

Root TM validation: 36 tests, 3,040 assertions, zero failures; actual pickup smoke passes. Four authored electric discs now reach the ordinary item and teaching systems. Broader build/save/Stormwood selection initially had 297 tests with one stale catalogue requirement (the new hand-built arch has no generic mesh field); the catalogue recognizes its dedicated piece now. Focused replacement run: 31 tests, 309 assertions, zero failures in `stormwood-crown-resume-focused.log`. Full code CI is required before merge because this wave touches shared building/world state. The regions CI job now also runs the three Stormwood arch/heartstone/pickup smokes.

Visual evidence failed before frames: `tools/survey.sh --stormwood` on the GTX 1060 3GB exits 139 with native allocation failure while loading character/creature models. Logs: `D:/CodexWork/stormwood-crown-survey/survey.log` and `D:/CodexWork/stormwood-crown-survey-resume/survey.log`. The second attempt followed the parse fix and ran with no competing Godot process; it still failed. No third identical capture will be attempted. There are no new frames for a blind judge and no visual acceptance credit. Memory investigation is a separate bounded task.

Implemented but not yet accepted: physical authored footings, normal build menu and ghost, persisted stable pair UIDs with atomic unlink, three-road cap including Crown, correct Crown-grade recipe/refund, destination player/companion capsule clearance, host simulation-shell arch collision, Crown heartstone, all-phase Glass Sink strike eligibility. Save/ledger tests prove serialized claims and metadata; actual two-process construction/travel/late join is still required. Normal build costs retain the existing character-local inventory preflight/settlement design, without a new inventory escrow guarantee.

Dynamo files on this branch provide tested phase rules, kitbashed arena components and combat adapters only. They are not mounted as a playable finale. Six named encounters remain unmounted; the Crown guardian does not yet gate Wen. Client-originated wild/trainer combat inherits a broader limitation in the existing director. The next integration must address host-owned encounter starts for the Dynamo and named progression, not claim that local combat supplies multiplayer acceptance.
