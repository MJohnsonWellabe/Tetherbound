# Stormwood foundation construction evidence — 2026-09-07

This is branch evidence, not a closed visual gate or a completed chapter. Phase 1 PR #70 merged as 84125fcd008d93c0fefdfff51a2752b94b4faada after every actual code job passed in CI run 34070027359, including four unit and five multiplayer shards. Root fetched origin and verified reviewed head 7a07d889b1b1f5a339ca1b915a54148e31965298 is an ancestor of main. This dependent foundation work branches from that merged main.

The physical tree foundation now contains Outer Works, a four-turn internal ascent, a core ring and crown chamber. A real CharacterBody3D with planar steering and gravity walked to the core in both visual-world and simulation-shell geometry. Root inspected tests/smoke_stormheart_ascent.log: both reached fraction 1.000, grounded, within 3.08 m of the core anchor; all 26 assertions passed. This tests a representative collision body, not the complete production-player/controller path or any arena mechanics. The initial complete core disk trapped the ascending body below it; a stairwell opening and overlapping terminal landing corrected that. A separate premature fixture exit was corrected without loosening thresholds. Later platform fascia/material edits still require revalidation.

The v2 and v4 independent reviews both reject visual acceptance. Both reports are retained. The source twisted-tree leaf is crimson; tint multiplication could not make it green. The revised material now uses the installed Meadows derived green leaf texture, preserving its existing provenance. v4's review recognizes the natural-green palette but still rejects route, landmark and inhabited-world composition. Fifteen civilian prefab footprints have since been authored at the Rodfolk settlements and shelters, using the existing prefab collision/door/interior system. Services and inhabitants are not yet bound, so these are not completed camps.

The 119,663-placement experimental bake failed its actual Forward+ capture with LLVM out of memory before any frame. It is not evidence of a viable forest. The revised 33,899-placement bake (108 regions, 1,248,658 bytes) produced all eight v4 frames with exit 0 on Vulkan / Forward+ / GTX 1060 3GB. Root verified the log header and READY/capture lines. Its boot reported 680 batches, 11,209 harvest nodes and 11,274 collision solids; these are rendering/runtime counters, not the contract's authored harvest-site census. A sampled GPU query during the successful capture reported 919 MiB used out of 3,072 MiB; that is not a peak-memory or frame-time benchmark.

Preserve shots/stormwood-foundation-baseline and tree-v1 through tree-v5 (v3 contains its failure log). The v5 Forward+ capture reached READY with 15 prefab structures and all eight frames, exit 0; the revised bake has 33,773 placements in 108 regions. Building footprints now exclude baked plants and tree colliders, and two authored sightlines clear canopy along the final approach. Root read the actual renderer header and capture log. A fresh independent v5 judge still rejects both bars: the landmark reads as a thin spiral tower and the forest lacks distinct inhabited composition. This is not a passed visual phase. A bounded asset-transform investigation is separate from further composition work.

The post-fascia physical ascent rerun passes all 26 assertions. Root also ran the focused catalogue/Surge policy checks: 15 tests, 4,215 assertions, zero failures. These do not prove runtime encounters or weather. The settlement audit finds that reused authored doors retain local transient state; shared door-state behavior remains unproven. The local full unit process begun at 23:22 ceased to exist without a final summary after the execution environment changed; do not call it passed or revive it from a stale handle. All four unit CI shards passed on the PR #70 head; this next wave requires its own CI.

Post-settlement two-peer regression: net-run-local-2021033 passed the strict Stormwood entry/return, separate host shell, 108-region ready, positive client fog growth and unchanged host map assertions. Root observed coordinator exit 0 and SUMMARY.md confirms both children exited normally. This verifies the increased world-build workload did not break that tested crossing; it does not prove door replication or co-op combat.
