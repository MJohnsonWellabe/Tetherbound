# T1-PERF findings — 2026-08-30

**Superseded by `ralph/reports/handover-T1-PERF-2026-08-30.md` and
`docs/PERFORMANCE_BUDGET.md`, both finished later the same session.** This
file was a running scratch draft from before the coordinator redirected the
lane's priority twice (first to the Warrens den's new shadow light, then to
the Meadows Hall's draw-call overage after `T1-HALL-REBUILD` stood down
without resolving it) — kept for its own real content below (the
collision-streaming verification table is accurate and complete) but its
"no edits to stronghold.gd" ownership note is **stale**: the coordinator
explicitly reassigned the Hall's draw-call fix to this lane mid-session.
Read the handover for what actually shipped.

Branch `ralph/T1-PERF`, off `origin/main`. Mission: nobody owns performance,
`ralph/MEADOWS_EXIT_CRITERION.md` J4/K make it pass/fail, and the owner's
2026-08-30 playtest (`ralph/OWNER_PLAYTEST_2026-08-30.md` OP-0830-6) confirms
it is still failing on the real device — unchanged since 2026-08-21. This is
the first lane actually pointed at measuring it.

Tools/environment: Godot 4.7.stable (matching `docs/decisions/D01`), this
session's own container (no ROG Ally hardware, no GPU — Mesa llvmpipe
software rasteriser under `xvfb-run --rendering-driver opengl3`). Every
number below is structural (draw calls, primitives, objects, light-
reachability counts) — hardware-independent in shape, never a frame time.

## Method

- `tools/perf_render_stats.gd` (existing, unmodified) — cross-checked against
  `HALL_DESIGN_2026-08-30.md`'s own baseline.
- `tools/perf_site_survey.gd` (new this session) — all seven authored
  locations (village + bands 1-5 + stronghold/Hall), day AND night, plus the
  Burrow Warrens den (the one interior chamber T1-LIGHT gave a real
  shadow-casting light this session) via a small `ABSOLUTE_VIEWS` addition.
  Reports draw calls, primitives, objects, and light-reachability (total +
  shadow-casting) per location per time.
- `tools/perf_scatter_density.gd` (new this session) — scatter density per
  hectare, whole-band bounding-box average and local (60m radius) at each
  sampled point.
- `tools/perf_profile.gd` (existing, unmodified) — CPU-side cost profile,
  used to verify the T3-INSTALL collision-streaming config levers.

Raw logs committed alongside this file:
- `perf_profile_baseline.log` / `.json`
- `perf_site_survey.log` / `.json`
- `perf_scatter_density.log`

## Headline: worst offenders, ranked

Not the seven-location matrix originally planned — see the handover for why
(a mid-session terrain regen + scatter re-bake superseded the partial
results, and priority then shifted to the Hall's draw-call overage, a
higher-value use of this session's remaining time per the coordinator).
What IS ranked, from actual measurements this session:

1. **`band1_open`** (open field) — 5712–5929 draw calls, the highest number
   measured anywhere this session, though pre-dating the newest re-bake.
2. **`hall_approach`** (the Hall, looking AT it — `stronghold_approach`'s
   old yaw looks away and undercounts) — 2743 draw calls pre-fix, 2665 after
   this lane's merlon-parapet fix. The chapter's climax, and the one this
   session spent the most effort on; see `docs/PERFORMANCE_BUDGET.md` §0.5.
3. **`band3_river`** — 2516–2649.
4. **`village_high`** — 1815–2011.
5. **`band2_stone`**, **`band4_ironwood`** — under 1500 each.

`band5_approach`, the Warrens den, and a post-rebake `stronghold_approach`
were never measured — genuinely blank, not estimated.

## The T3-INSTALL collision-streaming levers: verified wired

`performance.json`'s `collision_stream_radius_m` / `_interval_s` / `_cell_m`
were re-tested by directly changing each value and re-measuring with
`tools/perf_profile.gd`, not by reading the code and trusting the comment:

| config change | measured effect |
|---|---|
| `collision_stream_radius_m` 100→30 | resident colliders: village 151→0, band1 410→2 |
| `collision_stream_interval_s` 0.5→2.0 | profiler's own reported sweep rate: "2.0 Hz"→"0.5 Hz" |
| `collision_stream_cell_m` 32→16 | sweep cost stayed low, consistent with the smaller radius (not independently isolated from the radius change) |

All three restored to shipped defaults (100.0 / 0.5 / 32.0) immediately after.
**Verdict: correctly wired.** This item, at least, is not part of OP-0830-6.

## The Warrens den's new shadow-casting light

**Not measured — the tool was built (`perf_site_survey.gd`'s
`ABSOLUTE_VIEWS`) but the run never reached it** before priority shifted to
the Hall. The tooling is in place and correct (verified via
`tools/_probe_warrens_markers.gd`'s coordinates); a follow-up run with
`--sites=village_high` (any cheap exterior site — `ABSOLUTE_VIEWS` always
runs regardless of `--sites`) would produce it in one exterior-site's worth
of time, not the full seven.

## Scatter density

See `docs/PERFORMANCE_BUDGET.md` §5 for the full table. Headline: whole-band
density is fairly flat (78–257/ha) but LOCAL density at the exact points the
render tools sample swings 17.7–1168.9/ha — a 66x range in the same
corridor. No single density number can budget this system; draw calls
(§3) are the real lever, because Compatibility's draw-call counter tracks
MultiMesh batches, not instances, so density inside an existing batch is
nearly free in draw-call terms.

## What this session touched, and what it did not

The original ownership boundary (no edits to `stronghold.gd`/`landmark.gd`,
terrain/grass/scatter configs, UI, creature meshes) held until the
coordinator explicitly reassigned the Hall's draw-call overage to this lane
— see the handover for the one `stronghold.gd` edit that resulted
(`_build_keep_parapets()`, verified 2743→2665 draw calls, smoke-tested
green). Nothing else outside this lane's own tools/docs was touched.

### Routed findings

- **`band1_open`'s draw-call number (5712–5929, this session's highest)
  needs a post-rebake reconfirmation.** Whichever lane owns the open-field
  scatter should re-run `perf_render_stats.gd --views=band1_open` after any
  future density change, since it is the number `docs/PERFORMANCE_BUDGET.md`
  §0.5 uses as the corridor's worst-case comparison for the Hall budget.
- **`ralph/T1-HALL-3`** (rebuilding the Hall's fortress geometry per
  `JUDGE-5-2026-08-30.md`): build to ≤ 4000 draw calls at `hall_approach`;
  full reasoning in `docs/PERFORMANCE_BUDGET.md` §0.5.
