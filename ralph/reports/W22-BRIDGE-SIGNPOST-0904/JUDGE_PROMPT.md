# What the code-blind judge was given

Two contact sheets and nothing else about this lane: `_sheet_signpost_ab.png` and
`_sheet_bridge_ab.png` (each row is the same stand rendered twice, labelled A and B,
with no statement of which is older), `docs/reference/`, the owner's board
`docs/art/reference/18_Signpost_Bridge_Modular_Props.png`, and
`.claude/skills/visual-judge/SKILL.md`. Questions asked:

1. For each signpost row, which of A/B is closer to board 18's "Directional (Multi)"
   panel, and why; are the destination names legible at 1280×800 in both?
2. For each bridge deck row, which of A/B is closer to board 18's "Bridge Plank & Rail"
   panel, and why?
3. For the bridge approach rows: does the crossing read as HELD by a faction from the
   approach — barricade, banners, a guard, a light — in A, in B, in neither?
4. What is still wrong in the better of the two, ranked.

The judge was not told which sheet column was the new work, what had changed, or what
this lane hoped to hear.
