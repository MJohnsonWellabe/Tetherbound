# Water runtime wave 3 — in progress

Delivery remains `ralph/water-foundation-0906` in `D:\Tetherbound-Water`. The owner will merge later; PR 69 stays a draft. The last committed/pushed starting point was `934d6b34a279266afbeec7f329973fc0eada2b23`. This report does not close a phase or change the fixed 03:55:38 UTC score checkpoint.

## Proven subpaths, not chapter acceptance

Root ran `tests/smoke_water_alpha_runtime.gd` against the actual Water scene. Initial engagement and incoming damage passed 12 checks. The extended victory run passed 16 checks, exit 0, without script errors: a level-49 owned Mosshell fixture was summoned normally; the player position was explicitly teleported to the encounter; normal quick-attack input defeated the actual level-49 Aquaryn through the three HP phases, journaled `water_aquaryn_resolved`, and granted the participating character `water_swim_stone_earned`. Enemy HP and the Stone were not injected. Final enemy HP was 0, ally HP 359.6578. Logs: `%TEMP%\water-alpha-runtime-smoke.log` and `%TEMP%\water-alpha-victory-smoke.log`. This is not a first-shore journey, a surface-channel demonstration, a catch, or a multiplayer victory.

Root independently read the new reward and manager-begin tests and ran the combined suite: 13 tests, 147 runner assertions, zero failures, clean engine output. The begin test also executes 30 child-process assertions covering preserved shared-enemy position/arena/target and unchanged ordinary combat staging. `%TEMP%\water-alpha-integration-unit.log` records this run. Reward tests use the actual world file writer and reconstruct stable-character entitlements, including failed-journal rollback and reconnect under a different peer ID.

## Failures which change the next action

The four-shard full regression snapshot at `934d6b34a` completed shard 1 with 639 tests, 2,438,414 assertions, zero assertion failures. Its raw log contains 200 ERROR/SCRIPT ERROR lines from off-tree fixture paths; this is not a clean full-suite pass. Shard 2 terminated with native allocation failures and an out-of-bounds crash at `interaction_arbiter.gd::_providers_getter`, invoked by `test_interaction_arbiter_register_perf`. Shards 3/4 did not finish. Evidence is `%TEMP%\water-full-suite-20260907-021927\`. Process handles and OS process listings were checked: these processes are no longer running. Do not repeatedly restart the same memory-heavy concurrent workload.

The first Alpha cross-realm two-peer run also terminated during Meadows vegetation construction with native allocation failures in `_spawn_harvest_point`; it never proved an Alpha network interaction. `%TEMP%\water-alpha-net.log` and its named peer logs preserve the failure. The strategy changed to a separate two-Water-player test with host on First Shore and client at Tidal Cradle, leaving the heavier cross-realm variant available for a later isolated run.

The first two-island run completed with four failed encounter assertions. Client deployment occurred at the host's stale First Shore trainer position before the fixture's cross-island landing acknowledgement. It therefore did not prove host-authorized engagement. The harness previously counted unrelated incoming damage despite failed engagement; that false-positive check was corrected to require a bound Alpha encounter and abort on engagement failure. A later run must prove this correction. Logs: `%TEMP%\water-alpha-net-islands.log`, run ID `net-run-local-2234132`. Disconnect also exposed three Water paths calling `get_unique_id()` after peer teardown; they now use the existing Session fallback identity. No repeat run is counted before its result is inspected.

The corrected two-island run subsequently passed all 21 checks, exit 0, and root inspected both peer logs with no ERROR/SCRIPT ERROR matches. Evidence: `%TEMP%\water-alpha-net-islands2.log`, run ID `net-run-local-2110146`. The fixture now lets the cross-island trainer landing acknowledgement settle before normal deployment. Host and client retain different islands more than 1 km apart; only the client joins the host's single Alpha record, receives its pose/HP, and takes a real host-decided strike. A forged catch-completion request does not resolve the encounter or grant Stone. Session exit is clean. This proves the corrected encounter assertions and disconnect paths; it does not prove cross-realm residency, catch animation completion, or multiplayer victory/rewards.

## Implemented but unproven / pending

Catch completion now waits for the Alpha authority's confirmation instead of allowing the local wobble animation to award a rejected catch. Shared-enemy combat entry preserves realm-owned movement and prevents local arena confinement. Other participants receive the resolved encounter outcome rather than remaining in combat forever. These are required production paths; actual multiplayer catch and catch-full-roster ceremony evidence remains outstanding.

The extended victory-to-saddle smoke now passes 33 checks, exit 0 (`D:\Tetherbound-Water-alpha-saddle.log`). It exposed and fixed an actual dialogue scheduling bug: NPC final-line eligibility was polled on idle while dialogue advances on physics ticks. The panel now signals text presentation synchronously. The run earned the Stone from normal attacks, completed Iona's actual dialogue, opened the camp workbench, consumed eight reed/six driftwood/four reefstone through the real recipe, and mounted the same owned Mosshell with the crafted saddle. Material quantities and travel positions were fixture supplied; neither reward was. Aquaryn is still at the original dry basin placeholder position and its surface route is empty: the interface fight is incomplete.

Veilfall's four-room kitbash, waterfall entrance, persistent host-control router, grilles, bridge/channel floors, crystal and banners are now attached to the production Water world. The existing Captain Nerissa body/team is placed in the Heart Chamber and gated on the return sluice. This has no physical journey or visual acceptance yet. Guardian release ceremony, relic, blessing and world-response integration remain outstanding. Meshy generations: zero.

Root inspected the agent's production dialogue regression and independently ran dialogue delivery, generated Veilfall geometry, Alpha reward and combat-begin tests together: seven wrapper/unit tests, 78 assertions, zero failures (`D:\water-wave3-focused.log`). Child suites cover 18 dialogue, 353 geometry and 30 manager-begin assertions. Geometry fixtures are not actual player traversal evidence. The actual one-world Veilfall smoke remains a separate pending check.

Remote main advanced to `c5e4d14d101275de2f96b3918ae6ddcd19dafd35`; rebase is required at the next committed wave. PR 69 is open, draft and unmerged. GitHub returned no PR workflow runs for `934d6b34a`; CI is not declared green.
