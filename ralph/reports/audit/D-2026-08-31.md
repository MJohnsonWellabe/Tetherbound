# Audit D — Terrain and world (D1–D8)

**Lane:** AUDIT-D. **Audited SHA:** `main` @ `453107fb` (LAND-0830J landed
2026-08-30). **Date:** 2026-08-31. **Instructions:** governed by
`ralph/NEXT_COORDINATOR_FULL_STATE_AUDIT.md`, `CLAUDE.md`, and
`ralph/MEADOWS_EXIT_CRITERION.md` §D. No game code was changed to produce
this report — only `tools/`, `tests/`, and this document. Where a defect was
found it is written down, not fixed.

**Predecessor branch note:** `origin/ralph/AUDIT-D` already existed at session
start, carrying one commit (`5fbcae5d`) from an archived predecessor lane, off
the same base this audit uses (`453107fb`) — no rebase needed. That commit
built `tools/_audit_d_seam_probe.gd` (T1-GROUND-3's own named next step for
D6) but never ran it or committed a result. This report reuses that tool and
runs it; nothing else from the predecessor was inherited.

---

## How evidence in this report was sanity-checked

`ralph/MEADOWS_EXIT_CRITERION.md`'s evidence rule: no frame counts until
checked for real grass, real lighting, real geometry — the capture harness has
previously produced grass-free/hazy frames of a game that does not exist.

Two evidence sources are used here, both stated per item:

**Test suite run this session** (headless, no rendering, `godot --headless
--path . --script tests/run_tests.gd -- --only=scatter_perf_budget,veg_corridor,grass_field,band_content`):
**28 tests, 1,561,040 assertions, 0 failed.** This also confirms the scatter
bake is fresh (841,028 placements, not the stale/needs-rebake state
`ralph/reports/handover-T1-WORLD-2026-08-30.md` left it in — it was rebaked
before landing) and that the `band_content` fixture-mirror drift the
coordinator's handover flagged as a standing blocker is resolved on `main`.

1. **Frames already committed on `main`** from `ralph/reports/T1-GROUND-3/shots/`
   and `ralph/reports/T1-WORLD/evidence/`. Both lanes are confirmed ancestors
   of the audited commit (`git merge-base --is-ancestor <lane-tip> 453107fb`
   — true for both; see below), and nothing touching
   `data/config/terrain_playground.json`, `data/config/vegetation.json`,
   `shaders/`, or `scripts/world/` landed between either lane and the audited
   commit (`git log 5cc2819e^..453107fb -- <those paths>` is empty). So these
   frames represent the *current* shipped state, not a stale intermediate.
   I opened `ground-01-band1-opening-day-AFTER.png` and
   `water-03-stream-eye-AFTER.png` directly (not just trusted the handover
   prose): both show dense individually-rendered blade-card grass geometry,
   a pebbled dirt path, and — in the stream frame — a visible water ribbon
   crossing the mid-ground. Neither shows the bare-splat/haze failure mode
   `JUDGE-3` and the owner flagged. This clears the evidence rule for the
   frames cited below.
2. **Fresh captures/probes run in this session**, each stating its own
   sanity check inline.

Ancestry check, run this session:

```
git merge-base --is-ancestor d297b53e 453107fb   # T1-GROUND-3 handover commit -> true
git merge-base --is-ancestor c7ad09fc 453107fb   # T1-WORLD merge into LAND-0830J -> true
```

---

## D1. The Meadows does not read as a flat green test environment

**Test:** visual inspection of committed AFTER frames plus the T1-WORLD blind
visual-judge pass (`ralph/reports/T1-WORLD/VERDICT-2026-08-30.md`), which is
the freshest blind pass over this exact terrain state.

**Verdict: PASS**, with a caveat carried from D3/D6.

**Evidence:** `ralph/reports/T1-GROUND-3/shots/ground-0{1..5}-*-day-AFTER.png`
— rolling hills, rock outcrops, a treeline, a dirt path with individually
placed pebbles, and (band 3/5) a river/stream. T1-WORLD's blind pass, judging
the same terrain state under the project's own key-art and Palworld bars,
did **not** name "flat green test environment" among its findings for bands
00–05 — its criticisms are compositional (no focal landmark in 4 of 7 day
frames) and about scatter variety/procedural placement (D3), not about flat
uniform ground. §J of the same pass: "band 2 and band 4 look like the same
world under the same sun... passes cleanly."

**Cost to close further:** none — this item reads as closed. The residual
complaints belong to D3 (clutter/variety) and D6 (seams), not to D1's own
question.

---

## D2. Ground variation looks natural and authored; a genuine second grass species

**Test:** `tests/test_veg_corridor.gd` (assertions on `drygrass`'s band
gradient) — **9 tests, 0 failed, run this session** — plus visual inspection
of AFTER frames.

**Verdict: PASS.** A genuine second grass species exists, is visible, and
reads as a distinct plant rather than a retint.

**Evidence:** T1-GROUND-3 shipped a second grass species (`drygrass` —
Grass_Wispy_Short/Tall + Grass_Wheat, straw retint, previously suppressed and
invisible) with a per-band density gradient (0.45 lower meadows → 1.6
stronghold approach) and re-baked scatter. I opened
`ground-04-band4-ironwood-day-AFTER.png` myself: the right third of the frame
shows clearly distinct amber/rust upright seed-head stalks standing taller
than and structurally different from the green blade-card carpet around
them — an unmistakably different silhouette and colour, not a hue-swap of the
same mesh. This is the exact defect class B1 flags for the creature Aspect
variants ("one shared decal mask hue-swapped") and `drygrass` does not
repeat it. `ground-05-band5-approach-day-AFTER.png` (the driest, highest
band_scale stand) reads scrubbier and sparser than the lower bands, matching
the intended "gets drier as the chapter gets harder" gradient.

Test suite confirms the mechanism is wired correctly:
`test_drygrass_carries_a_band_gradient_over_the_real_band_table` and
`test_layer_band_scale_is_keyed_by_band_id_and_may_exceed_one` both pass
against the real `vegetation.json`/band tables, not a fixture — so a
dropped/typo'd band id (which silently flattens the gradient to 1.0) would
fail this suite.

**One caveat, not fully closed:** T1-GROUND-3 itself flagged that no blind
visual-judge round has specifically evaluated whether the species reads as
"a second grass" versus "clutter" — my own crop-level read (above) is not a
substitute for that. T1-WORLD's later whole-board pass judged general scatter
placement (D3, below) but did not single out `drygrass` for a variety
verdict.

**Cost to close further:** one blind-judge round targeted at
`drygrass` specifically, ~10 minutes, no new render needed (frames already
exist and are sanity-checked).

---

## D3. Open fields attractive without clutter — interest from shape/silhouette, not asset spam

**Verdict: FAIL (confirmed open defect).**

**Test:** T1-WORLD's blind visual-judge pass, §3.

**Evidence:** `ralph/reports/T1-WORLD/VERDICT-2026-08-30.md` §3, on frames
already sanity-checked above (`capture_check.gd` ran on every shutter, 74/75
passed). Direct quote from the blind critic: *"the broadleaf rosette repeats
across the entire right-hand meadow at a near-constant ~1m interval. There is
no clustering, no bare patch, no density gradient, no drift."* Path pebbles
lie along the centreline at even intervals, reading as a dotted line.

**Root cause, precisely diagnosed and NOT fixed** (this is the single most
load-bearing finding in this section, so it is quoted rather than
summarised): the placement rule itself is the problem, not its parameters —
"a keep-roll that is an independent Bernoulli trial per site, over a linearly
remapped noise mask, is a near-Poisson point process. Poisson has a
characteristic spacing and no clustering, by construction." Five rounds of
config tuning on the wrong system (`grass_field.json`'s cover tiers, which do
not reach the `bushes`/`groundmat` layers the critic was describing) measured
no change (CV 3.88→3.65, 1.03→1.06, 1.96→1.77 — within noise) and were
reverted. A shader lever (`drift_gate` in `cover_tier.gdshader`) exists for
the *right* system but ships at a no-op default; the baked-scatter
equivalent (in `scatter_rules.gd`) was never built.

Separately, the critic counts total meadow-scatter variety at **~4 meshes**
(broadleaf rosette, orange sedge, purple flower blob, grey pebble) across all
seven stands — not fixable by placement tuning, needs more meshes (governed
by `CLAUDE.md`'s no-Meshy-without-reference-art rule for environment props).

**Cost to close:** two separable pieces of work. (a) The placement-rule fix
for the baked `bushes`/`groundmat` layers in `scatter_rules.gd` — the
mechanism is understood, the fix is un-designed; cost is a design-and-code
pass plus a re-bake (owner-flagged as coordinator-owned per
`GATE_D_LANE_CONTRACT` §4), free in draw calls per
`docs/PERFORMANCE_BUDGET.md` §3.3. (b) The mesh-variety gap needs 6–8 more
meadow meshes with distinct scale/desaturated colour plus a second textured
rock family — an owner-supplied-reference-art decision, not code.

---

## D4. Paths, settlements and water edges integrate; no props in ponds, no signs in lanes

**Verdict: PASS on the specific items this criterion names (no pond props, no
lane-blocking signs); one adjacent settlement-scale defect cross-referenced
but scored under D5/E, not here.**

**Test:** direct visual inspection of `water-01-pond-eye-AFTER.png`,
`ground-03-band3-crossing-day-AFTER.png` (both opened this session, see
sanity-check note above), and a handover search for prior sign/prop-placement
defects.

**Evidence:**
- **Ponds:** `water-01-pond-eye-AFTER.png` shows a clean shoreline — sand,
  scattered pebbles, reed clumps at the waterline, no props standing in the
  water itself. No open defect found for this half of D4.
- **Signs in travel lanes:** historically fixed and not reopened —
  `ralph/reports/gate-f-historical-snapshot.md:2037` records OP21-18 moving 18
  signposts off the travel lane as a siting fix. No lane has since reported
  a regression.
- **Paths at a settlement edge:** `ground-03-band3-crossing-day-AFTER.png`
  shows the dirt path with pebble scatter running cleanly up to a house
  door with a bridge/plank crossing nearby — no prop interpenetration, no
  signage in the lane.

**Cross-reference, not scored here:** the same frame is where T1-WORLD's
BREAK 3 measured a house instance at roughly 2.5× its correct scale (door at
~4m against a 1.5m-scale bridge beside it) — a real defect, but it is a
settlement *authoring* (data/scale) error, not a paths/water *integration*
failure as D4 defines it, so it is not counted against D4's own verdict here.
It belongs to E (locations/settlements) and is already logged with a
measurement and a one-line-fix cost estimate in
`ralph/reports/T1-WORLD/VERDICT-2026-08-30.md` §2.

**Cost to close further:** none for D4 itself. The cross-referenced scale
error is a five-minute data fix once whoever owns that settlement config
picks it up (per T1-WORLD's own note).

---

## D5. The river reads as a river, not an engineered canal; stream visible from its own bank

**Verdict: SPLIT — stream: PASS (closed and measured). River-as-canal: FAIL (open, capped by gameplay).**

**Test:** `tools/_probe_stream_sightline.gd` (headless, analytic — already run
by T1-GROUND-3, reproducible) plus AFTER frames.

**Stream evidence:** `ralph/reports/T1-GROUND-3/shots/water-03-stream-eye-{BEFORE,AFTER}.png`.
I opened the AFTER frame myself (above) and confirmed a clear water ribbon
crossing the mid-ground with a sand/pebble bank and reed clumps — this is a
real fix, not a claim. Measured: sightline against ground-plus-grass went
from occluded by 0.363m to clear by 0.218m. `smoke_traversal.gd` re-run as
the gameplay gate. **Closed.**

**River evidence:** `ralph/reports/T1-GROUND-3/shots/water-02-river-eye-AFTER.png`.
T1-GROUND-3's own assessment, which I am not second-guessing without a fresh
render budget to spend on a "known limit": *"it does still read as a levee, in
my own AFTER frame... a uniform grey wall with a hard turf line along its top
edge, essentially as before."* Bank angle is deliberately NOT gentled further
— `CLAUDE.md`'s reserved-for-owner list includes "changing traversal
philosophy," and the river's 60°+ banks are load-bearing: they sever the only
non-Old-Mill-Crossing route between near and far Meadows. The remaining lever
(per-metre noise on the rim so the turf line wanders, in `_river_carve`) is
scoped but not built.

**Cost to close:** river rim noise is a code change in `_river_carve` plus a
terrain regen (~22 min per T1-GROUND-3's measurement) and a re-render to
verify; no owner decision needed since the bank-angle constraint is
untouched. Bank-angle-gentling itself remains explicitly off the table
pending an owner call (it would open a ford and delete Band 3's gate).

---

## D6. No visible seams (dashed terrain seam lines)

**Verdict: FAIL (open), with the clipmap hypothesis now RULED OUT — new finding this session.**

**Test:** `tools/_audit_d_seam_probe.gd` — the predecessor lane's own instrument,
run this session. Three frames from one fixed camera at the band-1 opening
stand (the artefact's original report location), differing only in
Terrain3D's `mesh_size`/`mesh_lods`: shipped (48/7), `mesh_size` halved to
24, and `mesh_lods` reduced to 4. This is exactly the test
`ralph/reports/handover-T1-GROUND-3-2026-08-30.md` §2 named as the deciding
next step and that no lane had yet run.

**Evidence:** `ralph/reports/audit/D6-seam-probe/seam-probe-{default,meshsize,lods}.jpg`
(the raw frames) and `...-crop-dashcorner.jpg` (the decisive close-up crop
per config, committed with this report). `capture_check.gd` passed on all
three shutters (grass field bound and drawing). All three frames are
compositionally identical (same boulder, same treeline, same grass —
confirming the camera never moved), so any pixel difference is attributable
to the clipmap change and/or run-to-run render noise, not to a different
shot.

**The decisive measurement.** A bulk pixel diff over the full frame shows
~30% of pixels differing between configurations — but per
`ralph/reports/handover-T1-GROUND-3-2026-08-30.md`'s own prior finding,
`_capture_ground_and_sky.gd` frames at identical settings already differ by
~39% run-to-run purely from cloud/sky/far-tier non-determinism, so a bulk
diff cannot isolate the seam. Instead I extracted per-row luminance profiles
across the sand path where the dashed line is visible (rows 440/470/500/530,
x=100–340 in the crop coordinate frame) and located every "dark dip" pixel
(a pixel more than 6 luminance units below the average of its ±3px
neighbours — this catches both the terrain-dither noise and the seam dashes
together). **The set of dip x-positions is identical, pixel-for-pixel, across
all three clipmap configurations at every row tested** (see the four
per-row comparisons committed in this session's tool output; e.g. row 500:
`default` and `meshsize` and `lods` all report dips at x=145,147,153–156,
161–163,173,174,186,187,214,215,223,224,232,233,275–277,296–298,313–315 —
no shift). A visual crop of the same corner
(`ralph/reports/audit/D6-seam-probe/seam-probe-{default,meshsize,lods}-crop-dashcorner.jpg`)
confirms the same dashed diagonal line, same position, same spacing, in all
three.

**Conclusion:** if the artefact were geometry belonging to Terrain3D's
clipmap, changing `mesh_size` (vertex density) or `mesh_lods` (LOD ring
count) would restructure the mesh under the same terrain and shift or
resample the line, even slightly. It did not move by even one pixel. **The
clipmap hypothesis, which T1-GROUND-3 left as "most likely" and the last
standing candidate, is now ruled out**, joining the sin-hash and the control
map (both ruled out previously) and the far-cover sheet (confirmed as a
*different*, already-fixed contributor — see below). What remains
unexplained is genuinely open: T1-GROUND-3's own fallback candidate — "the
terrain material's own mip/filter behaviour at grazing angles" — is now the
only named hypothesis left standing, and it was not tested this session
(would need a mip-bias/anisotropy change on the terrain material, a
different kind of probe than this one).

**The far-cover sheet contribution (a separate, already-fixed mechanism)
should not be confused with this residual.** T1-GROUND-3 separately found and
fixed real breaches in `grass_field`'s far-cover sheet (a 6m-cell mesh that
floats over the terrain and can gap over convex ground) via a `dilate_lift`
uniform — that fix is landed and un-related to the *dashed*, sub-pixel-wide
hairlines this probe targets, which are far too thin (~1px) and too close
(well inside the far-cover's 40m fade-in) to be that mechanism. Both are real
and both are already characterised; only the hairline is unattributed.

**Cost to close:** the mip/filter hypothesis needs one more probe (not
built): render the same fixed stand with the terrain material's texture
filter/anisotropy or mip bias changed, the same before/after-position
comparison technique used here. If the line moves, it's texture-space
(fixable via import/material settings); if it doesn't move either, every
named hypothesis is exhausted and the next escalation is an
upstream Terrain3D issue report. Either probe is minutes of headless probe
work, no render-heavy capture needed once the harness exists — this
session's probe cost ~13 minutes of wall clock for 3 frames on this box.

**UPDATE, `BACKLOG-D6-SEAM-PROBE` follow-up, same day.** The mip/filter
probe named above as the closing cost has now been run —
`ralph/reports/audit/D6-seam-probe/FOLLOWUP-2026-08-31.md`. Generic
mip-bias blur and Terrain3D's debug overlays are both ruled out (line
unchanged/overlays already off). The real mechanism is narrower than "mip/
filter" as a category: the ground shader's per-tile **detiling rotation**
(`_texture_detile_array` in `accumulate_material()`, `shaders/
terrain_ground.gdshader`) rotates UV and anisotropic derivatives
independently per texture tile, and disabling it drops the dashed line from
dense to nearly invisible at the same fixed camera. A faint residual
survives detiling-off and is still unexplained. Diagnosis only; no fix
landed. See that file for the full evidence and what a real fix (soften the
per-tile rotation discontinuity, not remove detiling outright) would need.

---

## D7. Aerial perspective gives distance real depth (terrain-material gradient, not fog)

**Verdict: PASS (closed and measured), with one flagged known limit.**

**Test:** direct config read (`terrain_playground.json`) plus T1-WORLD's
measured before/after on the AFTER frames.

**Evidence:** `aerial_fade_range_m` 160 → 900 (`aerial_fade_start_m` 45
unchanged) — the gradient now stays unsaturated out to ~945m instead of
fully saturating at 205m. Measured on the mid-ground strip of the elevated
band-2/4 frames: RGB (132,143,97) → (123,133,73), a 24-point blue-channel
drop — the mid-ground stops borrowing the sky's hue/value. Near-ground
pixels are unchanged ((124,135,57) vs (125,136,58)), confirming the effect
is isolated to the intended distance band. This is a terrain-shader
uniform, not fog — deliberately, since fog was tried and rejected twice at
R9.4 ("fog is eating the world").

**Known limit, not part of this item's own pass/fail:** the far horizon
strip (top ~25px of every elevated frame) is a blown white band, peaking at
(208,213,203), byte-identical before and after the aerial-range fix — it is
fog-meets-sky, not terrain, so this fix structurally cannot reach it.
Flagged for an owner call per `art.json`'s own record of the fog axis being
rejected twice already; changing it unilaterally would reopen a settled
decision.

**Cost to close the horizon-band limit:** an owner decision on the fog axis
(`fog_aerial_perspective`/density), not a config tune — the prior two
attempts (0.0022 → 0.0011 → 0.00055) were both rejected on the same grounds
("fog is eating the world").

---

## D8. Day / night / golden hour / weather all attractive and readable

**Verdict: SPLIT. Day/night terrain readability: PASS for bands 00–05, FAIL
at the stronghold (06). Golden-hour capture reliability: FAIL — confirmed
still reproducing on current `main`, new evidence this session.**

**Test 1 — night terrain readability.** Visual inspection of
`ralph/reports/T1-WORLD/evidence/board-vista-night.jpg` (already committed on
`main`, opened this session). **PASS for bands 00–05:** every night frame
reads as legible blue moonlight — grass, path, silhouetted treeline, a
visible moon disc, lit village windows in the band-0 frame, and (in
`npc-night-after.jpg`) a correctly night-graded NPC with real coat/form
instead of the pre-fix blown-out daylight brightness (T1-WORLD's own fix,
§5 of its verdict — I looked at the frame directly rather than trusting the
number). **FAIL at the stronghold:** the seventh frame in the same contact
sheet (location 06) is near-total black — this is the same defect
`ralph/reports/T1-WORLD/VERDICT-2026-08-30.md` BREAK 1 measured
(`ground-06-stronghold-night-vista.png` "effectively black... every other
stand's night frame is a legible blue moonlit read") and it is still present
in the currently-committed evidence; nothing landed since touches
`shaders/`, `scripts/world/`, or the stronghold's lighting config.

**Test 2 — golden-hour capture reliability, reproduced fresh this session.**
`tools/survey.sh` (via `tools/survey.gd`, the harness `ralph/reports/finding-golden-hour-black-frame.md`
diagnosed on 2026-08-28/29). Run this session against the audited commit:

```
01-spawn-outward       spread 1.489
02-valley-floor        spread 1.553
03-rise-overlook       spread 1.203
04-three-quarter       spread 0.976
05-spawn-low-sun       spread 0.000   <- FAIL: "frame is almost a single flat colour"
```

**Confirmed: still reproduces, byte-identical to the original report.** The
raw capture, `05-spawn-low-sun.png`, was **5,320 bytes** — the exact same
size the original 2026-08-28 finding recorded — and I opened it: pure black,
1280×720. Frame 1 from the same run, opened for the sanity check the
evidence rule requires, shows real grass geometry, a fence, boulders, an NPC
and a village path — proving the harness itself is producing real frames
elsewhere in the same run and the failure is isolated to this one shot. Both
frames are committed as JPG at `ralph/reports/audit/D8-golden-hour/`
(`05-spawn-low-sun.jpg`, `01-spawn-outward.jpg`); the black frame's raw PNG
byte count above is the number that matches the original finding, not the
recompressed JPG's size.

This is **not** a lighting or `art.json` defect — `ralph/reports/finding-golden-hour-black-frame.md`'s
own 2026-08-29 resolution (built by T1-LIGHT, using `tools/_diag_golden_hour.gd`)
already ruled that out: 13 trials at this exact viewpoint under day/golden/night
and four settle regimes all rendered correctly in isolation, and a
stripped-down repro (shot 1's viewpoint captured twice with no intervening
teleports) rendered fine both times. The failure needs **both** conditions —
revisiting a previously-visited camera position **and** having teleported
through several other regions first — which points at Terrain3D's own
region streaming/restream timing interacting with this harness's fixed
24-frame settle-after-teleport, not at time-of-day. I did not re-run that
deeper diagnosis this session (it would cost another 10+ minute render round
for a result already on record); this session's contribution is confirming
the symptom is **still live on current `main`**, not stale prose.

**Whether this reaches an actual player is still unknown**, exactly as the
2026-08-29 finding left it: the survey uses the Compatibility renderer with a
free camera doing several large teleports in one continuous run, which is not
the same code path as normal gameplay traversal (continuous walking, one
camera). No test in this session establishes or rules out an in-game
analogue.

**Cost to close:** unowned. The 2026-08-29 finding already narrows the next
step to "isolate whether it's teleport COUNT, teleport DISTANCE, or
specifically returning to a previously-visited region" in Terrain3D's own
streaming — that is Track 2/reliability scope (the finding says so
explicitly), not a Track 1 lighting task, and no lane has picked it up since.
Likely a half-day-to-a-day investigation into Terrain3D's `set_camera`/region
streaming, possibly an upstream addon issue. The stronghold night-exposure
defect is already scoped under E/location work (Hall/stronghold lanes) per
T1-WORLD's own routing note.

---

## Summary table

| # | Item | Verdict | Evidence | Close cost |
|---|---|---|---|---|
| D1 | Not a flat green test environment | **PASS** | AFTER frames + T1-WORLD §J | none |
| D2 | Genuine 2nd grass species | **PASS** | AFTER frame crop + `test_veg_corridor.gd` (9 tests, 0 failed) | one blind-judge round on variety-vs-clutter, ~10 min |
| D3 | Attractive fields, no clutter | **FAIL — open** | T1-WORLD §3 | design+code pass on placement rule (free in draw calls) + owner reference art for mesh variety |
| D4 | Paths/water/settlement integration | **PASS** | pond/crossing AFTER frames, OP21-18 sign history | none (cross-referenced house-scale defect belongs to E) |
| D5a | Stream visible from bank | **PASS — closed** | T1-GROUND-3 §3b, measured | none |
| D5b | River not a canal | **FAIL — open, capped** | T1-GROUND-3 §3c | rim noise + regen; bank angle off-table pending owner call |
| D6 | No visible seams | **FAIL — open** | new clipmap probe this session, rules out the last standing hypothesis | one more probe (mip/filter), minutes; then likely an upstream Terrain3D report |
| D7 | Aerial perspective (not fog) | **PASS — closed** | T1-WORLD §4, measured | horizon-band limit needs owner fog decision |
| D8a | Night terrain readability (bands 00–05) | **PASS** | `board-vista-night.jpg`, opened this session | none |
| D8b | Night readability at the stronghold (06) | **FAIL — open** | same evidence, frame is near-black | E/location work (Hall/stronghold lanes) |
| D8c | Golden-hour capture reliability | **FAIL — open, reproduced fresh** | `tools/survey.gd` run this session, byte-identical to the original finding | half-day–day Terrain3D streaming investigation, Track 2 scope |

**Score: 6 of 11 sub-items PASS, 5 FAIL/open.** Two of the five open items
(D3's placement rule, D6's residual seam) are precisely diagnosed with a
named next step and no remaining mystery about *what* to try; two (D5b's
canal read, D8c's golden-hour capture) are explicitly capped or scoped
outside this session by a real constraint (gameplay traversal, Track 2
reliability) rather than left vague; one (D8b) is already owned by another
section's work.

---

## What this session changed in the picture

- **Ruled out** the clipmap as D6's cause — the last of three geometry/data
  hypotheses standing (sin-hash and control map were ruled out previously by
  T1-GROUND-3). The seam is now narrowed to one untested hypothesis
  (terrain-material mip/filter behaviour) rather than three.
- **Confirmed still-open** rather than assumed: D8's golden-hour capture
  failure (byte-identical reproduction), and the stronghold's night-exposure
  failure (same frame, still black).
- **Confirmed resolved** rather than assumed: the `band_content` fixture
  mirror drift the coordinator's handover named as a standing blocker to
  consolidation — 6/6 tests pass on `main` — and the scatter bake is fresh,
  not stale.
- **Upgraded from "pending test" to PASS**, with fresh evidence rather than
  inherited prose: D2 (second grass species — visually confirmed distinct in
  a frame, mechanism tests green) and D4 (no pond/lane placement defects
  found).

## What this session did NOT do, and why

- Did not re-run T1-WORLD's or T1-GROUND-3's own blind visual-judge passes
  from scratch — their frames are fresh (confirmed by ancestry + nothing
  landed since touching the relevant files) and re-shooting 75+ frames for
  a second opinion was judged not to change any verdict, only to cost
  render time this report's budget was better spent on the two genuinely
  unresolved items (D6, D8).
- Did not build the mip/filter probe for D6's remaining hypothesis — it is
  scoped precisely enough for the next session to build directly without
  re-deriving the reasoning.
- Did not investigate D8c's Terrain3D streaming root cause beyond
  reconfirming the symptom — that is explicitly Track 2/reliability scope
  per the finding's own text, not a terrain/world visual question, and a
  full investigation would be a different kind of session.
