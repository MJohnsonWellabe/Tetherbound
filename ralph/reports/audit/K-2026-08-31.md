# AUDIT-K — Section K: it actually works, played end to end

**Status:** IN PROGRESS — skeleton pushed as checkpoint; measurements running.
**Lane:** `ralph/AUDIT-K`, spawned under `ralph/NEXT_COORDINATOR_FULL_STATE_AUDIT.md`.
**Auditing:** `main` at `453107fb` (LAND-0830J landed 2026-08-30). All verdicts in
this report are true on that SHA unless a line says otherwise.
**Rules honoured:** no game code touched; defects written down, not fixed; the
only writes are this report, its evidence directory, and (if needed) instruments
under `tests/`/`tools/`.

## Scope

`ralph/MEADOWS_EXIT_CRITERION.md` §K:

- **K1** — 3–4 hour focused pacing without cutting required beats.
- **K2** — a continuous fresh-save run from waking to legendary, no developer
  intervention.
- **K3** — no major core-verb reliability failures across that run.
- **K4** — every change survives integration (the three-track rule).

A parallel lane, `GATE-F-FULL`, is running the instrumented S01–S10/X01–X08
playthrough; its report will land at `ralph/reports/audit/GATE-F-FULL-2026-08-31.md`
and on branch `ralph/GATE-F-FULL`. Where its chain is the deciding instrument
(K1, K2's continuous run), this report references it rather than duplicating the
run. This lane's own value is the checks that chain does NOT make:

1. fresh-save boot repeatability (repeat boots, not one boot);
2. save/reload integrity mid-chapter and across gates;
3. a per-segment error census (script errors, leaked ObjectDB instances and
   resource leaks at exit);
4. flake-rate measurement of the arbiter prompt-press defect family — the
   known intermittent `smoke_authored_camps` failure at `trail_camp`
   (1-in-4 local reproduction measured by the coordinator 2026-08-30) plus the
   other "prompt offered, press does nothing" sightings recorded in
   `ralph/reports/PARTY_COUNT_STRADDLE_2026-08-30.md` — treated as ONE family;
5. whether the three consolidation blockers (band_content fixture drift, stale
   scatter bake, `party_count_after_catches`) are actually resolved on `main`.

## Verdicts

(to be filled from measurements — placeholders below are not verdicts)

- K1: pending
- K2: pending
- K3: pending
- K4: pending

## Evidence directory

`ralph/reports/audit/K-evidence/` — raw logs of every measured run, named
`<test>-run<N>.log`, plus timing and census tables.
