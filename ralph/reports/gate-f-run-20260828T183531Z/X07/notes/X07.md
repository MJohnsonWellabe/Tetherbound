# X07 — DIAG world and regional audit (protocol E.7 / section 10)

### preflight — capture not required
- segment declares no captures and no continuous record
- predicted cost 159 s over 26835 frames at 0.0059 s/frame (re-priced after each boot)

### X07-001 — what this segment is
- expected: a note event carries this observation into events.jsonl
- actual: E.7's audit grid over every region. The journey segments capture every ARRIVAL shot naturally (protocol G classes 2-13); this revisits each region under DIAG teleport for the grid.
- events: t=0.25
- verdict: PASS

### X07-002 — the clock pin, and why the notes below are no longer the pin
- expected: a note event carries this observation into events.jsonl
- actual: SUPERSEDED, and kept rather than deleted because the run it describes is on the record. This step used to read 'VOCABULARY GAP: no action pins the WorldLook clock' and concluded that every pin had to be a note. That was true when X07 was transcribed and is not true now: operator_harness.gd implements a `pin_clock` action (SEGMENT_SCHEMA.md documents it under Setup and time -- `hour`, `preset`, `weather`, `freeze`, DIAG-only, and its own doc comment carries capture_band3_region.gd's pin-AFTER-the-settle ordering). X07 never called it, so the 2026-08-25 run's six 'night' frames and four 'weather' frames were captured with the clock free-running: X07's own route.csv reads clock_hour 8.16 to 11.14 across the whole segment and weather 'clear' on all 135 rows. That is what the visual pass measured when it found night frames at day luminance (grandpas_village day 96.6 vs night 99.2) -- mid-morning frames labelled night, not a renderer that cannot draw night. Every DIAG PIN step below is now a real `pin_clock` and carries its own record.
- events: t=0.25
- verdict: PASS
- observation: SANCTIONED CHANGE to a FROZEN file (tools/gate_f/** is frozen; the run handover's standing rules permit a single genuinely blocking error to be fixed when it is recorded both in ralph/reports/gate-f-lane-log.md and in the step's own `observation`, which is how every change in the handover's section 5 is recorded). The pin notes below claimed a pin that never happened, which put a false label on ten frames of the run's only visual evidence. Blocking for the re-run: X07 is the segment the night and weather variants come from.

### X07-003 — boot the real Meadows
- expected: the real world scene stands up with the real autoloads and the point-of-interest scan finds the chapter's content
- actual: booted world in 29133 ms (240 settle frames); re-priced at boot:world: 0.0066 s/frame (was 0.0059), 26593 frames left + 29 s boot = 205 s against 14371 s of budget left
- events: t=4.53
- verdict: PASS

### X07-004 — region: Grandpa's Village
- expected: a note event carries this observation into events.jsonl
- actual: E.7 audit grid for Grandpa's Village, at the region's own centre (6,-22) from data/config/map_landmarks.json. E.7's per-region record: vegetation density impression with a paired frame, creature/trainer/resource cadence within the region (counts from live queries), empty areas (position + frame), invalid prop placement, water intrusion, collision anomalies, repetition, sightlines to the next landmark, path/road legibility, environmental storytelling present or absent, regional identity ('could a player name where they are from this frame alone' -- operator observation), and vision fit.
- events: t=4.53
- verdict: PASS

### X07-005 — DIAG: go to Grandpa's Village
- expected: the player stands at Grandpa's Village. A DIAG jump rather than a walk, because this audits the world and not the route; the distance and dead-travel accumulators reset so nothing about this jump can reach a pacing figure.
- actual: DIAG teleport to (6, -22); distance/dead-travel accumulators reset
- events: t=5.53
- verdict: PASS

### X07-006 — the teleport landed where it said (Grandpa's Village)
- expected: the player is on the region centre, standing on the ground rather than falling through it
- actual: 0.0 m from (6, -22), wanted within 5.0
- events: t=5.53
- verdict: PASS

### X07-007 — region containment agrees (Grandpa's Village)
- expected: map_state.gd's own containment puts the player in grandpas_village
- actual: region=grandpas_village (wanted grandpas_village)
- events: t=5.53
- verdict: PASS

### X07-008 — count what lives here (Grandpa's Village)
- expected: creature/trainer/resource cadence within the region, counted from live queries
- actual: 1014 points of interest in the tree
- events: t=5.53
- verdict: PASS

### X07-009 — DIAG PIN: Grandpa's Village, day
- expected: the WorldLook clock is pinned to hour 11.0 and the weather to 'clear' through the game's own _apply_blended/set_weather, then both nodes are frozen, and the harness reports the clock reading it actually got -- so the frames below carry the state their ids claim
- actual: DIAG pinned weather=clear, hour=11.00 and froze WorldLook, WorldWeather; clock now reads hour=11.00 preset=day weather=clear
- events: t=5.67
- verdict: PASS
- observation: SANCTIONED CHANGE to a FROZEN file (tools/gate_f/** is frozen; the run handover's standing rules permit a single genuinely blocking error to be fixed when it is recorded both in ralph/reports/gate-f-lane-log.md and in the step's own `observation`, which is how every change in the handover's section 5 is recorded). Was a `note` reading: "The WorldLook clock is pinned and frozen at the region's day preset before the frames below, using the capture_band3_region.gd pattern rather than the unpinned variant. This note is the pin's telemetry record, marked DIAG." -- a note is not a pin. The 2026-08-25 run proves it: clock_hour ran 8.16-11.14 and weather stayed 'clear' for all 135 route rows, so this step's own frames were captured at mid-morning whatever their ids said. `hour` rather than `preset` because it drives world_look.gd::_apply_blended, the same continuous path _process uses, and because it makes the clock_hour recorded on every manifest row below honest -- a preset snap would have left the telemetry still reading morning and a future reader repeating this exact mistake. Pinned AFTER the settle, per _step_pin_clock's own ordering note.

### X07-010 — face the intended entry route direction (Grandpa's Village)
- expected: the camera turns on the right stick to the intended entry route direction, so the arrival frame is taken from where a player would arrive
- actual: camera turned to 0 deg in 0 frames
- events: t=5.68
- verdict: PASS
- observation: SANCTIONED CHANGE to a FROZEN file (tools/gate_f/** is frozen; the run handover's standing rules permit a single genuinely blocking error to be fixed when it is recorded both in ralph/reports/gate-f-lane-log.md and in the step's own `observation`, which is how every change in the handover's section 5 is recorded). Unchanged as a step. It is now the entry-route bearing for the `gameplay` and `arrival` frames only; the four other variants each get their own bearing below. The 2026-08-25 run took all six frames from this one camera, which is why the visual pass measured `arrival`, `gameplay` and `landmark` differing by 0.1-2.3% of pixels: three of six variant slots were one shot. The position itself is NOT moved -- E.7 fixes it before play and forbids moving it to improve a composition; only the bearing and the pitch change.

### X07-011 — GF-AUD-grandpas_village-gameplay
- expected: normal-gameplay frame (HUD on) at Grandpa's Village, HUD on. This is the paired hud:on context frame for the five hud:off frames that follow it in this block (protocol G).
- actual: DELEGATED GF-AUD-grandpas_village-gameplay to capture lane X07C (this is the logic lane; it takes no frames)
- events: t=5.68
- verdict: DELEGATED

### X07-012 — GF-AUD-grandpas_village-arrival
- expected: arrival view (from the intended entry route direction) at Grandpa's Village, HUD off. A composition audit frame; its paired hud:on context frame is the gameplay frame captured seconds earlier in this block.
- actual: DELEGATED GF-AUD-grandpas_village-arrival to capture lane X07C (this is the logic lane; it takes no frames)
- events: t=5.68
- verdict: DELEGATED

### X07-010a — aim the landmark frame (grandpas_village)
- expected: the camera turns on the right stick to a bearing the arrival frame does not already cover, so the landmark slot captures its own shot
- actual: camera turned to 95 deg in 45 frames
- events: t=6.45
- verdict: PASS
- observation: SANCTIONED CHANGE to a FROZEN file (tools/gate_f/** is frozen; the run handover's standing rules permit a single genuinely blocking error to be fixed when it is recorded both in ralph/reports/gate-f-lane-log.md and in the step's own `observation`, which is how every change in the handover's section 5 is recorded). New step. aimed at grandpa_house, data/config/map_landmarks.json, 28.6 m from this position. Taken from the game's own map data rather than chosen: nothing here is an invented coordinate.

### X07-013 — GF-AUD-grandpas_village-landmark
- expected: signature landmark at Grandpa's Village, HUD off. A composition audit frame; its paired hud:on context frame is the gameplay frame captured seconds earlier in this block.
- actual: DELEGATED GF-AUD-grandpas_village-landmark to capture lane X07C (this is the logic lane; it takes no frames)
- events: t=6.45
- verdict: DELEGATED

### X07-010b — aim the ecology frame (grandpas_village)
- expected: the camera turns on the right stick to the entry bearing turned 144 degrees
- actual: camera turned to -152 deg in 52 frames
- events: t=7.33
- verdict: PASS
- observation: SANCTIONED CHANGE to a FROZEN file (tools/gate_f/** is frozen; the run handover's standing rules permit a single genuinely blocking error to be fixed when it is recorded both in ralph/reports/gate-f-lane-log.md and in the step's own `observation`, which is how every change in the handover's section 5 is recorded). New step. Third arm of the compass sweep. The ecology frame asks what lives here, and asking it down the same sightline as the arrival frame can only ever answer for that sightline.

### X07-015 — GF-AUD-grandpas_village-ecology
- expected: ecology/activity frame (wilds visible behaving) at Grandpa's Village, HUD off. A composition audit frame; its paired hud:on context frame is the gameplay frame captured seconds earlier in this block.
- actual: DELEGATED GF-AUD-grandpas_village-ecology to capture lane X07C (this is the logic lane; it takes no frames)
- events: t=7.33
- verdict: DELEGATED

### X07-010c — aim the vegetation frame (grandpas_village)
- expected: the camera turns on the right stick to the entry bearing turned 216 degrees
- actual: camera turned to 152 deg in 83 frames
- events: t=8.73
- verdict: PASS
- observation: SANCTIONED CHANGE to a FROZEN file (tools/gate_f/** is frozen; the run handover's standing rules permit a single genuinely blocking error to be fixed when it is recorded both in ralph/reports/gate-f-lane-log.md and in the step's own `observation`, which is how every change in the handover's section 5 is recorded). New step. Fourth arm of the compass sweep. The visual pass's finding that grass tufts occur 'in one narrow band and nowhere else on the visible floor' is a claim about one bearing until the audit looks down more than one.

### X07-016 — GF-AUD-grandpas_village-vegetation
- expected: vegetation density impression with a paired frame at Grandpa's Village, HUD off. A composition audit frame; its paired hud:on context frame is the gameplay frame captured seconds earlier in this block.
- actual: DELEGATED GF-AUD-grandpas_village-vegetation to capture lane X07C (this is the logic lane; it takes no frames)
- events: t=8.73
- verdict: DELEGATED

### X07-010d — back to the entry bearing for the ground frame (grandpas_village)
- expected: the camera returns to the entry-route bearing
- actual: camera turned to 8 deg in 150 frames
- events: t=11.25
- verdict: PASS
- observation: SANCTIONED CHANGE to a FROZEN file (tools/gate_f/** is frozen; the run handover's standing rules permit a single genuinely blocking error to be fixed when it is recorded both in ralph/reports/gate-f-lane-log.md and in the step's own `observation`, which is how every change in the handover's section 5 is recorded). New step. The terrain frame is pitched down the route a player actually walks in on, so it is returned to the entry bearing first. It is also what leaves the camera on the entry bearing for the night and weather variants that follow in some blocks.

### X07-010e — pitch down to the ground (grandpas_village)
- expected: the right stick pitches the camera down to its floor (camera.pitch_min_deg, -60) -- a ground close view instead of a sixth copy of the standing shot
- actual: right stick held (0.00, 1.00) for 40 frames
- events: t=11.93
- verdict: PASS
- observation: SANCTIONED CHANGE to a FROZEN file (tools/gate_f/** is frozen; the run handover's standing rules permit a single genuinely blocking error to be fixed when it is recorded both in ralph/reports/gate-f-lane-log.md and in the step's own `observation`, which is how every change in the handover's section 5 is recorded). New step. 40 frames at full deflection is 0.67 s against movement.json's 190 deg/s, which is 127 degrees of travel into a 48-degree range: the pitch CLAMPS at its floor, so this lands on exactly -60 every time rather than on whatever the frame budget happened to buy.

### X07-014 — GF-AUD-grandpas_village-terrain
- expected: terrain/ground close view at Grandpa's Village, HUD off. A composition audit frame; its paired hud:on context frame is the gameplay frame captured seconds earlier in this block.
- actual: DELEGATED GF-AUD-grandpas_village-terrain to capture lane X07C (this is the logic lane; it takes no frames)
- events: t=11.93
- verdict: DELEGATED

### X07-010f — pitch back up to the ceiling (grandpas_village)
- expected: the right stick pitches the camera up to its ceiling (camera.pitch_max_deg, 32)
- actual: right stick held (0.00, -1.00) for 40 frames
- events: t=12.62
- verdict: PASS
- observation: SANCTIONED CHANGE to a FROZEN file (tools/gate_f/** is frozen; the run handover's standing rules permit a single genuinely blocking error to be fixed when it is recorded both in ralph/reports/gate-f-lane-log.md and in the step's own `observation`, which is how every change in the handover's section 5 is recorded). New step. Half of restoring the camera deterministically: drive it to the OTHER clamp first, so the next step starts from a known angle rather than from wherever this block left it.

### X07-010g — settle the pitch back to its start (grandpas_village)
- expected: the camera returns to roughly camera.pitch_start_deg (-12), where the block found it
- actual: right stick held (0.00, 1.00) for 14 frames
- events: t=12.87
- verdict: PASS
- observation: SANCTIONED CHANGE to a FROZEN file (tools/gate_f/** is frozen; the run handover's standing rules permit a single genuinely blocking error to be fixed when it is recorded both in ralph/reports/gate-f-lane-log.md and in the step's own `observation`, which is how every change in the handover's section 5 is recorded). New step. From the +32 ceiling, 14 frames at 190 deg/s is 44.3 degrees, landing at about -12.3 -- pitch_start_deg. The block hands the camera on as it found it, so the night and weather frames that follow in some blocks are comparable with the day frames above them.

### X07-017 — what the Grandpa's Village grid recorded
- expected: a note event carries this observation into events.jsonl
- actual: Operator record for Grandpa's Village: E.7's per-region record: vegetation density impression with a paired frame, creature/trainer/resource cadence within the region (counts from live queries), empty areas (position + frame), invalid prop placement, water intrusion, collision anomalies, repetition, sightlines to the next landmark, path/road legibility, environmental storytelling present or absent, regional identity ('could a player name where they are from this frame alone' -- operator observation), and vision fit.
- events: t=12.87
- verdict: PASS

### X07-018 — DIAG PIN: Grandpa's Village, night
- expected: the WorldLook clock is pinned to hour 23.0 and the weather to 'clear' through the game's own _apply_blended/set_weather, then both nodes are frozen, and the harness reports the clock reading it actually got -- so the frames below carry the state their ids claim
- actual: DIAG pinned weather=clear, hour=23.00 and froze WorldLook, WorldWeather; clock now reads hour=23.00 preset=golden weather=clear
- events: t=13.00
- verdict: PASS
- observation: SANCTIONED CHANGE to a FROZEN file (tools/gate_f/** is frozen; the run handover's standing rules permit a single genuinely blocking error to be fixed when it is recorded both in ralph/reports/gate-f-lane-log.md and in the step's own `observation`, which is how every change in the handover's section 5 is recorded). Was a `note` reading: "E.7: 'Day/night/weather variants where materially relevant (village, pond, stronghold approach at minimum)'. The clock is pinned and frozen at the night preset. This note is the pin's telemetry record, marked DIAG." -- a note is not a pin. The 2026-08-25 run proves it: clock_hour ran 8.16-11.14 and weather stayed 'clear' for all 135 route rows, so this step's own frames were captured at mid-morning whatever their ids said. `hour` rather than `preset` because it drives world_look.gd::_apply_blended, the same continuous path _process uses, and because it makes the clock_hour recorded on every manifest row below honest -- a preset snap would have left the telemetry still reading morning and a future reader repeating this exact mistake. Pinned AFTER the settle, per _step_pin_clock's own ordering note.

### X07-019 — GF-AUD-grandpas_village-night-gameplay
- expected: the night variant's paired hud:on context frame at Grandpa's Village
- actual: DELEGATED GF-AUD-grandpas_village-night-gameplay to capture lane X07C (this is the logic lane; it takes no frames)
- events: t=13.00
- verdict: DELEGATED

### X07-020 — GF-AUD-grandpas_village-night-arrival
- expected: the night variant's composition frame at Grandpa's Village, paired with the hud:on frame captured seconds earlier
- actual: DELEGATED GF-AUD-grandpas_village-night-arrival to capture lane X07C (this is the logic lane; it takes no frames)
- events: t=13.00
- verdict: DELEGATED

### X07-021 — DIAG PIN: Grandpa's Village, weather variant (rain)
- expected: the WorldLook clock is pinned to hour 11.0 and the weather to 'rain' through the game's own _apply_blended/set_weather, then both nodes are frozen, and the harness reports the clock reading it actually got -- so the frames below carry the state their ids claim
- actual: DIAG pinned weather=rain, hour=11.00 and froze WorldLook, WorldWeather; clock now reads hour=11.00 preset=day weather=rain
- events: t=13.13
- verdict: PASS
- observation: SANCTIONED CHANGE to a FROZEN file (tools/gate_f/** is frozen; the run handover's standing rules permit a single genuinely blocking error to be fixed when it is recorded both in ralph/reports/gate-f-lane-log.md and in the step's own `observation`, which is how every change in the handover's section 5 is recorded). Was a `note` reading: "The clock and the weather preset are pinned and frozen for the weather variant. This note is the pin's telemetry record, marked DIAG." -- a note is not a pin. The 2026-08-25 run proves it: clock_hour ran 8.16-11.14 and weather stayed 'clear' for all 135 route rows, so this step's own frames were captured at mid-morning whatever their ids said. `hour` rather than `preset` because it drives world_look.gd::_apply_blended, the same continuous path _process uses, and because it makes the clock_hour recorded on every manifest row below honest -- a preset snap would have left the telemetry still reading morning and a future reader repeating this exact mistake. Pinned AFTER the settle, per _step_pin_clock's own ordering note.

### X07-022 — GF-AUD-grandpas_village-weather-gameplay
- expected: the weather variant's paired hud:on context frame at Grandpa's Village
- actual: DELEGATED GF-AUD-grandpas_village-weather-gameplay to capture lane X07C (this is the logic lane; it takes no frames)
- events: t=13.13
- verdict: DELEGATED

### X07-023 — GF-AUD-grandpas_village-weather-arrival
- expected: the weather variant's composition frame at Grandpa's Village, paired with the hud:on frame captured seconds earlier
- actual: DELEGATED GF-AUD-grandpas_village-weather-arrival to capture lane X07C (this is the logic lane; it takes no frames)
- events: t=13.13
- verdict: DELEGATED

### X07-024 — region: The Rise
- expected: a note event carries this observation into events.jsonl
- actual: E.7 audit grid for The Rise, at the region's own centre (88,-43) from data/config/map_landmarks.json. E.7's per-region record: vegetation density impression with a paired frame, creature/trainer/resource cadence within the region (counts from live queries), empty areas (position + frame), invalid prop placement, water intrusion, collision anomalies, repetition, sightlines to the next landmark, path/road legibility, environmental storytelling present or absent, regional identity ('could a player name where they are from this frame alone' -- operator observation), and vision fit.
- events: t=13.13
- verdict: PASS

### X07-025 — DIAG: go to The Rise
- expected: the player stands at The Rise. A DIAG jump rather than a walk, because this audits the world and not the route; the distance and dead-travel accumulators reset so nothing about this jump can reach a pacing figure.
- actual: DIAG teleport to (88, -43); distance/dead-travel accumulators reset
- events: t=14.13
- verdict: PASS

### X07-026 — the teleport landed where it said (The Rise)
- expected: the player is on the region centre, standing on the ground rather than falling through it
- actual: 0.0 m from (88, -43), wanted within 5.0
- events: t=14.13
- verdict: PASS

### X07-027 — region containment agrees (The Rise)
- expected: map_state.gd's own containment puts the player in the_rise
- actual: region=the_rise (wanted the_rise)
- events: t=14.13
- verdict: PASS

### X07-028 — count what lives here (The Rise)
- expected: creature/trainer/resource cadence within the region, counted from live queries
- actual: 1014 points of interest in the tree
- events: t=14.13
- verdict: PASS

### X07-029 — DIAG PIN: The Rise, day
- expected: the WorldLook clock is pinned to hour 11.0 and the weather to 'clear' through the game's own _apply_blended/set_weather, then both nodes are frozen, and the harness reports the clock reading it actually got -- so the frames below carry the state their ids claim
- actual: DIAG pinned weather=clear, hour=11.00 and froze WorldLook, WorldWeather; clock now reads hour=11.00 preset=day weather=clear
- events: t=14.27
- verdict: PASS
- observation: SANCTIONED CHANGE to a FROZEN file (tools/gate_f/** is frozen; the run handover's standing rules permit a single genuinely blocking error to be fixed when it is recorded both in ralph/reports/gate-f-lane-log.md and in the step's own `observation`, which is how every change in the handover's section 5 is recorded). Was a `note` reading: "The WorldLook clock is pinned and frozen at the region's day preset before the frames below, using the capture_band3_region.gd pattern rather than the unpinned variant. This note is the pin's telemetry record, marked DIAG." -- a note is not a pin. The 2026-08-25 run proves it: clock_hour ran 8.16-11.14 and weather stayed 'clear' for all 135 route rows, so this step's own frames were captured at mid-morning whatever their ids said. `hour` rather than `preset` because it drives world_look.gd::_apply_blended, the same continuous path _process uses, and because it makes the clock_hour recorded on every manifest row below honest -- a preset snap would have left the telemetry still reading morning and a future reader repeating this exact mistake. Pinned AFTER the settle, per _step_pin_clock's own ordering note.

### X07-030 — face the intended entry route direction (The Rise)
- expected: the camera turns on the right stick to the intended entry route direction, so the arrival frame is taken from where a player would arrive
- actual: camera turned to 8 deg in 0 frames
- events: t=14.28
- verdict: PASS
- observation: SANCTIONED CHANGE to a FROZEN file (tools/gate_f/** is frozen; the run handover's standing rules permit a single genuinely blocking error to be fixed when it is recorded both in ralph/reports/gate-f-lane-log.md and in the step's own `observation`, which is how every change in the handover's section 5 is recorded). Unchanged as a step. It is now the entry-route bearing for the `gameplay` and `arrival` frames only; the four other variants each get their own bearing below. The 2026-08-25 run took all six frames from this one camera, which is why the visual pass measured `arrival`, `gameplay` and `landmark` differing by 0.1-2.3% of pixels: three of six variant slots were one shot. The position itself is NOT moved -- E.7 fixes it before play and forbids moving it to improve a composition; only the bearing and the pitch change.

### X07-031 — GF-AUD-the_rise-gameplay
- expected: normal-gameplay frame (HUD on) at The Rise, HUD on. This is the paired hud:on context frame for the five hud:off frames that follow it in this block (protocol G).
- actual: DELEGATED GF-AUD-the_rise-gameplay to capture lane X07C (this is the logic lane; it takes no frames)
- events: t=14.28
- verdict: DELEGATED

### X07-032 — GF-AUD-the_rise-arrival
- expected: arrival view (from the intended entry route direction) at The Rise, HUD off. A composition audit frame; its paired hud:on context frame is the gameplay frame captured seconds earlier in this block.
- actual: DELEGATED GF-AUD-the_rise-arrival to capture lane X07C (this is the logic lane; it takes no frames)
- events: t=14.28
- verdict: DELEGATED

### X07-030a — aim the landmark frame (the_rise)
- expected: the camera turns on the right stick to a bearing the arrival frame does not already cover, so the landmark slot captures its own shot
- actual: camera turned to 106 deg in 46 frames
- events: t=15.07
- verdict: PASS
- observation: SANCTIONED CHANGE to a FROZEN file (tools/gate_f/** is frozen; the run handover's standing rules permit a single genuinely blocking error to be fixed when it is recorded both in ralph/reports/gate-f-lane-log.md and in the step's own `observation`, which is how every change in the handover's section 5 is recorded). New step. aimed at road_gate, data/config/map_landmarks.json, 66.4 m from this position. Taken from the game's own map data rather than chosen: nothing here is an invented coordinate.

### X07-033 — GF-AUD-the_rise-landmark
- expected: signature landmark at The Rise, HUD off. A composition audit frame; its paired hud:on context frame is the gameplay frame captured seconds earlier in this block.
- actual: DELEGATED GF-AUD-the_rise-landmark to capture lane X07C (this is the logic lane; it takes no frames)
- events: t=15.07
- verdict: DELEGATED

### X07-030b — aim the ecology frame (the_rise)
- expected: the camera turns on the right stick to the entry bearing turned 144 degrees
- actual: camera turned to -151 deg in 48 frames
- events: t=15.88
- verdict: PASS
- observation: SANCTIONED CHANGE to a FROZEN file (tools/gate_f/** is frozen; the run handover's standing rules permit a single genuinely blocking error to be fixed when it is recorded both in ralph/reports/gate-f-lane-log.md and in the step's own `observation`, which is how every change in the handover's section 5 is recorded). New step. Third arm of the compass sweep. The ecology frame asks what lives here, and asking it down the same sightline as the arrival frame can only ever answer for that sightline.

### X07-035 — GF-AUD-the_rise-ecology
- expected: ecology/activity frame (wilds visible behaving) at The Rise, HUD off. A composition audit frame; its paired hud:on context frame is the gameplay frame captured seconds earlier in this block.
- actual: DELEGATED GF-AUD-the_rise-ecology to capture lane X07C (this is the logic lane; it takes no frames)
- events: t=15.88
- verdict: DELEGATED

### X07-030c — aim the vegetation frame (the_rise)
- expected: the camera turns on the right stick to the entry bearing turned 216 degrees
- actual: camera turned to 152 deg in 84 frames
- events: t=17.30
- verdict: PASS
- observation: SANCTIONED CHANGE to a FROZEN file (tools/gate_f/** is frozen; the run handover's standing rules permit a single genuinely blocking error to be fixed when it is recorded both in ralph/reports/gate-f-lane-log.md and in the step's own `observation`, which is how every change in the handover's section 5 is recorded). New step. Fourth arm of the compass sweep. The visual pass's finding that grass tufts occur 'in one narrow band and nowhere else on the visible floor' is a claim about one bearing until the audit looks down more than one.

### X07-036 — GF-AUD-the_rise-vegetation
- expected: vegetation density impression with a paired frame at The Rise, HUD off. A composition audit frame; its paired hud:on context frame is the gameplay frame captured seconds earlier in this block.
- actual: DELEGATED GF-AUD-the_rise-vegetation to capture lane X07C (this is the logic lane; it takes no frames)
- events: t=17.30
- verdict: DELEGATED

### X07-030d — back to the entry bearing for the ground frame (the_rise)
- expected: the camera returns to the entry-route bearing
- actual: camera turned to 8 deg in 150 frames
- events: t=19.82
- verdict: PASS
- observation: SANCTIONED CHANGE to a FROZEN file (tools/gate_f/** is frozen; the run handover's standing rules permit a single genuinely blocking error to be fixed when it is recorded both in ralph/reports/gate-f-lane-log.md and in the step's own `observation`, which is how every change in the handover's section 5 is recorded). New step. The terrain frame is pitched down the route a player actually walks in on, so it is returned to the entry bearing first. It is also what leaves the camera on the entry bearing for the night and weather variants that follow in some blocks.

### X07-030e — pitch down to the ground (the_rise)
- expected: the right stick pitches the camera down to its floor (camera.pitch_min_deg, -60) -- a ground close view instead of a sixth copy of the standing shot
- actual: right stick held (0.00, 1.00) for 40 frames
- events: t=20.50
- verdict: PASS
- observation: SANCTIONED CHANGE to a FROZEN file (tools/gate_f/** is frozen; the run handover's standing rules permit a single genuinely blocking error to be fixed when it is recorded both in ralph/reports/gate-f-lane-log.md and in the step's own `observation`, which is how every change in the handover's section 5 is recorded). New step. 40 frames at full deflection is 0.67 s against movement.json's 190 deg/s, which is 127 degrees of travel into a 48-degree range: the pitch CLAMPS at its floor, so this lands on exactly -60 every time rather than on whatever the frame budget happened to buy.

### X07-034 — GF-AUD-the_rise-terrain
- expected: terrain/ground close view at The Rise, HUD off. A composition audit frame; its paired hud:on context frame is the gameplay frame captured seconds earlier in this block.
- actual: DELEGATED GF-AUD-the_rise-terrain to capture lane X07C (this is the logic lane; it takes no frames)
- events: t=20.50
- verdict: DELEGATED

### X07-030f — pitch back up to the ceiling (the_rise)
- expected: the right stick pitches the camera up to its ceiling (camera.pitch_max_deg, 32)
- actual: right stick held (0.00, -1.00) for 40 frames
- events: t=21.18
- verdict: PASS
- observation: SANCTIONED CHANGE to a FROZEN file (tools/gate_f/** is frozen; the run handover's standing rules permit a single genuinely blocking error to be fixed when it is recorded both in ralph/reports/gate-f-lane-log.md and in the step's own `observation`, which is how every change in the handover's section 5 is recorded). New step. Half of restoring the camera deterministically: drive it to the OTHER clamp first, so the next step starts from a known angle rather than from wherever this block left it.

### X07-030g — settle the pitch back to its start (the_rise)
- expected: the camera returns to roughly camera.pitch_start_deg (-12), where the block found it
- actual: right stick held (0.00, 1.00) for 14 frames
- events: t=21.43
- verdict: PASS
- observation: SANCTIONED CHANGE to a FROZEN file (tools/gate_f/** is frozen; the run handover's standing rules permit a single genuinely blocking error to be fixed when it is recorded both in ralph/reports/gate-f-lane-log.md and in the step's own `observation`, which is how every change in the handover's section 5 is recorded). New step. From the +32 ceiling, 14 frames at 190 deg/s is 44.3 degrees, landing at about -12.3 -- pitch_start_deg. The block hands the camera on as it found it, so the night and weather frames that follow in some blocks are comparable with the day frames above them.

### X07-037 — what the The Rise grid recorded
- expected: a note event carries this observation into events.jsonl
- actual: Operator record for The Rise: E.7's per-region record: vegetation density impression with a paired frame, creature/trainer/resource cadence within the region (counts from live queries), empty areas (position + frame), invalid prop placement, water intrusion, collision anomalies, repetition, sightlines to the next landmark, path/road legibility, environmental storytelling present or absent, regional identity ('could a player name where they are from this frame alone' -- operator observation), and vision fit.
- events: t=21.43
- verdict: PASS

### X07-038 — region: The Pond
- expected: a note event carries this observation into events.jsonl
- actual: E.7 audit grid for The Pond, at the region's own centre (-342,507) from data/config/map_landmarks.json. E.7's per-region record: vegetation density impression with a paired frame, creature/trainer/resource cadence within the region (counts from live queries), empty areas (position + frame), invalid prop placement, water intrusion, collision anomalies, repetition, sightlines to the next landmark, path/road legibility, environmental storytelling present or absent, regional identity ('could a player name where they are from this frame alone' -- operator observation), and vision fit.
- events: t=21.43
- verdict: PASS

### X07-039 — DIAG: go to The Pond
- expected: the player stands at The Pond. A DIAG jump rather than a walk, because this audits the world and not the route; the distance and dead-travel accumulators reset so nothing about this jump can reach a pacing figure.
- actual: DIAG teleport to (-342, 507); distance/dead-travel accumulators reset
- events: t=22.43
- verdict: PASS

### X07-040 — the teleport landed where it said (The Pond)
- expected: the player is on the region centre, standing on the ground rather than falling through it
- actual: 0.0 m from (-342, 507), wanted within 5.0
- events: t=22.43
- verdict: PASS

### X07-041 — region containment agrees (The Pond)
- expected: map_state.gd's own containment puts the player in the_pond
- actual: region=the_pond (wanted the_pond)
- events: t=22.43
- verdict: PASS

### X07-042 — count what lives here (The Pond)
- expected: creature/trainer/resource cadence within the region, counted from live queries
- actual: 1014 points of interest in the tree
- events: t=22.43
- verdict: PASS

### X07-043 — DIAG PIN: The Pond, day
- expected: the WorldLook clock is pinned to hour 11.0 and the weather to 'clear' through the game's own _apply_blended/set_weather, then both nodes are frozen, and the harness reports the clock reading it actually got -- so the frames below carry the state their ids claim
- actual: DIAG pinned weather=clear, hour=11.00 and froze WorldLook, WorldWeather; clock now reads hour=11.00 preset=day weather=clear
- events: t=22.57
- verdict: PASS
- observation: SANCTIONED CHANGE to a FROZEN file (tools/gate_f/** is frozen; the run handover's standing rules permit a single genuinely blocking error to be fixed when it is recorded both in ralph/reports/gate-f-lane-log.md and in the step's own `observation`, which is how every change in the handover's section 5 is recorded). Was a `note` reading: "The WorldLook clock is pinned and frozen at the region's day preset before the frames below, using the capture_band3_region.gd pattern rather than the unpinned variant. This note is the pin's telemetry record, marked DIAG." -- a note is not a pin. The 2026-08-25 run proves it: clock_hour ran 8.16-11.14 and weather stayed 'clear' for all 135 route rows, so this step's own frames were captured at mid-morning whatever their ids said. `hour` rather than `preset` because it drives world_look.gd::_apply_blended, the same continuous path _process uses, and because it makes the clock_hour recorded on every manifest row below honest -- a preset snap would have left the telemetry still reading morning and a future reader repeating this exact mistake. Pinned AFTER the settle, per _step_pin_clock's own ordering note.

### X07-044 — face the intended entry route direction (The Pond)
- expected: the camera turns on the right stick to the intended entry route direction, so the arrival frame is taken from where a player would arrive
- actual: camera turned to 8 deg in 0 frames
- events: t=22.58
- verdict: PASS
- observation: SANCTIONED CHANGE to a FROZEN file (tools/gate_f/** is frozen; the run handover's standing rules permit a single genuinely blocking error to be fixed when it is recorded both in ralph/reports/gate-f-lane-log.md and in the step's own `observation`, which is how every change in the handover's section 5 is recorded). Unchanged as a step. It is now the entry-route bearing for the `gameplay` and `arrival` frames only; the four other variants each get their own bearing below. The 2026-08-25 run took all six frames from this one camera, which is why the visual pass measured `arrival`, `gameplay` and `landmark` differing by 0.1-2.3% of pixels: three of six variant slots were one shot. The position itself is NOT moved -- E.7 fixes it before play and forbids moving it to improve a composition; only the bearing and the pitch change.

### X07-045 — GF-AUD-the_pond-gameplay
- expected: normal-gameplay frame (HUD on) at The Pond, HUD on. This is the paired hud:on context frame for the five hud:off frames that follow it in this block (protocol G).
- actual: DELEGATED GF-AUD-the_pond-gameplay to capture lane X07C (this is the logic lane; it takes no frames)
- events: t=22.58
- verdict: DELEGATED

### X07-046 — GF-AUD-the_pond-arrival
- expected: arrival view (from the intended entry route direction) at The Pond, HUD off. A composition audit frame; its paired hud:on context frame is the gameplay frame captured seconds earlier in this block.
- actual: DELEGATED GF-AUD-the_pond-arrival to capture lane X07C (this is the logic lane; it takes no frames)
- events: t=22.58
- verdict: DELEGATED

### X07-044a — aim the landmark frame (the_pond)
- expected: the camera turns on the right stick to a bearing the arrival frame does not already cover, so the landmark slot captures its own shot
- actual: camera turned to -64 deg in 76 frames
- events: t=23.87
- verdict: PASS
- observation: SANCTIONED CHANGE to a FROZEN file (tools/gate_f/** is frozen; the run handover's standing rules permit a single genuinely blocking error to be fixed when it is recorded both in ralph/reports/gate-f-lane-log.md and in the step's own `observation`, which is how every change in the handover's section 5 is recorded). New step. data/config/map_landmarks.json carries no landmark for this region more than 20 m from this position, so there is no authored landmark to aim at and one is not invented here. The bearing is the entry bearing turned 72 degrees, the first arm of the five-way compass sweep the other variants complete -- a different sixth of the region rather than the same sixth six times.

### X07-047 — GF-AUD-the_pond-landmark
- expected: signature landmark at The Pond, HUD off. A composition audit frame; its paired hud:on context frame is the gameplay frame captured seconds earlier in this block.
- actual: DELEGATED GF-AUD-the_pond-landmark to capture lane X07C (this is the logic lane; it takes no frames)
- events: t=23.87
- verdict: DELEGATED

### X07-044b — aim the ecology frame (the_pond)
- expected: the camera turns on the right stick to the entry bearing turned 144 degrees
- actual: camera turned to -136 deg in 95 frames
- events: t=25.47
- verdict: PASS
- observation: SANCTIONED CHANGE to a FROZEN file (tools/gate_f/** is frozen; the run handover's standing rules permit a single genuinely blocking error to be fixed when it is recorded both in ralph/reports/gate-f-lane-log.md and in the step's own `observation`, which is how every change in the handover's section 5 is recorded). New step. Third arm of the compass sweep. The ecology frame asks what lives here, and asking it down the same sightline as the arrival frame can only ever answer for that sightline.

### X07-049 — GF-AUD-the_pond-ecology
- expected: ecology/activity frame (wilds visible behaving) at The Pond, HUD off. A composition audit frame; its paired hud:on context frame is the gameplay frame captured seconds earlier in this block.
- actual: DELEGATED GF-AUD-the_pond-ecology to capture lane X07C (this is the logic lane; it takes no frames)
- events: t=25.47
- verdict: DELEGATED

### X07-044c — aim the vegetation frame (the_pond)
- expected: the camera turns on the right stick to the entry bearing turned 216 degrees
- actual: camera turned to 152 deg in 95 frames
- events: t=27.07
- verdict: PASS
- observation: SANCTIONED CHANGE to a FROZEN file (tools/gate_f/** is frozen; the run handover's standing rules permit a single genuinely blocking error to be fixed when it is recorded both in ralph/reports/gate-f-lane-log.md and in the step's own `observation`, which is how every change in the handover's section 5 is recorded). New step. Fourth arm of the compass sweep. The visual pass's finding that grass tufts occur 'in one narrow band and nowhere else on the visible floor' is a claim about one bearing until the audit looks down more than one.

### X07-050 — GF-AUD-the_pond-vegetation
- expected: vegetation density impression with a paired frame at The Pond, HUD off. A composition audit frame; its paired hud:on context frame is the gameplay frame captured seconds earlier in this block.
- actual: DELEGATED GF-AUD-the_pond-vegetation to capture lane X07C (this is the logic lane; it takes no frames)
- events: t=27.07
- verdict: DELEGATED

### X07-044d — back to the entry bearing for the ground frame (the_pond)
- expected: the camera returns to the entry-route bearing
- actual: camera turned to 8 deg in 150 frames
- events: t=29.58
- verdict: PASS
- observation: SANCTIONED CHANGE to a FROZEN file (tools/gate_f/** is frozen; the run handover's standing rules permit a single genuinely blocking error to be fixed when it is recorded both in ralph/reports/gate-f-lane-log.md and in the step's own `observation`, which is how every change in the handover's section 5 is recorded). New step. The terrain frame is pitched down the route a player actually walks in on, so it is returned to the entry bearing first. It is also what leaves the camera on the entry bearing for the night and weather variants that follow in some blocks.

### X07-044e — pitch down to the ground (the_pond)
- expected: the right stick pitches the camera down to its floor (camera.pitch_min_deg, -60) -- a ground close view instead of a sixth copy of the standing shot
- actual: right stick held (0.00, 1.00) for 40 frames
- events: t=30.27
- verdict: PASS
- observation: SANCTIONED CHANGE to a FROZEN file (tools/gate_f/** is frozen; the run handover's standing rules permit a single genuinely blocking error to be fixed when it is recorded both in ralph/reports/gate-f-lane-log.md and in the step's own `observation`, which is how every change in the handover's section 5 is recorded). New step. 40 frames at full deflection is 0.67 s against movement.json's 190 deg/s, which is 127 degrees of travel into a 48-degree range: the pitch CLAMPS at its floor, so this lands on exactly -60 every time rather than on whatever the frame budget happened to buy.

### X07-048 — GF-AUD-the_pond-terrain
- expected: terrain/ground close view at The Pond, HUD off. A composition audit frame; its paired hud:on context frame is the gameplay frame captured seconds earlier in this block.
- actual: DELEGATED GF-AUD-the_pond-terrain to capture lane X07C (this is the logic lane; it takes no frames)
- events: t=30.27
- verdict: DELEGATED

### X07-044f — pitch back up to the ceiling (the_pond)
- expected: the right stick pitches the camera up to its ceiling (camera.pitch_max_deg, 32)
- actual: right stick held (0.00, -1.00) for 40 frames
- events: t=30.95
- verdict: PASS
- observation: SANCTIONED CHANGE to a FROZEN file (tools/gate_f/** is frozen; the run handover's standing rules permit a single genuinely blocking error to be fixed when it is recorded both in ralph/reports/gate-f-lane-log.md and in the step's own `observation`, which is how every change in the handover's section 5 is recorded). New step. Half of restoring the camera deterministically: drive it to the OTHER clamp first, so the next step starts from a known angle rather than from wherever this block left it.

### X07-044g — settle the pitch back to its start (the_pond)
- expected: the camera returns to roughly camera.pitch_start_deg (-12), where the block found it
- actual: right stick held (0.00, 1.00) for 14 frames
- events: t=31.20
- verdict: PASS
- observation: SANCTIONED CHANGE to a FROZEN file (tools/gate_f/** is frozen; the run handover's standing rules permit a single genuinely blocking error to be fixed when it is recorded both in ralph/reports/gate-f-lane-log.md and in the step's own `observation`, which is how every change in the handover's section 5 is recorded). New step. From the +32 ceiling, 14 frames at 190 deg/s is 44.3 degrees, landing at about -12.3 -- pitch_start_deg. The block hands the camera on as it found it, so the night and weather frames that follow in some blocks are comparable with the day frames above them.

### X07-051 — what the The Pond grid recorded
- expected: a note event carries this observation into events.jsonl
- actual: Operator record for The Pond: E.7's per-region record: vegetation density impression with a paired frame, creature/trainer/resource cadence within the region (counts from live queries), empty areas (position + frame), invalid prop placement, water intrusion, collision anomalies, repetition, sightlines to the next landmark, path/road legibility, environmental storytelling present or absent, regional identity ('could a player name where they are from this frame alone' -- operator observation), and vision fit.
- events: t=31.20
- verdict: PASS

### X07-052 — DIAG PIN: The Pond, night
- expected: the WorldLook clock is pinned to hour 23.0 and the weather to 'clear' through the game's own _apply_blended/set_weather, then both nodes are frozen, and the harness reports the clock reading it actually got -- so the frames below carry the state their ids claim
- actual: DIAG pinned weather=clear, hour=23.00 and froze WorldLook, WorldWeather; clock now reads hour=23.00 preset=golden weather=clear
- events: t=31.33
- verdict: PASS
- observation: SANCTIONED CHANGE to a FROZEN file (tools/gate_f/** is frozen; the run handover's standing rules permit a single genuinely blocking error to be fixed when it is recorded both in ralph/reports/gate-f-lane-log.md and in the step's own `observation`, which is how every change in the handover's section 5 is recorded). Was a `note` reading: "E.7: 'Day/night/weather variants where materially relevant (village, pond, stronghold approach at minimum)'. The clock is pinned and frozen at the night preset. This note is the pin's telemetry record, marked DIAG." -- a note is not a pin. The 2026-08-25 run proves it: clock_hour ran 8.16-11.14 and weather stayed 'clear' for all 135 route rows, so this step's own frames were captured at mid-morning whatever their ids said. `hour` rather than `preset` because it drives world_look.gd::_apply_blended, the same continuous path _process uses, and because it makes the clock_hour recorded on every manifest row below honest -- a preset snap would have left the telemetry still reading morning and a future reader repeating this exact mistake. Pinned AFTER the settle, per _step_pin_clock's own ordering note.

### X07-053 — GF-AUD-the_pond-night-gameplay
- expected: the night variant's paired hud:on context frame at The Pond
- actual: DELEGATED GF-AUD-the_pond-night-gameplay to capture lane X07C (this is the logic lane; it takes no frames)
- events: t=31.33
- verdict: DELEGATED

### X07-054 — GF-AUD-the_pond-night-arrival
- expected: the night variant's composition frame at The Pond, paired with the hud:on frame captured seconds earlier
- actual: DELEGATED GF-AUD-the_pond-night-arrival to capture lane X07C (this is the logic lane; it takes no frames)
- events: t=31.33
- verdict: DELEGATED

### X07-055 — DIAG PIN: The Pond, weather variant (fog)
- expected: the WorldLook clock is pinned to hour 11.0 and the weather to 'fog' through the game's own _apply_blended/set_weather, then both nodes are frozen, and the harness reports the clock reading it actually got -- so the frames below carry the state their ids claim
- actual: DIAG pinned weather=fog, hour=11.00 and froze WorldLook, WorldWeather; clock now reads hour=11.00 preset=day weather=fog
- events: t=31.47
- verdict: PASS
- observation: SANCTIONED CHANGE to a FROZEN file (tools/gate_f/** is frozen; the run handover's standing rules permit a single genuinely blocking error to be fixed when it is recorded both in ralph/reports/gate-f-lane-log.md and in the step's own `observation`, which is how every change in the handover's section 5 is recorded). Was a `note` reading: "The clock and the weather preset are pinned and frozen for the weather variant. This note is the pin's telemetry record, marked DIAG." -- a note is not a pin. The 2026-08-25 run proves it: clock_hour ran 8.16-11.14 and weather stayed 'clear' for all 135 route rows, so this step's own frames were captured at mid-morning whatever their ids said. `hour` rather than `preset` because it drives world_look.gd::_apply_blended, the same continuous path _process uses, and because it makes the clock_hour recorded on every manifest row below honest -- a preset snap would have left the telemetry still reading morning and a future reader repeating this exact mistake. Pinned AFTER the settle, per _step_pin_clock's own ordering note.

### X07-056 — GF-AUD-the_pond-weather-gameplay
- expected: the weather variant's paired hud:on context frame at The Pond
- actual: DELEGATED GF-AUD-the_pond-weather-gameplay to capture lane X07C (this is the logic lane; it takes no frames)
- events: t=31.47
- verdict: DELEGATED

### X07-057 — GF-AUD-the_pond-weather-arrival
- expected: the weather variant's composition frame at The Pond, paired with the hud:on frame captured seconds earlier
- actual: DELEGATED GF-AUD-the_pond-weather-arrival to capture lane X07C (this is the logic lane; it takes no frames)
- events: t=31.47
- verdict: DELEGATED

### X07-058 — region: The Old Quarry
- expected: a note event carries this observation into events.jsonl
- actual: E.7 audit grid for The Old Quarry, at the region's own centre (403,1794) from data/config/map_landmarks.json. E.7's per-region record: vegetation density impression with a paired frame, creature/trainer/resource cadence within the region (counts from live queries), empty areas (position + frame), invalid prop placement, water intrusion, collision anomalies, repetition, sightlines to the next landmark, path/road legibility, environmental storytelling present or absent, regional identity ('could a player name where they are from this frame alone' -- operator observation), and vision fit.
- events: t=31.47
- verdict: PASS

### X07-059 — DIAG: go to The Old Quarry
- expected: the player stands at The Old Quarry. A DIAG jump rather than a walk, because this audits the world and not the route; the distance and dead-travel accumulators reset so nothing about this jump can reach a pacing figure.
- actual: DIAG teleport to (403, 1794); distance/dead-travel accumulators reset
- events: t=32.47
- verdict: PASS

### X07-060 — the teleport landed where it said (The Old Quarry)
- expected: the player is on the region centre, standing on the ground rather than falling through it
- actual: 0.0 m from (403, 1794), wanted within 5.0
- events: t=32.47
- verdict: PASS

### X07-061 — region containment agrees (The Old Quarry)
- expected: map_state.gd's own containment puts the player in the_old_quarry
- actual: region=the_old_quarry (wanted the_old_quarry)
- events: t=32.47
- verdict: PASS

### X07-062 — count what lives here (The Old Quarry)
- expected: creature/trainer/resource cadence within the region, counted from live queries
- actual: 1014 points of interest in the tree
- events: t=32.47
- verdict: PASS

### X07-063 — DIAG PIN: The Old Quarry, day
- expected: the WorldLook clock is pinned to hour 11.0 and the weather to 'clear' through the game's own _apply_blended/set_weather, then both nodes are frozen, and the harness reports the clock reading it actually got -- so the frames below carry the state their ids claim
- actual: DIAG pinned weather=clear, hour=11.00 and froze WorldLook, WorldWeather; clock now reads hour=11.00 preset=day weather=clear
- events: t=32.60
- verdict: PASS
- observation: SANCTIONED CHANGE to a FROZEN file (tools/gate_f/** is frozen; the run handover's standing rules permit a single genuinely blocking error to be fixed when it is recorded both in ralph/reports/gate-f-lane-log.md and in the step's own `observation`, which is how every change in the handover's section 5 is recorded). Was a `note` reading: "The WorldLook clock is pinned and frozen at the region's day preset before the frames below, using the capture_band3_region.gd pattern rather than the unpinned variant. This note is the pin's telemetry record, marked DIAG." -- a note is not a pin. The 2026-08-25 run proves it: clock_hour ran 8.16-11.14 and weather stayed 'clear' for all 135 route rows, so this step's own frames were captured at mid-morning whatever their ids said. `hour` rather than `preset` because it drives world_look.gd::_apply_blended, the same continuous path _process uses, and because it makes the clock_hour recorded on every manifest row below honest -- a preset snap would have left the telemetry still reading morning and a future reader repeating this exact mistake. Pinned AFTER the settle, per _step_pin_clock's own ordering note.

### X07-064 — face the intended entry route direction (The Old Quarry)
- expected: the camera turns on the right stick to the intended entry route direction, so the arrival frame is taken from where a player would arrive
- actual: camera turned to 8 deg in 0 frames
- events: t=32.62
- verdict: PASS
- observation: SANCTIONED CHANGE to a FROZEN file (tools/gate_f/** is frozen; the run handover's standing rules permit a single genuinely blocking error to be fixed when it is recorded both in ralph/reports/gate-f-lane-log.md and in the step's own `observation`, which is how every change in the handover's section 5 is recorded). Unchanged as a step. It is now the entry-route bearing for the `gameplay` and `arrival` frames only; the four other variants each get their own bearing below. The 2026-08-25 run took all six frames from this one camera, which is why the visual pass measured `arrival`, `gameplay` and `landmark` differing by 0.1-2.3% of pixels: three of six variant slots were one shot. The position itself is NOT moved -- E.7 fixes it before play and forbids moving it to improve a composition; only the bearing and the pitch change.

### X07-065 — GF-AUD-the_old_quarry-gameplay
- expected: normal-gameplay frame (HUD on) at The Old Quarry, HUD on. This is the paired hud:on context frame for the five hud:off frames that follow it in this block (protocol G).
- actual: DELEGATED GF-AUD-the_old_quarry-gameplay to capture lane X07C (this is the logic lane; it takes no frames)
- events: t=32.62
- verdict: DELEGATED

### X07-066 — GF-AUD-the_old_quarry-arrival
- expected: arrival view (from the intended entry route direction) at The Old Quarry, HUD off. A composition audit frame; its paired hud:on context frame is the gameplay frame captured seconds earlier in this block.
- actual: DELEGATED GF-AUD-the_old_quarry-arrival to capture lane X07C (this is the logic lane; it takes no frames)
- events: t=32.62
- verdict: DELEGATED

### X07-064a — aim the landmark frame (the_old_quarry)
- expected: the camera turns on the right stick to a bearing the arrival frame does not already cover, so the landmark slot captures its own shot
- actual: camera turned to -64 deg in 75 frames
- events: t=33.88
- verdict: PASS
- observation: SANCTIONED CHANGE to a FROZEN file (tools/gate_f/** is frozen; the run handover's standing rules permit a single genuinely blocking error to be fixed when it is recorded both in ralph/reports/gate-f-lane-log.md and in the step's own `observation`, which is how every change in the handover's section 5 is recorded). New step. data/config/map_landmarks.json carries no landmark for this region more than 20 m from this position, so there is no authored landmark to aim at and one is not invented here. The bearing is the entry bearing turned 72 degrees, the first arm of the five-way compass sweep the other variants complete -- a different sixth of the region rather than the same sixth six times.

### X07-067 — GF-AUD-the_old_quarry-landmark
- expected: signature landmark at The Old Quarry, HUD off. A composition audit frame; its paired hud:on context frame is the gameplay frame captured seconds earlier in this block.
- actual: DELEGATED GF-AUD-the_old_quarry-landmark to capture lane X07C (this is the logic lane; it takes no frames)
- events: t=33.88
- verdict: DELEGATED

### X07-064b — aim the ecology frame (the_old_quarry)
- expected: the camera turns on the right stick to the entry bearing turned 144 degrees
- actual: camera turned to -136 deg in 95 frames
- events: t=35.48
- verdict: PASS
- observation: SANCTIONED CHANGE to a FROZEN file (tools/gate_f/** is frozen; the run handover's standing rules permit a single genuinely blocking error to be fixed when it is recorded both in ralph/reports/gate-f-lane-log.md and in the step's own `observation`, which is how every change in the handover's section 5 is recorded). New step. Third arm of the compass sweep. The ecology frame asks what lives here, and asking it down the same sightline as the arrival frame can only ever answer for that sightline.

### X07-069 — GF-AUD-the_old_quarry-ecology
- expected: ecology/activity frame (wilds visible behaving) at The Old Quarry, HUD off. A composition audit frame; its paired hud:on context frame is the gameplay frame captured seconds earlier in this block.
- actual: DELEGATED GF-AUD-the_old_quarry-ecology to capture lane X07C (this is the logic lane; it takes no frames)
- events: t=35.48
- verdict: DELEGATED

### X07-064c — aim the vegetation frame (the_old_quarry)
- expected: the camera turns on the right stick to the entry bearing turned 216 degrees
- actual: camera turned to 152 deg in 95 frames
- events: t=37.08
- verdict: PASS
- observation: SANCTIONED CHANGE to a FROZEN file (tools/gate_f/** is frozen; the run handover's standing rules permit a single genuinely blocking error to be fixed when it is recorded both in ralph/reports/gate-f-lane-log.md and in the step's own `observation`, which is how every change in the handover's section 5 is recorded). New step. Fourth arm of the compass sweep. The visual pass's finding that grass tufts occur 'in one narrow band and nowhere else on the visible floor' is a claim about one bearing until the audit looks down more than one.

### X07-070 — GF-AUD-the_old_quarry-vegetation
- expected: vegetation density impression with a paired frame at The Old Quarry, HUD off. A composition audit frame; its paired hud:on context frame is the gameplay frame captured seconds earlier in this block.
- actual: DELEGATED GF-AUD-the_old_quarry-vegetation to capture lane X07C (this is the logic lane; it takes no frames)
- events: t=37.08
- verdict: DELEGATED

### X07-064d — back to the entry bearing for the ground frame (the_old_quarry)
- expected: the camera returns to the entry-route bearing
- actual: camera turned to 8 deg in 150 frames
- events: t=39.60
- verdict: PASS
- observation: SANCTIONED CHANGE to a FROZEN file (tools/gate_f/** is frozen; the run handover's standing rules permit a single genuinely blocking error to be fixed when it is recorded both in ralph/reports/gate-f-lane-log.md and in the step's own `observation`, which is how every change in the handover's section 5 is recorded). New step. The terrain frame is pitched down the route a player actually walks in on, so it is returned to the entry bearing first. It is also what leaves the camera on the entry bearing for the night and weather variants that follow in some blocks.

### X07-064e — pitch down to the ground (the_old_quarry)
- expected: the right stick pitches the camera down to its floor (camera.pitch_min_deg, -60) -- a ground close view instead of a sixth copy of the standing shot
- actual: right stick held (0.00, 1.00) for 40 frames
- events: t=40.28
- verdict: PASS
- observation: SANCTIONED CHANGE to a FROZEN file (tools/gate_f/** is frozen; the run handover's standing rules permit a single genuinely blocking error to be fixed when it is recorded both in ralph/reports/gate-f-lane-log.md and in the step's own `observation`, which is how every change in the handover's section 5 is recorded). New step. 40 frames at full deflection is 0.67 s against movement.json's 190 deg/s, which is 127 degrees of travel into a 48-degree range: the pitch CLAMPS at its floor, so this lands on exactly -60 every time rather than on whatever the frame budget happened to buy.

### X07-068 — GF-AUD-the_old_quarry-terrain
- expected: terrain/ground close view at The Old Quarry, HUD off. A composition audit frame; its paired hud:on context frame is the gameplay frame captured seconds earlier in this block.
- actual: DELEGATED GF-AUD-the_old_quarry-terrain to capture lane X07C (this is the logic lane; it takes no frames)
- events: t=40.28
- verdict: DELEGATED

### X07-064f — pitch back up to the ceiling (the_old_quarry)
- expected: the right stick pitches the camera up to its ceiling (camera.pitch_max_deg, 32)
- actual: right stick held (0.00, -1.00) for 40 frames
- events: t=40.97
- verdict: PASS
- observation: SANCTIONED CHANGE to a FROZEN file (tools/gate_f/** is frozen; the run handover's standing rules permit a single genuinely blocking error to be fixed when it is recorded both in ralph/reports/gate-f-lane-log.md and in the step's own `observation`, which is how every change in the handover's section 5 is recorded). New step. Half of restoring the camera deterministically: drive it to the OTHER clamp first, so the next step starts from a known angle rather than from wherever this block left it.

### X07-064g — settle the pitch back to its start (the_old_quarry)
- expected: the camera returns to roughly camera.pitch_start_deg (-12), where the block found it
- actual: right stick held (0.00, 1.00) for 14 frames
- events: t=41.22
- verdict: PASS
- observation: SANCTIONED CHANGE to a FROZEN file (tools/gate_f/** is frozen; the run handover's standing rules permit a single genuinely blocking error to be fixed when it is recorded both in ralph/reports/gate-f-lane-log.md and in the step's own `observation`, which is how every change in the handover's section 5 is recorded). New step. From the +32 ceiling, 14 frames at 190 deg/s is 44.3 degrees, landing at about -12.3 -- pitch_start_deg. The block hands the camera on as it found it, so the night and weather frames that follow in some blocks are comparable with the day frames above them.

### X07-071 — what the The Old Quarry grid recorded
- expected: a note event carries this observation into events.jsonl
- actual: Operator record for The Old Quarry: E.7's per-region record: vegetation density impression with a paired frame, creature/trainer/resource cadence within the region (counts from live queries), empty areas (position + frame), invalid prop placement, water intrusion, collision anomalies, repetition, sightlines to the next landmark, path/road legibility, environmental storytelling present or absent, regional identity ('could a player name where they are from this frame alone' -- operator observation), and vision fit.
- events: t=41.22
- verdict: PASS

### X07-072 — region: The Burrow Warrens
- expected: a note event carries this observation into events.jsonl
- actual: E.7 audit grid for The Burrow Warrens, at the region's own centre (-420,2470) from data/config/map_landmarks.json. E.7's per-region record: vegetation density impression with a paired frame, creature/trainer/resource cadence within the region (counts from live queries), empty areas (position + frame), invalid prop placement, water intrusion, collision anomalies, repetition, sightlines to the next landmark, path/road legibility, environmental storytelling present or absent, regional identity ('could a player name where they are from this frame alone' -- operator observation), and vision fit.
- events: t=41.22
- verdict: PASS

### X07-073 — DIAG: go to The Burrow Warrens
- expected: the player stands at The Burrow Warrens. A DIAG jump rather than a walk, because this audits the world and not the route; the distance and dead-travel accumulators reset so nothing about this jump can reach a pacing figure.
- actual: DIAG teleport to (-420, 2470); distance/dead-travel accumulators reset
- events: t=42.22
- verdict: PASS

### X07-074 — the teleport landed where it said (The Burrow Warrens)
- expected: the player is on the region centre, standing on the ground rather than falling through it
- actual: 0.0 m from (-420, 2470), wanted within 5.0
- events: t=42.22
- verdict: PASS

### X07-075 — region containment agrees (The Burrow Warrens)
- expected: map_state.gd's own containment puts the player in the_burrow_warrens
- actual: region=the_burrow_warrens (wanted the_burrow_warrens)
- events: t=42.22
- verdict: PASS

### X07-076 — count what lives here (The Burrow Warrens)
- expected: creature/trainer/resource cadence within the region, counted from live queries
- actual: 1014 points of interest in the tree
- events: t=42.22
- verdict: PASS

### X07-077 — DIAG PIN: The Burrow Warrens, day
- expected: the WorldLook clock is pinned to hour 11.0 and the weather to 'clear' through the game's own _apply_blended/set_weather, then both nodes are frozen, and the harness reports the clock reading it actually got -- so the frames below carry the state their ids claim
- actual: DIAG pinned weather=clear, hour=11.00 and froze WorldLook, WorldWeather; clock now reads hour=11.00 preset=day weather=clear
- events: t=42.35
- verdict: PASS
- observation: SANCTIONED CHANGE to a FROZEN file (tools/gate_f/** is frozen; the run handover's standing rules permit a single genuinely blocking error to be fixed when it is recorded both in ralph/reports/gate-f-lane-log.md and in the step's own `observation`, which is how every change in the handover's section 5 is recorded). Was a `note` reading: "The WorldLook clock is pinned and frozen at the region's day preset before the frames below, using the capture_band3_region.gd pattern rather than the unpinned variant. This note is the pin's telemetry record, marked DIAG." -- a note is not a pin. The 2026-08-25 run proves it: clock_hour ran 8.16-11.14 and weather stayed 'clear' for all 135 route rows, so this step's own frames were captured at mid-morning whatever their ids said. `hour` rather than `preset` because it drives world_look.gd::_apply_blended, the same continuous path _process uses, and because it makes the clock_hour recorded on every manifest row below honest -- a preset snap would have left the telemetry still reading morning and a future reader repeating this exact mistake. Pinned AFTER the settle, per _step_pin_clock's own ordering note.

### X07-078 — face the intended entry route direction (The Burrow Warrens)
- expected: the camera turns on the right stick to the intended entry route direction, so the arrival frame is taken from where a player would arrive
- actual: camera turned to 8 deg in 0 frames
- events: t=42.37
- verdict: PASS
- observation: SANCTIONED CHANGE to a FROZEN file (tools/gate_f/** is frozen; the run handover's standing rules permit a single genuinely blocking error to be fixed when it is recorded both in ralph/reports/gate-f-lane-log.md and in the step's own `observation`, which is how every change in the handover's section 5 is recorded). Unchanged as a step. It is now the entry-route bearing for the `gameplay` and `arrival` frames only; the four other variants each get their own bearing below. The 2026-08-25 run took all six frames from this one camera, which is why the visual pass measured `arrival`, `gameplay` and `landmark` differing by 0.1-2.3% of pixels: three of six variant slots were one shot. The position itself is NOT moved -- E.7 fixes it before play and forbids moving it to improve a composition; only the bearing and the pitch change.

### X07-079 — GF-AUD-the_burrow_warrens-gameplay
- expected: normal-gameplay frame (HUD on) at The Burrow Warrens, HUD on. This is the paired hud:on context frame for the five hud:off frames that follow it in this block (protocol G).
- actual: DELEGATED GF-AUD-the_burrow_warrens-gameplay to capture lane X07C (this is the logic lane; it takes no frames)
- events: t=42.37
- verdict: DELEGATED

### X07-080 — GF-AUD-the_burrow_warrens-arrival
- expected: arrival view (from the intended entry route direction) at The Burrow Warrens, HUD off. A composition audit frame; its paired hud:on context frame is the gameplay frame captured seconds earlier in this block.
- actual: DELEGATED GF-AUD-the_burrow_warrens-arrival to capture lane X07C (this is the logic lane; it takes no frames)
- events: t=42.37
- verdict: DELEGATED

### X07-078a — aim the landmark frame (the_burrow_warrens)
- expected: the camera turns on the right stick to a bearing the arrival frame does not already cover, so the landmark slot captures its own shot
- actual: camera turned to -64 deg in 74 frames
- events: t=43.62
- verdict: PASS
- observation: SANCTIONED CHANGE to a FROZEN file (tools/gate_f/** is frozen; the run handover's standing rules permit a single genuinely blocking error to be fixed when it is recorded both in ralph/reports/gate-f-lane-log.md and in the step's own `observation`, which is how every change in the handover's section 5 is recorded). New step. data/config/map_landmarks.json carries no landmark for this region more than 20 m from this position, so there is no authored landmark to aim at and one is not invented here. The bearing is the entry bearing turned 72 degrees, the first arm of the five-way compass sweep the other variants complete -- a different sixth of the region rather than the same sixth six times.

### X07-081 — GF-AUD-the_burrow_warrens-landmark
- expected: signature landmark at The Burrow Warrens, HUD off. A composition audit frame; its paired hud:on context frame is the gameplay frame captured seconds earlier in this block.
- actual: DELEGATED GF-AUD-the_burrow_warrens-landmark to capture lane X07C (this is the logic lane; it takes no frames)
- events: t=43.62
- verdict: DELEGATED

### X07-078b — aim the ecology frame (the_burrow_warrens)
- expected: the camera turns on the right stick to the entry bearing turned 144 degrees
- actual: camera turned to -136 deg in 95 frames
- events: t=45.22
- verdict: PASS
- observation: SANCTIONED CHANGE to a FROZEN file (tools/gate_f/** is frozen; the run handover's standing rules permit a single genuinely blocking error to be fixed when it is recorded both in ralph/reports/gate-f-lane-log.md and in the step's own `observation`, which is how every change in the handover's section 5 is recorded). New step. Third arm of the compass sweep. The ecology frame asks what lives here, and asking it down the same sightline as the arrival frame can only ever answer for that sightline.

### X07-083 — GF-AUD-the_burrow_warrens-ecology
- expected: ecology/activity frame (wilds visible behaving) at The Burrow Warrens, HUD off. A composition audit frame; its paired hud:on context frame is the gameplay frame captured seconds earlier in this block.
- actual: DELEGATED GF-AUD-the_burrow_warrens-ecology to capture lane X07C (this is the logic lane; it takes no frames)
- events: t=45.22
- verdict: DELEGATED

### X07-078c — aim the vegetation frame (the_burrow_warrens)
- expected: the camera turns on the right stick to the entry bearing turned 216 degrees
- actual: camera turned to 152 deg in 95 frames
- events: t=46.82
- verdict: PASS
- observation: SANCTIONED CHANGE to a FROZEN file (tools/gate_f/** is frozen; the run handover's standing rules permit a single genuinely blocking error to be fixed when it is recorded both in ralph/reports/gate-f-lane-log.md and in the step's own `observation`, which is how every change in the handover's section 5 is recorded). New step. Fourth arm of the compass sweep. The visual pass's finding that grass tufts occur 'in one narrow band and nowhere else on the visible floor' is a claim about one bearing until the audit looks down more than one.

### X07-084 — GF-AUD-the_burrow_warrens-vegetation
- expected: vegetation density impression with a paired frame at The Burrow Warrens, HUD off. A composition audit frame; its paired hud:on context frame is the gameplay frame captured seconds earlier in this block.
- actual: DELEGATED GF-AUD-the_burrow_warrens-vegetation to capture lane X07C (this is the logic lane; it takes no frames)
- events: t=46.82
- verdict: DELEGATED

### X07-078d — back to the entry bearing for the ground frame (the_burrow_warrens)
- expected: the camera returns to the entry-route bearing
- actual: camera turned to 8 deg in 150 frames
- events: t=49.33
- verdict: PASS
- observation: SANCTIONED CHANGE to a FROZEN file (tools/gate_f/** is frozen; the run handover's standing rules permit a single genuinely blocking error to be fixed when it is recorded both in ralph/reports/gate-f-lane-log.md and in the step's own `observation`, which is how every change in the handover's section 5 is recorded). New step. The terrain frame is pitched down the route a player actually walks in on, so it is returned to the entry bearing first. It is also what leaves the camera on the entry bearing for the night and weather variants that follow in some blocks.

### X07-078e — pitch down to the ground (the_burrow_warrens)
- expected: the right stick pitches the camera down to its floor (camera.pitch_min_deg, -60) -- a ground close view instead of a sixth copy of the standing shot
- actual: right stick held (0.00, 1.00) for 40 frames
- events: t=50.02
- verdict: PASS
- observation: SANCTIONED CHANGE to a FROZEN file (tools/gate_f/** is frozen; the run handover's standing rules permit a single genuinely blocking error to be fixed when it is recorded both in ralph/reports/gate-f-lane-log.md and in the step's own `observation`, which is how every change in the handover's section 5 is recorded). New step. 40 frames at full deflection is 0.67 s against movement.json's 190 deg/s, which is 127 degrees of travel into a 48-degree range: the pitch CLAMPS at its floor, so this lands on exactly -60 every time rather than on whatever the frame budget happened to buy.

### X07-082 — GF-AUD-the_burrow_warrens-terrain
- expected: terrain/ground close view at The Burrow Warrens, HUD off. A composition audit frame; its paired hud:on context frame is the gameplay frame captured seconds earlier in this block.
- actual: DELEGATED GF-AUD-the_burrow_warrens-terrain to capture lane X07C (this is the logic lane; it takes no frames)
- events: t=50.02
- verdict: DELEGATED

### X07-078f — pitch back up to the ceiling (the_burrow_warrens)
- expected: the right stick pitches the camera up to its ceiling (camera.pitch_max_deg, 32)
- actual: right stick held (0.00, -1.00) for 40 frames
- events: t=50.70
- verdict: PASS
- observation: SANCTIONED CHANGE to a FROZEN file (tools/gate_f/** is frozen; the run handover's standing rules permit a single genuinely blocking error to be fixed when it is recorded both in ralph/reports/gate-f-lane-log.md and in the step's own `observation`, which is how every change in the handover's section 5 is recorded). New step. Half of restoring the camera deterministically: drive it to the OTHER clamp first, so the next step starts from a known angle rather than from wherever this block left it.

### X07-078g — settle the pitch back to its start (the_burrow_warrens)
- expected: the camera returns to roughly camera.pitch_start_deg (-12), where the block found it
- actual: right stick held (0.00, 1.00) for 14 frames
- events: t=50.95
- verdict: PASS
- observation: SANCTIONED CHANGE to a FROZEN file (tools/gate_f/** is frozen; the run handover's standing rules permit a single genuinely blocking error to be fixed when it is recorded both in ralph/reports/gate-f-lane-log.md and in the step's own `observation`, which is how every change in the handover's section 5 is recorded). New step. From the +32 ceiling, 14 frames at 190 deg/s is 44.3 degrees, landing at about -12.3 -- pitch_start_deg. The block hands the camera on as it found it, so the night and weather frames that follow in some blocks are comparable with the day frames above them.

### X07-085 — what the The Burrow Warrens grid recorded
- expected: a note event carries this observation into events.jsonl
- actual: Operator record for The Burrow Warrens: E.7's per-region record: vegetation density impression with a paired frame, creature/trainer/resource cadence within the region (counts from live queries), empty areas (position + frame), invalid prop placement, water intrusion, collision anomalies, repetition, sightlines to the next landmark, path/road legibility, environmental storytelling present or absent, regional identity ('could a player name where they are from this frame alone' -- operator observation), and vision fit.
- events: t=50.95
- verdict: PASS

### X07-086 — region: The Tether Relay
- expected: a note event carries this observation into events.jsonl
- actual: E.7 audit grid for The Tether Relay, at the region's own centre (348,3756) from data/config/map_landmarks.json. E.7's per-region record: vegetation density impression with a paired frame, creature/trainer/resource cadence within the region (counts from live queries), empty areas (position + frame), invalid prop placement, water intrusion, collision anomalies, repetition, sightlines to the next landmark, path/road legibility, environmental storytelling present or absent, regional identity ('could a player name where they are from this frame alone' -- operator observation), and vision fit.
- events: t=50.95
- verdict: PASS

### X07-087 — DIAG: go to The Tether Relay
- expected: the player stands at The Tether Relay. A DIAG jump rather than a walk, because this audits the world and not the route; the distance and dead-travel accumulators reset so nothing about this jump can reach a pacing figure.
- actual: DIAG teleport to (348, 3756); distance/dead-travel accumulators reset
- events: t=51.95
- verdict: PASS

### X07-088 — the teleport landed where it said (The Tether Relay)
- expected: the player is on the region centre, standing on the ground rather than falling through it
- actual: 0.0 m from (348, 3756), wanted within 5.0
- events: t=51.95
- verdict: PASS

### X07-089 — region containment agrees (The Tether Relay)
- expected: map_state.gd's own containment puts the player in the_tether_relay
- actual: region=the_tether_relay (wanted the_tether_relay)
- events: t=51.95
- verdict: PASS

### X07-090 — count what lives here (The Tether Relay)
- expected: creature/trainer/resource cadence within the region, counted from live queries
- actual: 1014 points of interest in the tree
- events: t=51.95
- verdict: PASS

### X07-091 — DIAG PIN: The Tether Relay, day
- expected: the WorldLook clock is pinned to hour 11.0 and the weather to 'clear' through the game's own _apply_blended/set_weather, then both nodes are frozen, and the harness reports the clock reading it actually got -- so the frames below carry the state their ids claim
- actual: DIAG pinned weather=clear, hour=11.00 and froze WorldLook, WorldWeather; clock now reads hour=11.00 preset=day weather=clear
- events: t=52.08
- verdict: PASS
- observation: SANCTIONED CHANGE to a FROZEN file (tools/gate_f/** is frozen; the run handover's standing rules permit a single genuinely blocking error to be fixed when it is recorded both in ralph/reports/gate-f-lane-log.md and in the step's own `observation`, which is how every change in the handover's section 5 is recorded). Was a `note` reading: "The WorldLook clock is pinned and frozen at the region's day preset before the frames below, using the capture_band3_region.gd pattern rather than the unpinned variant. This note is the pin's telemetry record, marked DIAG." -- a note is not a pin. The 2026-08-25 run proves it: clock_hour ran 8.16-11.14 and weather stayed 'clear' for all 135 route rows, so this step's own frames were captured at mid-morning whatever their ids said. `hour` rather than `preset` because it drives world_look.gd::_apply_blended, the same continuous path _process uses, and because it makes the clock_hour recorded on every manifest row below honest -- a preset snap would have left the telemetry still reading morning and a future reader repeating this exact mistake. Pinned AFTER the settle, per _step_pin_clock's own ordering note.

### X07-092 — face the intended entry route direction (The Tether Relay)
- expected: the camera turns on the right stick to the intended entry route direction, so the arrival frame is taken from where a player would arrive
- actual: camera turned to 8 deg in 0 frames
- events: t=52.10
- verdict: PASS
- observation: SANCTIONED CHANGE to a FROZEN file (tools/gate_f/** is frozen; the run handover's standing rules permit a single genuinely blocking error to be fixed when it is recorded both in ralph/reports/gate-f-lane-log.md and in the step's own `observation`, which is how every change in the handover's section 5 is recorded). Unchanged as a step. It is now the entry-route bearing for the `gameplay` and `arrival` frames only; the four other variants each get their own bearing below. The 2026-08-25 run took all six frames from this one camera, which is why the visual pass measured `arrival`, `gameplay` and `landmark` differing by 0.1-2.3% of pixels: three of six variant slots were one shot. The position itself is NOT moved -- E.7 fixes it before play and forbids moving it to improve a composition; only the bearing and the pitch change.

### X07-093 — GF-AUD-the_tether_relay-gameplay
- expected: normal-gameplay frame (HUD on) at The Tether Relay, HUD on. This is the paired hud:on context frame for the five hud:off frames that follow it in this block (protocol G).
- actual: DELEGATED GF-AUD-the_tether_relay-gameplay to capture lane X07C (this is the logic lane; it takes no frames)
- events: t=52.10
- verdict: DELEGATED

### X07-094 — GF-AUD-the_tether_relay-arrival
- expected: arrival view (from the intended entry route direction) at The Tether Relay, HUD off. A composition audit frame; its paired hud:on context frame is the gameplay frame captured seconds earlier in this block.
- actual: DELEGATED GF-AUD-the_tether_relay-arrival to capture lane X07C (this is the logic lane; it takes no frames)
- events: t=52.10
- verdict: DELEGATED

### X07-092a — aim the landmark frame (the_tether_relay)
- expected: the camera turns on the right stick to a bearing the arrival frame does not already cover, so the landmark slot captures its own shot
- actual: camera turned to -64 deg in 74 frames
- events: t=53.35
- verdict: PASS
- observation: SANCTIONED CHANGE to a FROZEN file (tools/gate_f/** is frozen; the run handover's standing rules permit a single genuinely blocking error to be fixed when it is recorded both in ralph/reports/gate-f-lane-log.md and in the step's own `observation`, which is how every change in the handover's section 5 is recorded). New step. data/config/map_landmarks.json carries no landmark for this region more than 20 m from this position, so there is no authored landmark to aim at and one is not invented here. The bearing is the entry bearing turned 72 degrees, the first arm of the five-way compass sweep the other variants complete -- a different sixth of the region rather than the same sixth six times.

### X07-095 — GF-AUD-the_tether_relay-landmark
- expected: signature landmark at The Tether Relay, HUD off. A composition audit frame; its paired hud:on context frame is the gameplay frame captured seconds earlier in this block.
- actual: DELEGATED GF-AUD-the_tether_relay-landmark to capture lane X07C (this is the logic lane; it takes no frames)
- events: t=53.35
- verdict: DELEGATED

### X07-092b — aim the ecology frame (the_tether_relay)
- expected: the camera turns on the right stick to the entry bearing turned 144 degrees
- actual: camera turned to -136 deg in 95 frames
- events: t=54.95
- verdict: PASS
- observation: SANCTIONED CHANGE to a FROZEN file (tools/gate_f/** is frozen; the run handover's standing rules permit a single genuinely blocking error to be fixed when it is recorded both in ralph/reports/gate-f-lane-log.md and in the step's own `observation`, which is how every change in the handover's section 5 is recorded). New step. Third arm of the compass sweep. The ecology frame asks what lives here, and asking it down the same sightline as the arrival frame can only ever answer for that sightline.

### X07-097 — GF-AUD-the_tether_relay-ecology
- expected: ecology/activity frame (wilds visible behaving) at The Tether Relay, HUD off. A composition audit frame; its paired hud:on context frame is the gameplay frame captured seconds earlier in this block.
- actual: DELEGATED GF-AUD-the_tether_relay-ecology to capture lane X07C (this is the logic lane; it takes no frames)
- events: t=54.95
- verdict: DELEGATED

### X07-092c — aim the vegetation frame (the_tether_relay)
- expected: the camera turns on the right stick to the entry bearing turned 216 degrees
- actual: camera turned to 152 deg in 95 frames
- events: t=56.55
- verdict: PASS
- observation: SANCTIONED CHANGE to a FROZEN file (tools/gate_f/** is frozen; the run handover's standing rules permit a single genuinely blocking error to be fixed when it is recorded both in ralph/reports/gate-f-lane-log.md and in the step's own `observation`, which is how every change in the handover's section 5 is recorded). New step. Fourth arm of the compass sweep. The visual pass's finding that grass tufts occur 'in one narrow band and nowhere else on the visible floor' is a claim about one bearing until the audit looks down more than one.

### X07-098 — GF-AUD-the_tether_relay-vegetation
- expected: vegetation density impression with a paired frame at The Tether Relay, HUD off. A composition audit frame; its paired hud:on context frame is the gameplay frame captured seconds earlier in this block.
- actual: DELEGATED GF-AUD-the_tether_relay-vegetation to capture lane X07C (this is the logic lane; it takes no frames)
- events: t=56.55
- verdict: DELEGATED

### X07-092d — back to the entry bearing for the ground frame (the_tether_relay)
- expected: the camera returns to the entry-route bearing
- actual: camera turned to 8 deg in 150 frames
- events: t=59.07
- verdict: PASS
- observation: SANCTIONED CHANGE to a FROZEN file (tools/gate_f/** is frozen; the run handover's standing rules permit a single genuinely blocking error to be fixed when it is recorded both in ralph/reports/gate-f-lane-log.md and in the step's own `observation`, which is how every change in the handover's section 5 is recorded). New step. The terrain frame is pitched down the route a player actually walks in on, so it is returned to the entry bearing first. It is also what leaves the camera on the entry bearing for the night and weather variants that follow in some blocks.

### X07-092e — pitch down to the ground (the_tether_relay)
- expected: the right stick pitches the camera down to its floor (camera.pitch_min_deg, -60) -- a ground close view instead of a sixth copy of the standing shot
- actual: right stick held (0.00, 1.00) for 40 frames
- events: t=59.75
- verdict: PASS
- observation: SANCTIONED CHANGE to a FROZEN file (tools/gate_f/** is frozen; the run handover's standing rules permit a single genuinely blocking error to be fixed when it is recorded both in ralph/reports/gate-f-lane-log.md and in the step's own `observation`, which is how every change in the handover's section 5 is recorded). New step. 40 frames at full deflection is 0.67 s against movement.json's 190 deg/s, which is 127 degrees of travel into a 48-degree range: the pitch CLAMPS at its floor, so this lands on exactly -60 every time rather than on whatever the frame budget happened to buy.

### X07-096 — GF-AUD-the_tether_relay-terrain
- expected: terrain/ground close view at The Tether Relay, HUD off. A composition audit frame; its paired hud:on context frame is the gameplay frame captured seconds earlier in this block.
- actual: DELEGATED GF-AUD-the_tether_relay-terrain to capture lane X07C (this is the logic lane; it takes no frames)
- events: t=59.75
- verdict: DELEGATED

### X07-092f — pitch back up to the ceiling (the_tether_relay)
- expected: the right stick pitches the camera up to its ceiling (camera.pitch_max_deg, 32)
- actual: right stick held (0.00, -1.00) for 40 frames
- events: t=60.43
- verdict: PASS
- observation: SANCTIONED CHANGE to a FROZEN file (tools/gate_f/** is frozen; the run handover's standing rules permit a single genuinely blocking error to be fixed when it is recorded both in ralph/reports/gate-f-lane-log.md and in the step's own `observation`, which is how every change in the handover's section 5 is recorded). New step. Half of restoring the camera deterministically: drive it to the OTHER clamp first, so the next step starts from a known angle rather than from wherever this block left it.

### X07-092g — settle the pitch back to its start (the_tether_relay)
- expected: the camera returns to roughly camera.pitch_start_deg (-12), where the block found it
- actual: right stick held (0.00, 1.00) for 14 frames
- events: t=60.68
- verdict: PASS
- observation: SANCTIONED CHANGE to a FROZEN file (tools/gate_f/** is frozen; the run handover's standing rules permit a single genuinely blocking error to be fixed when it is recorded both in ralph/reports/gate-f-lane-log.md and in the step's own `observation`, which is how every change in the handover's section 5 is recorded). New step. From the +32 ceiling, 14 frames at 190 deg/s is 44.3 degrees, landing at about -12.3 -- pitch_start_deg. The block hands the camera on as it found it, so the night and weather frames that follow in some blocks are comparable with the day frames above them.

### X07-099 — what the The Tether Relay grid recorded
- expected: a note event carries this observation into events.jsonl
- actual: Operator record for The Tether Relay: E.7's per-region record: vegetation density impression with a paired frame, creature/trainer/resource cadence within the region (counts from live queries), empty areas (position + frame), invalid prop placement, water intrusion, collision anomalies, repetition, sightlines to the next landmark, path/road legibility, environmental storytelling present or absent, regional identity ('could a player name where they are from this frame alone' -- operator observation), and vision fit.
- events: t=60.68
- verdict: PASS

### X07-100 — region: The Long Water
- expected: a note event carries this observation into events.jsonl
- actual: E.7 audit grid for The Long Water, at the region's own centre (-150,4200) from data/config/map_landmarks.json. E.7's per-region record: vegetation density impression with a paired frame, creature/trainer/resource cadence within the region (counts from live queries), empty areas (position + frame), invalid prop placement, water intrusion, collision anomalies, repetition, sightlines to the next landmark, path/road legibility, environmental storytelling present or absent, regional identity ('could a player name where they are from this frame alone' -- operator observation), and vision fit.
- events: t=60.68
- verdict: PASS

### X07-101 — DIAG: go to The Long Water
- expected: the player stands at The Long Water. A DIAG jump rather than a walk, because this audits the world and not the route; the distance and dead-travel accumulators reset so nothing about this jump can reach a pacing figure.
- actual: DIAG teleport to (-150, 4200); distance/dead-travel accumulators reset
- events: t=61.68
- verdict: PASS

### X07-102 — the teleport landed where it said (The Long Water)
- expected: the player is on the region centre, standing on the ground rather than falling through it
- actual: 11.3 m from (-150, 4200), wanted within 5.0
- events: t=61.68
- verdict: FAIL

### X07-103 — region containment agrees (The Long Water)
- expected: map_state.gd's own containment puts the player in the_long_water
- actual: region=the_long_water (wanted the_long_water)
- events: t=61.68
- verdict: PASS

### X07-104 — count what lives here (The Long Water)
- expected: creature/trainer/resource cadence within the region, counted from live queries
- actual: 1014 points of interest in the tree
- events: t=61.68
- verdict: PASS

### X07-105 — DIAG PIN: The Long Water, day
- expected: the WorldLook clock is pinned to hour 11.0 and the weather to 'clear' through the game's own _apply_blended/set_weather, then both nodes are frozen, and the harness reports the clock reading it actually got -- so the frames below carry the state their ids claim
- actual: DIAG pinned weather=clear, hour=11.00 and froze WorldLook, WorldWeather; clock now reads hour=11.00 preset=day weather=clear
- events: t=61.82
- verdict: PASS
- observation: SANCTIONED CHANGE to a FROZEN file (tools/gate_f/** is frozen; the run handover's standing rules permit a single genuinely blocking error to be fixed when it is recorded both in ralph/reports/gate-f-lane-log.md and in the step's own `observation`, which is how every change in the handover's section 5 is recorded). Was a `note` reading: "The WorldLook clock is pinned and frozen at the region's day preset before the frames below, using the capture_band3_region.gd pattern rather than the unpinned variant. This note is the pin's telemetry record, marked DIAG." -- a note is not a pin. The 2026-08-25 run proves it: clock_hour ran 8.16-11.14 and weather stayed 'clear' for all 135 route rows, so this step's own frames were captured at mid-morning whatever their ids said. `hour` rather than `preset` because it drives world_look.gd::_apply_blended, the same continuous path _process uses, and because it makes the clock_hour recorded on every manifest row below honest -- a preset snap would have left the telemetry still reading morning and a future reader repeating this exact mistake. Pinned AFTER the settle, per _step_pin_clock's own ordering note.

### X07-106 — face the intended entry route direction (The Long Water)
- expected: the camera turns on the right stick to the intended entry route direction, so the arrival frame is taken from where a player would arrive
- actual: camera turned to 8 deg in 0 frames
- events: t=61.83
- verdict: PASS
- observation: SANCTIONED CHANGE to a FROZEN file (tools/gate_f/** is frozen; the run handover's standing rules permit a single genuinely blocking error to be fixed when it is recorded both in ralph/reports/gate-f-lane-log.md and in the step's own `observation`, which is how every change in the handover's section 5 is recorded). Unchanged as a step. It is now the entry-route bearing for the `gameplay` and `arrival` frames only; the four other variants each get their own bearing below. The 2026-08-25 run took all six frames from this one camera, which is why the visual pass measured `arrival`, `gameplay` and `landmark` differing by 0.1-2.3% of pixels: three of six variant slots were one shot. The position itself is NOT moved -- E.7 fixes it before play and forbids moving it to improve a composition; only the bearing and the pitch change.

### X07-107 — GF-AUD-the_long_water-gameplay
- expected: normal-gameplay frame (HUD on) at The Long Water, HUD on. This is the paired hud:on context frame for the five hud:off frames that follow it in this block (protocol G).
- actual: DELEGATED GF-AUD-the_long_water-gameplay to capture lane X07C (this is the logic lane; it takes no frames)
- events: t=61.83
- verdict: DELEGATED

### X07-108 — GF-AUD-the_long_water-arrival
- expected: arrival view (from the intended entry route direction) at The Long Water, HUD off. A composition audit frame; its paired hud:on context frame is the gameplay frame captured seconds earlier in this block.
- actual: DELEGATED GF-AUD-the_long_water-arrival to capture lane X07C (this is the logic lane; it takes no frames)
- events: t=61.83
- verdict: DELEGATED

### X07-106a — aim the landmark frame (the_long_water)
- expected: the camera turns on the right stick to a bearing the arrival frame does not already cover, so the landmark slot captures its own shot
- actual: camera turned to -73 deg in 84 frames
- events: t=63.25
- verdict: PASS
- observation: SANCTIONED CHANGE to a FROZEN file (tools/gate_f/** is frozen; the run handover's standing rules permit a single genuinely blocking error to be fixed when it is recorded both in ralph/reports/gate-f-lane-log.md and in the step's own `observation`, which is how every change in the handover's section 5 is recorded). New step. data/config/map_landmarks.json carries no landmark for this region more than 20 m from this position, so there is no authored landmark to aim at and one is not invented here. The bearing is the entry bearing turned 72 degrees, the first arm of the five-way compass sweep the other variants complete -- a different sixth of the region rather than the same sixth six times.

### X07-109 — GF-AUD-the_long_water-landmark
- expected: signature landmark at The Long Water, HUD off. A composition audit frame; its paired hud:on context frame is the gameplay frame captured seconds earlier in this block.
- actual: DELEGATED GF-AUD-the_long_water-landmark to capture lane X07C (this is the logic lane; it takes no frames)
- events: t=63.25
- verdict: DELEGATED

### X07-106b — aim the ecology frame (the_long_water)
- expected: the camera turns on the right stick to the entry bearing turned 144 degrees
- actual: camera turned to -145 deg in 95 frames
- events: t=64.85
- verdict: PASS
- observation: SANCTIONED CHANGE to a FROZEN file (tools/gate_f/** is frozen; the run handover's standing rules permit a single genuinely blocking error to be fixed when it is recorded both in ralph/reports/gate-f-lane-log.md and in the step's own `observation`, which is how every change in the handover's section 5 is recorded). New step. Third arm of the compass sweep. The ecology frame asks what lives here, and asking it down the same sightline as the arrival frame can only ever answer for that sightline.

### X07-111 — GF-AUD-the_long_water-ecology
- expected: ecology/activity frame (wilds visible behaving) at The Long Water, HUD off. A composition audit frame; its paired hud:on context frame is the gameplay frame captured seconds earlier in this block.
- actual: DELEGATED GF-AUD-the_long_water-ecology to capture lane X07C (this is the logic lane; it takes no frames)
- events: t=64.85
- verdict: DELEGATED

### X07-106c — aim the vegetation frame (the_long_water)
- expected: the camera turns on the right stick to the entry bearing turned 216 degrees
- actual: camera turned to 153 deg in 87 frames
- events: t=66.32
- verdict: PASS
- observation: SANCTIONED CHANGE to a FROZEN file (tools/gate_f/** is frozen; the run handover's standing rules permit a single genuinely blocking error to be fixed when it is recorded both in ralph/reports/gate-f-lane-log.md and in the step's own `observation`, which is how every change in the handover's section 5 is recorded). New step. Fourth arm of the compass sweep. The visual pass's finding that grass tufts occur 'in one narrow band and nowhere else on the visible floor' is a claim about one bearing until the audit looks down more than one.

### X07-112 — GF-AUD-the_long_water-vegetation
- expected: vegetation density impression with a paired frame at The Long Water, HUD off. A composition audit frame; its paired hud:on context frame is the gameplay frame captured seconds earlier in this block.
- actual: DELEGATED GF-AUD-the_long_water-vegetation to capture lane X07C (this is the logic lane; it takes no frames)
- events: t=66.32
- verdict: DELEGATED

### X07-106d — back to the entry bearing for the ground frame (the_long_water)
- expected: the camera returns to the entry-route bearing
- actual: camera turned to 14 deg in 147 frames
- events: t=68.78
- verdict: PASS
- observation: SANCTIONED CHANGE to a FROZEN file (tools/gate_f/** is frozen; the run handover's standing rules permit a single genuinely blocking error to be fixed when it is recorded both in ralph/reports/gate-f-lane-log.md and in the step's own `observation`, which is how every change in the handover's section 5 is recorded). New step. The terrain frame is pitched down the route a player actually walks in on, so it is returned to the entry bearing first. It is also what leaves the camera on the entry bearing for the night and weather variants that follow in some blocks.

### X07-106e — pitch down to the ground (the_long_water)
- expected: the right stick pitches the camera down to its floor (camera.pitch_min_deg, -60) -- a ground close view instead of a sixth copy of the standing shot
- actual: right stick held (0.00, 1.00) for 40 frames
- events: t=69.47
- verdict: PASS
- observation: SANCTIONED CHANGE to a FROZEN file (tools/gate_f/** is frozen; the run handover's standing rules permit a single genuinely blocking error to be fixed when it is recorded both in ralph/reports/gate-f-lane-log.md and in the step's own `observation`, which is how every change in the handover's section 5 is recorded). New step. 40 frames at full deflection is 0.67 s against movement.json's 190 deg/s, which is 127 degrees of travel into a 48-degree range: the pitch CLAMPS at its floor, so this lands on exactly -60 every time rather than on whatever the frame budget happened to buy.

### X07-110 — GF-AUD-the_long_water-terrain
- expected: terrain/ground close view at The Long Water, HUD off. A composition audit frame; its paired hud:on context frame is the gameplay frame captured seconds earlier in this block.
- actual: DELEGATED GF-AUD-the_long_water-terrain to capture lane X07C (this is the logic lane; it takes no frames)
- events: t=69.47
- verdict: DELEGATED

### X07-106f — pitch back up to the ceiling (the_long_water)
- expected: the right stick pitches the camera up to its ceiling (camera.pitch_max_deg, 32)
- actual: right stick held (0.00, -1.00) for 40 frames
- events: t=70.15
- verdict: PASS
- observation: SANCTIONED CHANGE to a FROZEN file (tools/gate_f/** is frozen; the run handover's standing rules permit a single genuinely blocking error to be fixed when it is recorded both in ralph/reports/gate-f-lane-log.md and in the step's own `observation`, which is how every change in the handover's section 5 is recorded). New step. Half of restoring the camera deterministically: drive it to the OTHER clamp first, so the next step starts from a known angle rather than from wherever this block left it.

### X07-106g — settle the pitch back to its start (the_long_water)
- expected: the camera returns to roughly camera.pitch_start_deg (-12), where the block found it
- actual: right stick held (0.00, 1.00) for 14 frames
- events: t=70.40
- verdict: PASS
- observation: SANCTIONED CHANGE to a FROZEN file (tools/gate_f/** is frozen; the run handover's standing rules permit a single genuinely blocking error to be fixed when it is recorded both in ralph/reports/gate-f-lane-log.md and in the step's own `observation`, which is how every change in the handover's section 5 is recorded). New step. From the +32 ceiling, 14 frames at 190 deg/s is 44.3 degrees, landing at about -12.3 -- pitch_start_deg. The block hands the camera on as it found it, so the night and weather frames that follow in some blocks are comparable with the day frames above them.

### X07-113 — what the The Long Water grid recorded
- expected: a note event carries this observation into events.jsonl
- actual: Operator record for The Long Water: E.7's per-region record: vegetation density impression with a paired frame, creature/trainer/resource cadence within the region (counts from live queries), empty areas (position + frame), invalid prop placement, water intrusion, collision anomalies, repetition, sightlines to the next landmark, path/road legibility, environmental storytelling present or absent, regional identity ('could a player name where they are from this frame alone' -- operator observation), and vision fit.
- events: t=70.40
- verdict: PASS

### X07-114 — region: The Ironwood Grove
- expected: a note event carries this observation into events.jsonl
- actual: E.7 audit grid for The Ironwood Grove, at the region's own centre (-345,5060) from data/config/map_landmarks.json. E.7's per-region record: vegetation density impression with a paired frame, creature/trainer/resource cadence within the region (counts from live queries), empty areas (position + frame), invalid prop placement, water intrusion, collision anomalies, repetition, sightlines to the next landmark, path/road legibility, environmental storytelling present or absent, regional identity ('could a player name where they are from this frame alone' -- operator observation), and vision fit.
- events: t=70.40
- verdict: PASS

### X07-115 — DIAG: go to The Ironwood Grove
- expected: the player stands at The Ironwood Grove. A DIAG jump rather than a walk, because this audits the world and not the route; the distance and dead-travel accumulators reset so nothing about this jump can reach a pacing figure.
- actual: DIAG teleport to (-345, 5060); distance/dead-travel accumulators reset
- events: t=71.40
- verdict: PASS

### X07-116 — the teleport landed where it said (The Ironwood Grove)
- expected: the player is on the region centre, standing on the ground rather than falling through it
- actual: 0.0 m from (-345, 5060), wanted within 5.0
- events: t=71.40
- verdict: PASS

### X07-117 — region containment agrees (The Ironwood Grove)
- expected: map_state.gd's own containment puts the player in the_ironwood_grove
- actual: region=the_ironwood_grove (wanted the_ironwood_grove)
- events: t=71.40
- verdict: PASS

### X07-118 — count what lives here (The Ironwood Grove)
- expected: creature/trainer/resource cadence within the region, counted from live queries
- actual: 1014 points of interest in the tree
- events: t=71.40
- verdict: PASS

### X07-119 — DIAG PIN: The Ironwood Grove, day
- expected: the WorldLook clock is pinned to hour 11.0 and the weather to 'clear' through the game's own _apply_blended/set_weather, then both nodes are frozen, and the harness reports the clock reading it actually got -- so the frames below carry the state their ids claim
- actual: DIAG pinned weather=clear, hour=11.00 and froze WorldLook, WorldWeather; clock now reads hour=11.00 preset=day weather=clear
- events: t=71.53
- verdict: PASS
- observation: SANCTIONED CHANGE to a FROZEN file (tools/gate_f/** is frozen; the run handover's standing rules permit a single genuinely blocking error to be fixed when it is recorded both in ralph/reports/gate-f-lane-log.md and in the step's own `observation`, which is how every change in the handover's section 5 is recorded). Was a `note` reading: "The WorldLook clock is pinned and frozen at the region's day preset before the frames below, using the capture_band3_region.gd pattern rather than the unpinned variant. This note is the pin's telemetry record, marked DIAG." -- a note is not a pin. The 2026-08-25 run proves it: clock_hour ran 8.16-11.14 and weather stayed 'clear' for all 135 route rows, so this step's own frames were captured at mid-morning whatever their ids said. `hour` rather than `preset` because it drives world_look.gd::_apply_blended, the same continuous path _process uses, and because it makes the clock_hour recorded on every manifest row below honest -- a preset snap would have left the telemetry still reading morning and a future reader repeating this exact mistake. Pinned AFTER the settle, per _step_pin_clock's own ordering note.

### X07-120 — face the intended entry route direction (The Ironwood Grove)
- expected: the camera turns on the right stick to the intended entry route direction, so the arrival frame is taken from where a player would arrive
- actual: camera turned to 7 deg in 1 frames
- events: t=71.57
- verdict: PASS
- observation: SANCTIONED CHANGE to a FROZEN file (tools/gate_f/** is frozen; the run handover's standing rules permit a single genuinely blocking error to be fixed when it is recorded both in ralph/reports/gate-f-lane-log.md and in the step's own `observation`, which is how every change in the handover's section 5 is recorded). Unchanged as a step. It is now the entry-route bearing for the `gameplay` and `arrival` frames only; the four other variants each get their own bearing below. The 2026-08-25 run took all six frames from this one camera, which is why the visual pass measured `arrival`, `gameplay` and `landmark` differing by 0.1-2.3% of pixels: three of six variant slots were one shot. The position itself is NOT moved -- E.7 fixes it before play and forbids moving it to improve a composition; only the bearing and the pitch change.

### X07-121 — GF-AUD-the_ironwood_grove-gameplay
- expected: normal-gameplay frame (HUD on) at The Ironwood Grove, HUD on. This is the paired hud:on context frame for the five hud:off frames that follow it in this block (protocol G).
- actual: DELEGATED GF-AUD-the_ironwood_grove-gameplay to capture lane X07C (this is the logic lane; it takes no frames)
- events: t=71.57
- verdict: DELEGATED

### X07-122 — GF-AUD-the_ironwood_grove-arrival
- expected: arrival view (from the intended entry route direction) at The Ironwood Grove, HUD off. A composition audit frame; its paired hud:on context frame is the gameplay frame captured seconds earlier in this block.
- actual: DELEGATED GF-AUD-the_ironwood_grove-arrival to capture lane X07C (this is the logic lane; it takes no frames)
- events: t=71.57
- verdict: DELEGATED

### X07-120a — aim the landmark frame (the_ironwood_grove)
- expected: the camera turns on the right stick to a bearing the arrival frame does not already cover, so the landmark slot captures its own shot
- actual: camera turned to -64 deg in 72 frames
- events: t=72.78
- verdict: PASS
- observation: SANCTIONED CHANGE to a FROZEN file (tools/gate_f/** is frozen; the run handover's standing rules permit a single genuinely blocking error to be fixed when it is recorded both in ralph/reports/gate-f-lane-log.md and in the step's own `observation`, which is how every change in the handover's section 5 is recorded). New step. data/config/map_landmarks.json carries no landmark for this region more than 20 m from this position, so there is no authored landmark to aim at and one is not invented here. The bearing is the entry bearing turned 72 degrees, the first arm of the five-way compass sweep the other variants complete -- a different sixth of the region rather than the same sixth six times.

### X07-123 — GF-AUD-the_ironwood_grove-landmark
- expected: signature landmark at The Ironwood Grove, HUD off. A composition audit frame; its paired hud:on context frame is the gameplay frame captured seconds earlier in this block.
- actual: DELEGATED GF-AUD-the_ironwood_grove-landmark to capture lane X07C (this is the logic lane; it takes no frames)
- events: t=72.78
- verdict: DELEGATED

### X07-120b — aim the ecology frame (the_ironwood_grove)
- expected: the camera turns on the right stick to the entry bearing turned 144 degrees
- actual: camera turned to -136 deg in 95 frames
- events: t=74.38
- verdict: PASS
- observation: SANCTIONED CHANGE to a FROZEN file (tools/gate_f/** is frozen; the run handover's standing rules permit a single genuinely blocking error to be fixed when it is recorded both in ralph/reports/gate-f-lane-log.md and in the step's own `observation`, which is how every change in the handover's section 5 is recorded). New step. Third arm of the compass sweep. The ecology frame asks what lives here, and asking it down the same sightline as the arrival frame can only ever answer for that sightline.

### X07-125 — GF-AUD-the_ironwood_grove-ecology
- expected: ecology/activity frame (wilds visible behaving) at The Ironwood Grove, HUD off. A composition audit frame; its paired hud:on context frame is the gameplay frame captured seconds earlier in this block.
- actual: DELEGATED GF-AUD-the_ironwood_grove-ecology to capture lane X07C (this is the logic lane; it takes no frames)
- events: t=74.38
- verdict: DELEGATED

### X07-120c — aim the vegetation frame (the_ironwood_grove)
- expected: the camera turns on the right stick to the entry bearing turned 216 degrees
- actual: camera turned to 152 deg in 95 frames
- events: t=75.98
- verdict: PASS
- observation: SANCTIONED CHANGE to a FROZEN file (tools/gate_f/** is frozen; the run handover's standing rules permit a single genuinely blocking error to be fixed when it is recorded both in ralph/reports/gate-f-lane-log.md and in the step's own `observation`, which is how every change in the handover's section 5 is recorded). New step. Fourth arm of the compass sweep. The visual pass's finding that grass tufts occur 'in one narrow band and nowhere else on the visible floor' is a claim about one bearing until the audit looks down more than one.

### X07-126 — GF-AUD-the_ironwood_grove-vegetation
- expected: vegetation density impression with a paired frame at The Ironwood Grove, HUD off. A composition audit frame; its paired hud:on context frame is the gameplay frame captured seconds earlier in this block.
- actual: DELEGATED GF-AUD-the_ironwood_grove-vegetation to capture lane X07C (this is the logic lane; it takes no frames)
- events: t=75.98
- verdict: DELEGATED

### X07-120d — back to the entry bearing for the ground frame (the_ironwood_grove)
- expected: the camera returns to the entry-route bearing
- actual: camera turned to 8 deg in 150 frames
- events: t=78.50
- verdict: PASS
- observation: SANCTIONED CHANGE to a FROZEN file (tools/gate_f/** is frozen; the run handover's standing rules permit a single genuinely blocking error to be fixed when it is recorded both in ralph/reports/gate-f-lane-log.md and in the step's own `observation`, which is how every change in the handover's section 5 is recorded). New step. The terrain frame is pitched down the route a player actually walks in on, so it is returned to the entry bearing first. It is also what leaves the camera on the entry bearing for the night and weather variants that follow in some blocks.

### X07-120e — pitch down to the ground (the_ironwood_grove)
- expected: the right stick pitches the camera down to its floor (camera.pitch_min_deg, -60) -- a ground close view instead of a sixth copy of the standing shot
- actual: right stick held (0.00, 1.00) for 40 frames
- events: t=79.18
- verdict: PASS
- observation: SANCTIONED CHANGE to a FROZEN file (tools/gate_f/** is frozen; the run handover's standing rules permit a single genuinely blocking error to be fixed when it is recorded both in ralph/reports/gate-f-lane-log.md and in the step's own `observation`, which is how every change in the handover's section 5 is recorded). New step. 40 frames at full deflection is 0.67 s against movement.json's 190 deg/s, which is 127 degrees of travel into a 48-degree range: the pitch CLAMPS at its floor, so this lands on exactly -60 every time rather than on whatever the frame budget happened to buy.

### X07-124 — GF-AUD-the_ironwood_grove-terrain
- expected: terrain/ground close view at The Ironwood Grove, HUD off. A composition audit frame; its paired hud:on context frame is the gameplay frame captured seconds earlier in this block.
- actual: DELEGATED GF-AUD-the_ironwood_grove-terrain to capture lane X07C (this is the logic lane; it takes no frames)
- events: t=79.18
- verdict: DELEGATED

### X07-120f — pitch back up to the ceiling (the_ironwood_grove)
- expected: the right stick pitches the camera up to its ceiling (camera.pitch_max_deg, 32)
- actual: right stick held (0.00, -1.00) for 40 frames
- events: t=79.87
- verdict: PASS
- observation: SANCTIONED CHANGE to a FROZEN file (tools/gate_f/** is frozen; the run handover's standing rules permit a single genuinely blocking error to be fixed when it is recorded both in ralph/reports/gate-f-lane-log.md and in the step's own `observation`, which is how every change in the handover's section 5 is recorded). New step. Half of restoring the camera deterministically: drive it to the OTHER clamp first, so the next step starts from a known angle rather than from wherever this block left it.

### X07-120g — settle the pitch back to its start (the_ironwood_grove)
- expected: the camera returns to roughly camera.pitch_start_deg (-12), where the block found it
- actual: right stick held (0.00, 1.00) for 14 frames
- events: t=80.12
- verdict: PASS
- observation: SANCTIONED CHANGE to a FROZEN file (tools/gate_f/** is frozen; the run handover's standing rules permit a single genuinely blocking error to be fixed when it is recorded both in ralph/reports/gate-f-lane-log.md and in the step's own `observation`, which is how every change in the handover's section 5 is recorded). New step. From the +32 ceiling, 14 frames at 190 deg/s is 44.3 degrees, landing at about -12.3 -- pitch_start_deg. The block hands the camera on as it found it, so the night and weather frames that follow in some blocks are comparable with the day frames above them.

### X07-127 — what the The Ironwood Grove grid recorded
- expected: a note event carries this observation into events.jsonl
- actual: Operator record for The Ironwood Grove: E.7's per-region record: vegetation density impression with a paired frame, creature/trainer/resource cadence within the region (counts from live queries), empty areas (position + frame), invalid prop placement, water intrusion, collision anomalies, repetition, sightlines to the next landmark, path/road legibility, environmental storytelling present or absent, regional identity ('could a player name where they are from this frame alone' -- operator observation), and vision fit.
- events: t=80.12
- verdict: PASS

### X07-128 — region: The Ridgeline Watch
- expected: a note event carries this observation into events.jsonl
- actual: E.7 audit grid for The Ridgeline Watch, at the region's own centre (-250,6490) from data/config/map_landmarks.json. E.7's per-region record: vegetation density impression with a paired frame, creature/trainer/resource cadence within the region (counts from live queries), empty areas (position + frame), invalid prop placement, water intrusion, collision anomalies, repetition, sightlines to the next landmark, path/road legibility, environmental storytelling present or absent, regional identity ('could a player name where they are from this frame alone' -- operator observation), and vision fit.
- events: t=80.12
- verdict: PASS

### X07-129 — DIAG: go to The Ridgeline Watch
- expected: the player stands at The Ridgeline Watch. A DIAG jump rather than a walk, because this audits the world and not the route; the distance and dead-travel accumulators reset so nothing about this jump can reach a pacing figure.
- actual: DIAG teleport to (-250, 6490); distance/dead-travel accumulators reset
- events: t=81.12
- verdict: PASS

### X07-130 — the teleport landed where it said (The Ridgeline Watch)
- expected: the player is on the region centre, standing on the ground rather than falling through it
- actual: 0.0 m from (-250, 6490), wanted within 5.0
- events: t=81.12
- verdict: PASS

### X07-131 — region containment agrees (The Ridgeline Watch)
- expected: map_state.gd's own containment puts the player in the_ridgeline_watch
- actual: region=the_ridgeline_watch (wanted the_ridgeline_watch)
- events: t=81.12
- verdict: PASS

### X07-132 — count what lives here (The Ridgeline Watch)
- expected: creature/trainer/resource cadence within the region, counted from live queries
- actual: 1014 points of interest in the tree
- events: t=81.12
- verdict: PASS

### X07-133 — DIAG PIN: The Ridgeline Watch, day
- expected: the WorldLook clock is pinned to hour 11.0 and the weather to 'clear' through the game's own _apply_blended/set_weather, then both nodes are frozen, and the harness reports the clock reading it actually got -- so the frames below carry the state their ids claim
- actual: DIAG pinned weather=clear, hour=11.00 and froze WorldLook, WorldWeather; clock now reads hour=11.00 preset=day weather=clear
- events: t=81.25
- verdict: PASS
- observation: SANCTIONED CHANGE to a FROZEN file (tools/gate_f/** is frozen; the run handover's standing rules permit a single genuinely blocking error to be fixed when it is recorded both in ralph/reports/gate-f-lane-log.md and in the step's own `observation`, which is how every change in the handover's section 5 is recorded). Was a `note` reading: "The WorldLook clock is pinned and frozen at the region's day preset before the frames below, using the capture_band3_region.gd pattern rather than the unpinned variant. This note is the pin's telemetry record, marked DIAG." -- a note is not a pin. The 2026-08-25 run proves it: clock_hour ran 8.16-11.14 and weather stayed 'clear' for all 135 route rows, so this step's own frames were captured at mid-morning whatever their ids said. `hour` rather than `preset` because it drives world_look.gd::_apply_blended, the same continuous path _process uses, and because it makes the clock_hour recorded on every manifest row below honest -- a preset snap would have left the telemetry still reading morning and a future reader repeating this exact mistake. Pinned AFTER the settle, per _step_pin_clock's own ordering note.

### X07-134 — face the intended entry route direction (The Ridgeline Watch)
- expected: the camera turns on the right stick to the intended entry route direction, so the arrival frame is taken from where a player would arrive
- actual: camera turned to 8 deg in 0 frames
- events: t=81.27
- verdict: PASS
- observation: SANCTIONED CHANGE to a FROZEN file (tools/gate_f/** is frozen; the run handover's standing rules permit a single genuinely blocking error to be fixed when it is recorded both in ralph/reports/gate-f-lane-log.md and in the step's own `observation`, which is how every change in the handover's section 5 is recorded). Unchanged as a step. It is now the entry-route bearing for the `gameplay` and `arrival` frames only; the four other variants each get their own bearing below. The 2026-08-25 run took all six frames from this one camera, which is why the visual pass measured `arrival`, `gameplay` and `landmark` differing by 0.1-2.3% of pixels: three of six variant slots were one shot. The position itself is NOT moved -- E.7 fixes it before play and forbids moving it to improve a composition; only the bearing and the pitch change.

### X07-135 — GF-AUD-the_ridgeline_watch-gameplay
- expected: normal-gameplay frame (HUD on) at The Ridgeline Watch, HUD on. This is the paired hud:on context frame for the five hud:off frames that follow it in this block (protocol G).
- actual: DELEGATED GF-AUD-the_ridgeline_watch-gameplay to capture lane X07C (this is the logic lane; it takes no frames)
- events: t=81.27
- verdict: DELEGATED

### X07-136 — GF-AUD-the_ridgeline_watch-arrival
- expected: arrival view (from the intended entry route direction) at The Ridgeline Watch, HUD off. A composition audit frame; its paired hud:on context frame is the gameplay frame captured seconds earlier in this block.
- actual: DELEGATED GF-AUD-the_ridgeline_watch-arrival to capture lane X07C (this is the logic lane; it takes no frames)
- events: t=81.27
- verdict: DELEGATED

### X07-134a — aim the landmark frame (the_ridgeline_watch)
- expected: the camera turns on the right stick to a bearing the arrival frame does not already cover, so the landmark slot captures its own shot
- actual: camera turned to -64 deg in 78 frames
- events: t=82.58
- verdict: PASS
- observation: SANCTIONED CHANGE to a FROZEN file (tools/gate_f/** is frozen; the run handover's standing rules permit a single genuinely blocking error to be fixed when it is recorded both in ralph/reports/gate-f-lane-log.md and in the step's own `observation`, which is how every change in the handover's section 5 is recorded). New step. data/config/map_landmarks.json carries no landmark for this region more than 20 m from this position, so there is no authored landmark to aim at and one is not invented here. The bearing is the entry bearing turned 72 degrees, the first arm of the five-way compass sweep the other variants complete -- a different sixth of the region rather than the same sixth six times.

### X07-137 — GF-AUD-the_ridgeline_watch-landmark
- expected: signature landmark at The Ridgeline Watch, HUD off. A composition audit frame; its paired hud:on context frame is the gameplay frame captured seconds earlier in this block.
- actual: DELEGATED GF-AUD-the_ridgeline_watch-landmark to capture lane X07C (this is the logic lane; it takes no frames)
- events: t=82.58
- verdict: DELEGATED

### X07-134b — aim the ecology frame (the_ridgeline_watch)
- expected: the camera turns on the right stick to the entry bearing turned 144 degrees
- actual: camera turned to -136 deg in 95 frames
- events: t=84.18
- verdict: PASS
- observation: SANCTIONED CHANGE to a FROZEN file (tools/gate_f/** is frozen; the run handover's standing rules permit a single genuinely blocking error to be fixed when it is recorded both in ralph/reports/gate-f-lane-log.md and in the step's own `observation`, which is how every change in the handover's section 5 is recorded). New step. Third arm of the compass sweep. The ecology frame asks what lives here, and asking it down the same sightline as the arrival frame can only ever answer for that sightline.

### X07-139 — GF-AUD-the_ridgeline_watch-ecology
- expected: ecology/activity frame (wilds visible behaving) at The Ridgeline Watch, HUD off. A composition audit frame; its paired hud:on context frame is the gameplay frame captured seconds earlier in this block.
- actual: DELEGATED GF-AUD-the_ridgeline_watch-ecology to capture lane X07C (this is the logic lane; it takes no frames)
- events: t=84.18
- verdict: DELEGATED

### X07-134c — aim the vegetation frame (the_ridgeline_watch)
- expected: the camera turns on the right stick to the entry bearing turned 216 degrees
- actual: camera turned to 152 deg in 95 frames
- events: t=85.78
- verdict: PASS
- observation: SANCTIONED CHANGE to a FROZEN file (tools/gate_f/** is frozen; the run handover's standing rules permit a single genuinely blocking error to be fixed when it is recorded both in ralph/reports/gate-f-lane-log.md and in the step's own `observation`, which is how every change in the handover's section 5 is recorded). New step. Fourth arm of the compass sweep. The visual pass's finding that grass tufts occur 'in one narrow band and nowhere else on the visible floor' is a claim about one bearing until the audit looks down more than one.

### X07-140 — GF-AUD-the_ridgeline_watch-vegetation
- expected: vegetation density impression with a paired frame at The Ridgeline Watch, HUD off. A composition audit frame; its paired hud:on context frame is the gameplay frame captured seconds earlier in this block.
- actual: DELEGATED GF-AUD-the_ridgeline_watch-vegetation to capture lane X07C (this is the logic lane; it takes no frames)
- events: t=85.78
- verdict: DELEGATED

### X07-134d — back to the entry bearing for the ground frame (the_ridgeline_watch)
- expected: the camera returns to the entry-route bearing
- actual: camera turned to 8 deg in 150 frames
- events: t=88.30
- verdict: PASS
- observation: SANCTIONED CHANGE to a FROZEN file (tools/gate_f/** is frozen; the run handover's standing rules permit a single genuinely blocking error to be fixed when it is recorded both in ralph/reports/gate-f-lane-log.md and in the step's own `observation`, which is how every change in the handover's section 5 is recorded). New step. The terrain frame is pitched down the route a player actually walks in on, so it is returned to the entry bearing first. It is also what leaves the camera on the entry bearing for the night and weather variants that follow in some blocks.

### X07-134e — pitch down to the ground (the_ridgeline_watch)
- expected: the right stick pitches the camera down to its floor (camera.pitch_min_deg, -60) -- a ground close view instead of a sixth copy of the standing shot
- actual: right stick held (0.00, 1.00) for 40 frames
- events: t=88.98
- verdict: PASS
- observation: SANCTIONED CHANGE to a FROZEN file (tools/gate_f/** is frozen; the run handover's standing rules permit a single genuinely blocking error to be fixed when it is recorded both in ralph/reports/gate-f-lane-log.md and in the step's own `observation`, which is how every change in the handover's section 5 is recorded). New step. 40 frames at full deflection is 0.67 s against movement.json's 190 deg/s, which is 127 degrees of travel into a 48-degree range: the pitch CLAMPS at its floor, so this lands on exactly -60 every time rather than on whatever the frame budget happened to buy.

### X07-138 — GF-AUD-the_ridgeline_watch-terrain
- expected: terrain/ground close view at The Ridgeline Watch, HUD off. A composition audit frame; its paired hud:on context frame is the gameplay frame captured seconds earlier in this block.
- actual: DELEGATED GF-AUD-the_ridgeline_watch-terrain to capture lane X07C (this is the logic lane; it takes no frames)
- events: t=88.98
- verdict: DELEGATED

### X07-134f — pitch back up to the ceiling (the_ridgeline_watch)
- expected: the right stick pitches the camera up to its ceiling (camera.pitch_max_deg, 32)
- actual: right stick held (0.00, -1.00) for 40 frames
- events: t=89.67
- verdict: PASS
- observation: SANCTIONED CHANGE to a FROZEN file (tools/gate_f/** is frozen; the run handover's standing rules permit a single genuinely blocking error to be fixed when it is recorded both in ralph/reports/gate-f-lane-log.md and in the step's own `observation`, which is how every change in the handover's section 5 is recorded). New step. Half of restoring the camera deterministically: drive it to the OTHER clamp first, so the next step starts from a known angle rather than from wherever this block left it.

### X07-134g — settle the pitch back to its start (the_ridgeline_watch)
- expected: the camera returns to roughly camera.pitch_start_deg (-12), where the block found it
- actual: right stick held (0.00, 1.00) for 14 frames
- events: t=89.92
- verdict: PASS
- observation: SANCTIONED CHANGE to a FROZEN file (tools/gate_f/** is frozen; the run handover's standing rules permit a single genuinely blocking error to be fixed when it is recorded both in ralph/reports/gate-f-lane-log.md and in the step's own `observation`, which is how every change in the handover's section 5 is recorded). New step. From the +32 ceiling, 14 frames at 190 deg/s is 44.3 degrees, landing at about -12.3 -- pitch_start_deg. The block hands the camera on as it found it, so the night and weather frames that follow in some blocks are comparable with the day frames above them.

### X07-141 — what the The Ridgeline Watch grid recorded
- expected: a note event carries this observation into events.jsonl
- actual: Operator record for The Ridgeline Watch: E.7's per-region record: vegetation density impression with a paired frame, creature/trainer/resource cadence within the region (counts from live queries), empty areas (position + frame), invalid prop placement, water intrusion, collision anomalies, repetition, sightlines to the next landmark, path/road legibility, environmental storytelling present or absent, regional identity ('could a player name where they are from this frame alone' -- operator observation), and vision fit.
- events: t=89.92
- verdict: PASS

### X07-142 — region: the Stronghold approach
- expected: a note event carries this observation into events.jsonl
- actual: E.7 audit grid for the Stronghold approach, at the region's own centre (-65,7028) from data/config/map_landmarks.json. E.7's per-region record: vegetation density impression with a paired frame, creature/trainer/resource cadence within the region (counts from live queries), empty areas (position + frame), invalid prop placement, water intrusion, collision anomalies, repetition, sightlines to the next landmark, path/road legibility, environmental storytelling present or absent, regional identity ('could a player name where they are from this frame alone' -- operator observation), and vision fit.
- events: t=89.92
- verdict: PASS

### X07-143 — DIAG: go to the Stronghold approach
- expected: the player stands at the Stronghold approach. A DIAG jump rather than a walk, because this audits the world and not the route; the distance and dead-travel accumulators reset so nothing about this jump can reach a pacing figure.
- actual: DIAG teleport to (-65, 7028); distance/dead-travel accumulators reset
- events: t=90.92
- verdict: PASS

### X07-144 — the teleport landed where it said (the Stronghold approach)
- expected: the player is on the region centre, standing on the ground rather than falling through it
- actual: 0.0 m from (-65, 7028), wanted within 5.0
- events: t=90.92
- verdict: PASS

### X07-145 — region containment agrees (the Stronghold approach)
- expected: map_state.gd's own containment puts the player in stronghold_approach
- actual: region=corridor (wanted stronghold_approach)
- events: t=90.92
- verdict: FAIL

### X07-146 — count what lives here (the Stronghold approach)
- expected: creature/trainer/resource cadence within the region, counted from live queries
- actual: 1014 points of interest in the tree
- events: t=90.92
- verdict: PASS

### X07-147 — DIAG PIN: the Stronghold approach, day
- expected: the WorldLook clock is pinned to hour 11.0 and the weather to 'clear' through the game's own _apply_blended/set_weather, then both nodes are frozen, and the harness reports the clock reading it actually got -- so the frames below carry the state their ids claim
- actual: DIAG pinned weather=clear, hour=11.00 and froze WorldLook, WorldWeather; clock now reads hour=11.00 preset=day weather=clear
- events: t=91.05
- verdict: PASS
- observation: SANCTIONED CHANGE to a FROZEN file (tools/gate_f/** is frozen; the run handover's standing rules permit a single genuinely blocking error to be fixed when it is recorded both in ralph/reports/gate-f-lane-log.md and in the step's own `observation`, which is how every change in the handover's section 5 is recorded). Was a `note` reading: "The WorldLook clock is pinned and frozen at the region's day preset before the frames below, using the capture_band3_region.gd pattern rather than the unpinned variant. This note is the pin's telemetry record, marked DIAG." -- a note is not a pin. The 2026-08-25 run proves it: clock_hour ran 8.16-11.14 and weather stayed 'clear' for all 135 route rows, so this step's own frames were captured at mid-morning whatever their ids said. `hour` rather than `preset` because it drives world_look.gd::_apply_blended, the same continuous path _process uses, and because it makes the clock_hour recorded on every manifest row below honest -- a preset snap would have left the telemetry still reading morning and a future reader repeating this exact mistake. Pinned AFTER the settle, per _step_pin_clock's own ordering note.

### X07-148 — face the intended entry route direction (the Stronghold approach)
- expected: the camera turns on the right stick to the intended entry route direction, so the arrival frame is taken from where a player would arrive
- actual: camera turned to 8 deg in 0 frames
- events: t=91.07
- verdict: PASS
- observation: SANCTIONED CHANGE to a FROZEN file (tools/gate_f/** is frozen; the run handover's standing rules permit a single genuinely blocking error to be fixed when it is recorded both in ralph/reports/gate-f-lane-log.md and in the step's own `observation`, which is how every change in the handover's section 5 is recorded). Unchanged as a step. It is now the entry-route bearing for the `gameplay` and `arrival` frames only; the four other variants each get their own bearing below. The 2026-08-25 run took all six frames from this one camera, which is why the visual pass measured `arrival`, `gameplay` and `landmark` differing by 0.1-2.3% of pixels: three of six variant slots were one shot. The position itself is NOT moved -- E.7 fixes it before play and forbids moving it to improve a composition; only the bearing and the pitch change.

### X07-149 — GF-AUD-stronghold_approach-gameplay
- expected: normal-gameplay frame (HUD on) at the Stronghold approach, HUD on. This is the paired hud:on context frame for the five hud:off frames that follow it in this block (protocol G).
- actual: DELEGATED GF-AUD-stronghold_approach-gameplay to capture lane X07C (this is the logic lane; it takes no frames)
- events: t=91.07
- verdict: DELEGATED

### X07-150 — GF-AUD-stronghold_approach-arrival
- expected: arrival view (from the intended entry route direction) at the Stronghold approach, HUD off. A composition audit frame; its paired hud:on context frame is the gameplay frame captured seconds earlier in this block.
- actual: DELEGATED GF-AUD-stronghold_approach-arrival to capture lane X07C (this is the logic lane; it takes no frames)
- events: t=91.07
- verdict: DELEGATED

### X07-148a — aim the landmark frame (stronghold_approach)
- expected: the camera turns on the right stick to a bearing the arrival frame does not already cover, so the landmark slot captures its own shot
- actual: camera turned to -151 deg in 141 frames
- events: t=93.43
- verdict: PASS
- observation: SANCTIONED CHANGE to a FROZEN file (tools/gate_f/** is frozen; the run handover's standing rules permit a single genuinely blocking error to be fixed when it is recorded both in ralph/reports/gate-f-lane-log.md and in the step's own `observation`, which is how every change in the handover's section 5 is recorded). New step. aimed at stronghold (the file's one `silhouette: true` landmark), data/config/map_landmarks.json, 607 m from this position. Taken from the game's own map data rather than chosen: nothing here is an invented coordinate.

### X07-151 — GF-AUD-stronghold_approach-landmark
- expected: signature landmark at the Stronghold approach, HUD off. A composition audit frame; its paired hud:on context frame is the gameplay frame captured seconds earlier in this block.
- actual: DELEGATED GF-AUD-stronghold_approach-landmark to capture lane X07C (this is the logic lane; it takes no frames)
- events: t=93.43
- verdict: DELEGATED

### X07-148b — aim the ecology frame (stronghold_approach)
- expected: the camera turns on the right stick to the entry bearing turned 144 degrees
- actual: camera turned to -151 deg in 0 frames
- events: t=93.45
- verdict: PASS
- observation: SANCTIONED CHANGE to a FROZEN file (tools/gate_f/** is frozen; the run handover's standing rules permit a single genuinely blocking error to be fixed when it is recorded both in ralph/reports/gate-f-lane-log.md and in the step's own `observation`, which is how every change in the handover's section 5 is recorded). New step. Third arm of the compass sweep. The ecology frame asks what lives here, and asking it down the same sightline as the arrival frame can only ever answer for that sightline.

### X07-153 — GF-AUD-stronghold_approach-ecology
- expected: ecology/activity frame (wilds visible behaving) at the Stronghold approach, HUD off. A composition audit frame; its paired hud:on context frame is the gameplay frame captured seconds earlier in this block.
- actual: DELEGATED GF-AUD-stronghold_approach-ecology to capture lane X07C (this is the logic lane; it takes no frames)
- events: t=93.45
- verdict: DELEGATED

### X07-148c — aim the vegetation frame (stronghold_approach)
- expected: the camera turns on the right stick to the entry bearing turned 216 degrees
- actual: camera turned to 152 deg in 83 frames
- events: t=94.85
- verdict: PASS
- observation: SANCTIONED CHANGE to a FROZEN file (tools/gate_f/** is frozen; the run handover's standing rules permit a single genuinely blocking error to be fixed when it is recorded both in ralph/reports/gate-f-lane-log.md and in the step's own `observation`, which is how every change in the handover's section 5 is recorded). New step. Fourth arm of the compass sweep. The visual pass's finding that grass tufts occur 'in one narrow band and nowhere else on the visible floor' is a claim about one bearing until the audit looks down more than one.

### X07-154 — GF-AUD-stronghold_approach-vegetation
- expected: vegetation density impression with a paired frame at the Stronghold approach, HUD off. A composition audit frame; its paired hud:on context frame is the gameplay frame captured seconds earlier in this block.
- actual: DELEGATED GF-AUD-stronghold_approach-vegetation to capture lane X07C (this is the logic lane; it takes no frames)
- events: t=94.85
- verdict: DELEGATED

### X07-148d — back to the entry bearing for the ground frame (stronghold_approach)
- expected: the camera returns to the entry-route bearing
- actual: camera turned to 8 deg in 150 frames
- events: t=97.37
- verdict: PASS
- observation: SANCTIONED CHANGE to a FROZEN file (tools/gate_f/** is frozen; the run handover's standing rules permit a single genuinely blocking error to be fixed when it is recorded both in ralph/reports/gate-f-lane-log.md and in the step's own `observation`, which is how every change in the handover's section 5 is recorded). New step. The terrain frame is pitched down the route a player actually walks in on, so it is returned to the entry bearing first. It is also what leaves the camera on the entry bearing for the night and weather variants that follow in some blocks.

### X07-148e — pitch down to the ground (stronghold_approach)
- expected: the right stick pitches the camera down to its floor (camera.pitch_min_deg, -60) -- a ground close view instead of a sixth copy of the standing shot
- actual: right stick held (0.00, 1.00) for 40 frames
- events: t=98.05
- verdict: PASS
- observation: SANCTIONED CHANGE to a FROZEN file (tools/gate_f/** is frozen; the run handover's standing rules permit a single genuinely blocking error to be fixed when it is recorded both in ralph/reports/gate-f-lane-log.md and in the step's own `observation`, which is how every change in the handover's section 5 is recorded). New step. 40 frames at full deflection is 0.67 s against movement.json's 190 deg/s, which is 127 degrees of travel into a 48-degree range: the pitch CLAMPS at its floor, so this lands on exactly -60 every time rather than on whatever the frame budget happened to buy.

### X07-152 — GF-AUD-stronghold_approach-terrain
- expected: terrain/ground close view at the Stronghold approach, HUD off. A composition audit frame; its paired hud:on context frame is the gameplay frame captured seconds earlier in this block.
- actual: DELEGATED GF-AUD-stronghold_approach-terrain to capture lane X07C (this is the logic lane; it takes no frames)
- events: t=98.05
- verdict: DELEGATED

### X07-148f — pitch back up to the ceiling (stronghold_approach)
- expected: the right stick pitches the camera up to its ceiling (camera.pitch_max_deg, 32)
- actual: right stick held (0.00, -1.00) for 40 frames
- events: t=98.73
- verdict: PASS
- observation: SANCTIONED CHANGE to a FROZEN file (tools/gate_f/** is frozen; the run handover's standing rules permit a single genuinely blocking error to be fixed when it is recorded both in ralph/reports/gate-f-lane-log.md and in the step's own `observation`, which is how every change in the handover's section 5 is recorded). New step. Half of restoring the camera deterministically: drive it to the OTHER clamp first, so the next step starts from a known angle rather than from wherever this block left it.

### X07-148g — settle the pitch back to its start (stronghold_approach)
- expected: the camera returns to roughly camera.pitch_start_deg (-12), where the block found it
- actual: right stick held (0.00, 1.00) for 14 frames
- events: t=98.98
- verdict: PASS
- observation: SANCTIONED CHANGE to a FROZEN file (tools/gate_f/** is frozen; the run handover's standing rules permit a single genuinely blocking error to be fixed when it is recorded both in ralph/reports/gate-f-lane-log.md and in the step's own `observation`, which is how every change in the handover's section 5 is recorded). New step. From the +32 ceiling, 14 frames at 190 deg/s is 44.3 degrees, landing at about -12.3 -- pitch_start_deg. The block hands the camera on as it found it, so the night and weather frames that follow in some blocks are comparable with the day frames above them.

### X07-155 — what the the Stronghold approach grid recorded
- expected: a note event carries this observation into events.jsonl
- actual: Operator record for the Stronghold approach: E.7's per-region record: vegetation density impression with a paired frame, creature/trainer/resource cadence within the region (counts from live queries), empty areas (position + frame), invalid prop placement, water intrusion, collision anomalies, repetition, sightlines to the next landmark, path/road legibility, environmental storytelling present or absent, regional identity ('could a player name where they are from this frame alone' -- operator observation), and vision fit.
- events: t=98.98
- verdict: PASS

### X07-156 — DIAG PIN: the Stronghold approach, night
- expected: the WorldLook clock is pinned to hour 23.0 and the weather to 'clear' through the game's own _apply_blended/set_weather, then both nodes are frozen, and the harness reports the clock reading it actually got -- so the frames below carry the state their ids claim
- actual: DIAG pinned weather=clear, hour=23.00 and froze WorldLook, WorldWeather; clock now reads hour=23.00 preset=golden weather=clear
- events: t=99.12
- verdict: PASS
- observation: SANCTIONED CHANGE to a FROZEN file (tools/gate_f/** is frozen; the run handover's standing rules permit a single genuinely blocking error to be fixed when it is recorded both in ralph/reports/gate-f-lane-log.md and in the step's own `observation`, which is how every change in the handover's section 5 is recorded). Was a `note` reading: "E.7: 'Day/night/weather variants where materially relevant (village, pond, stronghold approach at minimum)'. The clock is pinned and frozen at the night preset. This note is the pin's telemetry record, marked DIAG." -- a note is not a pin. The 2026-08-25 run proves it: clock_hour ran 8.16-11.14 and weather stayed 'clear' for all 135 route rows, so this step's own frames were captured at mid-morning whatever their ids said. `hour` rather than `preset` because it drives world_look.gd::_apply_blended, the same continuous path _process uses, and because it makes the clock_hour recorded on every manifest row below honest -- a preset snap would have left the telemetry still reading morning and a future reader repeating this exact mistake. Pinned AFTER the settle, per _step_pin_clock's own ordering note.

### X07-157 — GF-AUD-stronghold_approach-night-gameplay
- expected: the night variant's paired hud:on context frame at the Stronghold approach
- actual: DELEGATED GF-AUD-stronghold_approach-night-gameplay to capture lane X07C (this is the logic lane; it takes no frames)
- events: t=99.12
- verdict: DELEGATED

### X07-158 — GF-AUD-stronghold_approach-night-arrival
- expected: the night variant's composition frame at the Stronghold approach, paired with the hud:on frame captured seconds earlier
- actual: DELEGATED GF-AUD-stronghold_approach-night-arrival to capture lane X07C (this is the logic lane; it takes no frames)
- events: t=99.12
- verdict: DELEGATED

### X07-159 — DIAG PIN: the Stronghold approach, weather variant (rain)
- expected: the WorldLook clock is pinned to hour 11.0 and the weather to 'rain' through the game's own _apply_blended/set_weather, then both nodes are frozen, and the harness reports the clock reading it actually got -- so the frames below carry the state their ids claim
- actual: DIAG pinned weather=rain, hour=11.00 and froze WorldLook, WorldWeather; clock now reads hour=11.00 preset=day weather=rain
- events: t=99.25
- verdict: PASS
- observation: SANCTIONED CHANGE to a FROZEN file (tools/gate_f/** is frozen; the run handover's standing rules permit a single genuinely blocking error to be fixed when it is recorded both in ralph/reports/gate-f-lane-log.md and in the step's own `observation`, which is how every change in the handover's section 5 is recorded). Was a `note` reading: "The clock and the weather preset are pinned and frozen for the weather variant. This note is the pin's telemetry record, marked DIAG." -- a note is not a pin. The 2026-08-25 run proves it: clock_hour ran 8.16-11.14 and weather stayed 'clear' for all 135 route rows, so this step's own frames were captured at mid-morning whatever their ids said. `hour` rather than `preset` because it drives world_look.gd::_apply_blended, the same continuous path _process uses, and because it makes the clock_hour recorded on every manifest row below honest -- a preset snap would have left the telemetry still reading morning and a future reader repeating this exact mistake. Pinned AFTER the settle, per _step_pin_clock's own ordering note.

### X07-160 — GF-AUD-stronghold_approach-weather-gameplay
- expected: the weather variant's paired hud:on context frame at the Stronghold approach
- actual: DELEGATED GF-AUD-stronghold_approach-weather-gameplay to capture lane X07C (this is the logic lane; it takes no frames)
- events: t=99.25
- verdict: DELEGATED

### X07-161 — GF-AUD-stronghold_approach-weather-arrival
- expected: the weather variant's composition frame at the Stronghold approach, paired with the hud:on frame captured seconds earlier
- actual: DELEGATED GF-AUD-stronghold_approach-weather-arrival to capture lane X07C (this is the logic lane; it takes no frames)
- events: t=99.25
- verdict: DELEGATED

### X07-162 — region: the Hall
- expected: a note event carries this observation into events.jsonl
- actual: E.7 audit grid for the Hall, at the region's own centre (150,7595) from data/config/map_landmarks.json. E.7's per-region record: vegetation density impression with a paired frame, creature/trainer/resource cadence within the region (counts from live queries), empty areas (position + frame), invalid prop placement, water intrusion, collision anomalies, repetition, sightlines to the next landmark, path/road legibility, environmental storytelling present or absent, regional identity ('could a player name where they are from this frame alone' -- operator observation), and vision fit.
- events: t=99.25
- verdict: PASS

### X07-163 — DIAG: go to the Hall
- expected: the player stands at the Hall. A DIAG jump rather than a walk, because this audits the world and not the route; the distance and dead-travel accumulators reset so nothing about this jump can reach a pacing figure.
- actual: DIAG teleport to (150, 7595); distance/dead-travel accumulators reset
- events: t=100.25
- verdict: PASS

### X07-164 — the teleport landed where it said (the Hall)
- expected: the player is on the region centre, standing on the ground rather than falling through it
- actual: 0.0 m from (150, 7595), wanted within 5.0
- events: t=100.25
- verdict: PASS

### X07-165 — region containment agrees (the Hall)
- expected: map_state.gd's own containment puts the player in hall
- actual: region=corridor (wanted hall)
- events: t=100.25
- verdict: FAIL

### X07-166 — count what lives here (the Hall)
- expected: creature/trainer/resource cadence within the region, counted from live queries
- actual: 1014 points of interest in the tree
- events: t=100.25
- verdict: PASS

### X07-167 — DIAG PIN: the Hall, day
- expected: the WorldLook clock is pinned to hour 11.0 and the weather to 'clear' through the game's own _apply_blended/set_weather, then both nodes are frozen, and the harness reports the clock reading it actually got -- so the frames below carry the state their ids claim
- actual: DIAG pinned weather=clear, hour=11.00 and froze WorldLook, WorldWeather; clock now reads hour=11.00 preset=day weather=clear
- events: t=100.38
- verdict: PASS
- observation: SANCTIONED CHANGE to a FROZEN file (tools/gate_f/** is frozen; the run handover's standing rules permit a single genuinely blocking error to be fixed when it is recorded both in ralph/reports/gate-f-lane-log.md and in the step's own `observation`, which is how every change in the handover's section 5 is recorded). Was a `note` reading: "The WorldLook clock is pinned and frozen at the region's day preset before the frames below, using the capture_band3_region.gd pattern rather than the unpinned variant. This note is the pin's telemetry record, marked DIAG." -- a note is not a pin. The 2026-08-25 run proves it: clock_hour ran 8.16-11.14 and weather stayed 'clear' for all 135 route rows, so this step's own frames were captured at mid-morning whatever their ids said. `hour` rather than `preset` because it drives world_look.gd::_apply_blended, the same continuous path _process uses, and because it makes the clock_hour recorded on every manifest row below honest -- a preset snap would have left the telemetry still reading morning and a future reader repeating this exact mistake. Pinned AFTER the settle, per _step_pin_clock's own ordering note.

### X07-168 — face the intended entry route direction (the Hall)
- expected: the camera turns on the right stick to the intended entry route direction, so the arrival frame is taken from where a player would arrive
- actual: camera turned to 8 deg in 0 frames
- events: t=100.40
- verdict: PASS
- observation: SANCTIONED CHANGE to a FROZEN file (tools/gate_f/** is frozen; the run handover's standing rules permit a single genuinely blocking error to be fixed when it is recorded both in ralph/reports/gate-f-lane-log.md and in the step's own `observation`, which is how every change in the handover's section 5 is recorded). Unchanged as a step. It is now the entry-route bearing for the `gameplay` and `arrival` frames only; the four other variants each get their own bearing below. The 2026-08-25 run took all six frames from this one camera, which is why the visual pass measured `arrival`, `gameplay` and `landmark` differing by 0.1-2.3% of pixels: three of six variant slots were one shot. The position itself is NOT moved -- E.7 fixes it before play and forbids moving it to improve a composition; only the bearing and the pitch change.

### X07-169 — GF-AUD-hall-gameplay
- expected: normal-gameplay frame (HUD on) at the Hall, HUD on. This is the paired hud:on context frame for the five hud:off frames that follow it in this block (protocol G).
- actual: DELEGATED GF-AUD-hall-gameplay to capture lane X07C (this is the logic lane; it takes no frames)
- events: t=100.40
- verdict: DELEGATED

### X07-170 — GF-AUD-hall-arrival
- expected: arrival view (from the intended entry route direction) at the Hall, HUD off. A composition audit frame; its paired hud:on context frame is the gameplay frame captured seconds earlier in this block.
- actual: DELEGATED GF-AUD-hall-arrival to capture lane X07C (this is the logic lane; it takes no frames)
- events: t=100.40
- verdict: DELEGATED

### X07-168a — aim the landmark frame (hall)
- expected: the camera turns on the right stick to a bearing the arrival frame does not already cover, so the landmark slot captures its own shot
- actual: camera turned to -64 deg in 92 frames
- events: t=101.95
- verdict: PASS
- observation: SANCTIONED CHANGE to a FROZEN file (tools/gate_f/** is frozen; the run handover's standing rules permit a single genuinely blocking error to be fixed when it is recorded both in ralph/reports/gate-f-lane-log.md and in the step's own `observation`, which is how every change in the handover's section 5 is recorded). New step. data/config/map_landmarks.json carries no landmark for this region more than 20 m from this position, so there is no authored landmark to aim at and one is not invented here. The bearing is the entry bearing turned 72 degrees, the first arm of the five-way compass sweep the other variants complete -- a different sixth of the region rather than the same sixth six times.

### X07-171 — GF-AUD-hall-landmark
- expected: signature landmark at the Hall, HUD off. A composition audit frame; its paired hud:on context frame is the gameplay frame captured seconds earlier in this block.
- actual: DELEGATED GF-AUD-hall-landmark to capture lane X07C (this is the logic lane; it takes no frames)
- events: t=101.95
- verdict: DELEGATED

### X07-168b — aim the ecology frame (hall)
- expected: the camera turns on the right stick to the entry bearing turned 144 degrees
- actual: camera turned to -136 deg in 95 frames
- events: t=103.55
- verdict: PASS
- observation: SANCTIONED CHANGE to a FROZEN file (tools/gate_f/** is frozen; the run handover's standing rules permit a single genuinely blocking error to be fixed when it is recorded both in ralph/reports/gate-f-lane-log.md and in the step's own `observation`, which is how every change in the handover's section 5 is recorded). New step. Third arm of the compass sweep. The ecology frame asks what lives here, and asking it down the same sightline as the arrival frame can only ever answer for that sightline.

### X07-173 — GF-AUD-hall-ecology
- expected: ecology/activity frame (wilds visible behaving) at the Hall, HUD off. A composition audit frame; its paired hud:on context frame is the gameplay frame captured seconds earlier in this block.
- actual: DELEGATED GF-AUD-hall-ecology to capture lane X07C (this is the logic lane; it takes no frames)
- events: t=103.55
- verdict: DELEGATED

### X07-168c — aim the vegetation frame (hall)
- expected: the camera turns on the right stick to the entry bearing turned 216 degrees
- actual: camera turned to 152 deg in 95 frames
- events: t=105.15
- verdict: PASS
- observation: SANCTIONED CHANGE to a FROZEN file (tools/gate_f/** is frozen; the run handover's standing rules permit a single genuinely blocking error to be fixed when it is recorded both in ralph/reports/gate-f-lane-log.md and in the step's own `observation`, which is how every change in the handover's section 5 is recorded). New step. Fourth arm of the compass sweep. The visual pass's finding that grass tufts occur 'in one narrow band and nowhere else on the visible floor' is a claim about one bearing until the audit looks down more than one.

### X07-174 — GF-AUD-hall-vegetation
- expected: vegetation density impression with a paired frame at the Hall, HUD off. A composition audit frame; its paired hud:on context frame is the gameplay frame captured seconds earlier in this block.
- actual: DELEGATED GF-AUD-hall-vegetation to capture lane X07C (this is the logic lane; it takes no frames)
- events: t=105.15
- verdict: DELEGATED

### X07-168d — back to the entry bearing for the ground frame (hall)
- expected: the camera returns to the entry-route bearing
- actual: camera turned to 8 deg in 150 frames
- events: t=107.67
- verdict: PASS
- observation: SANCTIONED CHANGE to a FROZEN file (tools/gate_f/** is frozen; the run handover's standing rules permit a single genuinely blocking error to be fixed when it is recorded both in ralph/reports/gate-f-lane-log.md and in the step's own `observation`, which is how every change in the handover's section 5 is recorded). New step. The terrain frame is pitched down the route a player actually walks in on, so it is returned to the entry bearing first. It is also what leaves the camera on the entry bearing for the night and weather variants that follow in some blocks.

### X07-168e — pitch down to the ground (hall)
- expected: the right stick pitches the camera down to its floor (camera.pitch_min_deg, -60) -- a ground close view instead of a sixth copy of the standing shot
- actual: right stick held (0.00, 1.00) for 40 frames
- events: t=108.35
- verdict: PASS
- observation: SANCTIONED CHANGE to a FROZEN file (tools/gate_f/** is frozen; the run handover's standing rules permit a single genuinely blocking error to be fixed when it is recorded both in ralph/reports/gate-f-lane-log.md and in the step's own `observation`, which is how every change in the handover's section 5 is recorded). New step. 40 frames at full deflection is 0.67 s against movement.json's 190 deg/s, which is 127 degrees of travel into a 48-degree range: the pitch CLAMPS at its floor, so this lands on exactly -60 every time rather than on whatever the frame budget happened to buy.

### X07-172 — GF-AUD-hall-terrain
- expected: terrain/ground close view at the Hall, HUD off. A composition audit frame; its paired hud:on context frame is the gameplay frame captured seconds earlier in this block.
- actual: DELEGATED GF-AUD-hall-terrain to capture lane X07C (this is the logic lane; it takes no frames)
- events: t=108.35
- verdict: DELEGATED

### X07-168f — pitch back up to the ceiling (hall)
- expected: the right stick pitches the camera up to its ceiling (camera.pitch_max_deg, 32)
- actual: right stick held (0.00, -1.00) for 40 frames
- events: t=109.03
- verdict: PASS
- observation: SANCTIONED CHANGE to a FROZEN file (tools/gate_f/** is frozen; the run handover's standing rules permit a single genuinely blocking error to be fixed when it is recorded both in ralph/reports/gate-f-lane-log.md and in the step's own `observation`, which is how every change in the handover's section 5 is recorded). New step. Half of restoring the camera deterministically: drive it to the OTHER clamp first, so the next step starts from a known angle rather than from wherever this block left it.

### X07-168g — settle the pitch back to its start (hall)
- expected: the camera returns to roughly camera.pitch_start_deg (-12), where the block found it
- actual: right stick held (0.00, 1.00) for 14 frames
- events: t=109.28
- verdict: PASS
- observation: SANCTIONED CHANGE to a FROZEN file (tools/gate_f/** is frozen; the run handover's standing rules permit a single genuinely blocking error to be fixed when it is recorded both in ralph/reports/gate-f-lane-log.md and in the step's own `observation`, which is how every change in the handover's section 5 is recorded). New step. From the +32 ceiling, 14 frames at 190 deg/s is 44.3 degrees, landing at about -12.3 -- pitch_start_deg. The block hands the camera on as it found it, so the night and weather frames that follow in some blocks are comparable with the day frames above them.

### X07-175 — what the the Hall grid recorded
- expected: a note event carries this observation into events.jsonl
- actual: Operator record for the Hall: E.7's per-region record: vegetation density impression with a paired frame, creature/trainer/resource cadence within the region (counts from live queries), empty areas (position + frame), invalid prop placement, water intrusion, collision anomalies, repetition, sightlines to the next landmark, path/road legibility, environmental storytelling present or absent, regional identity ('could a player name where they are from this frame alone' -- operator observation), and vision fit.
- events: t=109.28
- verdict: PASS

### X07-176 — AMBIGUITY: the Stronghold approach has no region entry
- expected: a note event carries this observation into events.jsonl
- actual: E.7's region list ends 'plus the Stronghold approach and Hall', and data/config/map_landmarks.json has a region for neither -- the Hall is a LANDMARK at (150,7595) and the approach is not in the file at all. The Hall uses its landmark position. The approach uses (-65,7028), the midpoint of RT-11's two anchors -- the sigil gate at (-280,6460) from RT-10 and the Hall threshold at (150,7595) -- which is protocol G's own 'RT-11 mid' for GF-12-APPR-02. Both region_is asserts on those two blocks will FAIL, because no region id contains them; the FAIL is the record that the two places the protocol audits are not named on the map.
- events: t=109.28
- verdict: PASS

### X07-177 — CB-13's fallback lives here
- expected: a note event carries this observation into events.jsonl
- actual: E.1 CB-13: 'Lighting variants | at least one night fight (torch conditions) and one rain/fog fight, as natural play allows; else X07 pins and labels DIAG | shots'. Whether natural play delivered them is not knowable at transcription time, so the fallback is transcribed and labelled DIAG. If the journey already produced both, Phase B has two records rather than none; if it did not, the coverage is here.
- events: t=109.28
- verdict: PASS

### X07-178 — DIAG LABEL: the CB-13 fallback frames are DIAG
- expected: a note event carries this observation into events.jsonl
- actual: Protocol 0.6: no pacing, navigation, difficulty or economy claim may be sourced from a DIAG segment. The fight below is evidence about LIGHTING only -- what a night fight and a weather fight look like -- and carries no difficulty claim.
- events: t=109.28
- verdict: PASS

### X07-179 — DIAG: go to the band 1 field for the lighting fights
- expected: the player stands in the band 1 field, where a wild fight can be staged under a pinned clock
- actual: DIAG teleport to (195, 905); distance/dead-travel accumulators reset
- events: t=110.28
- verdict: PASS

### X07-180 — DIAG PIN: night, torch conditions
- expected: the WorldLook clock is pinned to hour 23.0 and the weather to 'clear' through the game's own _apply_blended/set_weather, then both nodes are frozen, and the harness reports the clock reading it actually got -- so the frames below carry the state their ids claim
- actual: DIAG pinned weather=clear, hour=23.00 and froze WorldLook, WorldWeather; clock now reads hour=23.00 preset=golden weather=clear
- events: t=110.42
- verdict: PASS
- observation: SANCTIONED CHANGE to a FROZEN file (tools/gate_f/** is frozen; the run handover's standing rules permit a single genuinely blocking error to be fixed when it is recorded both in ralph/reports/gate-f-lane-log.md and in the step's own `observation`, which is how every change in the handover's section 5 is recorded). Was a `note` reading: "The WorldLook clock is pinned and frozen at night for the fight below. This note is the pin's telemetry record, marked DIAG." -- a note is not a pin. The 2026-08-25 run proves it: clock_hour ran 8.16-11.14 and weather stayed 'clear' for all 135 route rows, so this step's own frames were captured at mid-morning whatever their ids said. `hour` rather than `preset` because it drives world_look.gd::_apply_blended, the same continuous path _process uses, and because it makes the clock_hour recorded on every manifest row below honest -- a preset snap would have left the telemetry still reading morning and a future reader repeating this exact mistake. Pinned AFTER the settle, per _step_pin_clock's own ordering note.

### X07-181 — draw the torch
- expected: the torch is drawn, so the night fight is under torch conditions
- actual: pressed torch_toggle x1 (tap, 1 frames each) on the default device, resolved to Key:0
- events: t=110.53
- verdict: PASS

### X07-182 — engage a wild at night
- expected: a wild fight starts under the pinned night preset
- actual: pressed combat_quick x2 (tap, 1 frames each) on the default device, resolved to JoyAxis:5:1.0
- events: t=111.00
- verdict: PASS

### X07-183 — GF-14-COMBAT-13: the night fight
- expected: a frame of a night fight under torch conditions
- actual: DELEGATED GF-14-COMBAT-13b to capture lane X07C (this is the logic lane; it takes no frames)
- events: t=111.00
- verdict: DELEGATED

### X07-184 — let the night fight end
- expected: the fight resolves
- actual: waited 5400 physics frames
- events: t=201.00
- verdict: PASS

### X07-185 — DIAG PIN: rain/fog
- expected: the WorldLook clock is pinned to hour 11.0 and the weather to 'rain' through the game's own _apply_blended/set_weather, then both nodes are frozen, and the harness reports the clock reading it actually got -- so the frames below carry the state their ids claim
- actual: DIAG pinned weather=rain, hour=11.00 and froze WorldLook, WorldWeather; clock now reads hour=11.00 preset=day weather=rain
- events: t=201.13
- verdict: PASS
- observation: SANCTIONED CHANGE to a FROZEN file (tools/gate_f/** is frozen; the run handover's standing rules permit a single genuinely blocking error to be fixed when it is recorded both in ralph/reports/gate-f-lane-log.md and in the step's own `observation`, which is how every change in the handover's section 5 is recorded). Was a `note` reading: "The weather preset is pinned and frozen at rain or fog for the fight below. This note is the pin's telemetry record, marked DIAG." -- a note is not a pin. The 2026-08-25 run proves it: clock_hour ran 8.16-11.14 and weather stayed 'clear' for all 135 route rows, so this step's own frames were captured at mid-morning whatever their ids said. `hour` rather than `preset` because it drives world_look.gd::_apply_blended, the same continuous path _process uses, and because it makes the clock_hour recorded on every manifest row below honest -- a preset snap would have left the telemetry still reading morning and a future reader repeating this exact mistake. Pinned AFTER the settle, per _step_pin_clock's own ordering note.

### X07-186 — engage a wild in rain or fog
- expected: a wild fight starts under the pinned weather preset
- actual: pressed combat_quick x2 (tap, 1 frames each) on the default device, resolved to JoyAxis:5:1.0
- events: t=201.60
- verdict: PASS

### X07-187 — GF-14-COMBAT-13-weather: the rain/fog fight
- expected: a frame of a fight in rain or fog
- actual: DELEGATED GF-14-COMBAT-13-weather to capture lane X07C (this is the logic lane; it takes no frames)
- events: t=201.60
- verdict: DELEGATED

### X07-188 — let the weather fight end
- expected: the fight resolves
- actual: waited 5400 physics frames
- events: t=291.60
- verdict: PASS

### X07-189 — segment close
- expected: a note event carries this observation into events.jsonl
- actual: Every frame in this segment is a GF-AUD or a labelled CB-13 fallback, every pin is recorded above as DIAG, and nothing here may be read as a pacing, navigation, difficulty or economy claim.
- events: t=291.60
- verdict: PASS
