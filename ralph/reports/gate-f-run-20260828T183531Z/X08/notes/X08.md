# X08 — DIAG performance audit (protocol E.9)

### preflight — capture not required
- segment declares no captures and no continuous record
- predicted cost 295 s over 49836 frames at 0.0059 s/frame (re-priced after each boot)

### X08-001 — what this segment is
- expected: a note event carries this observation into events.jsonl
- actual: E.9's performance audit, restricted to what is honestly measurable in this envelope: CPU frame/physics shape, directly-timed subsystem costs, draw calls and primitives under the render mode, placement/instance counts, and boot/save/load/transition durations.
- events: t=0.25
- verdict: PASS

### X08-002 — VOCABULARY GAP: this file cannot invoke tools/perf_profile.gd
- expected: a note event carries this observation into events.jsonl
- actual: E.9's first bullet is six sites x two modes of tools/perf_profile.gd with 'JSON output archived per site'. SEGMENT_SCHEMA.md's vocabulary drives the GAME; it has no action that launches a second Godot process or archives another tool's JSON, and instrumentation request section 10 explicitly forbids modifying perf_profile.gd. Those twelve runs therefore happen from tools/gate_f/run_segment.sh's own invocations, outside this step-script, and their JSON is archived beside this segment's telemetry. Named here so the run-end inventory check can see it was not silently dropped.
- events: t=0.25
- verdict: PASS

### X08-003 — VOCABULARY GAP: placement/instance counts have no assert check
- expected: a note event carries this observation into events.jsonl
- actual: E.9's second bullet is 'Placement/instance counts vs the 260,000 budget (test_scatter_perf_budget.gd context)'. SEGMENT_SCHEMA.md has no assert check for a scatter count, and the Compatibility renderer counts MultiMesh BATCHES rather than instances (protocol 0.4), so a count read off the render mode would not be the number the budget is about. The counts come from test_scatter_perf_budget.gd and the render-mode perf_profile output; this segment records the sites they were taken at.
- events: t=0.25
- verdict: PASS

### X08-004 — boot the real Meadows
- expected: the real world scene stands up with the real autoloads. This event's duration_ms IS one of E.9's third-bullet numbers: the boot duration, measured rather than guessed.
- actual: booted world in 28919 ms (240 settle frames); re-priced at boot:world: 0.0066 s/frame (was 0.0059), 49593 frames left + 29 s boot = 358 s against 14371 s of budget left
- events: t=4.53
- verdict: PASS

### X08-005 — settle before the first site
- expected: ten seconds of quiet so the stand-up's own cost is out of the rolling perf window before anything is sampled
- actual: waited 600 physics frames
- events: t=14.53
- verdict: PASS

### X08-006 — perf site: village
- expected: a note event carries this observation into events.jsonl
- actual: tools/perf_profile.gd's own site 'village' at (10,-10). The window below is the harness-side half: 60 s standing still and 60 s walking, so the perf field's avg/p95/max and route.csv's frame_ms/physics_ms columns carry both a static and a moving shape at this site.
- events: t=14.53
- verdict: PASS

### X08-007 — DIAG: go to the village perf site
- expected: the player stands at the village perf site. A DIAG jump rather than a walk: this audits the machine, not the route, and the distance and dead-travel accumulators reset so nothing about this jump can reach a pacing figure.
- actual: DIAG teleport to (10, -10); distance/dead-travel accumulators reset
- events: t=15.53
- verdict: PASS

### X08-008 — the teleport landed where it said (village)
- expected: the player is on the site, standing on the ground rather than falling through it
- actual: 0.0 m from (10, -10), wanted within 5.0
- events: t=15.53
- verdict: PASS

### X08-009 — let streaming settle at village
- expected: the region transition's streaming settles; this event's duration_ms is E.9's region-transition number for this site
- actual: waited 900 physics frames
- events: t=30.53
- verdict: PASS

### X08-010 — count what is placed here (village)
- expected: the points of interest in range are counted from live queries; the scatter and instance counts come from the render-mode perf_profile run at the same site
- actual: 1014 points of interest in the tree
- events: t=30.53
- verdict: PASS

### X08-011 — static frame-time shape at village
- expected: sixty seconds standing still. The perf field on every event in this window carries {frame_ms_avg, frame_ms_p95, frame_ms_max} over the trailing window -- CPU shape on this container's software rasteriser, and nothing else.
- actual: waited 3600 physics frames
- events: t=90.53
- verdict: PASS

### X08-012 — moving frame-time shape at village
- expected: sixty seconds of held stick. A moving sample, because a static one hides the cost of streaming new ground in.
- actual: left stick held (0.00, -1.00) for 3600 frames
- events: t=150.55
- verdict: PASS

### X08-013 — worst spike location at village
- expected: the world still owns input at the end of the window; this event's `pos`, `perf` and `region` are what the worst frame-time spike in route.csv is correlated to
- actual: input_context=world (wanted world)
- events: t=150.55
- verdict: PASS

### X08-014 — what the village site recorded
- expected: a note event carries this observation into events.jsonl
- actual: CPU frame and physics time, the trailing-window avg/p95/max, and the positions the worst spikes fell at. Device frame rate, GPU frame time, VRAM and thermals are [OWNER-ONLY] and are absent from this record rather than estimated.
- events: t=150.55
- verdict: PASS

### X08-015 — perf site: band1
- expected: a note event carries this observation into events.jsonl
- actual: tools/perf_profile.gd's own site 'band1' at (0,700). The window below is the harness-side half: 60 s standing still and 60 s walking, so the perf field's avg/p95/max and route.csv's frame_ms/physics_ms columns carry both a static and a moving shape at this site.
- events: t=150.55
- verdict: PASS

### X08-016 — DIAG: go to the band1 perf site
- expected: the player stands at the band1 perf site. A DIAG jump rather than a walk: this audits the machine, not the route, and the distance and dead-travel accumulators reset so nothing about this jump can reach a pacing figure.
- actual: DIAG teleport to (0, 700); distance/dead-travel accumulators reset
- events: t=151.55
- verdict: PASS

### X08-017 — the teleport landed where it said (band1)
- expected: the player is on the site, standing on the ground rather than falling through it
- actual: 0.0 m from (0, 700), wanted within 5.0
- events: t=151.55
- verdict: PASS

### X08-018 — let streaming settle at band1
- expected: the region transition's streaming settles; this event's duration_ms is E.9's region-transition number for this site
- actual: waited 900 physics frames
- events: t=166.55
- verdict: PASS

### X08-019 — count what is placed here (band1)
- expected: the points of interest in range are counted from live queries; the scatter and instance counts come from the render-mode perf_profile run at the same site
- actual: 1014 points of interest in the tree
- events: t=166.55
- verdict: PASS

### X08-020 — static frame-time shape at band1
- expected: sixty seconds standing still. The perf field on every event in this window carries {frame_ms_avg, frame_ms_p95, frame_ms_max} over the trailing window -- CPU shape on this container's software rasteriser, and nothing else.
- actual: waited 3600 physics frames
- events: t=226.55
- verdict: PASS

### X08-021 — moving frame-time shape at band1
- expected: sixty seconds of held stick. A moving sample, because a static one hides the cost of streaming new ground in.
- actual: left stick held (0.00, -1.00) for 3600 frames
- events: t=286.57
- verdict: PASS

### X08-022 — worst spike location at band1
- expected: the world still owns input at the end of the window; this event's `pos`, `perf` and `region` are what the worst frame-time spike in route.csv is correlated to
- actual: input_context=world (wanted world)
- events: t=286.57
- verdict: PASS

### X08-023 — what the band1 site recorded
- expected: a note event carries this observation into events.jsonl
- actual: CPU frame and physics time, the trailing-window avg/p95/max, and the positions the worst spikes fell at. Device frame rate, GPU frame time, VRAM and thermals are [OWNER-ONLY] and are absent from this record rather than estimated.
- events: t=286.57
- verdict: PASS

### X08-024 — perf site: band2
- expected: a note event carries this observation into events.jsonl
- actual: tools/perf_profile.gd's own site 'band2' at (0,2200). The window below is the harness-side half: 60 s standing still and 60 s walking, so the perf field's avg/p95/max and route.csv's frame_ms/physics_ms columns carry both a static and a moving shape at this site.
- events: t=286.57
- verdict: PASS

### X08-025 — DIAG: go to the band2 perf site
- expected: the player stands at the band2 perf site. A DIAG jump rather than a walk: this audits the machine, not the route, and the distance and dead-travel accumulators reset so nothing about this jump can reach a pacing figure.
- actual: DIAG teleport to (0, 2200); distance/dead-travel accumulators reset
- events: t=287.57
- verdict: PASS

### X08-026 — the teleport landed where it said (band2)
- expected: the player is on the site, standing on the ground rather than falling through it
- actual: 0.0 m from (0, 2200), wanted within 5.0
- events: t=287.57
- verdict: PASS

### X08-027 — let streaming settle at band2
- expected: the region transition's streaming settles; this event's duration_ms is E.9's region-transition number for this site
- actual: waited 900 physics frames
- events: t=302.57
- verdict: PASS

### X08-028 — count what is placed here (band2)
- expected: the points of interest in range are counted from live queries; the scatter and instance counts come from the render-mode perf_profile run at the same site
- actual: 1014 points of interest in the tree
- events: t=302.57
- verdict: PASS

### X08-029 — static frame-time shape at band2
- expected: sixty seconds standing still. The perf field on every event in this window carries {frame_ms_avg, frame_ms_p95, frame_ms_max} over the trailing window -- CPU shape on this container's software rasteriser, and nothing else.
- actual: waited 3600 physics frames
- events: t=362.57
- verdict: PASS

### X08-030 — moving frame-time shape at band2
- expected: sixty seconds of held stick. A moving sample, because a static one hides the cost of streaming new ground in.
- actual: left stick held (0.00, -1.00) for 3600 frames
- events: t=422.58
- verdict: PASS

### X08-031 — worst spike location at band2
- expected: the world still owns input at the end of the window; this event's `pos`, `perf` and `region` are what the worst frame-time spike in route.csv is correlated to
- actual: input_context=world (wanted world)
- events: t=422.58
- verdict: PASS

### X08-032 — what the band2 site recorded
- expected: a note event carries this observation into events.jsonl
- actual: CPU frame and physics time, the trailing-window avg/p95/max, and the positions the worst spikes fell at. Device frame rate, GPU frame time, VRAM and thermals are [OWNER-ONLY] and are absent from this record rather than estimated.
- events: t=422.58
- verdict: PASS

### X08-033 — perf site: band3
- expected: a note event carries this observation into events.jsonl
- actual: tools/perf_profile.gd's own site 'band3' at (0,4000). The window below is the harness-side half: 60 s standing still and 60 s walking, so the perf field's avg/p95/max and route.csv's frame_ms/physics_ms columns carry both a static and a moving shape at this site.
- events: t=422.58
- verdict: PASS

### X08-034 — DIAG: go to the band3 perf site
- expected: the player stands at the band3 perf site. A DIAG jump rather than a walk: this audits the machine, not the route, and the distance and dead-travel accumulators reset so nothing about this jump can reach a pacing figure.
- actual: DIAG teleport to (0, 4000); distance/dead-travel accumulators reset
- events: t=423.58
- verdict: PASS

### X08-035 — the teleport landed where it said (band3)
- expected: the player is on the site, standing on the ground rather than falling through it
- actual: 0.0 m from (0, 4000), wanted within 5.0
- events: t=423.58
- verdict: PASS

### X08-036 — let streaming settle at band3
- expected: the region transition's streaming settles; this event's duration_ms is E.9's region-transition number for this site
- actual: waited 900 physics frames
- events: t=438.58
- verdict: PASS

### X08-037 — count what is placed here (band3)
- expected: the points of interest in range are counted from live queries; the scatter and instance counts come from the render-mode perf_profile run at the same site
- actual: 1014 points of interest in the tree
- events: t=438.58
- verdict: PASS

### X08-038 — static frame-time shape at band3
- expected: sixty seconds standing still. The perf field on every event in this window carries {frame_ms_avg, frame_ms_p95, frame_ms_max} over the trailing window -- CPU shape on this container's software rasteriser, and nothing else.
- actual: waited 3600 physics frames
- events: t=498.58
- verdict: PASS

### X08-039 — moving frame-time shape at band3
- expected: sixty seconds of held stick. A moving sample, because a static one hides the cost of streaming new ground in.
- actual: left stick held (0.00, -1.00) for 3600 frames
- events: t=558.60
- verdict: PASS

### X08-040 — worst spike location at band3
- expected: the world still owns input at the end of the window; this event's `pos`, `perf` and `region` are what the worst frame-time spike in route.csv is correlated to
- actual: input_context=world (wanted world)
- events: t=558.60
- verdict: PASS

### X08-041 — what the band3 site recorded
- expected: a note event carries this observation into events.jsonl
- actual: CPU frame and physics time, the trailing-window avg/p95/max, and the positions the worst spikes fell at. Device frame rate, GPU frame time, VRAM and thermals are [OWNER-ONLY] and are absent from this record rather than estimated.
- events: t=558.60
- verdict: PASS

### X08-042 — perf site: band4
- expected: a note event carries this observation into events.jsonl
- actual: tools/perf_profile.gd's own site 'band4' at (0,6000). The window below is the harness-side half: 60 s standing still and 60 s walking, so the perf field's avg/p95/max and route.csv's frame_ms/physics_ms columns carry both a static and a moving shape at this site.
- events: t=558.60
- verdict: PASS

### X08-043 — DIAG: go to the band4 perf site
- expected: the player stands at the band4 perf site. A DIAG jump rather than a walk: this audits the machine, not the route, and the distance and dead-travel accumulators reset so nothing about this jump can reach a pacing figure.
- actual: DIAG teleport to (0, 6000); distance/dead-travel accumulators reset
- events: t=559.60
- verdict: PASS

### X08-044 — the teleport landed where it said (band4)
- expected: the player is on the site, standing on the ground rather than falling through it
- actual: 0.0 m from (0, 6000), wanted within 5.0
- events: t=559.60
- verdict: PASS

### X08-045 — let streaming settle at band4
- expected: the region transition's streaming settles; this event's duration_ms is E.9's region-transition number for this site
- actual: waited 900 physics frames
- events: t=574.60
- verdict: PASS

### X08-046 — count what is placed here (band4)
- expected: the points of interest in range are counted from live queries; the scatter and instance counts come from the render-mode perf_profile run at the same site
- actual: 1014 points of interest in the tree
- events: t=574.60
- verdict: PASS

### X08-047 — static frame-time shape at band4
- expected: sixty seconds standing still. The perf field on every event in this window carries {frame_ms_avg, frame_ms_p95, frame_ms_max} over the trailing window -- CPU shape on this container's software rasteriser, and nothing else.
- actual: waited 3600 physics frames
- events: t=634.60
- verdict: PASS

### X08-048 — moving frame-time shape at band4
- expected: sixty seconds of held stick. A moving sample, because a static one hides the cost of streaming new ground in.
- actual: left stick held (0.00, -1.00) for 3600 frames
- events: t=694.62
- verdict: PASS

### X08-049 — worst spike location at band4
- expected: the world still owns input at the end of the window; this event's `pos`, `perf` and `region` are what the worst frame-time spike in route.csv is correlated to
- actual: input_context=world (wanted world)
- events: t=694.62
- verdict: PASS

### X08-050 — what the band4 site recorded
- expected: a note event carries this observation into events.jsonl
- actual: CPU frame and physics time, the trailing-window avg/p95/max, and the positions the worst spikes fell at. Device frame rate, GPU frame time, VRAM and thermals are [OWNER-ONLY] and are absent from this record rather than estimated.
- events: t=694.62
- verdict: PASS

### X08-051 — perf site: stronghold
- expected: a note event carries this observation into events.jsonl
- actual: tools/perf_profile.gd's own site 'stronghold' at (0,7500). The window below is the harness-side half: 60 s standing still and 60 s walking, so the perf field's avg/p95/max and route.csv's frame_ms/physics_ms columns carry both a static and a moving shape at this site.
- events: t=694.62
- verdict: PASS

### X08-052 — DIAG: go to the stronghold perf site
- expected: the player stands at the stronghold perf site. A DIAG jump rather than a walk: this audits the machine, not the route, and the distance and dead-travel accumulators reset so nothing about this jump can reach a pacing figure.
- actual: DIAG teleport to (0, 7500); distance/dead-travel accumulators reset
- events: t=695.62
- verdict: PASS

### X08-053 — the teleport landed where it said (stronghold)
- expected: the player is on the site, standing on the ground rather than falling through it
- actual: 0.0 m from (0, 7500), wanted within 5.0
- events: t=695.62
- verdict: PASS

### X08-054 — let streaming settle at stronghold
- expected: the region transition's streaming settles; this event's duration_ms is E.9's region-transition number for this site
- actual: waited 900 physics frames
- events: t=710.62
- verdict: PASS

### X08-055 — count what is placed here (stronghold)
- expected: the points of interest in range are counted from live queries; the scatter and instance counts come from the render-mode perf_profile run at the same site
- actual: 1014 points of interest in the tree
- events: t=710.62
- verdict: PASS

### X08-056 — static frame-time shape at stronghold
- expected: sixty seconds standing still. The perf field on every event in this window carries {frame_ms_avg, frame_ms_p95, frame_ms_max} over the trailing window -- CPU shape on this container's software rasteriser, and nothing else.
- actual: waited 3600 physics frames
- events: t=770.62
- verdict: PASS

### X08-057 — moving frame-time shape at stronghold
- expected: sixty seconds of held stick. A moving sample, because a static one hides the cost of streaming new ground in.
- actual: left stick held (0.00, -1.00) for 3600 frames
- events: t=830.63
- verdict: PASS

### X08-058 — worst spike location at stronghold
- expected: the world still owns input at the end of the window; this event's `pos`, `perf` and `region` are what the worst frame-time spike in route.csv is correlated to
- actual: input_context=world (wanted world)
- events: t=830.63
- verdict: PASS

### X08-059 — what the stronghold site recorded
- expected: a note event carries this observation into events.jsonl
- actual: CPU frame and physics time, the trailing-window avg/p95/max, and the positions the worst spikes fell at. Device frame rate, GPU frame time, VRAM and thermals are [OWNER-ONLY] and are absent from this record rather than estimated.
- events: t=830.63
- verdict: PASS

### X08-060 — boot, save, load and region-transition durations
- expected: a note event carries this observation into events.jsonl
- actual: E.9's third bullet: 'Boot, save, load, and region-transition durations from X05/journey telemetry; worst frame-time spikes correlated to position and event from route.csv across the whole journey.' The save and load durations are X05's duration_ms fields and the journey's; the boot and region-transition durations are on this segment's own boot and region_enter events. Nothing is re-measured here that another segment already measured honestly.
- events: t=830.63
- verdict: PASS

### X08-061 — the corridor probe stays a valid instrument
- expected: a note event carries this observation into events.jsonl
- actual: Protocol B: 'tools/gate_f_chapter_run.py's existing head/corridor/tail chaining and tools/_probe_gate_f_corridor.gd's cross-band dead-walk counter remain valid instruments and are reused inside D and X08 -- they are inputs to, not substitutes for, this protocol.' They are run as themselves, unmodified, beside this segment.
- events: t=830.63
- verdict: PASS

### X08-062 — the coverage gap this segment is honest about
- expected: a note event carries this observation into events.jsonl
- actual: Protocol K.1: 'Handheld frame rate / GPU cost / VRAM / thermal / battery on ROG Ally. This run supplies CPU shape, structural counts, and durations only (X08).' That gap is declared, not discovered.
- events: t=830.63
- verdict: PASS
