# S08 is INCOMPLETE

- BLOCKED before step 1: predicted cost 15335 s (4.3 h) exceeds the 14400 s ceiling: 289897 planned frames at a MEASURED 0.053 s/frame on this box. The protocol's waits are not the problem -- they exist so fights resolve -- so this segment needs a GPU or a split evidence lane, not a shorter wait. X07 stopped at step 184 of 266 for exactly this, ~15 hours in. NOTE this number is the EMPTY TREE price and is the optimistic one: the re-price after each boot is the honest one.
- 0 of 150 steps executed (0 refused, 0 skipped).
- harness errors: ["capture pre-flight BLOCKER: predicted cost 15335 s (4.3 h) exceeds the 14400 s ceiling: 289897 planned frames at a MEASURED 0.053 s/frame on this box. The protocol's waits are not the problem -- they exist so fights resolve -- so this segment needs a GPU or a split evidence lane, not a shorter wait. X07 stopped at step 184 of 266 for exactly this, ~15 hours in. NOTE this number is the EMPTY TREE price and is the optimistic one: the re-price after each boot is the honest one."]

See INVENTORY.json for the per-capture ledger.