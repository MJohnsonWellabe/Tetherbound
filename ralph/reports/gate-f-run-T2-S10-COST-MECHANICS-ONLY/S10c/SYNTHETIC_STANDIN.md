# S10c-exit.json here is a SYNTHETIC STAND-IN, not a real S10c run

S10c was deliberately not run to completion against the stranded save (see
ralph/reports/handover-T2-S10-COST-2026-08-30.md's "S10c" section: its own
132,750-frame move_to would burn its full budget failing to reach an
unreachable destination from the stranded save's spawn point, for no
additional mechanics information).

To still validate S10d's own chaining and its no-capture_lane declaration,
this file is a copy of S10b-exit.json, placed here as S10c-exit.json. This
is sound for MECHANICS purposes only: S10c's own content (a walk + one
screenshot) does not change party composition, objective, or flags relative
to S10b-exit -- only position -- so using S10b-exit.json in its place does
not fabricate any game-state claim that S10d's own asserts depend on. It
does NOT stand in for actually running S10c, and no game-evidence claim
should ever be sourced from this file.
