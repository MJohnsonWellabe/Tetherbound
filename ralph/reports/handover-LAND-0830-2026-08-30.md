# Handover — LAND-0830, 2026-08-30

**Branch:** `ralph/LAND-0830`, off `origin/main` at `a97f3e84`. Pushed.
**Head:** `9cceab795b59dd459bb15606c64ebd5ae4fafb6d`.
**CI run:** `33265918109` — **green at job level, both pages**: 55 jobs total,
53 success, 2 skipped (`verify-continuous-core-known-red` and `export`, both
expected per the brief), 0 failed.

`main` has not moved. Landing is the coordinator's call, via a ref push, per
standing instructions — not done here.

---

## Tips integrated

All nine named branches, fetched fresh at the start of this session:

| Branch | Tip integrated |
|---|---|
| `ralph/T3-TYPECHART` | `94e7e0b2` |
| `ralph/T1-GROUND` | `831e852b` (matches `origin/ralph/T1-GROUND`) |
| `ralph/T1-CAST` | `3ade94f9` |
| `ralph/T1-WARRENS-EXT` | `4816bab8` |
| `ralph/T2-STRANDING` | `974e2ba8` |
| `ralph/T3-PICKUPS` | `c1059109` |
| `ralph/T1-SKY` | `a9cd2425` |
| `ralph/T1-UI` | `529fc81e` |
| `ralph/T1-HALL-DESIGN` | `3f0b313d` |

Re-fetched again before finalising (after the full test suite and CI check):
no new commits landed on any of these nine branches during this session, so
every tip above is still current.

**Merge order:** type chart first (highest risk), then T1-GROUND (scatter
bake owner), then the two `burrow_warrens.json` co-owners together
(T1-CAST, T1-WARRENS-EXT), then the rest. One merge at a time, per the brief.

## Conflicts and how each was resolved

**Zero git conflicts across all nine merges.** The one anticipated collision
resolved itself cleanly:

- **`data/config/burrow_warrens.json`** — T1-CAST added one entry to the
  `lights` array (the guardian's leg-height stand wash) plus three comment
  blocks; T1-WARRENS-EXT restructured `mound.skirt_models` into
  `skirt_rock_models`/`skirt_flora_models`. Textually disjoint sections of
  the file, so `git merge` (ort strategy) auto-merged with no marker. Verified
  both survived post-merge: the new light entry
  (`{"at": [3.0, 44.0], "y": 1.0, "energy": 0.9, ...}`) is present, the old
  `skirt_models` key is gone, `skirt_rock_models`/`skirt_flora_models` are
  both present, and `wander_radius` (the Warrens vault leash fix from a prior
  landing) is unchanged at 3 occurrences in the JSON, matching the count the
  previous coordinator recorded on `main`.

No other file was touched by more than one of the nine branches — confirmed
by diffing each branch's file list against every other's before merging, not
assumed from the branch table.

## The scatter bake — checked, not re-baked

Per the brief's specific warning about a merged config fingerprinting as
neither of two independently-baked branches: **only `T1-GROUND` touches any
scatter-bake input** (`data/config/vegetation.json`,
`data/config/terrain_playground.json`, or any
`data/config/bands/*/vegetation.json`) among all nine branches — confirmed by
diffing each branch individually against its own merge-base before merging
anything. `T1-GROUND` re-baked and committed its own bake in the same commit
as its config edits. No other branch invalidates that bake, so no re-bake was
needed on the integration branch. Verified after full integration:
`test_scatter_perf_budget.gd`'s three tests (including
`test_playground_bake_is_committed_and_fresh`) pass clean, and
`git diff origin/main..HEAD` on the three fingerprint inputs shows only
`T1-GROUND`'s own changes.

**The `T1-VILLAGE` re-bake branch the brief warned about never appeared on
origin** — checked at both the start and end of this session. Nothing to
reconcile there.

## The band-split fixture mirror — checked, not weakened

T1-CAST's Creek Hollow radius fix touched both the live
`data/config/bands/band1_lower_meadows/spawns.json` and the tracked mirror
`tests/fixtures/band_split_baseline/spawns.json` in the same commit, per the
fixture's own policy. `test_band_content.gd` (6 tests) and `test_spawns_data.gd`
(23 tests) both pass clean on the integrated branch — the mirror was never
touched independently of the live file, so there was nothing to reconcile.

## Combination-only findings

**One flake investigated in depth, concluded pre-existing — not a
combination defect.** `tests/smoke_relay.gd` failed intermittently
(`"standing in front of 'Sela' and pressing interact opened nothing"`) on
`ralph/LAND-0830`: 2 failures in 6 runs. Before accepting that as a landing
blocker or as something this integration introduced, I ran the identical test
6 times against a clean, unmerged `origin/main` worktree (no branches
merged at all): **1 failure in 6 runs, the exact same message.** The failure
rate is statistically consistent between the two (roughly 1-in-5 to 1-in-6
either way), and the message is byte-identical. **This is a pre-existing
intermittent defect in `smoke_relay.gd` or the interact/dialogue path it
exercises, unrelated to any of the nine branches landed here.** I did not
have budget to root-cause it further (the interactable/arbiter tie-break math
is deterministic by distance and registration order on paper, so the actual
nondeterministic factor is still unknown), but I want to be precise about
what I checked rather than wave it off: it is not a proximity collision with
any of T3-PICKUPS's new world pickups (the nearest is 169m from the captive),
and it is not caused by the scatter bake, the type chart, or anything else
that landed here. **Recommend a dedicated ticket** — "smoke_relay.gd fails
~1-in-5 to 1-in-6 runs with `pressing interact opened nothing` at the relay
captive, reproduces on unmerged `main`" — rather than folding it into this
integration's scope.

No other combination-only defect was found. Given the density of prior
landings (LAND-0829A/B) that surfaced three such defects, I looked
specifically at every declared collision point (`burrow_warrens.json`, the
scatter bake, the band-split mirror, the type chart's interaction with
combat-bearing content) rather than only the ones the brief already knew
about, and found nothing beyond the relay flake above.

## Test evidence

- **Full suite:** `tests/run_tests.gd`, unfiltered — **1535 tests, 3,366,857
  assertions, 0 failed.** (The ERROR/SCRIPT ERROR lines in the log are
  pre-existing negative-test fixture noise and post-shutdown leak warnings,
  confirmed benign by multiple prior lane handovers; judge by the
  `N tests, N assertions, 0 failed` line.)
- **Named smoke tests**, run individually:

| Test | Result |
|---|---|
| `tests/smoke_combat.gd` | OK |
| `tests/smoke_relay.gd` | Intermittent — see finding above; passes the majority of runs |
| `tests/smoke_stronghold.gd` | OK |
| `tests/smoke_stronghold_reload.gd` | OK |
| `tests/smoke_gate_e_finale.gd` | OK |
| `tests/smoke_gate_a_rest_torch.gd` | OK |

- **Targeted re-checks:** `test_scatter_perf_budget.gd` (3/3),
  `test_band_content.gd` + `test_spawns_data.gd` (29/29).
- **CI:** run `33265918109` on head `9cceab79`, job-level, both pages: 55
  jobs, 53 success, 2 skipped (expected), 0 failed.

## What I could not resolve / did not attempt

- **The `smoke_relay.gd` flake's actual root cause.** Confirmed pre-existing
  and not a combination defect; did not chase the nondeterministic factor
  itself (candidate mechanisms I ruled out: proximity to any newly-placed
  pickup, the scatter bake, tie-break order in `interaction_arbiter.gd`,
  which is deterministic by distance/registration on inspection — the actual
  cause is still open).
- I did not merge, and was not asked to merge, two branches that appeared on
  origin mid-session and are **not part of this integration**:
  `ralph/T1-HALL-BUILD` (new, not in my original nine, looks like the Hall
  implementation lane building on `T1-HALL-DESIGN`'s design doc) and
  `ralph/T2-GATEF` (received new commits, `de32fa27..afcdea06`, while I was
  working). Neither conflicts with anything pushed here since `main` hasn't
  moved, but they will need a **future** integration round. Flagging rather
  than pulling them in, to keep this lane's scope bounded to what was
  assigned.

## What the coordinator should check before landing

1. The `smoke_relay.gd` flake (see above) — decide whether it blocks landing
   (my recommendation: no, it's pre-existing on `main` and not worsened by
   this branch) or needs a fix first.
2. `ralph/T1-HALL-BUILD` and `ralph/T2-GATEF` are landing in parallel and
   are not part of this branch. Sequence the next integration round
   accordingly.
3. Standard: `git push origin origin/ralph/LAND-0830:refs/heads/main` — a ref
   push, never a checkout, per the local `main` branch being fixed via
   `git branch -f main origin/main` this session (it was still 277
   ahead/84 behind before that).

## Environment notes

- Godot 4.7-stable was not preinstalled; fetched per `CLAUDE.md`'s command.
  Cold `--import` on the integration worktree took ~7 minutes and exited 0.
  A second `--import` on a separate clean `origin/main` worktree (used only
  to establish the `smoke_relay.gd` baseline) took ~7 minutes as well.
- This is a 4-core/15GB box. Running the full test suite and a smoke-test
  loop concurrently is survivable (matches a prior lane's own finding) but
  slows both down — the full suite took a bit over 30 minutes with a smoke
  loop running alongside it. CPU contention was ruled out as the explanation
  for the `smoke_relay.gd` flake specifically: it reproduced with nothing
  else running, on a completely clean worktree.
